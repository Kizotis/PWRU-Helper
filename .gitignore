bin/
obj/
*.user
.vs/

# Claude Code local working data (worktrees, session state) — never commit
.claude/

# BMAD method framework + generated working artifacts — local AI tooling, same
# policy as .claude/ (the committed project-context.md is the durable output)
_bmad/
_bmad-output/

# Test/build output
[Tt]est[Rr]esults/
*.trx

# Installer build output (the .msi is a release artifact, not source)
installer/*.msi
installer/*.wixpdb
