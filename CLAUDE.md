# PWRU Helper

Critical rules, stack, and traps for AI agents working on this repo live in the project context file — read it before writing code:

@project-context.md

Quick facts: C#/WPF .NET 8 app, code-behind (no MVVM, deliberate). Tests: `dotnet test tests/PWRUHelper.Tests`. All changes land on `main` via PR.
