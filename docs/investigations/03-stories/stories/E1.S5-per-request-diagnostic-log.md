---
baseline_commit: d958ce2
---

# Story E1.S5: Emit the per-request diagnostic log line

Status: **done**

_Epic E1 (A) — Diagnosable translation pipeline · increment 0 · Release A.0 · ⛔ no production code until the
owner's explicit go._

## Story

As the owner reading a player's pasted error report,
I want one line per failed attempt with the status, the headers that matter and the request cadence,
So that the next P2 incident is a measurement instead of an argument.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E1.S5. Do not weaken._

1. **Given** `TranslationService` logs nothing today
   **When** an attempt ends non-success or exceptional
   **Then** exactly one `Logging.Warn` line is emitted in the shape of `architecture-cible.md` §10.1, carrying
   `provider`, `ep`, `dir`, `attempt=n/m`, `cid`, `status`, `elapsed`, `retry-after`, `ct`, `len`,
   `hdrs=[via/srv/xrl/set-cookie]`, `bytes`, `lines`, `burst60`, `ipv`, and `body` **only** for a non-JSON body
   (de-tagged, whitespace-collapsed, first 120 characters).
2. **Given** a successful attempt
   **When** it completes
   **Then** it is counted into `burst60` but **not** logged line by line (the log is capped at 1 MB with one
   rollover, `Logging.cs:69,95-105`).
3. **Given** I11
   **When** any line is written
   **Then** it contains no user text, no `q=`, no full URL and no API key — asserted by a test that translates a
   recognisable sentence with a recognisable key through the fake handler and greps the produced file.
4. **Given** `LogWriter.Write` (`Logging.cs:85`) writes second-resolution timestamps
   **When** this story lands
   **Then** the timestamp format gains milliseconds, so retry spacing is readable.

## Tasks / Subtasks

- [x] **T1 — Millisecond timestamps** (AC: 4)
  - [x] `Services/Logging.cs:85` — `$"{DateTime.Now:yyyy-MM-dd HH:mm:ss}"` ⇒ `…HH:mm:ss.fff`. That is the **only**
        change to `Logging`; the facade, the override, the roll and the 1 MB cap stay exactly as they are.
  - [x] **TP-LOG-08** — a regex case asserting the format carries `.fff`.
- [x] **T2 — The line builder** (AC: 1)
  - [x] Build the §10.1 line as a pure `internal static` helper so it is testable without HTTP. Put it beside the
        emitter (a small `Services/RequestLog.cs`, or a region of `ProviderErrorMapper`'s file if the reviewer
        prefers fewer files — state the choice in the PR; **E2.S5 moves the emission into `HttpProviderCore`**, so
        keep it a function, not a method on the provider).
  - [x] Fields, in the §10.1 order: `provider`, `ep` (**host + path only, never the query**), `dir` (e.g. `ru->en`),
        `attempt=n/m`, `cid`, `status`, `elapsed`, `retry-after`, `ct`, `len`,
        `hdrs=[via:… srv:… xrl:… set-cookie:yes|no]`, `bytes`, `lines`, `burst60`, `ipv`, and `body` **only** for a
        non-JSON body.
  - [x] `cid` — a short correlation id shared by **both attempts of one logical call** (TP-RET-08 later asserts it).
        Generate it once per logical call and thread it through the retry loop.
  - [x] `burst60` — a rolling count of requests issued in the trailing 60 s, **incremented by successes too**
        (AC 2). A small static ring/queue of timestamps; it must be cheap and lock-safe.
  - [x] `ipv` — the address family actually used (`4` or `6`). From `mecanismes…` Q4: correctly-built rate limiters
        bucket IPv6 by prefix, so a dual-stack machine silently switching families looks like a block that "cleared
        itself". If the family cannot be obtained cheaply on this path, log `ipv=?` rather than guessing — and say
        so in the PR.
  - [x] `body` — the de-tagged, whitespace-collapsed, **first 120 characters** produced by **E1.S4**. Never for a
        JSON body.
- [x] **T3 — Emit it from the existing retry loop** (AC: 1, 2)
  - [x] `Services/TranslationService.cs:132-153` (the non-success branches) and `:172-177` (the parse failure) —
        one `Logging.Warn` per **non-success or exceptional attempt**. Exactly one: a 429 retried three times
        today produces three lines, each with the same `cid` and `attempt=1/3`, `2/3`, `3/3`.
  - [x] Successes: **no line**. Count into `burst60` only. A 1 MB cap with one rollover is the reason
        (`Logging.cs:69,95-105`).
  - [x] Keep `provider` honest: this endpoint is `gtx` today. Use the id it will keep — `google-gtx`
        (`ProviderIds`, E2.S2) — so the field is stable across the rename in E3.S6.
  - [x] DeepL: this story's scope is `TranslationService`, per `epics.md`'s technical note. If routing DeepL
        through the same helper is one line, do it and say so; if it is not, leave it to **E2.S5**, which moves both
        providers onto `HttpProviderCore`.
- [x] **T4 — The I11 negative assertion — the load-bearing test** (AC: 3)
  - [x] **TP-LOG-01** — translate a distinctive sentinel sentence that fails; the sentinel appears **nowhere** in
        the produced file.
  - [x] **TP-LOG-02** — the line contains the host+path (`translate.googleapis.com/translate_a/single`) but **no
        query string** and no `q=`.
  - [x] **TP-LOG-03** — with a recognisable DeepL key set and sent, the key value appears neither in the log nor
        in what `Logging.ReadRecent()` returns (that is what "Copy error report" copies,
        `MainWindow.xaml.cs:312-322`).
  - [x] **TP-LOG-04** — the field set is complete for a 429. **TP-LOG-05** — `body=` present for the HTML fixture,
        de-tagged, ≤ 120 chars; absent for a JSON error. **TP-LOG-06** — 50 successful requests produce **zero**
        per-request lines.
  - [x] All of it through `Logging.DirectoryOverride` (`Logging.cs:31-39`), the discipline
        `LoggingTests.cs:89,101` already establishes.
- [x] **T5 — Do not fight the module initialiser**
  - [x] `tests/PWRUHelper.Tests/TestLogRedirect.cs:24-26` sets `Logging.DirectoryOverride` for the **whole test
        assembly** (a `[ModuleInitializer]`), and `LoggingTests.cs:89` asserts it is never the real `%AppData%`.
        A test here must set **its own** temp directory and restore the previous value in a `finally`, exactly like
        `LoggingTests.cs:101-110`. Do not assume an empty file: read back with `Logging.ReadRecent()` or read the
        temp file directly.

### Review Findings

_Code review of `71a86ff` against baseline `d958ce2` (Amelia — Blind Hunter, Edge Case Hunter and Acceptance
Auditor layers run sequentially, with Winston's five rulings applied). **No finding invalidated an acceptance
criterion**: AC 1–4 are met as written, no user-visible string moved, and I1/I3/I4/I5/I16 are untouched (the only
new statements in the retry loop are logging ones; TP-MAP-17's source scan is green). The review's own weight went
where Winston put it — I11 was attacked adversarially rather than re-read, and one leak was found and closed.
Fixed items landed in the review-fix commit; recorded items are accepted rulings or belong to a named later story._

**Fixed in review**

- [x] [Review][Patch] **I11 — the echo cut did not stop at a credential, nor at an HTML entity**
      [`Services/RequestLog.cs` `FirstEcho`] — the cut handled `q=`, `://` and `%XX`, which covers the URL a page
      quotes but not the two other shapes a page can quote it in. (i) A keyed provider's HTML error page naming the
      parameter it rejected — `auth_key=…`, `api_key …`, `token=…`, `Bearer …` — walked the credential straight
      into `body=`. No keyed provider emits a line in this story, but this gate is what **E2.S5 inherits**, and a
      gate that only holds because nothing reaches it is not a gate. (ii) A page that renders the sentence as HTML
      entities (`&#x43F;…`) is the escape form the de-tagger does not decode, so it survived where `%XX` did not.
      The cut now also stops at `&#` and at a word-boundary, case-insensitive match of
      `q= key= auth_key api_key apikey token= bearer authorization`, with the mid-word guard extended to all of
      them (`monkey=` is not `key=`, pinned as a case).
- [x] [Review][Patch] **The 429 storm would roll the evidence away** [`Services/RequestLog.cs` `LogSuppressor`] —
      Winston's ruling 4. One failing attempt wrote one line unconditionally: at LIVE rates against a hard block
      that is a few hundred lines a minute, and the 1 MB cap with one rollover (`Logging.cs:69,95-105`) would
      discard the *beginning* of the incident — the part that says what the app was doing when the block arrived,
      which is the only part nobody can reconstruct afterwards. Added a per-provider valve keyed on
      `provider` + `status`: the first `LogSuppressor.Threshold` (10) consecutive identical lines are written in
      full — so an ordinary retried call is never touched, pinned as a case — after which the run goes quiet and
      is represented by **one** `suppressed=N in=Ns` summary per 60 s window. Anything that changes (a different
      status, a different provider, the storm ending) ends the run and **flushes what it held**, so no suppressed
      line is silently lost. The clock is the caller's, like `BurstCounter`'s, so the five new cases need no wall
      clock; the summary carries strictly less than the lines it replaces. The status is read without building the
      line, so a suppressed attempt does not pay for the line it will not write. The run is a process-wide static,
      so a test asserting an exact line count takes `RequestLog.ResetSuppression()` the way it takes
      `Logging.DirectoryOverride`.
- [x] [Review][Patch] **`burst60` reported `0` for the first request after a capped storm**
      [`Services/RequestLog.cs` `BurstCounter.Note`] — the 4096 cap was checked **before** the window was trimmed,
      so a queue still holding an expired pathological minute refused the new hit and then returned the trimmed
      count (zero). The first request after a storm reading as "no traffic at all" is the opposite of what the
      field exists to say. Trim first, then enqueue, then return the count. Pinned.
- [x] [Review][Test] **The three adversarial I11 cases Winston named** [`tests/PWRUHelper.Tests/RequestLogTests.cs`]
      — (1) the provider's own JSON, echoing a Cyrillic + ASCII sentinel, served as `text/plain` and driven **end
      to end**: §4.3's wider rule makes the app itself call that 200 a block page and hand the body to the log, and
      the narrower body gate still refuses it — no `body=`, no sentinel. (2) the query hidden where prose is not:
      an `<input value="…">` and a percent-encoded `<a href="…?q=…">`, both dropped by the de-tagger, plus the
      entity-encoded third form cut by the echo rule. (3) an API key echoed by an error page, five cases including
      the false-positive guard. Plus five valve cases and the burst-cap case: **382 tests** (was 370).

**Recorded, not changed**

- **The error body is not fed into `Classify`** — Winston's ruling 2, accepted. The `Kind` on the non-success
  branch is byte-for-byte E1.S4's, and the log computes its own head. E2.S5's `HttpProviderCore` makes the two one
  read.
- **`Headers.NonValidated`** — Winston's ruling 3, accepted, and the allow-list is explicit rather than a dump:
  `Retry-After`, `Via`, `Server`, the first three `X-RateLimit-*`, and `Set-Cookie` as **presence only** (never a
  value). Every value is capped at 40 characters and ASCII-folded, quotes and backslashes dropped, so no header can
  split one event into two lines or end `body="…"` early.
- **The emission runs on the WPF dispatcher, and that is E2.S5's to fix.** There is no `ConfigureAwait(false)`
  anywhere in `Services/`, and the callers await from UI-thread methods (`MainWindow.Translate.cs:37,94`,
  `MainWindow.Live.cs:315,320`), so `LogWriter`'s synchronous `File.AppendAllText` happens on the dispatcher.
  Left as is deliberately: it is the failure path only, at most three ~300-byte appends per call; the app already
  calls `Logging.Warn` from the dispatcher today (`MainWindow.Ocr.cs:409`, `MainWindow.Live.cs:233`,
  `MainWindow.Phrasebook.cs:276,283`), so this is not a new class of behaviour; and offloading the write would move
  the timestamp off the moment being measured, which is exactly what AC 4 exists to prevent. **E2.S5's
  `HttpProviderCore` should `ConfigureAwait(false)` its awaits** — that fixes it without touching the timestamp.
- **`SafeBodyAsync` swallows a genuine cancel.** A Stop pressed during the error-body read is caught with
  everything else, so the method then leaves as a `TranslationException` instead of an `OperationCanceledException`.
  Bounded and benign — every caller treats a `TranslationException` on that branch the way it treats the failure
  that produced it — and the alternative is a diagnostic read that can break the request, which is the one thing
  `Logging.cs:89`'s rule forbids.
- **The residual I11 surface, stated so it is not rediscovered as a surprise.** An HTML page that quotes the user's
  **Latin-script** sentence as ordinary prose — no `q=`, no escape, no entity, no scheme, no parameter name — is
  still renderable into `body=`. Cyrillic folds to `.` in `Ascii`, so the script a PW-RU player actually types is
  safe; nothing bounded catches the Latin case generically. The three structural gates plus the echo cut are what
  is available, and they cover every shape anyone has captured. Relatedly, redacting a *configured* key value is
  not possible here — `RequestLog` is UI-free and does not know the key — so **E2.S5 owns key redaction** when it
  routes DeepL and Azure through the shared emitter.
- **`cid` wraps every 65 536 calls.** Two calls that far apart cannot both be inside one 1 MB log; recorded because
  TP-RET-08 will assert on it in E2.S5.
- **The suite's ~5 s is production backoff running for real**, not a harness that sleeps: `FakeHandler.TimesOut()`
  throws immediately and its `Delay` defaults to zero, but four end-to-end tests drive the real retry loop and pay
  its `Task.Delay(300 * (attempt + 1))` — 900 ms each. E2.S5's injected clock is what removes it; adding a seam for
  it here would have been this story inventing E2.S5's design.
- **The four deviations the Dev Agent Record already states stand as accepted**: `ipv=?` (sanctioned by the task,
  the real value arrives with E2.S5's handler), the field order taken from AC 1 where §10.1's example disagrees,
  the `MaxAttemptsToday` literal replacement (the log's `m` must be the loop's own bound or the log lies), and the
  body gate being narrower than §4.3 — which is what made adversarial case (1) above pass.
- **Millisecond timestamps break nothing.** `ReadRecent` does not parse timestamps and neither does
  `CopyErrorReport_Click` (`MainWindow.xaml.cs:312-322`); no test in the suite asserted the second-resolution form.

## Dev Notes

### Invariants this story must keep

- **I11 is the whole point.** No user text, no `q=`, no full URL, no API key — the log is pasted to Discord **by
  design** (`MainWindow.xaml.cs:312-322`). The negative assertions in T4 are the load-bearing tests of this story;
  if they are weak, the story is worse than not doing it.
- **I2** — the line builder is pure and headless. `Logging` is already UI-free.
- **I16** — the LIVE loop is not touched.

### Rulings and decisions that apply

- Emission moves to **`HttpProviderCore`** in E2.S5 (§7.0: the core calls `gate.TryEnter` before the request and
  `ReportSuccess`/`ReportFailure` after it, **and emits this line**). Write the builder as a function so that move
  is a call-site change, not a rewrite.
- **Gate-transition lines are E2.S6's**, not this story's — one line per *transition*, never per skipped request
  (§10.2). Do not pre-build them.
- What this story buys, stated as the DoD: **"Copy error report" stops being empty for a P2-shaped failure.** Today
  `TranslationService` logs nothing at all, so the About tab's report is empty for exactly the failure players
  report (`analyse…` §5.1 [CONFIRMED-ABSENT]).

### Repo traps

- `Logging` swallows its own errors by design — logging must never be the thing that breaks a feature
  (`Logging.cs:89`, `:104`, `:126`). Keep that property: a failure to build the line must not fail the request.
- The log is capped at **1 MB with one rollover** (`Logging.cs:69,95-105`). That cap is why successes are counted
  and not logged — a chatty success path would evict the evidence.
- Cost: the line is built only on the failure path, so it is off the hot path. Do not compute `burst60`,
  `ipv` or the header set on successes beyond the counter increment (footprint is a product requirement).
- `dotnet test tests/PWRUHelper.Tests` green and headless; `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and the
  `tests\**` exclusion (`:18`) intact. **No `.sln`.**

## Testing

- **TP-IDs landed:** TP-LOG-01, TP-LOG-02, TP-LOG-03, TP-LOG-04, TP-LOG-05, TP-LOG-06, TP-LOG-08.
  (TP-LOG-07 = gate transitions ⇒ **E2.S6**; TP-LOG-09 = the full report evidencing a 429 episode ⇒ needs the gate
  history, so it completes in **E2.S6**; TP-RET-08 = one `cid` per logical call ⇒ **E2.S5**.)
- **Fixtures:** reuses `Fixtures/google-429.html` from E1.S4 for TP-LOG-05.
- **Isolation items:** uses IS-8/IS-11 (E1.S1) and IS-9 (E1.S4); the `Logging.DirectoryOverride` discipline of
  `LoggingTests.cs:89,101` plus the `TestLogRedirect` caveat in T5.
- **Must keep passing unchanged:** `LoggingTests` (6 cases), the E1.S1 pins, the whole existing suite.

## File List

**Modify**
- `Services/Logging.cs` (`:85` — the timestamp format, and nothing else)
- `Services/TranslationService.cs` (`:132-153`, `:172-177` — emission points; `cid` threaded through the loop)

**Create**
- `Services/RequestLog.cs` (the pure §10.1 line builder + the `burst60` counter) — or an explicitly-justified
  equivalent home

**Create (tests)**
- `tests/PWRUHelper.Tests/RequestLogTests.cs`

## Size, dependencies, risk

**Size:** M · **Depends on:** E1.S1, E1.S3 · **Blocked by:** the owner's Phase-4 go · **Risk:** low-medium — the
I11 negative assertion is the load-bearing test.
**DoD:** "Copy error report" (`MainWindow.xaml.cs:312-322`) is no longer empty for a P2-shaped failure.

## How to verify manually

Disconnect the network, translate from the Translator tab, then About → **Copy error report** and paste into a text
editor. You must see one `[WARN] tr provider=google-gtx …` line per attempt, with millisecond timestamps, a status
or transport reason, and `burst60`. **Read the paste and confirm your own typed sentence is not in it, and that no
`q=` and no key appear.** That reading is the manual half of I11.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E1.S5]
- [Source: docs/investigations/02-traduction/architecture-cible.md#10.1 Per-request line, #10.3 Copy error report]
- [Source: docs/investigations/02-traduction/analyse-implementation-actuelle.md#5.1, #5.2 The minimal diagnostic line]
- [Source: docs/investigations/02-traduction/mecanismes-de-blocage-google.md#Q4 (the ipv field)]
- [Source: docs/investigations/03-stories/test-plan.md#3.10 Observability (TP-LOG-01..08); risk R-07]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on Claude Opus, workflow **DS — Dev Story**, non-interactive.
Baseline commit `d958ce2` (branch `feature/p2-a0-testable-foundation`, worktree `.claude/worktrees/p4-code`).

### Debug Log References

`dotnet test tests/PWRUHelper.Tests` — **347 passed before, 370 passed after, 0 failed, ~5 s**, headless, no
network (every body comes from `Fixtures/` or from E1.S1's `FakeHandler`), no `%AppData%` write. `dotnet build -c
Release` clean, 0 warnings. The suite was run three times end to end to check the new collection for flake: 370,
370, 370.

Red confirmed first — the tests were written against a `RequestLog` that did not exist, and the build failed on
exactly that (`CS0246`). Two reds after the implementation, both worth recording because both were the test
catching the implementation rather than the reverse.

1. **`Call` still carried the query.** `The_call_record_keeps_the_size_of_the_payload_and_never_the_payload`
   failed on `Call.ToString()`, which printed the `Uri` — and the `Uri` is the full URL, `q=` and all. The record
   now stores the endpoint **already rendered to host + path**, so the struct the retry loop carries cannot hold
   the user's sentence in any form. The fix is strictly better than the test that forced it.
2. **The typed header accessors rewrite the evidence.** `srv:HTTP,server,(unknown)` — `HttpResponseHeaders`
   parsed `Server: HTTP server (unknown)` into three product tokens and `string.Join` put it back together with
   commas. Headers are now read through `Headers.NonValidated` (`RequestLog.cs:170`), so the line carries what the
   server actually sent, and a header this app has no parser for is logged verbatim instead of being dropped.

### Completion Notes List

- **T1 — one character in `Logging`** (`Services/Logging.cs:88`): `HH:mm:ss` ⇒ `HH:mm:ss.fff`. Nothing else in
  that file moved — not the facade, not `DirectoryOverride`, not the roll, not the 1 MB cap. TP-LOG-08 pins the
  format with a regex over a real written file.
- **T2 — `Services/RequestLog.cs`**, the story's first option. A static class of functions, **not** a method on a
  provider, because **E2.S5** moves the emission into `HttpProviderCore` and that must be a call-site change:
  `Line` (`:201`) is pure over strings and numbers, and the three `Emit` overloads (`:296`, `:301`, `:305`) are
  the only things that touch `Logging`.
- **The line, rendered** (the shape is in the test as a comment too):
  `tr provider=google-gtx ep=translate.googleapis.com/translate_a/single dir=ru->en attempt=2/3 cid=0054`
  `status=429 elapsed=142ms retry-after=30 ct=text/html len=40 hdrs=[via:"1.1 google" srv:"HTTP server (unknown)"`
  `xrl:- set-cookie:yes] bytes=7 lines=2 burst60=37 ipv=? body="We're sorry..."` — 258 characters, one line,
  printable ASCII only.
- **Field order: AC 1's, not the §10.1 example's.** The two disagree about one field — the example prints `ipv`
  between `len` and `hdrs`, while AC 1, T2's bullet and TP-LOG-04's field list all put it after `burst60`. The
  binding text wins; the field SET is identical either way.
- **I11 is structural before it is asserted**, which is the one thing this story had to get right. Three
  constructions, each of which makes a whole class of leak unrepresentable rather than merely untested:
  1. **`Call` (`:92`) holds no address and no text** — only host + path, a byte count and a line count. The
     endpoint is reduced by `Endpoint` (`:234`) at construction, so the query never enters the record the line is
     rendered from and no future change to the format can start printing it.
  2. **`BodyHead` (`:253`) is the only door a body can walk through, and it takes markup only** — §4.3 step 2
     alone (`LooksLikeHtml(resp: null, …)`, deliberately skipping step 1's content-type half). The wider §4.3
     rule would log a body a proxy had relabelled `text/plain`; that body is the provider's own JSON, and the
     provider's JSON **contains the user's sentence and its translation**. Markup is the server's page and
     nobody's sentence.
  3. **`FirstEcho` (`:277`)** cuts the head at the first `q=`, `://` or `%XX` — the other way user text reaches a
     server page: an interstitial that quotes the URL it refused. Google's own `/sorry/` page does not, but "the
     page we have seen does not" is not a rule, and I11 has to hold for the page nobody has captured yet. Four
     cases pin it, including the false-positive guard (`100% of requests` is prose, not an escape).
  Every value that reaches the line then passes `Ascii` (`:331`): bounded, printable ASCII, no quote, no
  backslash — so one server header cannot split one event into two lines or end the `body="…"` field early.
- **`cid` (`:70`)** is an interlocked 16-bit counter rendered as four hex characters, not a Guid: cheaper, and
  monotonic, which is what makes two interleaved calls readable in one file. Generated once per `RequestAsync`
  and shared by all three attempts — TP-RET-08's shape is already visible in
  `TP_LOG_04_a_429_logs_one_line_per_attempt_under_a_single_correlation_id` (one cid, `1/3` `2/3` `3/3`).
- **`burst60`** is `BurstCounter` (`:366`), a lock plus a queue of caller-supplied timestamps (IS-6-shaped, so a
  test needs no wall clock and E2's injected clock can drive it later), self-trimming, capped at 4096 entries so
  a pathological minute cannot grow memory. `RequestLog.Burst` is the shared instance; a test that wants exact
  numbers builds its own. **A successful request pays for this increment and for nothing else** — no header set,
  no body read, no line (`TranslationService.cs:187`).
- **`ipv=?` — a recorded gap, not an oversight.** `HttpClient` does not expose the socket on this path, and the
  only cheap way in is a `ConnectCallback` on the **production** handler: a hot-path change to
  `CreatePooledHandler` that this story is not scoped to make and that a footprint requirement should not absorb
  casually. §10.1's own instruction for that case is to log `?` rather than guess, and the constant
  (`UnknownAddressFamily`, `:54`) plus `Line`'s parameter are already in place. **E2.S5 owns the handler and is
  where the real value arrives.** TP-LOG-04 asserts `ipv=?` so the gap is visible rather than forgotten.
- **T3 — four emission points, all of §5.2's**: the HTML-200 sniff throw (`TranslationService.cs:213`), the
  non-success branch **before the throw decisions** (`:261`), the transport catch (`:289`) and the parse failure
  below the loop (`:347`). Success is the fifth insertion point and is the counter increment at `:187`.
- **The error body is now read — for the log, and not for the classification.** E1.S4 left the non-success branch
  body-less and named this story as the one that would read it. It does (`SafeBodyAsync`, `:320`, which swallows
  its own errors), but the body is **not** fed back into `Classify`: the `kind` on that branch is byte-for-byte
  what E1.S4 shipped. Two reasons, and the second is the decisive one. (i) Feeding it in would move a 400/404
  carrying an HTML body from `Unknown` to `Blocked` — defensible, but a Kind change this story was not asked to
  make. (ii) It would not even produce the field: on the measured 429 row 4 answers **before** row 11, so
  `Classify`'s `htmlHead` is `null` for exactly the shape §10.1's example shows a `body=` for. The log therefore
  computes its own head through E1.S4's two `internal` helpers. **E2.S5's `HttpProviderCore` is where the two
  become one read.**
- **The parse-failure line is honest about what it does not know** (`:347`). Its response object is gone by then,
  so it carries the status that was kept and the payload size, and `-` for every header-derived field — because
  §10.1 forbids paying for the header set on the success path, which is what that response still was. Four
  locals (`okStatus`, `okAttempt`, `okBurst`, `okStarted`) carry it out of the loop rather than re-deriving it.
- **`TranslationPolicy.MaxAttemptsToday` now IS the loop's bound** (`TranslationService.cs:179`), replacing the
  `attempt < 3` literal, and its comment was updated to say so. **Recorded as a deviation** — the story did not
  ask for it. The reason is narrow: `attempt=n/m` renders `m`, and an `m` that can drift from the loop's own
  bound is a log that lies. Same value, same behaviour; `TranslationPolicyTests` is green unchanged.
- **DeepL is left to E2.S5, as the task's second branch allows.** Routing it through the same helper is not one
  line: a different endpoint, a POST form instead of a query, one attempt instead of three, and a key on the
  request. It also does not count into `burst60` — deliberately, because that number is `analyse…` §2.3's volume
  model **for the Google endpoint**, and mixing two endpoints' rates into one counter would make it unreadable.
  TP-LOG-03 still asserts the key against the **whole produced file**, through the `FallbackTranslator` path a
  keyed user's report is actually built from.
- **T4 — the negatives, over the real file.** `TP_LOG_01_no_user_text_no_query_and_no_full_url_reach_the_file`
  translates a Cyrillic + ASCII sentinel through the measured 429 abuse page, then greps both `log.txt` and
  `Logging.ReadRecent()` (what `MainWindow.xaml.cs:312-322` copies) for the sentence, its Cyrillic half and its
  percent-encoded form — and asserts the file is **not** empty, so a log that carried nothing could not pass. The
  URL rules (`q=`, `https://`, `client=gtx`) are asserted over this story's own lines rather than the whole file,
  because `DirectoryOverride` is process-wide and every other class's deliberate failure lands in the same file
  while the test holds it; the sentinel and the key are unique, so those two are grepped over everything.
- **T5 — the module initialiser is respected, and the race it implies is closed.** Every test takes
  `Logging.DirectoryOverride`, restores the previous value in a `finally` and bins the directory best-effort
  (`LoggingTests.cs:101-110`'s discipline). Two additions: each end-to-end case fingerprints its own lines with a
  language pair nothing else uses (`dir=zz->qc`) and filters on it instead of counting lines; and
  `RequestLogTests` and `LoggingTests` now share one xUnit collection (`LogFileCollection`), because they are the
  only two classes that take that process-wide static and two of them swapping it concurrently would each read
  the other's lines. That is the only change to `LoggingTests` — its six cases are untouched and green.
- **23 new cases.** TP-LOG-08; the field-order pin; absent-header rendering; `X-RateLimit-*` naming; the two
  transport statuses; the single-ASCII-line guard; TP-LOG-02 structurally; the `Call`-holds-no-text pin; the
  body-gate pin; the four echo-cut cases; the burst window; TP-LOG-04 through the real retry loop; TP-LOG-05 both
  halves; TP-LOG-06; both transport shapes end to end; the parse failure; TP-LOG-01; TP-LOG-03; and the
  "emitting never throws" guard driven by a response whose content throws on every access.
- **Invariants.** I1 `ITranslator` byte-identical · I2 `RequestLog` depends on `System.Net.Http`, `System.Text`,
  `ProviderErrorMapper` and `Logging` — no UI type, no WPF, headless · I3 no OCE catch changed, and the filtered
  rethrow still precedes the transport catch that now emits · I4/I5 untouched (the batch join, the count-mismatch
  fallback and the never-pad rule are not on this path) · I11 above · I16 the retry loop's shape, the `rateLimited`
  latch and LIVE are untouched — the only new statements in the loop are logging ones. **TP-MAP-17 green**: no
  production file names the banned token, and `RequestLog.cs` does not import the enum statically.
  `InternalsVisibleTo` / `DefaultItemExcludes` intact, no new NuGet package, no `.sln`.
- **No user-visible string changed or was added.** This story writes to a file and to nothing else; every
  sentence a player can read is the one E1.S4 left. E1.S6 rewords against an unchanged baseline.

**Deviations from the ACs:** none against AC 1–4 as written. Four decisions are recorded above rather than
hidden: `ipv=?` (sanctioned by the task, gap assigned to E2.S5); the field order taken from AC 1 where the §10.1
example disagrees; the `MaxAttemptsToday` literal replacement, which the story did not ask for; and the body gate
being **narrower** than §4.3 (markup only, plus the echo cut) — narrower than "only for a non-JSON body", so AC 1
holds, and it is the difference between an I11 that is asserted and an I11 that cannot be violated.

### File List

**Created**
- `Services/RequestLog.cs`
- `tests/PWRUHelper.Tests/RequestLogTests.cs`

**Modified**
- `Services/Logging.cs` (`:88` — millisecond timestamps, and nothing else)
- `Services/TranslationService.cs` (the `ProviderId` constant, the per-call `RequestLog.Call`, the burst
  increment and the four emission points; the E1.S4 comment that had become false about the body read)
- `Services/TranslationPolicy.cs` (`MaxAttemptsToday`'s comment — it is now the retry loop's bound)
- `tests/PWRUHelper.Tests/LoggingTests.cs` (the shared xUnit collection; the six cases are untouched)
- `docs/investigations/03-stories/stories/E1.S5-per-request-diagnostic-log.md` (this file)
- `docs/investigations/03-stories/sprint-status.yaml`

### Change Log

| Date | Change |
|---|---|
| 2026-09-06 | E1.S5 implemented: `Services/RequestLog.cs` builds §10.1's per-request line and `TranslationService` emits one per non-success or exceptional attempt (successes counted into `burst60`, never logged). I11 is held by construction — the call record holds no URL and no text, and only markup can become `body=`. Timestamps gain milliseconds. Tests 347 → 370, all green; no user-visible string changed. Status → review. |
| 2026-09-06 | **Code review** of `71a86ff`: I11's echo cut now stops at a credential parameter name and at an HTML entity (a keyed provider's error page could have leaked its key into `body=` — the gate E2.S5 inherits); a per-provider `LogSuppressor` keeps a 429 storm from rolling the evidence out of the 1 MB log; `burst60` no longer reports 0 for the first request after a capped storm. Three adversarial I11 cases added plus the valve's five and the burst cap's one — tests 370 → 382, green ×3 runs, ~5 s, Release build 0 warnings. Status → done. |
