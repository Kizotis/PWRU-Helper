# Story E3.S7: Build both chains once, in the constructor, per the target composition

Status: **done**

_Epic E3 (C) — Provider chain and the new default endpoint · increment 2 · **Release A.1: E2 and E3 are separate
PRs but ONE release** · the owner's Phase-4 go was given 2026-09-06 (`README.md` § Phase 4)._

> **This is the story that makes the epic visible to a player.** Everything before it adds parts; this one wires
> them, and it is the only E3 story that touches `MainWindow`'s constructor — the part of this codebase with the
> worst bug history (`wpf-xaml-load-clobbers-settings`, v0.12.3; the `_readTranslator`-before-`_settings`
> declaration-order trap this story exists to fix).
>
> **It is also where `Background` finally reaches the read chain.** E2.S3 built the `Interactive` reserve and
> E2.S5 left it inert on purpose (*"`Background` did not reach the read chain: doing so would have meant a
> call-site change in `MainWindow.*`, and TP-START-02 allows exactly one `ProviderGates` reference outside
> `Services/`"*). This story is where that is resolved — **without** adding a second reference.

## Story

As a player,
I want the free tiers behind my key and my key never behind the screen reader,
So that a metered allowance cannot be drained by a loop I did not ask to spend it.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E3.S7. Do not weaken._

1. **Given** §8.1
   **When** the `MainWindow` constructor runs, before `InitializeComponent()`
   **Then** the read chain is
   `[Azure if AzureApiKey set AND UseKeyForReading] → GoogleDict → Edge → GoogleGtx → [Bergamot if enabled and
   present]` and the write chain is
   `[DeepL if key set] → [Azure if key set] → GoogleDict → Edge → GoogleGtx → [Bergamot if enabled and present]`.
2. **Given** `_readTranslator` is a field initializer at `MainWindow.xaml.cs:43` while `_settings` is initialised
   at `:50`
   **When** the chains move into the constructor body
   **Then** `_readTranslator` stays `readonly` and is simply assigned there, so it can see `_settings`.
3. **Given** I8
   **When** any configuration is applied
   **Then** **no setting can put DeepL in the read chain** — its absence is structural, asserted by a test on the
   built chain, not by a runtime check.
4. **Given** `BuildTranslator()` at `MainWindow.Translate.cs:226-233`
   **When** it becomes `BuildWriteChain()`
   **Then** it is still called from the key-save handler at `:239`.

## Scope note: three of the five optional tiers do not exist yet

AC 1 names Azure and Bergamot. **Neither exists in A.1**: `AzureTranslator` is E6.S2, `AzureApiKey` /
`UseKeyForReading` are E6.S3, `BergamotTranslator` is E8.S2 and `OfflineFallbackEnabled` is E8.S3. Today's
`AppSettings` (`Services/SettingsService.cs:15-80`) has `DeepLApiKey` and nothing else of the five.

**What this story ships, therefore:**

| Chain | A.1 shape (this story) | A.1 shape if E3.S5 was cancelled |
|---|---|---|
| **Read** | `GoogleDict → Edge → GoogleGtx` | `GoogleDict → GoogleGtx` |
| **Write** | `[DeepL if key set] → GoogleDict → Edge → GoogleGtx` | `[DeepL if key set] → GoogleDict → GoogleGtx` |

**What it must leave behind:** a builder shaped so that E6 and E8 each add **one conditional tier in one place**,
and an I8 test that already passes over "every settings permutation" as the settings arrive. Write the two
builders so the optional slots are visible as named, commented gaps — not so that E6 has to redesign the method.

## Tasks / Subtasks

- [x] **T1 — `Services/TranslationChains.cs`: the composition lives in `Services/`, not in the code-behind.**
      This is not a style preference; it is TP-START-02. `ProviderStateStoreTests.No_startup_path_mentions_
      ProviderGates` (`:826-866`) asserts literally that the **only** `ProviderGates` reference outside
      `Services/` in the whole app is `MainWindow.xaml.cs: ProviderGates.Flush();`. A chain built in the ctor
      needs `ProviderGate`s; a `ChainTier` carries one. So the *tier list* is built inside `Services/`.
  - [x] `internal static class TranslationChains` with
        `internal static ITranslator BuildRead(AppSettings s)` and
        `internal static ITranslator BuildWrite(AppSettings s)`.
  - [x] `AppSettings` lives in `Services/SettingsService.cs`, so this stays UI-free (I2) and headless-testable —
        which is the whole point, because AC 3's test asserts on a **built chain** and must not need a window.
  - [x] Reuse E3.S3's `ChainTranslator.Of((id, translator), …)` helper, which resolves each gate through
        `ProviderGates.For(id)`. `For` constructs only; it reads no file (`ProviderStateStoreTests.cs:815-824`
        pins that), so calling it from the ctor keeps **I10** and TP-START-01.
- [x] **T2 — The read chain, with `Background` priority (AC 1).**
  - [x] `GoogleDict → [Edge] → GoogleGtx`, each provider constructed with
        `priority: RequestPriority.Background` (E3.S4 T6 / E3.S5 T1 put the priority on the instance, because I1
        forbids it on the call).
  - [x] **Two instances, one gate.** The read chain and the write chain each build their own
        `GoogleDictTranslator`, and both resolve the *same* `ProviderGates.For("google-dict")` — that shared
        identity **is** I9 (§5.1: one gate per provider, shared by both chains). Do not try to share provider
        instances to "save" a gate; the gate is already shared and the provider is a few bytes.
  - [x] A comment marking the two future slots: `// [Azure — E6.S4: AzureApiKey set AND UseKeyForReading]` at the
        front and `// [Bergamot — E8.S3: OfflineFallbackEnabled and the model present]` at the back.
  - [x] **DeepL is not written in this method at all.** That is what "structural, not configured" means (AC 3).
- [x] **T3 — The write chain (AC 1, AC 4).** `[DeepL if key set] → GoogleDict → [Edge] → GoogleGtx`, each provider
      `RequestPriority.Interactive` (the default — so it is the read chain that is the exception, and a future
      reader sees which one was deliberate). Same two future-slot comments, plus `// [Azure — E6.S3: key set]`
      after DeepL.
- [x] **T4 — Move `_readTranslator` into the constructor (AC 2).** `MainWindow.xaml.cs:43`:
      `private readonly ITranslator _readTranslator = new CachingTranslator(new GoogleGtxTranslator());`
  - [x] Becomes `private readonly ITranslator _readTranslator;` and, in the ctor **before**
        `InitializeComponent()` (`:83`):
        `_readTranslator = new CachingTranslator(TranslationChains.BuildRead(_settings));`
  - [x] It stays `readonly` — AC 2 says so, and it is what stops a later story from reassigning the read chain
        from a handler.
  - [x] `_settings` (`:50`) is a field initializer and therefore runs **before** the ctor body: assigning
        `_readTranslator` in the body is exactly what lets it see `_settings`. Write that reason as a comment; it
        is the whole content of AC 2 and it is one careless "tidy back into a field initializer" away from being
        a null-reference on launch.
  - [x] The existing ctor line `_writeTranslator = BuildTranslator();` (`:82`) becomes
        `_writeTranslator = BuildWriteChain();`, still **before** `InitializeComponent()`.
- [x] **T5 — `BuildTranslator()` → `BuildWriteChain()` (AC 4).** `MainWindow.Translate.cs:226-233` becomes a
      two-line delegation: `private ITranslator BuildWriteChain() => new
      CachingTranslator(TranslationChains.BuildWrite(_settings));`
  - [x] Still called from `DeepLSaveKey_Click` at **`:239`** — AC 4. Rename the call there.
  - [x] Update the method's doc comment (`:222-225`): it currently says the read side "uses `_readTranslator`
        (Google only)", which stops being the whole truth the moment E6 lands Azure-for-reading. Say instead:
        *DeepL is absent from the read chain by construction (I8)*.
  - [x] **Known accepted tradeoff, do not "fix" it here:** `:239` still throws the write cache away on a key
        save. The shared store that fixes it is **E4** (`architecture-cible.md` §8.1: *"`Translate.cs:239` no
        longer throws the cache away"* — that sentence describes the state **after E4**, not after this story).
        `project-context.md` already lists the sibling tradeoff as accepted.
- [x] **T6 — The I8 test, named so it cannot be deleted by accident (AC 3, DoD).**
      `tests/PWRUHelper.Tests/ChainCompositionTests.cs`, headless, no window.
  - [x] **TP-CHN-14** — `DeepL_is_structurally_absent_from_the_read_chain`: build the read chain over **every
        permutation of the settings that exist**, plus the ones that do not yet (set them by reflection or leave
        a `// E6/E8 permutations added here` marker with the loop already written over an array), and assert **no
        tier's translator is a `DeepLTranslator`**. Reflect over the built `ChainTier[]`, not over source text.
  - [x] The name is the mitigation. `epics.md`'s DoD: *"the I8 test exists and is named so a future reader cannot
        delete it by accident."* Add a comment saying what I8 costs if it goes: DeepL's free plan is a one-time
        1 M characters **in total**, and the read path would drain it in ~5 days of a heavy user
        (`benchmark…` §5.3).
  - [x] **Tier order** — assert both chains' `ProviderId` sequences equal the expected arrays, so a reordering is
        a failing test rather than a silent change of which vendor gets the traffic.
  - [x] **Priority** — assert the read chain's providers carry `Background` and the write chain's `Interactive`.
        This is the only automated proof that E2.S3's reserve is no longer inert.
- [x] **T7 — Do not touch `_restoringSettings`.** No control is added or persisted by this story, so the
      re-entrancy flag (`MainWindow.xaml.cs:35`, cleared at `:201`) is not engaged. Both chain assignments happen
      **before** `InitializeComponent()` (`:83`) and therefore before any handler can fire.
  - [x] The one thing to check by hand: `ApplySettings()` (ending `:200-201`) must **not** rebuild a chain. If a
        future story wants a setting to change the chain, it goes through the E6 pattern (an explicit rebuild
        from the save handler, like `:239`), never through a change handler during a restore.
- [x] **T8 — Run the suite and TP-START-02.** Green, headless, no network, no `%AppData%` write. In particular
      `ProviderStateStoreTests.No_startup_path_mentions_ProviderGates` must be **untouched and passing**: the ctor
      names `TranslationChains`, never `ProviderGates`.

### Review Findings

_Code review (Amelia, 2026-09-07) over `f2bc874` vs `01b17af`. Verdict: **approve-with-fixes** — the three
patches below were applied in the review commit; the rest is recorded, not changed._

- [x] **[Review][Patch] The I10 pin was vacuous** [`tests/PWRUHelper.Tests/ChainCompositionTests.cs`] —
      `Building_the_chains_touches_no_gate_state_file` wrote `{"version":1,"providers":[]}`: an ARRAY where
      `ProviderStateStore` parses a MAP, so the file could not have parsed, and it named no provider to seed
      either. `Assert.Null(Snapshot(google-dict)?.BlockedUntil)` therefore could not have failed even if a
      builder HAD read the file — the one thing the case exists to catch. Rewritten to write a real pause
      (valid schema, 30 s out, inside `OpenCapMinutes` so the seed needs no clamp and queues no corrective
      write) and to prove non-vacuity by calling `EnsureLoaded()` afterwards: the gate is unpaused after the
      three builders run, and paused after the load. That is I10 asserted rather than assumed.
- [x] **[Review][Patch] E3-g had no pin for "nothing translated AND nothing failed"**
      [`tests/PWRUHelper.Tests/PerLineFallbackTests.cs`] — the blank-only group (a truncated OCR row, the
      input LIVE produces most often) and the empty group both leave `translatedSomething == false` with
      `lastFailure == null`. The code is right (`if (!translatedSomething && lastFailure is not null)`), but
      nothing stopped a later reader from "simplifying" the predicate to `!translatedSomething` and turning a
      page of blanks into a throw. `E3_g_a_blank_only_batch_returns_blanks_and_throws_nothing` pins both
      shapes, with a `translateOne` that fails the case if a blank ever reaches a provider.
- [x] **[Review][Patch] Comments still describing the field-initializer shape this story removed**
      [`Services/ProviderGates.cs:123,181`, `Services/ProviderGate.cs:308`, `Services/HttpProviderCore.cs:95`,
      `tests/PWRUHelper.Tests/ProviderGatesTests.cs:43`] — the commit updated the two class-level ones and
      missed the five inner ones, which are precisely the "why the load hangs off `TryEnter` and not off
      `For`" comments a future reader consults about I10. They now say "MainWindow's constructor (E3.S7)".
- [x] **[Review][Defer] Read-once still reports `Done — N line(s) translated.` after a total failure**
      [`MainWindow.Ocr.cs:243-244` + `:300-304`] — `TranslateSentencesInto` swallows the exception (`return;`)
      and the caller writes the unconditional `Done`. Pre-existing **F-8 / G3 / V1.5**, with its own story and
      its own five strings (`epics.md:1589`, `ux-mode-degrade.md` §3.3, TP-ONCE-01) — fixing it here would
      poach that story's ACs. E3-g does not make it worse in KIND (the status was already false when the tier
      returned a column of placeholders), but it is now reached by a second path: the rows themselves are
      honest either way (`(Friendly(ex))`).
- [x] **[Review][Defer] `ProviderStateStoreTests.cs:818-819`'s comment is now self-contradictory** — "a field
      initializer (MainWindow.xaml.cs:43) that runs inside the constructor". Left alone on purpose: the DoD
      asks TP-START-02's file to stay **untouched**, and a comment is not worth reopening that question.
- [x] **[Review][Defer] LIVE can now stop where it used to fill with apologies** — a chain whose LAST tier
      translated nothing throws instead of returning placeholders, so the tick counts as a hiccup, and five
      consecutive ones trip the A.0-accepted auto-stop (E1.S3's record). That is the honest behaviour E3-g was
      ruled for and the state E5 replaces with a paused loop; recorded so the release note can say it.
- [x] **[Review][Defer] `EveryPermutation()` varies one property at a time** — a future COMBINATION
      (`AzureApiKey` set AND `UseKeyForReading` true) is not swept. Harmless for I8 as it stands (`BuildRead`
      does not name `DeepLTranslator` at all, so no combination can reach it), and E6 should add the pair when
      it adds the two settings.

**Verified and left alone (the reviewer's focus list):** ctor ordering — `_settings` is a field initializer
(`:65`) and both read chains are assigned in the ctor BODY (`:106-109`), above `InitializeComponent()`
(`:110`); `_readTranslator`/`_readOnceTranslator` are `readonly` and assigned exactly once; `_restoringSettings`
untouched and `ApplySettings()` rebuilds no chain; `OnClosing`'s `ProviderGates.Flush()` is still the only
reference outside `Services/` (TP-START-02 green). Both `TranslateBodiesAsync` call sites pass the right chain,
`ct` still reaches the core, and I6/I7 (slang expansion, per-message source) are untouched. The builders read
`AppSettings` and construct — no disk, no network, no registry read (I10), and no reference to a member that
does not exist yet: Azure, Bergamot and Edge are comments, not reflection and not string keys.

## Dev Notes

### Invariants this story must keep

- **I1** — `ITranslator` unchanged. The per-instance priority (T2) exists *because* of it.
- **I2** — `TranslationChains` is in `Services/`: it reads `AppSettings` and constructs providers; it must not
  touch a WPF type, a dispatcher or a `MessageBox`.
- **I8 — DeepL is unreachable from the read path by construction.** The single most important line of this story,
  and the only one with a money consequence for the user.
- **I9** — one gate per provider, shared by both chains. Achieved by `ProviderGates.For(id)` returning the same
  instance, not by sharing provider objects.
- **I10** — nothing reads `provider-state.json` before first paint. `For` constructs; `TryEnter` loads; the first
  `TryEnter` happens inside `HttpProviderCore` on the first real request.
- **I16** — LIVE's behaviour is unchanged by this story. It gets a chain instead of a single provider and a
  `Background` priority; the loop itself is **E5**.

### Rulings that apply

- **R-3:** `ChainTranslator.LastOutcome` exists (E3.S3) and the **code-behind owns the chain instance**, which is
  what lets E7 read it. This story is what puts that instance in a field the code-behind can reach — but it wires
  **no UI**. Do not add a status chip here; that is E7.S3.
- **OQ-a:** read-once is `Interactive`. It is a user click. **It is wired at its call site in E5.S4**, not here —
  and note the tension this creates: read-once and LIVE share `_readTranslator`, whose providers this story
  builds with `Background`. E5.S4 is where that is resolved (most likely a second, `Interactive` read chain, or a
  priority the call can carry). **Record it; do not solve it here** — solving it needs E5's context.
- **E5-a:** LIVE honours short `Wait`s inside a tick; a `Background` caller needs its own fall-through when the
  delay exceeds the tick budget. That fall-through is **E5.S1's**, on the loop, not on the chain.
- **R-7 (setting names):** `AzureApiKey`, `AzureRegion`, `UseKeyForReading`, `OfflineFallbackEnabled`,
  `LastRunVersion` — architecture §12's names win. Use exactly these in the future-slot comments so E6 does not
  have to rename them.
- **Owner's decision:** `UseKeyForReading` defaults to **false** — a metered key must be opted into for an
  unmetered loop.

### Repo traps

- **Constructor ordering has produced bugs in this codebase, and this story is entirely about it.** Field
  initializers run in declaration order, before the ctor body: `_writeTranslator` (`:42`, uninitialised),
  `_readTranslator` (`:43`), `_updates` (`:49`), `_settings` (`:50`). Moving `_readTranslator` down the file
  instead of into the ctor would "work" and would be the wrong fix — AC 2 asks for the ctor, and the ctor is
  where a future reader will look.
- **`InitializeComponent()` fires change handlers** (`MainWindow.xaml.cs:30-35`'s comment: a `<Slider Value="70"
  ValueChanged=…/>` raises `ValueChanged` during `InitializeComponent`, long before `ApplySettings`). Both chain
  assignments must be **above** `:83`, and neither may depend on a control.
- **TP-START-02's `strays` assertion is an exact-equality assert on a one-element array.** Any new
  `ProviderGates` mention in `MainWindow.*` fails it with a message about startup paths. That is the guard doing
  its job; route through `TranslationChains`.
- **`HttpSeamGuardTests`' IS-10 guard** will hold every provider this story constructs. Constructing a provider
  with no handler is *production*, which is correct — the guard's rule is that a **test** may not do it.
- No `.sln`; `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and `DefaultItemExcludes` (`:18`) intact.

### Line-number drift

| The docs say | Actually |
|---|---|
| `MainWindow.xaml.cs:43,80-104` | **correct** — `:43` the field, `:80-104` the ctor, `:82` `BuildTranslator()`, `:83` `InitializeComponent()` |
| `MainWindow.Translate.cs:226-233,239` | **correct** — but by the time this story runs, E3.S3 has already rewritten `:229-232`'s body off `FallbackTranslator` |
| §8.1's ctor sketch (`_cacheStore`, two `CachingTranslator`s over one store) | **E4's**, not this story's. A.1 still has one cache per decorator — `epics.md` E3.S7's technical note says so explicitly |
| §8.1 "`Translate.cs:239` no longer throws the cache away" | **after E4.** In A.1 it still does; accepted |

## Testing

- **TP-IDs landed:** **TP-CHN-14** (I8 — the headline), plus the tier-order and priority assertions of T6 (the
  test plan's §8.1 row).
- **Fixtures:** none.
- **Isolation:** **IS-5** (`TranslationChains` resolves gates ⇒ the test class names `ProviderGates` indirectly;
  join the `Gates` collection and let `ProviderGatesTests`' scan confirm it) · IS-8/IS-10 (providers built in a
  test must take a handler — so `ChainCompositionTests` inspects the **built** chain without translating, or
  hands handlers in).
- **Must keep passing unchanged:** **`ProviderStateStoreTests`' TP-START-02 pair** (the whole point),
  `StartupSettingsTests`, `DefaultsAndResizeTests`, `TemplateRenderTests`, `LiveDefaultsTests`,
  `CachingTranslatorTests`, and E3.S3's `ChainTranslatorTests`.
- **A note on STA:** this story adds no `Run.Text` binding and no ItemTemplate, so `TemplateRenderTests` needs no
  new case — but it must still pass, because the ctor changed.

## File List

**Create**
- `Services/TranslationChains.cs` — `BuildRead(AppSettings)` / `BuildWrite(AppSettings)`

**Modify**
- `MainWindow.xaml.cs` — `:43` (field initializer removed, field stays `readonly`), `:82` and a new line in the
  ctor body before `:83`
- `MainWindow.Translate.cs` — `:222-233` (`BuildTranslator` → `BuildWriteChain`, delegating), `:239` (the call)

**Create (tests)**
- `tests/PWRUHelper.Tests/ChainCompositionTests.cs` — TP-CHN-14, tier order, priorities

## Size, dependencies, risk

**Size:** M · **Depends on:** E3.S3 (`ChainTranslator`, `ChainTranslator.Of`), E3.S4 (`GoogleDict`), E3.S6
(`GoogleGtx`); **E3.S5 if U2 landed** — if it did not, ship the two-tier free chain and say so in the release
note ·
**Risk:** **medium — constructor ordering has produced bugs in this codebase.** The second risk is silent: an I8
test that passes because it asserts the wrong thing (source text instead of the built chain) would let a later
story put DeepL on the read path and drain a user's one-time 1 M characters ·
**DoD:** the I8 test exists and is named so a future reader cannot delete it by accident; TP-START-02 untouched
and green.

## How to verify manually

1. `dotnet test tests/PWRUHelper.Tests` — green, TP-START-02 included.
2. Launch the app with **no** DeepL key: the Translator tab translates through `google-dict` (check the log's
   `provider=` on a forced failure, or the About tab's report).
3. Paste a DeepL key, save, translate: `provider=deepl`. Then use **read-once** on a screen region: the log must
   **never** show `provider=deepl`. That is I8, by hand.
4. Hand-edit `%AppData%\PWRUHelper\provider-state.json` to block `google-dict` into the future, restart,
   read-once: the translation still appears, and the log says `provider=edge` (or `google-gtx`), with **no**
   `tr` line for `google-dict`.
5. Block **every** free provider the same way and read-once: exactly **one** message, the `AllProvidersPaused`
   sentence, and **zero** `tr` lines.
6. Read the ctor: both chain assignments are above `InitializeComponent()`, and `_readTranslator` is still
   `readonly`.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E3.S7; Epic 3 header (invariant I8)]
- [Source: docs/investigations/02-traduction/architecture-cible.md#8.1 Composition, #8.2 The shared cache (what is E4's), #5.1 one gate per provider, #7.4 DeepL write-path only, #12 Settings]
- [Source: docs/investigations/README.md#Architect's rulings — R-3, R-7, OQ-a, E5-a, E2-e (the one permitted ProviderGates reference); #Phase 4 (E2.S5's record: Background did not reach the read chain)]
- [Source: docs/investigations/03-stories/test-plan.md#TP-CHN-14; #TP-START-02; #2 IS-5, IS-8, IS-10; #6 invariant I8]
- [Source: docs/investigations/02-traduction/benchmark-fournisseurs.md#5.3 (DeepL's one-time 1 M characters)]
- [Source: docs/investigations/03-stories/stories/E2.S4-persist-gate-state.md, E2.S5-http-provider-core.md (the lazy-load trigger and the priority deferral)]
- [Source: project-context.md#Framework-Specific (WPF) Rules — settings-restore re-entrancy; #Translation Pipeline Rules; #Critical Don't-Miss Rules (accepted tradeoffs)]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on Claude Opus 5 — non-interactive Phase-4 run, 2026-09-07.

### Debug Log References

- Baseline before any change: `dotnet test tests/PWRUHelper.Tests` → **705 passed, 0 failed, 2 s**.
- After the code change, before the pins were updated: 3 failures, all expected —
  `HttpProviderCoreTests.Every_await_on_the_request_path_configures_away_the_context` (the rewritten
  `PerLineFallback` class comment had dropped the word `HttpProviderCore`, which is how that scan
  DERIVES the request-path file set — the reference was restored as a real sentence about the
  `NotSent` flag) plus the two E3-f pins ruling E3-g deliberately overturns.
- Final: **715 passed, 0 failed, 2 s**. One pre-existing warning (`OcrEngineChoiceTests.cs:50`,
  xUnit1031) and no new one.

### Completion Notes List

**What shipped**

- `Services/TranslationChains.cs` — `BuildRead(AppSettings, RequestPriority = Background)` and
  `BuildWrite(AppSettings)`. The tier list is assembled in `Services/`, `ChainTranslator.Of` resolves
  the gates, and the code-behind never names `ProviderGates` (ruling E3-c / TP-START-02).
  - Read: `google-dict → google-gtx`. Write: `[deepl if key set] → google-dict → google-gtx`.
  - `google-dict` is now the **default first free tier** on both paths (the owner's decision 2 — the
    endpoint switch, coupled with the hardening E3.S4/S8 put in place); `google-gtx` is the last tier.
  - Edge is a commented tier line carrying `[UNKNOWN until U2]` in both builders (ruling E3-d);
    Azure and Bergamot are named, commented slots with architecture §12's setting names (R-7).
- `MainWindow.xaml.cs:56-58` — `_readTranslator` lost its field initializer and stays `readonly`;
  `_readOnceTranslator` joins it. Both are assigned in the ctor body (`:106-109`), above
  `InitializeComponent()` (`:110`) and therefore above any change handler, and after `_settings`
  (`:65`) has been initialised by its own field initializer — which is the whole of AC 2.
- `MainWindow.Translate.cs:233` — `BuildTranslator()` → `BuildWriteChain()`, a one-line delegation;
  still called from the key-save handler at `:242` (AC 4). The doc comment no longer claims the read
  side is "Google only" — it says DeepL is absent by construction (I8), which is what survives E6.
- `Services/GoogleGtxTranslator.cs:63,78,201` — the per-instance `RequestPriority` seam
  `GoogleDictTranslator` already had (E3.S4), so the read chain can be built `Background`.
- **E3-g** in `Services/PerLineFallback.cs:111,149,183` — the predicate widened from "every failure
  was a gate reason" to **"nothing was translated"**. `ExceptionDispatchInfo? lastFailure` +
  `bool translatedSomething` replace `gated`/`anythingElse`; a failure of ANY kind is captured, and a
  loop that translated no line rethrows the last one with its stack, `RetryAt` and `NotSent` intact.

**How the priorities reach the core.** `ITranslator` may not grow a parameter (I1), so the priority
is on the provider INSTANCE and therefore fixed at construction. The LIVE chain's providers are built
`Background`; read-once gets a **second chain instance** built `Interactive` (ruling OQ-a) over the
**same** process-global gates — the cheapest option that keeps I1, since a gate is shared by id and a
provider is a URL, a payload and a parser. `TranslateBodiesAsync` (`MainWindow.Live.cs:304`) now takes
the reader as a parameter; LIVE passes `_readTranslator` (`:275-276`), read-once passes
`_readOnceTranslator` (`MainWindow.Ocr.cs:299`).

**Deviation from this story's text, on the architect's Phase-4 instruction.** The story deferred
read-once's `Interactive` priority to E5.S4 ("record it; do not solve it here"). The Phase-4 brief
directed that it be solved here and named the two candidate shapes; the second-instance shape was
taken. E5.S4 therefore inherits a wired call site rather than an open question — what remains for it
is the LIVE loop's own fall-through (E5-a), not the priority.

**Two read caches, and why that is E4's to fix.** Each chain keeps its own `CachingTranslator`, so a
line LIVE translated is not served from cache to read-once and vice versa. That is the same accepted
A.1 tradeoff as `Translate.cs:242` throwing the write cache away on a key save: §8.2's single shared
store behind thin decorators is **E4**, and it collapses all three caches at once. Recorded rather
than fudged — this story adding a third cache raises E4's value, it does not change its scope.

**Not touched:** `_restoringSettings` (no control is added or persisted; both chain assignments run
before `InitializeComponent()`), `ApplySettings()` (it rebuilds no chain), `ITranslator` (I1),
`DeepLTranslator` (write-path only, always `Interactive`), and
`ProviderStateStoreTests.No_startup_path_mentions_ProviderGates` (TP-START-02 — untouched and green).

### File List

**Created**
- `Services/TranslationChains.cs`
- `tests/PWRUHelper.Tests/ChainCompositionTests.cs`

**Modified**
- `MainWindow.xaml.cs` (`:37-58` the fields, `:97-109` the ctor body)
- `MainWindow.Translate.cs` (`:222-233` `BuildWriteChain`, `:239-242` the call from the key save)
- `MainWindow.Live.cs` (`:275-276` the LIVE call, `:295-305` `TranslateBodiesAsync`'s new parameter,
  `:324,329` the two batches)
- `MainWindow.Ocr.cs` (`:295-299` read-once passes the `Interactive` chain)
- `Services/GoogleGtxTranslator.cs` (`:57-63,71-79,196-201` the priority seam)
- `Services/PerLineFallback.cs` (E3-g: the class comment, `:107-111,149,159-183`)
- `Services/ProviderGate.cs`, `Services/ProviderGates.cs` (comments only: the chains are no longer in
  a field initializer, and `BuildTranslator()` is now `BuildWriteChain()`)
- `tests/PWRUHelper.Tests/PerLineFallbackTests.cs` (the two E3-f pins E3-g overturns, plus two new
  E3-g cases)
- `project-context.md` (the pipeline-shape line and the per-line-loop line)
- `docs/investigations/03-stories/sprint-status.yaml` (`e3-s7` → `review`)

### Tests

| ID | Case | What it stops |
|---|---|---|
| **TP-CHN-14** | `TP_CHN_14_DeepL_is_structurally_absent_from_the_read_chain` | I8. Sweeps every `AppSettings` permutation — including a reflection loop over every writable `string`/`bool` property, so E6's `AzureApiKey`/`UseKeyForReading` and E8's `OfflineFallbackEnabled` are covered the day they are declared — and asserts on the **built** `ChainTier[]`, never on source text. Non-vacuous: the same sweep finds DeepL on the write chain. |
| §8.1 order | `The_read_chain_is_google_dict_then_google_gtx`, `The_write_chain_puts_a_configured_key_first_and_the_free_tiers_behind_it` | A reordering (or a whitespace key counting as a key) is a failing test, not a silent change of which vendor gets the traffic. |
| §5.4 reserve | `The_read_chain_is_Background_read_once_and_the_write_chain_are_Interactive` | The structural half: the priority each chain's providers carry. |
| §5.4 reserve | `Background_stands_aside_for_the_reserve_where_Interactive_goes_straight_through` | The behavioural half, and the only automated proof E2.S3's reserve is no longer inert: after one `Interactive` request a `Background` caller is made to wait for a full bucket while a second `Interactive` one is not. Asserted on the **requested** delay (IS-7), nothing sleeps (CI-3). |
| I9 | `Both_chains_share_one_gate_per_provider` | Three chains, one gate per id, by reference — and the provider objects are deliberately NOT shared. |
| I10 | `Building_the_chains_touches_no_gate_state_file` | The ctor's new work reads no `provider-state.json` before first paint. |
| Wiring | `LIVE_reads_through_the_Background_chain_and_read_once_through_the_Interactive_one` | The two call sites, plus `readonly`, plus "above `InitializeComponent()`". |
| E3-g | `E3_g_soft_failures_on_every_line_now_throw_instead_of_returning_placeholders` (was `E3_f_..._still_return_placeholders`) | A tier whose every line failed softly throws instead of handing the chain a list of apologies it reads as a success. |
| E3-g | `E3_g_a_soft_failure_that_closes_the_gate_now_throws_instead_of_reading_as_a_success` (was `A_soft_failure_..._still_does_not_throw`) | The exact hole E3.S8's review found: one timeout + N `NotSent` refusals. Throws the LAST failure — the refusal, `NotSent` and `RetryAt` intact. |
| E3-g | `E3_g_one_translated_line_is_enough_to_keep_the_placeholders`, `E3_g_end_to_end_a_tier_that_translated_nothing_lets_the_chain_use_the_next_one` | The off switch (a partial failure still returns a list) and the point of the ruling (the next tier is really asked). |

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **ready-for-dev (Phase 4 — owner's go given)**. Scope reduced to the tiers that exist in A.1 (Azure and Bergamot are E6/E8), with the composition moved into `Services/TranslationChains.cs` so TP-START-02's single-reference rule survives. |
| 2026-09-07 | Implemented (Amelia DS). Both chains built by `TranslationChains`; `google-dict` promoted to default first free tier, `google-gtx` last, DeepL write-only by construction; read chain `Background` and read-once `Interactive` by construction; ruling **E3-g** widened `PerLineFallback`'s throw and its two pins were re-pointed. 705 → **715 tests**, all green. Status → **review**. |
| 2026-09-07 | Code review (Amelia CR): **approve-with-fixes**. Three patches applied — the I10 pin was vacuous (an array where the store parses a map) and now proves both halves; a blank-only/empty batch pin for E3-g; five stale field-initializer comments. Four items recorded (read-once's false `Done` is F-8/E7's, TP-START-02's own comment left untouched, LIVE's hiccup on a fully-failed chain, the one-property-at-a-time permutation sweep). 715 → **716 tests**, green, 2 s; Release build 0 warnings. Status → **done**. |
