# Story E6.S4: "Use my key for screen reading" opt-in, off by default

Status: **review**

_Epic E6 (F) — Bring your own key: Azure · increment 5 · **Release B.1** · the owner's Phase-4 go was given
2026-09-06 (`README.md` § Phase 4)._

> **One checkbox, and the whole story is about the word "not".** The AC that carries the risk is AC 1:
> `UseKeyForReading` off ⇒ Azure is **not constructed into the read chain — asserted by a test on the built
> chain, not by a runtime guard.** That is the same shape as I8's DeepL rule and it is deliberate: a runtime
> guard is a line someone can move; a builder that never constructs the tier cannot be talked into it by a
> setting, a refactor or a future story.
>
> **Why it is off by default is arithmetic, not caution.** `benchmark…` §8.1 puts a heavy LIVE user at ~48 k
> characters per hour of busy chat; F0 is 2 M/month. The LIVE loop translates **every new chat line it sees**,
> unattended, for as long as the app is open. Risk **R-15** in the test plan states the failure exactly:
> *"a user's Azure F0 quota drained by LIVE because the opt-in arrived pre-ticked or defaulted on."* Ruling
> **OQ-12** puts it in one line: *an opt-in that arrives pre-ticked is not an opt-in.*

## Story

As a player with a free monthly quota,
I want to decide whether the screen reader may spend it,
So that a free allowance cannot disappear in three evenings without my having chosen it.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E6.S4. Do not weaken._

1. **Given** `UseKeyForReading` defaults to **false** (§12)
   **When** a key is saved but the box is unticked
   **Then** Azure is **not constructed into the read chain** — asserted by a test on the built chain, not by a
   runtime guard.
2. **Given** the box is ticked
   **When** the handler runs
   **Then** the read chain is rebuilt immediately, exactly as `DeepLSaveKey_Click` rebuilds the write chain
   (`MainWindow.Translate.cs:239`), and the checkbox obeys `_restoringSettings` (I12).
3. **Given** UX-DR15 and the arithmetic of `ux-mode-degrade.md` §4.3
   **When** the option is rendered
   **Then** it carries its cost sentence: *"Azure gives you 2 million characters a month for free — roughly 20 to
   40 hours of busy chat. Screen reading is off by default because live mode reads every new line and can use it
   up in a few evenings."* — subject to E6.S1's answer, and re-derived if the shared cache measurably changes the
   multiplier.
4. **Given** OQ-12 (resolved by John): an opt-in that arrives pre-ticked is not an opt-in
   **When** an existing user upgrades with a key already saved
   **Then** the box is off.
5. **Given** R9
   **When** Azure returns `QuotaExhausted`
   **Then** the gate opens for an hour rather than retrying, and the chain falls to the free tiers.

## Tasks / Subtasks

- [x] **T1 — The read-chain tier (AC 1).** `Services/TranslationChains.cs:162-176` — the comment at `:162-164` is
      the slot and it already states the rule:
      *"[Azure — E6.S4: only if AzureApiKey is set AND UseKeyForReading is on. … `UseKeyForReading` defaults to
      FALSE — a metered key must be opted into for an unmetered loop.]"*
  ```
  var azure = (settings.AzureApiKey ?? "").Trim();
  var azureRegion = (settings.AzureRegion ?? "").Trim();
  if (settings.UseKeyForReading && azure.Length > 0 && azureRegion.Length > 0)
      tiers.Add((ProviderIds.Azure, new AzureTranslator(azure, azureRegion, priority: priority)));
  ```
  - [x] **Position: FIRST**, ahead of `GoogleDict` (§8.1's read path). A key the user has opted in is the tier
        they are paying attention to.
  - [x] **`priority: priority`** — the read chain is built twice, `Background` for LIVE and `Interactive` for
        read-once (`MainWindow.xaml.cs:137-138`). Forgetting it gives the LIVE loop `Interactive`, which lets a
        screen loop take the last token of the ceiling's capacity-2 bucket and the half-open probe — §5.4's
        reserve exists precisely to stop that. `GoogleDictTranslator.cs:65-71` states the rule; E6.S2's ctor
        exposes the parameter for this line.
  - [x] **All three conditions.** Region included: a key without one is a guaranteed 401 (§12), and putting a
        guaranteed-401 tier *first* in the LIVE chain would open its gate on the first tick of every session.
  - [x] **Do not add a runtime guard inside `AzureTranslator`.** AC 1 is explicit. The absence is structural.
- [x] **T2 — The checkbox (AC 2, AC 3, I12).** `MainWindow.xaml`, in the About tab under the Azure key row that
      E6.S3 added:
  - [x] `<CheckBox x:Name="AzureForReadingCheck" Checked="AzureForReading_Changed" Unchecked="AzureForReading_Changed" .../>`
        — **both** events. A `CheckBox` raises `Checked` and `Unchecked` separately; wiring only one is how a
        tick persists and an untick does not.
  - [x] Content: `Use my Azure key for screen reading too (uses your free quota faster)` — `ux` §4.2's mockup.
  - [x] The cost sentence (AC 3) as a wrapped `TextBlock` **under** the box, `TextMutedBrush`, indented — `ux`
        §4.2's layout. **The string lives in `Services/UserMessages.cs`** (GAP-4 / UX hint 11), not inline in
        XAML, so the "exists exactly once" scan can see it and so E6.S1's verdict changes **one** literal.
  - [x] **No new brush, font or colour** (UX-DR18).
- [x] **T3 — The handler (AC 2, I12).** `MainWindow.Translate.cs`, beside E6.S3's:
  ```
  private void AzureForReading_Changed(object sender, RoutedEventArgs e)
  {
      if (_restoringSettings) return;          // I12 — FIRST line, always
      _settings.UseKeyForReading = AzureForReadingCheck.IsChecked == true;
      SettingsService.Save(_settings);
      RebuildReadChains();                     // E6.S3's seam
      UpdateEngineStatusUi();
  }
  ```
  - [x] **`if (_restoringSettings) return;` is the first line.** XAML loading raises `Checked` during
        `InitializeComponent()` (`MainWindow.xaml.cs:30-35`), before `ApplySettings` runs. `SquadUppercaseCheck`
        is the existing checkbox to copy.
  - [x] `IsChecked == true`, never `IsChecked.Value` — `bool?`.
  - [x] **`RebuildReadChains()` reassigns all three**: `_readTranslator`, `_readOnceTranslator` **and**
        `_readChain` (`MainWindow.xaml.cs:137-138`). E6.S3 lands the helper; if it has not, land it here. Missing
        `_readChain` leaves E5.S1's `PauseNow()` on a stale chain that resolves the same gates (I9) and therefore
        still *works* — which is why it would ship unnoticed.
  - [x] **The cache survives the rebuild** (E4.S4): `TranslationChains.Cache` is the class's, not the chain's.
        Do not clear it.
- [x] **T4 — `ApplySettings` (AC 4, I12).** `MainWindow.xaml.cs`, with E6.S3's Azure restore around `:241-249`:
  - [x] `AzureForReadingCheck.IsChecked = s.UseKeyForReading;`
  - [x] The side effect is applied **explicitly** by `UpdateEngineStatusUi()` (already called at E6.S3's
        `:249` placement) — never left to the suppressed handler. **TP-SET-07.**
  - [x] **AC 4 needs no code**, and that is the point worth stating in a comment: `UseKeyForReading` is a **new**
        field with default `false`, so an old `settings.json` from a user who already has an Azure key
        deserialises it to `false`. No `Migrate` step, no bump (**AC 2 of E6.S3**, ruling OQ-e / R-5). The way to
        break AC 4 is to "helpfully" seed it from `AzureApiKey.Length > 0` — do not.
- [x] **T5 — AC 5 is verification, not implementation.** `QuotaExhausted` already opens the gate for **60
      minutes** — `TranslationPolicy`'s quota window, honoured by `ProviderGate` (ruling E2-f: `Retry-After` is a
      floor within the kind's own ceiling; E2-h deliberately discards a 30-day `Retry-After` on the quota row in
      favour of 60-minute windows with probes). `ChainTranslator` then **skips the tier while
      `BlockedUntil > Now()`** (ruling E3-a) and falls through to `GoogleDict`. **Assert it end-to-end on the
      built read chain** (a fake handler returning Azure's 403 quota envelope ⇒ the next tier answers, and the
      second call sends **zero** Azure requests). Write no new production code for AC 5; if you find yourself
      needing to, something upstream regressed.
- [x] **T6 — Re-derive AC 3's "20 to 40 hours" before shipping the sentence.** `ux` §4.3 derives it and then
      says, in as many words: *"If Winston merges the `ru`/`auto` groups and shares the cache, the multiplier
      drops and this sentence must be re-derived, not left to rot."*
      **E4.S4 shipped the shared cache** (one store behind all three chains), which removes the "two caches"
      term of the ×1.3–2.0 multiplier; the `ru`/`auto` merge was **evaluated and rejected with arithmetic**
      (README, Phase 2 closure), so that term stands. Re-run the arithmetic, record it in a comment beside the
      string with its inputs, and **write the range you actually get**. If it lands outside 20–40 h, the sentence
      changes — a promise that is merely inherited is the thing §4.3 warns against.
      **And it is subject to E6.S1 (U4):** if F0 turns out to be trial-limited, the first half of the sentence is
      rewritten before this ships. See E6.S1's copy table.
- [x] **T7 — Tests (TP-SET-04, TP-SET-08, plus the `EveryPermutation` pair).**
- [x] **T8 — `dotnet test tests/PWRUHelper.Tests`.** Green, headless, no network, no `%AppData%` write.

## Dev Notes

### Invariants this story must keep

- **I8 — DeepL is never in the read chain**, and this story is the first one that puts *anything* keyed there.
  Read `TranslationChains.cs:15-21` before touching `BuildRead`: the class comment is the invariant's home, and
  it already anticipates this story — *"E6 lands Azure-for-reading behind an opt-in, and the invariant that
  survives it is the one about DeepL"* (`MainWindow.Translate.cs:227-230`). **Update neither comment to say
  "no keys on the read path"; both are already worded correctly.**
- **I12** — the checkbox's handler bails on `_restoringSettings`; `ApplySettings` applies the side effect
  explicitly.
- **I13** — no default changes for an existing user, so no `Migrate` step and no bump. AC 4 is delivered *by*
  that fact.
- **I9** — one gate per provider, shared by both chains. The read chain's `AzureTranslator` and the write
  chain's are two objects over **one** `azure` gate. That is right, and it has a consequence worth knowing: a
  `QuotaExhausted` earned by the Translator tab also pauses the read path, which is exactly what a
  *quota* means.
- **I1** — `ITranslator` unchanged; priority is a ctor parameter.

### Rulings that apply

- **OQ-12** (John) — the opt-in stays off for a migrating user with a key already saved. AC 4.
- **R-7** — the setting is `UseKeyForReading`. `ux` §4.3's `AzureForReading` is the rejected spelling; the
  **control** may still be named `AzureForReadingCheck` (that is a control name, not a setting name) — keeping
  it makes `ux` §4.3's table row findable.
- **E3-a** — the chain skips a tier on `BlockedUntil > Now()`, never on `State == Open`. AC 5 rides on it.
- **E2-f / E2-h** — the quota window is 60 minutes with probes; a 30-day `Retry-After` is deliberately
  discarded. AC 5's "opens for an hour rather than retrying" is already true.
- **E4-a** — the shared cache is provider-agnostic: with the opt-in **off**, a line the Translator tab
  translated **through Azure** can still be served to the read path from the cache. That is accepted (§8.2
  trade-off) and it does **not** violate AC 1: *"I8 is about **requests**, not cached strings"*
  (`TranslationChains.cs:180-187`). Worth knowing before someone reports it as a bug.
- **GAP-4** — the cost sentence lives in `Services/UserMessages.cs`.

### Repo traps

- **`_readTranslator` / `_readOnceTranslator` are `readonly` today** (`MainWindow.xaml.cs:60-61`). E6.S3 drops
  it. If E6.S4 is implemented first for any reason, it drops it — but then E6.S3's AC 6 has nothing to reuse.
  **Keep the stated order.**
- **The read chain is built TWICE** (`:137-138`) with two different priorities. Both need the Azure tier and
  both get it automatically from `BuildRead` — but only if T1 passes `priority` through. A test that builds only
  the default (`Background`) chain will not catch the omission; sweep both, as `TP_CHN_14` does (`:162`).
- **`CheckBox` `Checked`/`Unchecked` are two events.** `SquadUppercaseCheck` is the shape already in the file.
- **`ApplySettings` is one `try`/`finally`** (`:212-257`); put the new line with the other Azure restores.
- **`ChainCompositionTests` scans files outside `Services/`** for `new CachingTranslator` and for
  `ProviderGates`. `RebuildReadChains` names `TranslationChains` only — keep it that way.
- No `.sln`; `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and `DefaultItemExcludes` (`:18`) intact.

### Line-number drift

| The docs say | Actually (this commit, after E5.S4) |
|---|---|
| epics AC 2: "exactly as `DeepLSaveKey_Click` rebuilds the write chain (`MainWindow.Translate.cs:239`)" | the rebuild is at **`:246`**; `DeepLSaveKey_Click` is `:238-251` |
| `ux` §4.3: `AzureForReadingCheck` … "rebuilds the read chain immediately, like `Translate.cs:239`" | same shift; and **the read chain has no rebuild seam** until E6.S3 lands one |
| epics AC 3 quota sentence | **subject to E6.S1 (U4)** for the "2 million … free" half **and** to T6's re-derivation for the "20 to 40 hours" half — §4.3's multiplier assumed two caches, E4.S4 shipped one |
| epics "**Depends on:** E6.S3, E3.S7" | correct; E3.S7 is **done** and is what gave `BuildRead` its `priority` parameter and its two instances |
| `TranslationChains.cs` Azure read slot | **`:162-164`**, already commented with this story's number and its rule |

## Testing

- **TP-IDs landed:** **TP-SET-08** — *"Azure key set, `UseKeyForReading` **off** ⇒ Azure is **not** a tier of the
  built read chain; **on** ⇒ it is the **first** tier"* (oracle: chain composition; maps R-15, story 29) — and
  **TP-SET-04** (fresh install ⇒ `UseKeyForReading == false`).
- **AC 1 is asserted on the BUILT CHAIN**, through `ChainCompositionTests`' existing `TiersOf`/`IdsOf` helpers —
  not through a behaviour test with a fake handler, and not through a source scan. Sweep **both** priorities.
- **`ChainCompositionTests.EveryPermutation()` (`:112-139`) must gain the Azure pair.** It varies **one**
  property per permutation by reflection, so `AzureApiKey = "set"` **and** `UseKeyForReading = true` — the only
  combination that puts Azure on the read path — is a shape it structurally cannot generate. Add the pair
  explicitly to the seed list at `:114-120`, beside the four `DeepLApiKey` shapes, **with a region**, and add its
  negatives: key+region without the tick, tick without a key, tick with a key but no region. That last one is
  the guaranteed-401-first-in-the-LIVE-chain case.
  TP-CHN-14 (I8) then sweeps the pair too, for free, which is the second reason to put it there.
- **AC 4** — a load-only case: a `TempSettings` file containing `AzureApiKey` and **no** `UseKeyForReading` key
  at all (an old file) loads with `UseKeyForReading == false`, `SettingsVersion` unchanged, and the built read
  chain has no Azure tier.
- **AC 2 / I12** — **TP-SET-05**'s shape extended to this control: a saved `"UseKeyForReading": true` survives a
  load → apply → save cycle with no user interaction (UX hint 8). And **TP-SET-06**: the handler entered during
  `ApplySettings` returns before writing.
- **AC 5** — L2 over the built read chain with the opt-in **on**: the fake handler returns Azure's
  `azure-error-quota.json` 403 envelope; the call is served by the next tier, the Azure gate's `BlockedUntil` is
  ~60 min out, and a second call issues **zero** Azure requests. Use a **local** `ProviderGate` with an injected
  clock (IS-6) so the file needs no `Gates` collection and **nothing sleeps** (CI-3) — `ReadOnceStatusTests` is
  the precedent.
- **Isolation:** IS-1/IS-3 (`TempSettings`) for the settings cases; IS-6 (injected clock) and IS-8/IS-10/IS-11
  (handler seam, recording handler) for AC 5; IS-9's `azure-error-quota.json` (E6.S2 creates it). CI-1, CI-3.
- **Must keep passing unchanged:** `TP_CHN_14_DeepL_is_structurally_absent_from_the_read_chain` — this is the
  story that puts a **keyed** provider on the read path, so that test stops being about "no keys there" and
  becomes about DeepL specifically. It already is, by construction and by name. Read it before you touch
  `BuildRead`. Also: `LivePauseTests`, `ReadOnceStatusTests`, `StartupSettingsTests`, `TemplateRenderTests`.

## File List

**Modify**
- `Services/TranslationChains.cs` — the Azure tier in `BuildRead` (`:162-164`'s slot), gated on
  `UseKeyForReading && key && region`, **first**, with `priority:` threaded
- `Services/UserMessages.cs` — the cost sentence (AC 3), re-derived per T6
- `MainWindow.xaml` — `AzureForReadingCheck` + the cost `TextBlock`, under the Azure key row
- `MainWindow.xaml.cs` — `ApplySettings`: `AzureForReadingCheck.IsChecked = s.UseKeyForReading;`
- `MainWindow.Translate.cs` — `AzureForReading_Changed`; `UpdateEngineStatusUi` reflects the opt-in

**Modify (tests)**
- `tests/PWRUHelper.Tests/ChainCompositionTests.cs` — **the Azure pair in `EveryPermutation()`** + TP-SET-08
- `tests/PWRUHelper.Tests/AzureSettingsTests.cs` (E6.S3's file) — AC 4's old-file case, TP-SET-05/06 for this
  control, AC 5's quota fall-through

## Size, dependencies, risk

**Size:** S · **Depends on:** **E6.S3** (the settings fields, the `RebuildReadChains` seam, the About-tab block)
and **E3.S7** (`BuildRead`'s `priority` parameter and its two instances — done) · **Risk:** **low-medium.** The
code is a conditional `tiers.Add` and a handler. The three ways to get it wrong are all silent: forgetting
`priority:` (the LIVE loop quietly gains `Interactive` and eats the reserve §5.4 exists to protect), forgetting
the region in the condition (a guaranteed-401 tier first in the LIVE chain, opening its own gate on tick one),
and seeding the checkbox from "a key exists" (AC 4 gone, R-15 realised) ·
**DoD:** the read chain test passes both ways.

## How to verify manually

1. `dotnet test tests/PWRUHelper.Tests` — green.
2. **AC 4, the migration case.** Take a `settings.json` with an Azure key and no `UseKeyForReading` key. Start:
   the box is **off**.
3. **AC 1.** With the key saved and the box **off**, run LIVE over a chat region and watch the diagnostic log:
   **no `tr` line names `azure`.** Tick the box: the next tick's line does.
4. **AC 2.** Tick, close, reopen: still ticked, and the read chain still starts at Azure — the setting persisted
   *and* the restore did not clobber it (that is the v0.12.3 class).
5. **AC 5.** Force Azure's 403 quota envelope: the feed keeps filling from Google with no error text anywhere
   (UX hint 5 — a successful fallback is silent), and no Azure request is sent again for an hour.
6. **The reserve.** With the box on, start LIVE and then press Translate on the Translator tab: the interactive
   translation is not starved by the loop. (§5.4's reserve — this is what `priority:` buys.)

## References

- [Source: docs/investigations/03-stories/epics.md#Story E6.S4; Epic 6 header (I8)]
- [Source: docs/investigations/02-traduction/architecture-cible.md#8.1 Composition (the read path); #12 Settings; #5.4 the interactive reserve; #15.2 R9]
- [Source: docs/investigations/02-traduction/ux-mode-degrade.md#4.2 ASCII mockup; #4.3 Control-by-control specification and the quota hint arithmetic; #5 flow (c).5]
- [Source: docs/investigations/README.md#Architect's rulings — R-7, E3-a, E2-f, E2-h, E4-a, GAP-4; #Owner's decisions (Azure as a second key slot)]
- [Source: docs/investigations/03-stories/test-plan.md#3.9 TP-SET-04/05/06/08; #1.1 R-15; #2 IS-6/IS-8/IS-10/IS-11]
- [Source: docs/investigations/02-traduction/plan-migration.md#Increment 5]
- [Source: docs/investigations/03-stories/stories/E3.S7-build-both-chains-once.md; E6.S3-azure-key-and-region-settings.md]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) — Claude Opus 5, CS / Create-Story, non-interactive.
Implementation (DS): Amelia — Claude Opus 5, non-interactive, bundled with rulings **E6-d** and **E6-e**.

### Debug Log References

- Baseline `dotnet test tests/PWRUHelper.Tests`: **991 passed, 0 failed, 3 s** (HEAD `3fb23ab`).
- Red wave 1 (chain composition + cache + DeepL, written before the production change): **7 failed**
  — `TP_SET_08_azure_leads_the_read_chain_only_when_the_opt_in_is_on`,
  `The_azure_read_tier_carries_the_priority_the_chain_was_built_with`,
  `The_read_and_write_azure_tiers_are_two_objects_over_one_gate`,
  `A_miscounted_inner_batch_is_a_BadResponse_and_is_never_padded` (×2 rows),
  `A_single_line_answer_that_is_not_one_line_is_a_BadResponse` (×2 rows). Green after the change.
- **Deviation from strict red-green, declared:** the wave-2 (WPF) cases were written *after* the
  XAML/handler change rather than before it — a test naming `AzureForReadingCheck` cannot compile
  until the control exists, so "red" would have been a build error and proved nothing about the
  assert. Non-vacuity was bought back by **mutation** instead, run and recorded:
  removing `RebuildReadChains();` from `AzureForReading_Changed` fails
  `AC2_Ticking_the_box_persists_rebuilds_the_read_chains_and_updates_the_status`; disabling the
  E6-e clearing block fails `E6e_Saving_an_empty_key_clears_the_region_too_and_removes_the_engine`.
  Both restored by hand and the suite re-run green.
- Final: **1011 passed, 0 failed, 3 s**, headless, no network, no `%AppData%` write.

### Completion Notes List

1. **AC 1 — the tier is structural.** `Services/TranslationChains.cs:186` — `BuildRead` adds
   `(ProviderIds.Azure, new AzureTranslator(key, region, priority: priority))` **first**, and only
   when `AzureReadsTheScreen(settings)` (`:328`) says yes: `UseKeyForReading` **and** a sendable
   key **and** a sendable region. No runtime guard anywhere; with the box unticked the provider is
   never constructed. The tier list is now `AddRange`d after the conditional `Add`, which is the
   only structural change to the method.
2. **One predicate, two readers.** `AzureReadsTheScreen` is `internal static` on
   `TranslationChains` and is what the builder itself branches on — `UpdateEngineStatusUi` asks the
   same method rather than re-deriving the rule from three fields, so the status line and the read
   chain's first tier cannot disagree. Same shape as `IsSendableAzureCredential` shared between
   `BuildWrite` and the Save button in E6.S3.
3. **`priority:` is threaded** and pinned separately
   (`The_azure_read_tier_carries_the_priority_the_chain_was_built_with`): the LIVE instance is
   `Background`, read-once `Interactive`, write `Interactive`. This is the silent failure the story
   flagged, and reflection over `_priority` is the only way to see it.
4. **AC 4 needed no code, and the comment says so** (`MainWindow.xaml.cs:265-271`): `UseKeyForReading`
   is a new field with default `false`, so an old `settings.json` with a key deserialises to off.
   No `Migrate` step, **no `SettingsVersion` bump** (I13). `AC4_An_old_file_with_a_key_and_no_opt_in…`
   asserts the file, the settings object, the checkbox and the predicate together.
5. **AC 3 / T6 — the hours figure was re-derived, and the sentence survives it.** 5.8 M chars over
   120 h ⇒ ≈48 k/h of busy chat; F0 2 M/month ⇒ **41 h at ×1.0**. §8.1's ×1.3–2.0 multiplier had
   three terms — the `ru`/`auto` split, retries + uncached placeholders, and **two caches billing
   the same string twice**. E4.S4 removed the third; the `ru`/`auto` merge was evaluated and
   rejected with arithmetic (README, Phase 2 closure), so the dominant term stands. The
   double-billing term was the smallest anyway (it only ever billed twice what BOTH paths
   translated, and a LIVE-heavy user writes a fraction of what they read): low end ×1.3 → ≈×1.2,
   high end unchanged. **41 ÷ 2.0 … 41 ÷ 1.2 = ≈21–35 h**, inside the 20–40 the copy promises — so
   `UserMessages.AzureForReadingHint()` ships §4.2's sentence unchanged, with the whole derivation
   and its inputs in the doc comment beside it (`Services/UserMessages.cs:351`). Still subject to
   **E6.S1 (U4)** for the "2 million … free" half; that is one literal.
6. **AC 5 needed no production code** and is asserted end-to-end on the read chain's shape with the
   opt-in on: Azure's 403 quota envelope ⇒ the next tier answers, `BlockedUntil` is exactly
   `QuotaOpenMinutes` (60) out, and 30 minutes later the same chain issues **zero** Azure requests
   (ruling E3-a). Local `ProviderGate`s with an injected clock (IS-6), so nothing sleeps and the
   file still needs no `Gates` collection.
7. **Ruling E6-d, three edits.** `CachingTranslator.TranslateLinesAsync` no longer splices
   `j < fresh.Count ? fresh[j] : missLines[j]` — a short **or** long inner list is a `BadResponse`
   thrown before anything is stored (`Services/CachingTranslator.cs:89`); both awaits carry
   `ConfigureAwait(false)`; `DeepLTranslator.TranslateAsync`'s `outp.Count > 0 ? outp[0] : ""` is
   now `outp.Count != 1 ⇒ BadResponse` (`:102`) — padding with a **blank** was the worse half of the
   two, because an empty string is not a `(`-prefixed placeholder, so the chain counted the tier as
   a success and the cache stored "" for the session. The scan floor gained `CachingTranslator.cs`
   through a **second derivation arm** (`decorators`), not a hand-written name: it consults no
   provider, so the "names `HttpProviderCore`" arm structurally cannot find it, and since E4.S4 it
   is the outermost await of every translation the app makes.
8. **Ruling E6-e.** An empty key is no longer half a pair: `AzureCredentialProblem` returns `null`
   for it and `AzureSaveKey_Click` clears the region with it, empties the region box under
   `_restoringSettings` (so the combo's own handler cannot persist the old key with no region
   half-way through the gesture) and refreshes the status. `UserMessages.AzureNeedsAKey()` is
   **deleted** — it answered a case that no longer exists, and E6.S3's review had already recorded
   it as a dead end. The `AC5_Only_a_complete_or_an_empty_pair_is_accepted` row for
   `("", "westeurope")` flips from refused to accepted, deliberately.
9. **`EveryPermutation()` gained the triple by hand** plus its three negatives (key+region without
   the tick, tick without a key, tick + key with **no region** — the guaranteed-401-first case), so
   TP-CHN-14 now sweeps the one settings shape the read chain has never had before, for free.
10. **Not changed, deliberately:** `TranslationChains`' I8 class comment and
    `MainWindow.Translate.cs`'s `BuildWriteChain` comment — both were already worded for this story
    ("the invariant that survives it is the one about DeepL") and neither may be rewritten to say
    "no keys on the read path", which is now false and was never the invariant.
    `architecture-cible.md` §8.1/§12 still trail the code on the `key AND region` gate; E6-e records
    that as noted-not-rewritten. No `SettingsVersion` bump, no new NuGet, no `.sln`,
    `InternalsVisibleTo` / `DefaultItemExcludes` intact.

### File List

**Modified (app)**
- `Services/TranslationChains.cs` — the Azure tier first in `BuildRead` (`:186`); new
  `AzureReadsTheScreen` predicate (`:328`); `AzureCredentialProblem` accepts an empty key (E6-e)
- `Services/CachingTranslator.cs` — E6-d: never pads a mis-counted batch; `ConfigureAwait(false)`
- `Services/DeepLTranslator.cs` — E6-d: a single-line answer that is not one line is `BadResponse`
- `Services/UserMessages.cs` — `AzureForReadingHint()` (with the re-derivation),
  `AzureKeySetForReadingStatus(region)`; `AzureNeedsAKey()` removed (E6-e)
- `MainWindow.xaml` — `AzureForReadingCheck` + the `AzureForReadingHint` block under the Azure row
- `MainWindow.xaml.cs` — `ApplySettings` restores the tick; the `_restoringSettings` handler list
- `MainWindow.Translate.cs` — `AzureForReading_Changed`; E6-e in `AzureSaveKey_Click`;
  `UpdateEngineStatusUi` reflects the read chain's first tier and drives the box + hint

**Modified (tests)**
- `tests/PWRUHelper.Tests/ChainCompositionTests.cs` — the Azure triple + three negatives in
  `EveryPermutation()`; TP-SET-08; the priority case; the one-gate case
- `tests/PWRUHelper.Tests/AzureSettingsTests.cs` — AC 4, AC 2, AC 3's hint, the disabled state,
  E6-e, AC 5's quota fall-through; TP-SET-06 extended to both handlers; the AC 5 theory row flipped
- `tests/PWRUHelper.Tests/StartupSettingsTests.cs` — TP-SET-05 now asserts the restored tick
- `tests/PWRUHelper.Tests/CachingTranslatorTests.cs` — E6-d, both drifts + the cached-batch case
- `tests/PWRUHelper.Tests/TranslationBackendTests.cs` — E6-d's single-line rows
- `tests/PWRUHelper.Tests/HttpProviderCoreTests.cs` — the `decorators` derivation arm and the floor

**Modified (tracking)**
- `docs/investigations/03-stories/sprint-status.yaml` — `e6-s4` → `review`
- this story file

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | **Implemented (Amelia DS), bundled with rulings E6-d and E6-e.** `BuildRead` gains the Azure tier **first** iff `UseKeyForReading` **and** a sendable key **and** a sendable region, with `priority:` threaded — asserted on the BUILT chain at both priorities (TP-SET-08), never by a runtime guard. One predicate, `TranslationChains.AzureReadsTheScreen`, is what the builder branches on **and** what the status line asks, so the About tab cannot claim a tier the builder did not construct. `AzureForReadingCheck` + the quota hint land in the About tab (`_restoringSettings` first line, both `Checked` and `Unchecked`, disabled with no credential); a toggle rebuilds the three READ references only and leaves `_writeTranslator` alone. **AC 4 needed no code** — new field, default false, no `Migrate`, no bump (I13). **T6 re-derived the hours**: 41 h at ×1.0, multiplier now ≈×1.2–2.0 with E4.S4's one shared cache (the `ru`/`auto` term stands, rejected with arithmetic) ⇒ **≈21–35 h**, inside 20–40, so §4.2's sentence ships unchanged with the derivation in the doc comment. **E6-d:** `CachingTranslator` never pads a mis-counted batch (a `BadResponse` before anything is stored), `ConfigureAwait(false)` on both awaits, and the scan floor reaches it through a second **derivation arm** rather than a typed name; DeepL's single-line blank padding aligned. **E6-e:** an empty key clears the region too, `AzureNeedsAKey()` deleted, the AC 5 theory row flipped. `EveryPermutation()` gained the key+region+opt-in triple and its three negatives, so TP-CHN-14 sweeps them for free. Tests **991 → 1011**, green in 3 s, headless. One declared deviation: the WPF cases were written after the control existed (a red there is a build error) and their non-vacuity was bought back by two recorded mutation runs. Status → **review**. |
| 2026-09-07 | Story file created (Amelia CS). Status **ready-for-dev (Phase 4 — owner's go given)**. Records: **`ChainCompositionTests.EveryPermutation()` varies one property at a time**, so the key+opt-in pair AC 1 turns on is a shape it structurally cannot generate — the pair (and its three negatives) must be added by hand, which also gives TP-CHN-14 the sweep for free; the read tier must carry **`priority:`** or the LIVE loop silently gains `Interactive` and spends §5.4's reserve; the condition must include the **region** or a guaranteed-401 tier leads the LIVE chain; and AC 4 is delivered by I13's *inaction* (new field, default false, no `Migrate` step) — the way to break it is to seed the box from "a key exists". Adds T6: §4.3's 20–40 h multiplier assumed two caches and **E4.S4 shipped one**, so the sentence is re-derived here, not inherited. |
