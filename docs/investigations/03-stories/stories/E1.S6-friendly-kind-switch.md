# Story E1.S6: `Friendly()` renders by `Kind`, from `Services/UserMessages.cs`

Status: **done**

> **⚠ Scope changed after this file was written — read this before the ACs.** As written below, this story was
> "map the switch onto `Kind`, change no wording, and leave `Services/UserMessages.cs` to E7.S1". Winston's
> Phase-4 direction (given with the owner's go, and grounded in E1.S3's review finding that *"E1.S6 rewords
> against a different baseline than recorded"*) **inverts two of those three**: GAP-4's
> `Services/UserMessages.cs` is created **here**, and this **is** the story that changes the user-visible
> wording, adopting Sally's copy deck (`ux-mode-degrade.md` §3.1) for the kinds that exist today.
> AC 2 is unchanged and met. **AC 1 and AC 3 are deliberately superseded** — the before/after table that
> replaces AC 3's empty diff is in the Dev Agent Record below, with the reasons.

_Epic E1 (A) — Diagnosable translation pipeline · increment 0 · Release A.0 · ⛔ no production code until the
owner's explicit go. **This is the last story of the increment** — it is the mitigation for E1.S3's risk._

## Story

As a player,
I want this increment to be invisible,
So that a refactor cannot be blamed for a message I have never seen before.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E1.S6. Do not weaken._

1. **Given** `MainWindow.xaml.cs:404-410` builds its message from exception types
   **When** it becomes a `Kind` switch in the shape of §4.4
   **Then** each `Kind` returns **one of today's existing strings**, chosen so that every string a user can
   currently reach is still reachable, and **no new string is introduced in this increment**.
2. **Given** the parenthesised feed-row wrapper `$"({Friendly(ex)})"` at `MainWindow.Live.cs:281` and
   `MainWindow.Ocr.cs:298`
   **When** this story lands
   **Then** those two call sites are untouched.
3. **Given** a diff of the user-visible strings before and after
   **When** it is taken
   **Then** it is empty.

## Tasks / Subtasks

- [x] **T1 — `Friendly` renders by `Kind`** (AC: 1, as superseded) — `MainWindow.xaml.cs:403-420`
  - [x] Today: `TranslationException te => te.Message`, `HttpRequestException => "no Internet connection"`,
        `TaskCanceledException => "the request timed out"`, `_ => ex.Message`.
  - [x] After: `internal static string Friendly(Exception ex) => UserMessages.For(ex);` — the §4.4 shape lives in
        `Services/UserMessages.cs` (`For`), which keeps the four arms in the same order and the `_ => ex.Message`
        default exactly as it was. The two raw-exception arms are **kept, and re-pointed** at the same sentences
        as their typed twins (`Network`, `Timeout`), so a dead network reads identically whether or not the
        pipeline classified it on the way up.
  - [x] ~~Every arm returns a string that exists in the product today~~ — **superseded.** Nine arms now return
        Sally's copy-deck sentence for their `Kind`. `Unknown` keeps §4.4's `te.Message` pass-through, which is
        the one arm that still returns today's string (and the only user-visible string left carrying an HTTP
        code — H10 is E7's scan). Provider `Message`s are unchanged and are now the **log's** account.
  - [x] The cancel `Kind` is unreachable by contract (E1.S2) and the lookup stays **total** without an
        `ArgumentOutOfRangeException` arm: `UserMessages.Sentence` ends in `_ => null`, so that `Kind`, `Unknown`
        and any future member all fall back to the exception's own message. The default arm is also what keeps
        TP-MAP-17 green — production source may not even *name* the banned token, which an explicit arm would.
  - [x] `AllProvidersPaused` (nothing raises it until E2.S3/E3.S3) has a sentence and an
        `E7.S1 replaces this` marker naming what replaces it: §3.1's countdown form, which cannot be written
        here because `{t}` is formatting and formatting stays out of `Services/` (I2).
- [x] **T2 — Do not touch the two feed-row call sites** (AC: 2)
  - [x] `MainWindow.Live.cs:281` and `MainWindow.Ocr.cs:298` both do
        `foreach (var it in items) it.TranslationBody = $"({Friendly(ex)})";`. **Unchanged.** The parentheses are
        load-bearing twice over: they are what the owner's screenshot shows (`README.md`, owner's answer (b)), and
        the `(`-prefix is the **I4** rule that keeps failures out of the cache (`CachingTranslator.cs:82-85`).
  - [x] The other four call sites — `MainWindow.Translate.cs:41`, `:106`, `MainWindow.Live.cs:235`, `:238`,
        `MainWindow.Ocr.cs:248` — are also untouched by this story. **All six call sites are byte-identical to
        the baseline**; only what `Friendly` returns changed. Pinned by
        `The_feed_rows_still_wrap_the_sentence_in_parentheses`.
- [x] **T3 — Make it testable without restructuring `MainWindow`** (AC: 1)
  - [x] `Friendly` is now `internal static` — the `InternalsVisibleTo` route (`PWRUHelper.csproj:28`), so the
        suite calls it headlessly exactly as `DefaultsAndResizeTests` already calls `MainWindow.LiveIntervalMs`.
        **Nothing in `MainWindow` was restructured**: the method kept its place among the shared helpers and lost
        its body to a one-line delegation. No MVVM, no view-model, no new type in the code-behind.
  - [x] Kept a **pure function of `Kind`** — no settings read, no timer, no `RetryAt` formatting. `{countdown}`
        is rendered from `RetryAt` **at display time by `MainWindow`**, never by `Services/` (I2, §4.4), and that
        arrives in E7.S1/E7.S2. Pinned by `The_table_is_a_static_internal_type_with_no_UI_dependency`, which
        scans the file's code (not its comments) for `System.Windows`, `DispatcherTimer`, `MainWindow`, `RetryAt`.
- [x] **T4 — ~~The empty-diff proof~~ → the recorded before/after table** (AC: 3, superseded)
  - [x] A unit case per `Kind` asserting the returned sentence, listed explicitly so a future edit to the wording
        fails here first (`Friendly_renders_one_fixed_sentence_per_kind`, nine cases). This half survives intact:
        the wording is pinned literally, only against the new deck rather than the old one.
  - [x] ~~The empty diff~~ — **superseded**, and it could not have been empty in any case: E1.S3's review already
        recorded three rendered sentences that changed at `3ec06be` without a string literal changing. Replaced by
        the **before → after table** in the Dev Agent Record, which is what the PR description pastes.

## Dev Notes

### Invariants this story must keep

- **I4** — the `(`-prefix contract. `Friendly` returns the *inner* sentence; the parentheses are added by the two
  feed-row call sites, and `CachingTranslator.IsCacheable` (`CachingTranslator.cs:82-85`) refuses to cache anything
  starting with `(`. Never return a string that already starts with `(`, and never remove the wrapper.
- **I2** — no formatting logic moves into `Services/`.
- **I14** — no new copy path; the WPF `Clipboard` ban is untouched (all clipboard writes stay in
  `Services/ClipboardService`).
- **I16** — the LIVE loop is untouched.

### Rulings and decisions that apply

- **GAP-4 ruling (binding):** the copy deck becomes **`Services/UserMessages.cs`**, a UI-free static class holding
  every user-facing sentence keyed by error kind / state; tests assert on it and XAML/code-behind reads it.
  **That file is E7.S1's to create, not this story's** — increment 0 introduces no new string and no new file the
  copy has to move through twice. What this story owes E7.S1 is that the switch is a **pure function of `Kind`
  with one sentence per arm**, so the later move is mechanical (sprint-plan note 3).
- `architecture-cible.md` §4.4's `«Sally: …»` placeholders are **not** to be written into the code here. They are
  Sally's copy, landing in **E7.S1** together with the countdown.
- This story is deliberately last in the increment: it is the mitigation for E1.S3's "a mis-classification changes
  a user-visible string by accident" risk. If T4's diff is not empty, **E1.S3 has a bug** — fix the classification,
  not the wording.

### Repo traps

- `MainWindow.xaml.cs` is the core partial (fields, ctor, lifecycle, hotkeys, shared helpers). `Friendly` lives
  there with the other shared helpers; leave it there.
- Do not touch `_restoringSettings` (`MainWindow.xaml.cs:35`, cleared at `:201`) or `ApplySettings` — this story
  persists nothing and adds no control.
- `dotnet test tests/PWRUHelper.Tests` green and headless. **No `.sln`.** `InternalsVisibleTo` and the `tests\**`
  `DefaultItemExcludes` intact.

### Current state of the code being modified

```csharp
// MainWindow.xaml.cs:403-410 at the baseline
/// <summary>Turn an exception into a short, non-technical message for the user.</summary>
private static string Friendly(Exception ex) => ex switch
{
    TranslationException te => te.Message,
    System.Net.Http.HttpRequestException => "no Internet connection",
    TaskCanceledException => "the request timed out",
    _ => ex.Message,
};
```

## Testing

- **TP-IDs landed:** none of the numbered cases target this switch directly. `test-plan.md` covers the wording from
  **E7.S1** onward (H10/H11 → GAP-4's source scan over the copy-deck table). This story adds its own per-`Kind`
  case and the empty-diff proof, and ~~it must leave **TP-MAP-16**'s pair (403 ⇒ `Blocked`, 400/404 ⇒ `Unknown`)
  producing today's sentences~~ — **superseded with AC 1/AC 3** (found in review, and the last sentence written
  against the old scope that had not been struck through). TP-MAP-16's *pair of `Kind`s* is untouched and still
  green; what changed is what those `Kind`s render. A 403 now shows `Blocked`'s copy-deck sentence, not today's
  `Translation service error (HTTP 403)…`; only `Unknown` (400/404) still renders today's sentence, which is
  §4.4's pass-through.
- **Fixtures:** none.
- **Isolation items:** none.
- **Must keep passing unchanged:** `TemplateRenderTests` (2 STA cases — the feed templates are untouched, and this
  story adds no `Run.Text` binding, so **I15** is not engaged), and the whole existing suite.

## File List

_Written against the original scope; **the authoritative list is the Dev Agent Record's**, below. Kept here with
the corrections marked, because the difference is the scope change itself._

**Modify**
- `MainWindow.xaml.cs` (`:404-410` only; visibility of `Friendly` if the `internal` route is taken)
- _also, per the superseding direction:_ `Services/UserMessages.cs` (**created** — GAP-4's table),
  `Services/TranslationService.cs` and `Services/DeepLTranslator.cs` (comments; plus the one-word
  `Settings` → `About` correction found in review).

**Create (tests)**
- ~~`tests/PWRUHelper.Tests/FriendlyMessageTests.cs`~~ → `tests/PWRUHelper.Tests/UserMessagesTests.cs` (the file is
  named for the table it pins, not for the code-behind helper that delegates to it).

**Explicitly NOT modified**
- `MainWindow.Live.cs:281`, `MainWindow.Ocr.cs:298` (AC 2), and the four other `Friendly` call sites.

## Size, dependencies, risk

**Size:** S · **Depends on:** E1.S2 · **Blocked by:** the owner's Phase-4 go · **Risk:** low — this is the
mitigation for E1.S3's risk.
**DoD:** ~~no user-visible string changed, verified by diff~~ → **every** user-visible failure sentence
changed deliberately, one per `Kind`, recorded in the before/after table and pinned by a test.

## How to verify manually

Run the app and reach as many failure messages as you can without the network: Translator tab with no connection
(`Failed: …`), a wrong DeepL key, a LIVE tick with the network down (a parenthesised feed row). Compare each
sentence against the **after** column of the table in the Dev Agent Record — not against v0.14.0, which is now
the *before* column. Three things must still hold whatever the sentence says: the feed rows keep their
parentheses, a Stop still says nothing at all beyond "Live stopped.", and no sentence names an HTTP code except
the `Unknown` pass-through (a 400/404 from Google).

## References

- [Source: docs/investigations/03-stories/epics.md#Story E1.S6]
- [Source: docs/investigations/02-traduction/architecture-cible.md#4.4 Friendly() — one message per Kind]
- [Source: docs/investigations/README.md#Architect's rulings — GAP-4 (Services/UserMessages.cs)]
- [Source: docs/investigations/02-traduction/ux-mode-degrade.md#3.1 (the copy that lands in E7.S1)]
- [Source: project-context.md#Code Layout; No MVVM]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on `claude-opus-5[1m]` — non-interactive DS run, worktree
`.claude/worktrees/p4-code`, branch `feature/p2-a0-testable-foundation`, baseline `76f6f7c`.

### Debug Log References

| Run | Command | Result |
|---|---|---|
| baseline | `dotnet test tests/PWRUHelper.Tests` | **382 passed, 0 failed**, 5 s |
| red | same, after adding `UserMessagesTests.cs` | build error `CS0117: 'MainWindow' does not contain a definition for 'Friendly'` × 6 — the visibility change is the story's first failing test |
| red 2 | same, after `UserMessages.cs` + the delegation | 1 failed: `The_table_is_a_static_internal_type_with_no_UI_dependency` — the I2 scan read the file's own doc comments, which name `System.Windows` while promising not to use it. Scan narrowed to code lines. |
| after | `dotnet test tests/PWRUHelper.Tests` | **403 passed, 0 failed**, 5 s (+21 new cases) |

No network, no `%AppData%`, no new NuGet, no `.sln`. `InternalsVisibleTo` and the `tests\**`
`DefaultItemExcludes` untouched. The two STA `TemplateRenderTests` cases pass unchanged — no template and no
`Run.Text` binding was touched (I15 not engaged).

### Completion Notes List

**What landed.** `Friendly` is now one line — `internal static string Friendly(Exception ex) => UserMessages.For(ex);`
(`MainWindow.xaml.cs:420`) — and the §4.4 switch lives in the new `Services/UserMessages.cs`: nine `const`
sentences, `Sentence(TranslationErrorKind) → string?` and `For(Exception) → string`. The four arms of the old
switch keep their order and their default; what changed is that a `TranslationException` is rendered from its
`Kind` instead of from whichever sentence the throw site happened to carry.

**Why the scope is not the one written above.** Winston's Phase-4 direction, on the back of E1.S3's review
finding *"E1.S6 rewords against a different baseline than recorded"*. Two facts made the original plan
unbuyable: (1) the empty diff AC 3 asks for was already impossible — `3ec06be` changed three rendered sentences
without changing a literal, so "no user-visible string changed" could not be honestly claimed either way; and
(2) keeping `te.Message` as the arm for most kinds means one dead network still reads three ways
("Couldn't reach the translation service…", "Couldn't reach DeepL…", "no Internet connection") depending on
which provider noticed it first, which is the exact defect the `Kind` vocabulary exists to end. Doing the
reword here also means GAP-4's table exists before E2 starts adding states to it, instead of after.

**Deviations from Sally's §3.1 deck.** _D1–D3 as written at implementation; D4 added in review; D1's statement
corrected in review and D5 added there — the review found that three sentences deviate in ways D1–D3 did not
cover, and Winston's ruling is that D1/D2 stand for A.0 with E7.S1 restoring the full forms. **Sally owns
D5**: it is the only open copy question on this story._

- **D1 — no `{P}`.** ~~Every sentence says "the translation service" instead of naming the engine.~~ **Corrected
  in review:** seven of the nine say "the translation service"; the two key-bearing ones do not, because the
  substitution does not survive contact with English. `AuthFailed` says "Your **API** key was refused" (not "Your
  the-translation-service key") and `QuotaExhausted` says "Your **free translation** quota" (not "Your {P} free
  quota"). The rule D1 actually enforces is the one that matters — **no sentence names an engine** — and all nine
  keep it. Naming the
  answering provider needs `ChainTranslator.LastOutcome` (E4/E6/E7); guessing it here would be the same class
  of lie the epic is retiring. `QuotaExhausted` and `AuthFailed` therefore lose the word "DeepL" they had — the
  About tab holds exactly one key today, so nothing is ambiguous, and E6 (Azure) is what makes `{P}` necessary
  rather than decorative.
- **D2 — no "— trying another engine".** `Friendly` is only ever reached **after the whole attempt has already
  failed**; promising a retry that is not happening breaks §1 principle 4 ("honest status only"). Sally's
  first clause is kept verbatim and the trailing promise becomes "— try again shortly". When the sentence moves
  to the status line of a live chain (E7.S1), the promise becomes true and should come back.
- **D3 — no `{t}`.** No countdown: `RetryAt` is formatted by `MainWindow` at display time (I2, §4.4), and
  there is no gate to count down from until E2. `AllProvidersPaused` gets the no-countdown placeholder and an
  `E7.S1 replaces this` marker; nothing raises that `Kind` yet. **Corrected in review:** `{t}` is in three of
  §3.1's rows, not one — `RateLimited` ("paused for {t}") and `Blocked` ("paused for {t}") lose it too, and each
  resolved differently: `RateLimited` → "— try again in a moment", `Blocked` → the clause dropped entirely for
  "right now". Same reason, three outcomes; see D5.
- **D4 — no terminal full stop (added in review, Winston's ruling 2).** §3.1 writes its sentences terminated
  because it writes them for E7.S1's **standalone** status lines. Not one of today's six call sites renders the
  sentence alone — all six *join* it — so with the deck's own stop the app shipped
  `Live stopped after repeated errors (…try again shortly.).` (a double stop),
  `(The translation service is down right now — try again shortly.)` (a stop inside an inline parenthetical) and
  `⚠ …shortly. — your text is kept, press Enter to retry.` (a stop before a continuing clause). AC 2 freezes two
  of the six joins, so the sentence is the only place the fix could live. The stop comes back in E7.S1, together
  with §3.3's "lower-cased at the join" rule for the joins that story rewrites. Pinned by
  `Every_surface_reads_as_a_sentence_once_the_wrapper_is_applied`, which composes all six wrappers × all nine
  sentences — the class of check that was missing, since nine cases pinned the bare sentence and nothing pinned
  what a player actually reads.
- **D5 — three clauses changed beyond D1–D3 (recorded in review, Sally's call, not fixed).** Listed so the
  question reaches her rather than being settled by a reviewer:
  - `QuotaExhausted` — §3.1's `— using the free engines instead` is **deleted**, not substituted. D2's logic
    covers it (the fallback has already happened or already failed by the time this renders), but D2 names only
    "— trying another engine", and §1 principle 2 wants the "must you act?" answer written, not implied. The
    added "for this month" is imported from the retired DeepL literal; it is what both free tiers do.
  - `RateLimited` — "— try again in a moment" sits close to the one phrasing §3.1's own note bans ("never 'wait
    a minute': we now know the real number"). We do **not** know the number until E2's gate, so the sentence
    hedges instead of promising; D2's recorded substitution was "— try again shortly", and the inconsistency
    between the two is unintended.
  - `Blocked` — the only sentence of the nine that answers none of §1 principle 2's three questions: it says what
    happened, not when it ends nor whether the user must act. "Nothing you need to do" is a valid third answer
    and §1 says it must be written.

**Before → after, per surface.** The six call sites are byte-identical, so the wrappers below are unchanged and
only the inner sentence moved. Reachable paths at `76f6f7c`, verified by reading the throw sites, not by memory.

| `Kind` | Where it comes from | Before (v0.14.0 / `76f6f7c`) | After |
|---|---|---|---|
| `RateLimited` | Google 429 on the last attempt | `Google is limiting translations right now — wait a minute and try again.` | `The translation service asked us to slow down — try again in a moment` |
| | Google "automated queries" HTML page | `The translation service returned an unexpected response (it may be temporarily blocked). Try again shortly.` | *(same as above)* |
| | DeepL 429 | `DeepL is rate-limiting right now — try again shortly.` | *(same as above)* |
| `Blocked` | Google 403 (no key sent) | `Translation service error (HTTP 403). Please try again later.` | `The translation service is refusing requests from your connection right now` |
| | Google captcha / consent page | `The translation service returned an unexpected response (…). Try again shortly.` | *(same as above)* |
| `Unavailable` | Google 5xx after the retries | `Translation service is unavailable (HTTP 503). Try again shortly.` | `The translation service is down right now — try again shortly` |
| | DeepL 5xx | `DeepL service error (HTTP 503).` | *(same as above)* |
| `Timeout` | Google 12 s timeout | `the request timed out` | `The translation service took too long to answer — try again shortly` |
| | DeepL 12 s timeout | `DeepL timed out — check your connection or try again.` | *(same as above)* |
| `Network` | Google transport failure | `Couldn't reach the translation service. Check your Internet connection.` | `No internet connection — nothing can be translated until it is back` |
| | DeepL transport failure | `Couldn't reach DeepL. Check your Internet connection.` | *(same as above)* |
| `BadResponse` | Google body that is not JSON | `The translation service returned an unexpected response (it may be temporarily blocked). Try again shortly.` | `The translation service sent something we could not read — try again shortly` |
| | DeepL bad body / count mismatch | `DeepL returned an unexpected response.` | *(same as above)* |
| `QuotaExhausted` | DeepL 456 | `DeepL free quota is used up for this month.` | `Your free translation quota is used up for this month` |
| `AuthFailed` | DeepL 401/403 | `DeepL rejected the API key — check it in Settings.` | `Your API key was refused — check it in About, or clear it` |
| | ~~DeepL with an empty key~~ — **unreachable**, found in review: `BuildTranslator` (`Translate.cs:229-230`) only constructs a `DeepLTranslator` when `key.Length > 0`, so `DeepLTranslator.cs:83` cannot fire in the app | `No DeepL API key set.` | *(no user-visible change; the row's collapse would also have been wrong — "no key set" is not "key refused")* |
| `Unknown` | Google 400/404 | `Translation service error (HTTP 400). Please try again later.` | **unchanged** (§4.4 pass-through) |
| `AllProvidersPaused` | nothing, until E2.S3 | *(unreachable)* | `All engines are paused — nothing you need to do; it retries on its own` |
| *cancel* | a user Stop | *(never rendered)* | **unchanged — still nothing** |
| — | raw `HttpRequestException` | `no Internet connection` | `No internet connection — nothing can be translated until it is back` |
| — | raw `TaskCanceledException` | `the request timed out` | `The translation service took too long to answer — try again shortly` |
| — | anything else | `ex.Message` | **unchanged** |

Two user-visible corrections fall out of the deck rather than being aimed at: "check it in **Settings**" pointed
at a tab this app has never had (the DeepL key lives on **About**, `MainWindow.xaml:625`), and ~~every sentence but
the `Unknown` pass-through has stopped showing HTTP status codes to players~~ — **narrowed in review: every
sentence *this table renders*.** Two other user-visible paths still show a code, and neither is
`Friendly`'s: the `Unknown` pass-through (by §4.4's design), and `TranslationService`'s per-line fallback, which
writes `(translation failed: {ex.Message})` straight into a feed row as if it were a translation. See the review
findings. The `Settings` half was also only half-done at `8c8b56f` — the literal survived in
`DeepLTranslator.cs:133` and the log is pasted to Discord by design, so a human still read it; corrected in the
review fix.

**Wrappers, for reading the table in context** (all unchanged): Translator tab `Failed: {…}`
(`Translate.cs:106`) · quick reply `ReplyOutcome` (`Translate.cs:41`) · feed rows `({…})` (`Live.cs:281`,
`Ocr.cs:298`) · `OCR failed: {…}` (`Ocr.cs:248`) · `Live hiccup ({…}) — retrying…` (`Live.cs:238`) ·
`Live stopped after repeated errors ({…}).` (`Live.cs:235`).

**Comments retired because this commit made them false** (comment-only, no behaviour): three in
`TranslationService.cs` (`:216`, `:268`, `:305`) and one in `DeepLTranslator.cs` (`:125`) all said some version
of "the sentence is unchanged / E1.S6 owns the wording". They now say what is true — those literals are the
**log's** account of what the provider said, and are no longer what the player reads. That also defuses the
E1.S3 review's DeepL two-switch warning at the user's end: a `bodyHead`-driven `QuotaExhausted` can no longer
arrive on screen as "check the API key". It can still make a log line disagree with the `Kind` beside it, so the
warning is kept, narrowed, and still addressed to E6.

**Invariants.** I1 (`ITranslator` untouched) · I2 (`UserMessages` names no `System.Windows` type, no timer, no
`RetryAt` formatting — scanned by test) · I3 (no OCE catch touched) · I4 (no sentence starts with `(`, and the
two wrappers still add it — both scanned by test) · I11/I14 (no clipboard path, no settings, no
`_restoringSettings`) · I15 (no template, no `Run.Text`) · I16 (LIVE loop untouched). TP-MAP-17 green:
production source still never names the banned `Kind`, which is why `Sentence` ends in `_ => null` rather than
an explicit arm.

**Tests added (21).** `tests/PWRUHelper.Tests/UserMessagesTests.cs`:
`Every_kind_a_user_can_reach_has_a_non_empty_sentence` (enumerates `Enum.GetValues`, skips the cancel member
**by value** and `Unknown` with its §4.4 reason) · `Friendly_renders_one_fixed_sentence_per_kind` (9 cases, the
literal copy of this increment, asserted through both `UserMessages.For` and `MainWindow.Friendly`) ·
`Unknown_still_renders_the_exceptions_own_message` · `An_unmapped_kind_falls_back_instead_of_throwing` (a cast
`(TranslationErrorKind)9999` — totality without an `ArgumentOutOfRangeException`) ·
`The_raw_transport_exceptions_render_the_same_sentences_as_their_kinds` ·
`Any_other_exception_still_shows_its_own_message` · `No_sentence_starts_with_the_character_that_marks_a_failure`
(I4, plus a source pin on `CachingTranslator`'s guard) · `No_sentence_leaks_a_status_code_or_a_provider_internal`
(H10: no HTTP code, no `gtx`/`dict-chrome-ex`, no bare "error", no `!`, no unrendered `{P}`/`{t}`, ≤ 80 chars) ·
`Every_sentence_is_distinct` (H11) · `The_table_is_a_static_internal_type_with_no_UI_dependency` (I2) ·
`The_code_behind_holds_no_copy_of_its_own` (the ruling itself: the sentences exist in the table and nowhere
else) · `The_feed_rows_still_wrap_the_sentence_in_parentheses` (AC 2, both files).

**Left for the reviewer to weigh.** D1/D2 are wording judgements against Sally's deck and are the two places a
different reader could land differently — they are marked in the code, not buried. Sally should confirm the
no-`{P}` / no-promise forms before E7.S1 restores the full ones.

### Review Findings

_Code review of `8c8b56f` against baseline `76f6f7c` (Amelia — Blind Hunter, Edge Case Hunter and Acceptance
Auditor layers in parallel, with Winston's four rulings applied). **AC 2 holds**: the diff touches no `Friendly`
call site, both frozen ones are byte-identical to the baseline, and I2/I4/I11/I14/I15/I16 are untouched
(TP-MAP-17 green — `UserMessages.cs` does not name the banned token). D1 and D2 stand as Winston ruled; E7.S1
restores the full `{P}` and "trying another engine" forms. **The one finding all three layers raised
independently was the rendered result**: the deck was correct and every surface still read badly, because nine
cases pinned the bare sentence and nothing pinned a composed one. That is fixed and now pinned._

**Fixed in review**

- [x] [Review][Patch] **Every failure surface rendered ungrammatically** [`Services/UserMessages.cs`] — the deck's
      sentences are terminated for E7.S1's standalone status lines, but all six of today's call sites *join* them.
      Shipped output: `Live stopped after repeated errors (…try again shortly.).` (double stop),
      `(The translation service is down right now — try again shortly.)` (stop inside an inline parenthetical),
      `⚠ …shortly. — your text is kept, press Enter to retry.` (stop before a continuing clause). AC 2 freezes two
      of the six joins, so the sentence was the only place the fix could live: **terminal stop dropped from all
      nine**, recorded as **D4**. Feed rows now measure 55–78 characters, inside the 110 Winston set.
- [x] [Review][Test] **Nothing asserted on a composed string**
      [`tests/PWRUHelper.Tests/UserMessagesTests.cs`] — added
      `Every_surface_reads_as_a_sentence_once_the_wrapper_is_applied`: all six wrappers × all nine sentences, with
      the four punctuation rules and the feed row's 110-character budget. Mutation-checked (re-adding one stop
      fails it, plus two sibling cases).
- [x] [Review][Test] **Winston's ruling 4, pinned rather than read** [`UserMessagesTests.cs`] — `Unknown` hands the
      **provider's** message to the player, so that message may never carry the user's text or a key. Added
      `No_provider_message_the_player_can_read_carries_the_users_text_or_a_key`: nine failures driven through both
      providers with a sentinel Cyrillic text and a sentinel API key (non-retried statuses only, so it costs no
      backoff), asserting neither survives into `ex.Message` or into `Friendly`.
- [x] [Review][Test] **The reflection scan could go vacuous and never say so** [`UserMessagesTests.Sentences`] —
      `IsLiteral` sees only `const`, so a sentence added as `static readonly` (the natural shape once one is
      composed) or as a property escaped **all five** house-rule tests at once, and the `count >= 9` floor could
      never notice because the nine existing consts keep it green forever. The helper now asserts set-equality
      against what `Sentence(kind)` actually returns — it fails both if the scan stops seeing a sentence and if a
      const becomes unreachable through the lookup.
- [x] [Review][Patch] **The I4 rationale in the new file was a false premise** [`Services/UserMessages.cs`] — it
      claimed "a leading paren here would be a failure cached as a translation". Nothing this table returns is
      ever a translator's return value (every call site assigns to `TranslationBody`, a status `TextBlock` or
      `ReplyOutcome.Error`), so `CachingTranslator.IsCacheable` never sees it. The rule is kept — a leading paren
      renders `((…))`, and the deck must never mint something mistakable for a placeholder — with the blast radius
      stated correctly and the real `(`-strings pointed at.
- [x] [Review][Patch] **"check it in Settings" survived in the shipped binary**
      [`Services/DeepLTranslator.cs:133`] — the story bills the wrong-tab pointer as one of its two corrections,
      but only the copy-deck half landed; the provider literal still said `Settings`. It is the log's account now,
      and the log is pasted to Discord by design, so a human still reads it and still cannot find the tab. One
      word: `Settings` → `About`. No test pinned it.
- [x] [Review][Patch] **Two comments were wrong about their own code** — `TranslationService.cs:268` claimed
      "a 400 (`Unknown`) is the one case that still falls through to this text"; the branch is guarded only by
      `!(429 || >= 500)`, so 404 and every other unmapped non-transient status does too (only 401/403/456 get a
      named `Kind`). And `ProviderErrorMapperTests.cs:277` still said a DeepL timeout's literal is "the sentence
      `Friendly()` renders today — so nothing the user reads changes", which this very story made false. Both now
      say what is true. Also: `UserMessages`'s header said "two rules" above a list of three, and its 80-character
      note credited the compact overlay with a budget it does not have (the overlay's own line adds 46 characters
      of chrome, and §3.4 asks for ~60 in total — reachable only with E7.S1's short forms).
- [x] [Review][Patch] **The story contradicted itself in four places** — the Testing section still required
      TP-MAP-16's pair to produce "today's sentences" (superseded with AC 1/AC 3, and the last line written
      against the old scope that had not been struck); the File List still named `FriendlyMessageTests.cs`; the
      before/after table listed "DeepL with an empty key" as a reachable path when `BuildTranslator`
      (`Translate.cs:229-230`) only builds a `DeepLTranslator` behind a non-empty key; and "every sentence but the
      `Unknown` pass-through has stopped showing HTTP status codes" was true of this table and false of the app.
      D1's own statement was also inaccurate for two of the nine sentences. All corrected in place, and the
      after-column now matches the shipped strings.

**Recorded, not changed**

- **D5 — three clauses deviate from §3.1 beyond D1–D3, and they are Sally's call, not a reviewer's.**
  `QuotaExhausted` drops "— using the free engines instead"; `RateLimited`'s "— try again in a moment" sits close
  to the one phrasing §3.1's note bans and is inconsistent with D2's recorded "— try again shortly";
  `Blocked` is the only sentence answering none of §1 principle 2's three questions. Written up under the
  deviations above. **This is the one open question on the story.**
- **A user Stop still stamps the feed rows** — `MainWindow.Live.cs:277-283` writes `$"({Friendly(ex)})"` into every
  pending row *before* the `if (ct.IsCancellationRequested) return;` below it, so pressing Stop mid-batch paints
  "(The translation service took too long to answer — try again shortly)" over rows the user abandoned. This is
  §7 acceptance hint 1 and §2.1's "`Cancelled` must render **nothing at all**", it is **pre-existing** (the
  baseline painted "(the request timed out)" there), and the call site is AC-2 frozen. The fix is a
  `ct.IsCancellationRequested` check at the call site, not in the table — `Friendly` is a pure function and cannot
  see a token. **E7.S1 owns it** (it is one of the surfaces that story rewrites).
- **A bare `OperationCanceledException` falls to `_ => ex.Message`** and renders the CLR's own
  "The operation was canceled." — which is **localised** on a non-English Windows, in a deliberately English-only
  UI. `ct.ThrowIfCancellationRequested()` throws the base type, not `TaskCanceledException`, so the two siblings
  take two different arms. Not a regression (the baseline switch had the same two arms), and broadening the arm to
  `OperationCanceledException` would make a genuine cancel read as a timeout — the wrong trade while the guard is
  `when (ct.IsCancellationRequested)` at the call sites. Recorded for E7.S1 with the item above; they have the
  same fix.
- **Three user-visible failure strings still live outside the deck** —
  `TranslationService.cs:131,139,140,153` write `(skipped — rate-limited, try again shortly)`,
  `(rate-limited — try again shortly)` and `(translation failed: {ex.Message})` straight into a feed row as if
  they were translations. The middle one is produced by a `catch (TranslationException)` that fires for **every**
  `Kind`, so a dead network still reads "rate-limited" on the per-line OCR path — the exact defect the `Kind`
  vocabulary exists to end — and the last one can put an HTTP code on screen. They are *results*, not statuses:
  they start with `(` by design (I4's marker, and the one place `CachingTranslator.IsCacheable` really matters),
  and they never pass through `Friendly`. Folding them into the table is **E2.S5's `HttpProviderCore`**. Named in
  `UserMessages`'s header so the file does not read as a finished job.
- **A 401 on the keyless Google path renders "Your API key was refused"** to a user who has never typed one.
  §4.2 row 5 is `if (code == 401) return AuthFailed;` — unconditional on `keyWasSent`, unlike rows 6/7/8 for 403.
  Reachable through an authenticating proxy or a captive portal, and the read path is Google-only by design so it
  can never have a key. The fix belongs in the mapper rule, which is E1.S3's shipped contract and pinned by its
  cases; reopening it here would be a correct-course, not a review patch. Noted at `UserMessages.AuthFailed` and
  addressed to **E6**, where a second keyed provider makes the row worth reopening.
- **The Translator tab clips a long sentence with no ellipsis** — `TranslateStatus` (`MainWindow.xaml:278`) has no
  `TextWrapping` and no `TextTrimming`, and it sits in a horizontal `StackPanel`, which hands a child its full
  desired width and lets the Grid column clip the overflow; neither attribute would help without a layout change.
  `Failed: {sentence}` is now 60–85 characters where it used to be ~30, so the tail is silently cut at MinWidth
  460. **E7.S1 owns it**: §2.3 puts the provider chip in that same row, which forces the row to be re-laid-out
  anyway. Not fixed here — the sentences are Sally's length, and shortening them to fit a layout bug would be the
  wrong end.
- **The overlay quick reply is over §3.4's ~60-character budget.** `⚠ {sentence} — your text is kept, press Enter
  to retry.` runs 97–120 characters. Meeting §3.4 needs the per-surface **short forms** §3.4 specifies, which are
  E7.S1's; the wrapper is not the deck's to shrink. The `UserMessages` comment no longer claims the 80-character
  rule buys the overlay anything.
- **`Sentence(kind) ?? te.Message` only guards null**, so a future throw site passing `""` would render `()` in a
  feed row. Every throw site today passes a non-empty literal (pinned, as of this review, by the sentinel case),
  and there is no honest generic sentence to substitute — inventing one is what §4.4's pass-through exists to
  avoid. Left alone deliberately.
- **The I2 source scan strips whole comment lines only**, so a trailing `// … MainWindow` on a code line would
  fail it, and a UI type reached through a `using` alias would pass it. Both failure modes are safe-side or
  exotic; the scan errs towards a loud red, which is the right direction for a guard nobody watches.
- **The copy cases call `MainWindow.Friendly`, which runs `MainWindow`'s type initialiser.** Deliberate — the
  point is that what the *code-behind* renders comes from the table — and safe today (the three static fields are
  `Lang[]`, a `record struct` of two strings). If a WPF-typed static is ever added to `MainWindow`, these cases
  fail for a reason unrelated to copy; `UserMessages.For` is asserted alongside in every case, so the diagnosis
  is one line away.
- **`AllProvidersPaused`'s wording is pinned for a path no user can reach** until E2.S3. Kept: the lookup has to
  be total the day it *is* raised, and a sentence added under pressure later is exactly what GAP-4 exists to stop.

### File List

**Created**
- `Services/UserMessages.cs`
- `tests/PWRUHelper.Tests/UserMessagesTests.cs`

**Modified**
- `MainWindow.xaml.cs` (`Friendly` at `:403-420` — `private` → `internal`, body → `UserMessages.For(ex)`)
- `Services/TranslationService.cs` (comments only, `:216`, `:268`, `:305`)
- `Services/DeepLTranslator.cs` (comments only, `:125`; **plus, in review**, the one-word `Settings` → `About` at `:133`)
- `tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs` (**review**, one doc comment this story made false)
- `docs/investigations/03-stories/stories/E1.S6-friendly-kind-switch.md`
- `docs/investigations/03-stories/sprint-status.yaml` (`e1-s6-friendly-kind-switch` → `review`, then → `done`)

**Not modified** — the six `Friendly` call sites, every `Run.Text` template, `Theme.xaml`, `PWRUHelper.csproj`.

### Change Log

| Date | Change |
|---|---|
| 2026-09-06 | Code review of `8c8b56f` (three layers + Winston's four rulings). AC 2 holds and no finding invalidated an acceptance criterion. The rendered result was the one real defect — every sentence carried a full stop into six joins that never render it alone, producing `.).`, `(… shortly.)` and `. — your text is kept`; terminal stops dropped (**D4**) and the composed strings pinned for the first time. Also: ruling 4 pinned with a sentinel case, the reflection scan made non-vacuous by construction, the I4 rationale corrected, `Settings` → `About` in DeepL's literal, three false comments retired, and five story contradictions fixed. **D5** (three §3.1 clause changes) recorded for Sally. 403 → 405 tests green, 5 s. Status → `done`. |
| 2026-09-06 | E1.S6 implemented. `Friendly()` renders one sentence per `Kind` from the new `Services/UserMessages.cs` (ruling GAP-4). Scope superseded by Winston's Phase-4 direction: this story adopts Sally's copy deck instead of preserving today's wording; AC 1/AC 3 recorded as superseded, AC 2 met. 382 → 403 tests green. Status → `review`. |
