---
baseline_commit: d8df030
---

# Story E3.S3: `ChainTranslator` replacing `FallbackTranslator`

Status: **done**

_Epic E3 (C) — Provider chain and the new default endpoint · increment 2 · **Release A.1: E2 and E3 are separate
PRs but ONE release** · the owner's Phase-4 go was given 2026-09-06 ("go pour la Phase 4, ne t'arrête pas",
`README.md` § Phase 4)._

> **Release rule, non-negotiable.** E2 shipped a breaker in front of an endpoint that 429s on request #1. This
> story is the first half of the answer: a paused tier is *skipped*, not *fatal*. It must ship in the same release
> as E2 (`architecture-cible.md` R7, owner's decision 2).
>
> **This is the first story of E3 and nothing else in the epic can start before it.** E3.S4, E3.S5 and E3.S7 all
> depend on the chain existing.

## Story

As a player,
I want a paused provider to be skipped instead of called,
So that the app stops spending requests on servers that have already refused.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E3.S3. Do not weaken._

1. **Given** the algorithm of §6.2, identical for `TranslateAsync` and `TranslateLinesAsync`
   **When** a tier's gate returns `Open(retryAt)`
   **Then** the tier is skipped, its `retryAt` is remembered, and **no HTTP request and no measurable delay** are
   spent on it.
2. **Given** a tier's gate returns `Wait(t)`
   **When** `t <= MaxSpacingWaitMs`
   **Then** the chain awaits `t`; when `t` is larger, the tier is skipped with `now + t` remembered.
3. **Given** every tier was skipped
   **When** the chain finishes
   **Then** it throws exactly one `TranslationException(AllProvidersPaused, …)` whose `RetryAt` is the
   **earliest** remembered `retryAt` — not the latest, not the first tier's.
4. **Given** at least one tier actually tried and failed
   **When** the chain finishes
   **Then** it throws that tier's most specific failure, not `AllProvidersPaused`.
5. **Given** I3
   **When** a real user cancel occurs mid-chain
   **Then** the `OperationCanceledException` propagates (`catch … when (ct.IsCancellationRequested) throw;`), and
   a `TaskCanceledException` from an `HttpClient` timeout falls through to the next tier instead.
6. **Given** I1
   **When** `ChainTranslator` is introduced
   **Then** `ITranslator` is unchanged; `ChainTier` is `(ProviderGate Gate, ITranslator Translator)`;
   `Services/FallbackTranslator.cs` is deleted.

## The three collisions with the shipped E2 code, settled before the tasks

`epics.md` E3.S3 and `architecture-cible.md` §6 were written **before** E2.S5 landed `HttpProviderCore`. Three
things they assume are no longer true in this worktree. Each is settled here, with the reasoning, and each is
flagged for Winston in the Dev Agent Record.

### D-1 — the chain must **not** call `TryEnter`. `HttpProviderCore` already does.

§6.2's pseudocode opens with `decision = tier.Gate.TryEnter(priority)`. But since `b592800`, **every request
already consults the gate** — `HttpProviderCore.SendAsync` calls `AdmitAsync` before the first attempt
(`HttpProviderCore.cs:169`, `:317-339`) and reports the outcome exactly once after (`:363-375`). If the chain also
called `TryEnter`, two things break, both of them in the "permanently paused app" risk class (R-01):

| | What breaks |
|---|---|
| **The rate ceiling** | `BucketCapacity = 2`. Two `TryEnter`s per logical request halves the effective ceiling: the *second* request in a burst would find an empty bucket where today the third does. `TP-RET`'s ceiling case and E2.S3's whole reserve model assume one token per request. |
| **The half-open probe (E2-h)** | A `TryEnter` on an elapsed open window returns `Probe(token)` and **latches the gate half-open**. The chain has no way to report that token — `ITranslator` has no channel for it (I1) — so the probe would be granted to the chain, spent by the core's *second* `TryEnter` (which now sees `HalfOpen` and refuses), and never reported. The gate stays half-open until `ProbeTimeout`, and the tier is dead for the window. This is exactly the failure E2-h's probe token exists to prevent, re-introduced from above. |

**Ruling adopted here: the chain reads `Gate.Snapshot()`, never `TryEnter`.** `Snapshot()` is side-effect free by
contract (`ProviderGate.cs:718-723` — "no clock advanced, no probe taken, no token spent"), which is precisely
what a *skip decision* needs. Admission, waiting, the probe and the report stay inside `HttpProviderCore`, once.

**And the skip predicate is `BlockedUntil`, not `State`.** This is the single most dangerous line in the story.
`GateState.Open` deliberately covers the whole window **including the moment it has elapsed** — the gate becomes
`HalfOpen` only when a caller actually *takes* the probe (`ProviderGate.cs:734-738`, and the comment there says
so). A chain that skipped on `State == Open` would skip the tier *forever*: nobody would ever call `TryEnter`, so
nobody would ever take the probe, so the gate would never close. **R-01, shipped.** The correct predicate is:

```csharp
var snap = tier.Gate.Snapshot();
if (snap.BlockedUntil is { } until && until > tier.Gate.Now())
{ skipped.Add((tier.ProviderId, until)); continue; }      // skip: the window is still running
// window elapsed, or never opened → let the core's TryEnter decide (it may take the probe)
```

### D-2 — "skipped" and "tried and failed" must be distinguishable, and today they are not

AC 2's *"`Wait(t)` larger than `MaxSpacingWaitMs` ⇒ the tier is skipped with `now + t` remembered"* and AC 4's
*"a tier that really tried"* need the chain to tell the two apart. With D-1, a tier refused by the ceiling raises
from inside the core (`HttpProviderCore.cs:333` → `Paused(...)`) as an ordinary `TranslationException` carrying
the gate's `LastKind` — indistinguishable from a real 429 the provider actually received.

**Minimal mechanism adopted: `TranslationException` gains `internal bool NotSent { get; init; }`**, set only by
`HttpProviderCore.Paused()`. One bool, no public surface change, no new type. The chain then reads:

```csharp
catch (TranslationException tex) when (tex.NotSent) { skipped.Add((id, tex.RetryAt ?? now)); continue; }
catch (TranslationException tex) { lastFailure = tex; continue; }
```

That makes **TP-CHN-04** (all paused ⇒ `AllProvidersPaused` with the earliest `retryAt` and **zero** requests),
**TP-CHN-05** (a real 403 wins over `AllProvidersPaused`) and **TP-CHN-06** (short `Wait` awaited and the tier
used; long `Wait` ⇒ skipped, `now + t` in the set) all literally true, with the wait itself still owned by the
core — which is where the injectable delay (IS-7) and `TestBackoffRedirect` already live, so no test sleeps.

### D-3 — `ProviderGate` has **no** `Id`

§6.1 says *"the tier carries its gate … the gate already has `Id` for logging"*. It does not:
`ProviderGates.For` closes over the id precisely **because** "a gate does not know its own"
(`ProviderGates.cs:118-128`, comment verbatim). But R-3 requires `LastOutcome.ProviderId` and
`LastOutcome.Skipped: [(ProviderId, Reason)]`, and AC 1's per-tier `Logging.Warn` line wants a name too.

**Adopted: `ChainTier` is `(string ProviderId, ProviderGate Gate, ITranslator Translator)`.** AC 6 names the
two-member shape; the third member is a superset that makes R-3 implementable and is recorded as a deliberate
deviation. Do not add `Id` to `ProviderGate` — the registry is the only thing that knows an id, on purpose.

## Tasks / Subtasks

- [x] **T1 — `Services/ChainTranslator.cs`: the type (AC 6).**
  - [x] `internal readonly record struct ChainTier(string ProviderId, ProviderGate Gate, ITranslator Translator);`
        (see D-3).
  - [x] `public sealed class ChainTranslator : ITranslator` with
        `internal ChainTranslator(IReadOnlyList<ChainTier> tiers)`. `ITranslator` (`TranslationService.cs:14-19`)
        is **not touched** — assert it byte-identical as E1.S1 did.
  - [x] An empty tier list is a programming error, not a runtime state: throw `ArgumentException` in the ctor
        rather than inventing an `AllProvidersPaused` with no `retryAt` at call time.
- [x] **T2 — The algorithm, once, shared by both methods (AC 1–5).** §6.2 says "identical for `TranslateAsync`
      and `TranslateLinesAsync`" — so write it **once**, as a private generic
      `Task<T> RunAsync<T>(Func<ITranslator, Task<T>> call, CancellationToken ct)`, and let the two public
      methods be two lambdas. Two copies of this loop is two places for the OCE filter to rot.
  - [x] The skip predicate of D-1 (`BlockedUntil > Now()`), remembering `(ProviderId, until)`.
  - [x] `catch (OperationCanceledException) when (ct.IsCancellationRequested) { throw; }` — **first**, before
        every other catch (AC 5, I3).
  - [x] `catch (TranslationException tex) when (tex.NotSent)` → skipped (D-2).
  - [x] `catch (TranslationException tex)` → `lastFailure = tex`; one `Logging.Warn` line naming the tier and the
        `Kind`; continue.
  - [x] `catch (Exception ex)` → `lastFailure = new TranslationException(ProviderErrorMapper.Classify(ex, ct), …)`
        (TP-CHN-13); continue. Use the mapper — do not re-classify by hand; E1.S3 owns that vocabulary. Check the
        mapper's exact cancel contract at the call site: E1.S3's review made `Classify` refuse to name
        `Cancelled`, and this catch must not be the place that reopens it.
  - [x] Exit: `lastFailure is null` ⇒ `throw new TranslationException(AllProvidersPaused,
        UserMessages.AllProvidersPaused, retryAt: skipped.Min(x => x.RetryAt))` (AC 3 — **earliest**, and write a
        comment saying so, because `Max` is the plausible typo). Otherwise `throw lastFailure` (AC 4).
- [x] **T3 — `LastOutcome` (ruling R-3).** Nested immutable record on `ChainTranslator`:
      `{ string? ProviderId, IReadOnlyList<(string ProviderId, string Reason)> Skipped, DateTimeOffset? RetryAt,
      TranslationErrorKind? Kind }`, assigned after **every** call — success and failure both.
  - [x] Set it exactly once per call, at the end, from a local; never mutate it field-by-field mid-loop (the
        code-behind may read it from the dispatcher while a second call is in flight).
  - [x] Nothing reads it yet. E7.S1/E7.S3 do. Do **not** wire any UI in this story; do write the property so E7
        does not have to reopen this file.
- [x] **T4 — `AllProvidersPaused` copy: it already exists — do not add a string (drift).** `epics.md`'s copy note
      asks for Sally's line to be added "in this release". **E1.S6 already landed it**:
      `UserMessages.AllProvidersPaused` (`Services/UserMessages.cs:111`) reads *"All engines are paused — nothing
      you need to do; it retries on its own"*, and the doc comment at `:108-110` records that §3.1's countdown
      form (`…next try in {t}…`) is **E7.S1's**, because `{t}` is formatting and formatting stays out of
      `Services/` (I2). Use the constant. Add no sentence.
- [x] **T5 — Re-point the two I3 regression guards, keeping their names (technical note).**
      `tests/PWRUHelper.Tests/TranslationBackendTests.cs`, class `FallbackTranslatorTests` (`:49-110`):
  - [x] `Cancellation_is_not_turned_into_a_fallback` (**`:88`**, `epics.md` says `:84` — drift) → drive
        `ChainTranslator`, **same name**.
  - [x] `Timeout_OCE_with_uncancelled_token_falls_back` (**`:102`**, `epics.md` says `:98` — drift) → drive
        `ChainTranslator`, **same name**.
  - [x] `Uses_primary_when_it_succeeds` (`:65`) and `Falls_back_when_primary_fails` (`:75`) become TP-CHN-02 /
        TP-CHN-03. Rename the class to `ChainTranslatorTests`; keep the `Fake` double (`:51-63`).
  - [x] These four are the *only* cases that reference `FallbackTranslator`. Confirm with a repo-wide grep before
        deleting the type.
- [x] **T6 — Delete `Services/FallbackTranslator.cs` (AC 6) and rewire its one production call site.**
      `MainWindow.Translate.cs:230` is the only one. Minimal rewire in **this** story so the commit compiles and
      behaviour is unchanged; **E3.S7 owns the real §8.1 composition**:
  - [x] Add a Services-side factory so the composition site never names the registry (see the TP-START-02 trap
        below): `internal static ChainTranslator ChainTranslator.Of(params (string Id, ITranslator T)[] tiers)`,
        resolving each gate through `ProviderGates.For(id)`. `For` **constructs only** — it reads no file
        (`ProviderStateStoreTests.cs:815-824` pins that) — so calling it from a constructor keeps I10.
  - [x] `BuildTranslator()` (`MainWindow.Translate.cs:226-233`) becomes, for now:
        `key.Length > 0 ? ChainTranslator.Of((ProviderIds.DeepL, new DeepLTranslator(key)),
        (ProviderIds.GoogleGtx, new TranslationService())) : ChainTranslator.Of((ProviderIds.GoogleGtx, new
        TranslationService()))`, still wrapped in `new CachingTranslator(...)`, still called from `:239`.
  - [x] `_readTranslator` (`MainWindow.xaml.cs:43`) stays exactly as it is in this story — a one-tier chain is a
        behaviour change nobody asked for here, and E3.S7 moves the field into the ctor anyway.
- [x] **T7 — Tests (TP-CHN-01 … TP-CHN-08, TP-CHN-13).** New
      `tests/PWRUHelper.Tests/ChainTranslatorTests.cs`, `[Collection("Gates")] : GatesTestBase` (IS-5), driving
      real `ProviderGate`s on the injected clock (IS-6) and `FakeHandler`s (IS-11).
  - [x] **TP-CHN-01 (T9)** — tier 1's gate blocked into the future, tier 2 healthy: tier 1's **fake handler is
        never called** (`fake.Requests == 0`) and tier 2's value comes back. Assert on the handler's count, not on
        the chain's own bookkeeping.
  - [x] **TP-CHN-04 (T10)** — every tier blocked: **exactly one** `TranslationException`, `Kind ==
        AllProvidersPaused`, `RetryAt` == the **earliest** of the three, and **total** requests across all fake
        handlers `== 0`. Give the three gates *different* `retryAt`s and put the earliest one **last** in tier
        order, so a `First()` implementation fails the case.
  - [x] **TP-CHN-05** — tier 1 blocked, tier 2 returns 403: the thrown `Kind` is `Blocked`, not
        `AllProvidersPaused`.
  - [x] **TP-CHN-06** — via `TestBackoffRedirect`: a short ceiling wait is absorbed and the tier is used; a wait
        past `MaxSpacingWaitMs` leaves the tier unused and `now + t` in the skipped set. **No `Task.Delay`**
        (CI-3).
  - [x] **TP-CHN-07 / TP-CHN-08 (T11)** — the two re-pointed guards of T5.
  - [x] **TP-CHN-13** — a tier throws `InvalidOperationException`: it is classified and the next tier is called.
  - [x] **The I6/I7 pin tests (ruling GAP-1/GAP-2, S-size, and they belong to this story).**
    - [x] **I6 — the glossary stays upstream of every engine.** A source scan: no file under `Services/` other
          than `SlangGlossary.cs` calls `.Expand(`; the only caller is `MainWindow.Live.cs:302`. The failure mode
          this pins is a new provider "helpfully" expanding slang itself, which would double-expand and would put
          expanded text in the displayed original.
    - [x] **I7 — per-message `ru`/`auto`.** A pin on `MainWindow.Live.cs:304-310`'s split (the two calls are at
          `:315` and `:320`) reaching the chain intact: two groups, two calls, the sources `"ru"` and `"auto"`,
          and the original order restored. Assert
          through a fake `ITranslator` recording `(source, lines)` — no window needed.
- [x] **T8 — Run the suite.** `dotnet test tests/PWRUHelper.Tests`. Green, headless, no network, no `%AppData%`
      write, no new flake. Baseline at `b592800` is **602 passed**.

### Review Findings

_Code review (Amelia, CR), 2026-09-07, against `d8df030..79059b7`: three adversarial layers (Blind
Hunter, Edge Case Hunter, Acceptance Auditor), Winston's three rulings, and a **mutation pass** on the
two lines this story itself calls the most dangerous. **[Patch]** items are fixed in the review commit;
**[Defer]** items are deliberate or belong to a named later story._

**Verdict: AC 1–6 all met.** The algorithm is right where it matters — the skip predicate really is
`BlockedUntil > Now()`, `Min` really is `Min`, the OCE filter really is first, `NotSent` really has one
writer, and D-1's "one admission per logical call" holds (every `HttpProviderCoreTests` request count is
unchanged). The one hole two layers found independently was in the tests, not in the code.

#### Fixed

- [x] [Review][Patch] **The I3 guards were vacuous — both cancel cases were answered before a tier was
      ever entered.** `Cancellation_is_not_turned_into_a_fallback` and
      `A_real_cancel_from_a_later_tier_stops_the_chain` cancelled the token *before* calling the chain,
      so `RunAsync`'s opening `ct.ThrowIfCancellationRequested()` threw on iteration 1: no tier ran, the
      `Fake` that throws the OCE was never called, and the
      `catch (OperationCanceledException) when (ct.IsCancellationRequested)` filter the cases are
      **named after** was never reached. Proved by mutation — replacing the filter with `when (false)`
      left all 22 chain cases green. **That is R-03's own failure mode: the guard whose name is the
      point, surviving the deletion of the line it guards.** Both cases now cancel **mid-tier** (the tier
      cancels the CTS, then throws), and the first also asserts `Assert.Same` on the propagated instance,
      because the generic catch's `ct.ThrowIfCancellationRequested()` net produces the same *visible*
      behaviour with a fresh exception — which is precisely why the old assertions could not tell the two
      apart. The "cancelled before the call" timing is real too and is kept as its own case
      (`A_token_cancelled_before_the_call_reaches_no_tier_at_all`). Re-mutated after the fix: the filter
      can no longer be deleted silently.
- [x] [Review][Patch] **The chain aliased the tier list it was handed.** `_tiers = tiers` kept the
      caller's collection, so the empty-chain guard — checked once, in the constructor — could be walked
      straight past by a caller that cleared its list afterwards, landing on the `AllProvidersPaused`
      exit with an empty skipped set and a **null** `retryAt`: "wait for something that is never
      coming", the one sentence that constructor exists to prevent. A mid-call mutation would also throw
      a raw `InvalidOperationException` from the loop **outside** the `try`, past every catch. Now
      `_tiers = tiers.ToArray()`, pinned by `A_chain_does_not_alias_the_list_it_was_built_from`. E3.S7
      rebuilds chains, so this was the story that had to close it.
- [x] [Review][Patch] **`Of` validated the array but not its contents.** A blank id registered a gate
      under `""` that no E7 status list would ever show; a **repeated** id gave two tiers **one gate**,
      so the second was skipped exactly when the first was, refused by the same token bucket, and
      counted twice in `Outcome.Skipped` — a fallback that structurally cannot be one. Blank id, null
      translator and duplicate id now all throw at composition time, beside the empty-chain guard.
      Pinned by `Of_refuses_a_malformed_tier_list`.
- [x] [Review][Patch] **The per-tier `Warn` line said "trying the next tier" on the last tier**, where
      nothing follows — and reading that log is step 3 of this story's own manual verification.
      `LogTierFailure(...)` now states what actually follows. A skipped tier still logs nothing, and the
      line still carries the `Kind` and never the provider's message (I11).
- [x] [Review][Patch] **`ChainTranslator.cs` was not in the `ConfigureAwait(false)` scan**, although it
      is now the **outermost** await on the request path and is awaited from the same UI-thread methods
      as the four files that are. Added to
      `HttpProviderCoreTests.Every_await_on_the_request_path_configures_away_the_context`.
- [x] [Review][Patch] **`NotSent`'s "exactly one writer" was asserted by nobody.** It is the whole of
      ruling E3-b and the reason the chain may trust the flag: a second writer would turn a real failure
      into a skip and hand the player `AllProvidersPaused` while an engine was actually refusing. Now a
      source scan (`NotSent_is_written_in_exactly_one_place`) with a non-vacuity check on the reader.
- [x] [Review][Patch] **The exit rule was implemented but neither stated nor pinned.** §6.2 says the
      caller gets "the real reason **the last provider that actually tried** gave" — `lastFailure`
      overwritten; not a ranking of Kinds (nothing in this app defines one) and not the first failure.
      Now written at the throw site and pinned by `The_last_tier_that_tried_is_the_failure_the_caller_gets`.
- [x] [Review][Patch] **`LastOutcome` on the cancel path was undecided.** R-3 says "set after every
      call"; the cancel exit publishes nothing. **Decision: it stays unchanged** — a cancelled call
      produced no account of the chain, and overwriting the previous one with a half-account would have
      E7 render a status for an attempt the player stopped. Recorded at the rethrow and pinned by
      `LastOutcome_is_left_untouched_by_a_cancelled_call`. This is a deviation from R-3 as literally
      worded, flagged for Winston rather than buried.
- [x] [Review][Patch] **`Outcome.RetryAt`'s doc claimed a single meaning it does not always have.** On
      the failure exit the record carries `Earliest(skipped)` while the thrown exception carries the
      failing provider's own `RetryAt` — two different instants. Documented, together with the two
      values a countdown must not render raw: an instant already in the past, and the `AuthFailed`
      `DateTimeOffset.MaxValue` sentinel. **E7.S1 must read this before writing the countdown.**
- [x] [Review][Patch] **The I6 pin asserted something false and scanned one directory deep.** T7 and the
      test both said `MainWindow.Live.cs:302` is the app's only `.Expand(` call; there is a second,
      `MainWindow.Translate.cs:93` (the write path). The invariant is about *where* the expansion lives,
      not how many there are — the comment now says so — and the scan is recursive, so a provider added
      under `Services/Providers/` (E3 adds three) cannot walk out of it.
- [x] [Review][Patch] **I7's third clause was unpinned.** T7 asks for "the original order restored"; the
      test pinned the two sources and the chain's pass-through but not the scatter-back, so a refactor
      that concatenated the two answers instead of interleaving them would have passed while handing
      every row the wrong translation. `result[ruIdx[i]] = t[i]` and its `auto` twin are now pinned.
- [x] [Review][Patch] **`project-context.md` still documented the deleted class as the pipeline shape**
      — the file every agent is told to read first, and the repo's own rule is to update it when the
      pipeline shape changes. Rewritten to the chain, with the two rules that cost the most if forgotten
      (one admission point; `BlockedUntil`, never `State`). Two stale prose references to the dead type
      were re-pointed with it (`ProviderErrorMapperTests.cs:445`, `UserMessagesTests.cs:175`), so
      manual-verification step 2 above is now true.
- [x] [Review][Patch] **TP-CHN-06's recorded justification named one refusal condition out of three.**
      `AdmitAsync` also refuses on `wait >= MaxWaits`. It is equally unreachable — IS-7 credits every
      requested delay to the virtual clock, so the bucket has always refilled by the next turn of the
      loop — but the note now says all three, and why, instead of one.

#### Verified rather than assumed

- [x] [Review] **Winston's ruling 3 — the R-01 pin is not vacuous.** Mutating the predicate to
      `State == Open` fails `R_01_an_elapsed_window_is_not_skipped_and_the_probe_really_goes_out`;
      mutating `Min` to `Max` fails TP-CHN-04. Both were run, not reasoned about.
- [x] [Review] **Winston's ruling 2 — the keyless one-tier chain's sentence.** `AllProvidersPaused`
      already goes through `UserMessagesTests.Every_surface_reads_as_a_sentence_once_the_wrapper_is_applied`,
      which composes **all six** wrappers over every deck sentence and forbids `..`, `.)` and `. —`;
      the no-terminal-stop and ≤ 80-character rules cover it too. `RetryAt` is carried on that throw and
      asserted by TP-CHN-04. Nothing to add.
- [x] [Review] Catch order · batch semantics (I5) · one `Snapshot()` per tier per call · no `TryEnter`
      · no registry touch in the read chain's field initializer (`MainWindow.xaml.cs:43` still builds
      `CachingTranslator(TranslationService())`, whose core resolves its gate lazily) · priority
      untouched (still the core's `Interactive`) · TP-START-02's `strays` assertion untouched and green.

#### Deferred

- [x] [Review][Defer] **`LastOutcome` is unreachable from the only composition site, and stale on a
      cache hit.** `BuildTranslator()` returns `new CachingTranslator(chain)` typed `ITranslator`, so
      nothing outside can reach the property; and `CachingTranslator` answers a hit *without* entering
      the chain, so a reader would get the previous call's account presented as this one's. T3's stated
      benefit ("E7 does not have to reopen this file") is not real yet — **E3.S7** owns the composition,
      **E7.S1/E7.S3** the reader, and whoever wires it needs a per-call handle or a cache-aware outcome,
      not just the property.
- [x] [Review][Defer] **Saving a corrected DeepL key still cannot un-pause DeepL.** A 401/403 with a key
      sets `_keyBlockedUntil = DateTimeOffset.MaxValue`; `ProviderGates.ClearAuthBlock` has **no**
      production caller (it is E6.S3's), and `DeepLSaveKey_Click` only rebuilds the chain — which
      resolves the *same* gate. The chain now makes that silent: DeepL is skipped for the life of the
      process while the status text still reads "● DeepL for what you write". Pre-existing since E2.S5;
      the chain neither caused it nor can fix it from `Services/`. **E6.S3 must not ship without it.**
- [x] [Review][Defer] **A provider that makes several core calls per logical call can be recorded as
      skipped after it has already sent requests.** `TranslationService` chunks anything over
      `MaxQueryBytes`; if chunk 1 is admitted and chunk 2 refused, the tier raises `NotSent` for a call
      that spent requests, so the chain counts it as skipped. `NotSent` is per *core call* while the
      chain's unit is the whole `ITranslator` call, and I1 leaves no channel to say otherwise. Contract
      note for **E3.S6/E3.S8** as they add providers.
- [x] [Review][Defer] **A tier that returns failure placeholders instead of throwing reads as a
      success.** `TranslationService`'s per-line fallback returns `"(rate-limited — …)"` strings and
      returns *normally*, so the chain would publish "this came from Google" over a batch of failures
      and never try the next tier. Not reachable today (only the write path is a chain, and it calls
      `TranslateAsync`); it goes live the moment **E5.S1/E3.S7** points the read path at the chain.
      **E3.S8** owns the batch policy that closes it.
- [x] [Review][Defer] `RequestLogTests.TP_LOG_03` drives real registry gates from the **log-file**
      collection while `ChainTranslatorTests` opens those same gates from the parallel `Gates`
      collection. Pre-existing (the core has consulted the registry per request since E2.S5, and the old
      `FallbackTranslator` would have been refused identically), but the flake class is real and a
      key-redaction test is the kind that gets re-run rather than investigated.
- [x] [Review][Defer] `UserMessages.Sentence(kind) ?? ex.Message` can put a raw exception message on a
      user-facing sentence — for `Unknown` only. It mirrors `UserMessages.For`'s own §4.4 pass-through,
      so GAP-4 is intact, but it is the one line in this file that can surface text a provider wrote.
      Copy is **E7.S1**'s.
- [x] [Review][Defer] `CachingTranslator` pads a short batch with the raw source lines for display (the
      `aligned` flag keeps it out of the cache). The chain itself never pads (I5, pinned), but the
      *pipeline* does — **E3.S8**'s batch policy.
- [x] [Review][Defer] Ruling **E3-c** names `Services/TranslationChains.cs`; `Of` is on
      `ChainTranslator`. Recorded in the Completion Notes and accepted by Winston (E3.S7 may split it).
      `throw lastFailure` also resets the stored exception's stack trace; nothing reads it today.

## Dev Notes

### Invariants this story must keep

- **I1 — `ITranslator` does not change.** It is why the priority cannot ride on the call (E3.S7 puts it on the
  provider instance) and why `LastOutcome` lives on the chain and not on the interface.
- **I2** — `ChainTranslator` is in `Services/`: no WPF type, no dispatcher, no settings read, no formatting.
- **I3 — the OCE trap, and this story is a prime site for it.** `FallbackTranslator.cs:26,38` are the two catches
  being deleted; the chain's replacement must keep the filter. `test-plan.md` R-03 names `ChainTranslator`
  explicitly as one of the three places the trap can reopen. It cost this project three releases once.
- **I5** — the chain itself never pads. Batch mismatch policy is E3.S8's; the chain must not "helpfully" pad a
  short list from a tier.
- **I6 / I7** — pinned here by ruling GAP-1/GAP-2 (T7).
- **I9 / I10** — `ProviderGates.For` at construction is fine (it reads no file); the first `TryEnter` is still
  what loads `provider-state.json`, and it still happens inside `HttpProviderCore` after first paint.
- **I16** — the LIVE loop is not touched by this story. Its behaviour under an open gate is **E5.S1**.

### Rulings that apply

- **R-3 (this story's):** `ChainTranslator.LastOutcome`, immutable, set after every call, read by the code-behind
  in E7. `ITranslator` unchanged.
- **E5-a (binding, lands in E5.S1 but constrains the shape here):** LIVE honours short `Wait`s inside a tick and
  never drops a group on `Wait`; `WorthWaiting` never says "give up" on the ceiling. With D-1 that behaviour is
  already the core's, and the chain inherits it — do not add a second, different wait policy.
- **E2-d:** the three per-line placeholder strings (`TranslationService.cs:152`, `:160`, `:161`, `:174`) belong to
  **E3.S8** and **E7.S1**. Do not touch them here.
- **GAP-4:** every user-facing sentence comes from `Services/UserMessages.cs`. The chain constructs no new one.

### Repo traps

- **TP-START-02 is a hard, automated constraint on the composition site.**
  `ProviderStateStoreTests.No_startup_path_mentions_ProviderGates` (`:826-866`) asserts, literally:
  `Assert.Equal(new[] { "MainWindow.xaml.cs: ProviderGates.Flush();" }, strays);` — the **only** `ProviderGates`
  reference permitted outside `Services/`, in the whole app. T6's `ChainTranslator.Of` factory exists exactly so
  the composition site can name providers without naming the registry. If you find yourself editing that
  assertion, stop: the next reference must be a decision, and this story is not it.
- **`ProviderErrorMapper.Classify`'s cancel contract.** E1.S3's review made the mapper refuse to produce a
  `Cancelled` kind, and `TranslationErrorsTests` runs a source scan banning a `Cancelled` exception in production
  code. The chain's generic `catch (Exception)` must respect both.
- **`FakeHandler` repeats its last step past the end of the script** (`FakeHandler.cs:103`).
- **`Logging.Warn` swallows its own errors by design.** A failed log line must never fail a translation.
- No settings control is touched, so `_restoringSettings` (`MainWindow.xaml.cs:35`, cleared at `:201`) is not
  engaged. **E3.S7** is the story that touches the constructor; this one changes one method body in
  `MainWindow.Translate.cs`.
- `InternalsVisibleTo PWRUHelper.Tests` (`PWRUHelper.csproj:28`) and the `tests\**` `DefaultItemExcludes` (`:18`)
  intact. **No `.sln` at repo root.**

### Line-number drift (`epics.md` / `architecture-cible.md` vs this worktree at `b592800`)

| The docs say | Actually |
|---|---|
| `TranslationBackendTests.cs:84` — real-cancel-propagates | **`:88`** (`Cancellation_is_not_turned_into_a_fallback`); the class is `:49-110` |
| `TranslationBackendTests.cs:98` — timeout-falls-through | **`:102`** (`Timeout_OCE_with_uncancelled_token_falls_back`) |
| §6.1 "the gate already has `Id`" | **It does not** — see D-3 |
| §6.2 "`decision = tier.Gate.TryEnter(priority)`" | **The core already does this** — see D-1 |
| `epics.md` copy note: "`AllProvidersPaused` needs a user-visible sentence in this release" | **Already landed** by E1.S6 at `UserMessages.cs:111` — see T4 |
| `TranslationService.cs:99-105` — the `rateLimited` latch | **`:149-160`** (E3.S6/E3.S8's business, not this story's) |

## Testing

- **TP-IDs landed:** **TP-CHN-01, 02, 03, 04, 05, 06, 07, 08, 13** (= `epics.md`'s T9, T10, T11). TP-CHN-09/10/11
  are **E3.S8**'s (batch policy); TP-CHN-12 is E3.S6/E3.S8's (`rateLimited` latch); TP-CHN-14 is **E3.S7**'s (I8).
- **Fixtures:** none new. `GoogleOk` and the existing `FakeHandler` scripts suffice.
- **Isolation:** **IS-5** (the non-parallel `Gates` collection — a chain test drives real gates, so the class must
  join it) · **IS-6** (the shared injected clock: every `retryAt` assertion) · **IS-7 / `TestBackoffRedirect`**
  (TP-CHN-06 without sleeping) · **IS-11** (the fake handler's request count is the assertion in TP-CHN-01/04).
- **Must keep passing unchanged:** `HttpProviderCoreTests` (the whole TP-RET family — if the chain has changed a
  request count, D-1 was violated), `ProviderGateTests`, `ProviderGatesTests`, `ProviderStateStoreTests`
  (**especially** TP-START-02's two cases), `CachingTranslatorTests`, `ProviderErrorMapperTests`,
  `TranslationErrorsTests`, `UserMessagesTests`, `RequestLogTests`, `HttpSeamGuardTests`.
- **Changed on purpose:** `TranslationBackendTests.cs`'s `FallbackTranslatorTests` class only.

## File List

**Create**
- `Services/ChainTranslator.cs` — `ChainTier`, `ChainTranslator`, `ChainTranslator.Of`, `LastOutcome`

**Modify**
- `Services/TranslationErrors.cs` — `internal bool NotSent { get; init; }` on `TranslationException` (D-2)
- `Services/HttpProviderCore.cs` — `Paused()` (`:347-358`) sets `NotSent = true`; the doc comment at `:304-316`
  ("E3.S3 owns the chain's own version of this rule") is updated to say what actually happened
- `MainWindow.Translate.cs` — `BuildTranslator()` (`:226-233`) rewired off `FallbackTranslator` (T6)

**Delete**
- `Services/FallbackTranslator.cs`

**Modify (tests)**
- `tests/PWRUHelper.Tests/TranslationBackendTests.cs` — `FallbackTranslatorTests` → `ChainTranslatorTests`, the
  two I3 guards re-pointed and **renamed to nothing** (their names are the point)

**Create (tests)**
- `tests/PWRUHelper.Tests/ChainTranslatorTests.cs` — TP-CHN-01…08, 13, plus the I6/I7 pins

## Size, dependencies, risk

**Size:** M · **Depends on:** E2.S1 (the gate), E2.S3 (`WorthWaiting`, the probe token), E2.S5 (`HttpProviderCore`
— **the reason D-1 exists**), E1.S3 (`ProviderErrorMapper`), E1.S6 (`UserMessages.AllProvidersPaused`) ·
**Risk:** **medium-high, and the highest of the unblocked E3 stories.** Two of the project's three worst failure
classes converge here: **R-03** (the OCE trap — `test-plan.md` names `ChainTranslator` by name) and **R-01** (a
permanently paused app — the `State`-vs-`BlockedUntil` predicate of D-1 would ship it silently, since every test
that only checks "an open gate is skipped" would pass) ·
**DoD:** TP-CHN-01, TP-CHN-04 and the two re-pointed I3 guards pass; `Services/FallbackTranslator.cs` is gone;
TP-START-02's `strays` assertion is untouched and green.

## How to verify manually

1. `dotnet test tests/PWRUHelper.Tests` — green, and **no slower**: if it got slower, something is sleeping.
2. `git grep FallbackTranslator` returns nothing but this story file and the older investigation documents.
3. Run the app with a DeepL key set, no network: the Translator tab shows **one** sentence, not a cascade, and the
   log shows one `tr` line per tier that actually tried and none for a tier that was skipped.
4. Force a 429 on `gtx` (or hand-edit `%AppData%\PWRUHelper\provider-state.json` to a future `blockedUntil`),
   restart, and translate: **no `tr` line at all** for that provider, and the gate log's `OPEN` line explains it.
5. Then wait past the `blockedUntil` and translate again: the request **must** go out (the half-open probe). If it
   does not, the D-1 predicate is wrong and R-01 is shipped.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E3.S3; Epic 3 header (invariants, DoD)]
- [Source: docs/investigations/02-traduction/architecture-cible.md#6 ChainTranslator (6.1 shape, 6.2 algorithm, 6.3 batch semantics), #5.4 rate ceiling, #16 Glossary]
- [Source: docs/investigations/README.md#Architect's rulings on the readiness report's gaps — R-3; #…test architect's questions — GAP-1/GAP-2, GAP-4; #Epic E2 rulings — E2-d, E2-e, E2-h, E5-a; #Phase 4 (E2.S5's record: one admission per call, Wait honoured, RequestPriority still Interactive)]
- [Source: docs/investigations/03-stories/test-plan.md#TP-CHN-01..08, 13; #1.1 risks R-01, R-03, R-09; #2 IS-5, IS-6, IS-7, IS-11; #5 CI-3; #6 invariant traceability I1/I3/I6/I7]
- [Source: docs/investigations/03-stories/stories/E2.S5-http-provider-core.md#Dev Agent Record (the admission model, the Wait ruling, RequestPriority left Interactive, the per-line strings deferred)]
- [Source: docs/investigations/02-traduction/plan-migration.md#Increment 2]
- [Source: project-context.md#Language-Specific Rules (the OCE trap), #Translation Pipeline Rules, #Testing Rules]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on Claude Opus 5 — `claude-opus-5[1m]`, 2026-09-07.

### Debug Log References

- Baseline at `d8df030`: `dotnet test tests/PWRUHelper.Tests` — **609 passed, 0 failed, 2 s**.
- After: **624 passed, 0 failed, 2 s** (+19 new cases, −4 re-pointed ones moved out of
  `TranslationBackendTests.cs`). Same wall time as the baseline, which is this story's own "if it got slower,
  something is sleeping" check: nothing sleeps, and no case reaches the network or `%AppData%`.
- Red phase observed before green: the first run after the test file landed failed to compile on
  `CS0246: ChainTranslator` (the type did not exist yet).
- Re-checked `file:line` in this worktree after the change: `Services/ChainTranslator.cs` (new) ·
  `Services/TranslationErrors.cs:56` (`NotSent`) · `Services/HttpProviderCore.cs:337-342` (the re-written §5.4
  note) and `:400-405` (`Paused` setting `NotSent`) · `MainWindow.Translate.cs:232-239` (`BuildTranslator`) ·
  `Services/DeepLTranslator.cs:12` and `:107` (two stale references to the deleted type, re-pointed) ·
  `tests/PWRUHelper.Tests/RequestLogTests.cs:644` (a **fifth** `FallbackTranslator` call site — see below).

### Completion Notes List

**The three flagged deviations were implemented as argued, and all three held up in code.**

- **D-1 / ruling E3-a — the chain reads `Snapshot()`, never `TryEnter`.** Implemented as the skip predicate
  `snapshot.BlockedUntil is { } until && until > tier.Gate.Now()` (`Services/ChainTranslator.cs:145`).
  `HttpProviderCore` is still the only admission point: every `HttpProviderCoreTests` case passes unchanged,
  which is the story's own detector for a second `TryEnter` (a request count would have moved).
  **The R-01 half of this is now pinned in both directions.** Skipping while the window runs is the easy half,
  and every "an open gate is skipped" test would pass a `State == Open` implementation too — so
  `R_01_an_elapsed_window_is_not_skipped_and_the_probe_really_goes_out` asserts the other side: after the
  window elapses the gate is **still** `GateState.Open` (asserted), the chain must **not** skip, the request
  really goes out, and the gate closes. That case is the one that fails if the predicate ever drifts to the
  state. It was added beyond the story's list on purpose: without it the DoD's own manual step 5 had no
  automated equivalent.
- **D-2 / ruling E3-b — `TranslationException.NotSent`.** `internal bool { get; init; }`
  (`Services/TranslationErrors.cs:56`), set in exactly one place, `HttpProviderCore.Paused`. The chain reads it
  in a filtered catch, so a refusal is a skip and a real failure is a failure. AC 4
  (`TP_CHN_05_a_real_403_beats_AllProvidersPaused`) is what proves the distinction is real.
- **D-3 — `ChainTier(string ProviderId, ProviderGate Gate, ITranslator Translator)`.** `ProviderGate` grew
  nothing. AC 6 names the two-member shape; the third member is the recorded superset R-3 needs.

**Where the implementation went beyond or around the tasks, and why.**

1. **T5 and T7 collided: two classes could not both be `ChainTranslatorTests`.** T5 asks for
   `TranslationBackendTests.FallbackTranslatorTests` to be *renamed* `ChainTranslatorTests`; T7 asks for a
   *new file* `ChainTranslatorTests.cs`. Same name, same namespace, same assembly — that does not compile.
   Resolved by **moving** the four cases into the new file rather than renaming in place: the class is
   `ChainTranslatorTests`, the `Fake` double came with it, and **the two I3 guards keep their exact names**
   (`Cancellation_is_not_turned_into_a_fallback`, `Timeout_OCE_with_uncancelled_token_falls_back`), which is
   the part T5 said was the point. `TranslationBackendTests.cs` now holds `DeepLTranslatorTests` only.
2. **T5's "these four are the only cases that reference `FallbackTranslator`" was wrong — there is a fifth.**
   `RequestLogTests.TP_LOG_03` (`:644`) built one to produce a keyed DeepL failure and a Google failure in one
   call. Re-pointed to `ChainTranslator.Of`; the case still proves the key never reaches the log. Found by the
   compiler, not by the story's grep, which was scoped to the two guard names.
   `Services/DeepLTranslator.cs:12` also carried a `<see cref="FallbackTranslator"/>` that would have become a
   dangling cref — re-pointed to `ChainTranslator`, and the prose at `:107` with it.
3. **TP-CHN-06's long-wait half is driven on the shape, not on the policy.** With today's constants the real
   bucket can never ask for more than `MinSpacingMs × BucketCapacity` = 1 s, i.e. never more than
   `MaxSpacingWaitMs` = 2 s, so `HttpProviderCore` cannot be made to refuse on the ceiling without moving a
   policy number this story does not own. The short half is therefore **real and end-to-end** (the bucket is
   drained with `DrainBucketForTests`, the wait is really requested and recorded through IS-7, the tier is
   used); the long half drives the exact shape the core raises when it does refuse — a `NotSent`
   `TranslationException` carrying `now + t`. Recorded rather than hidden: it is the one case whose
   end-to-end reach is bounded by a constant rather than by the design.
4. **`Of` lives on `ChainTranslator`, not in a new `Services/TranslationChains.cs`.** The story's File List
   puts it there (`ChainTranslator.Of`); ruling E3-c names a file. One factory, one call site, no third type —
   Rule of Three. **E3.S7**, which owns the real §8.1 composition and will build two different chains, is the
   story that should split it out if it is worth splitting.
5. **`LastOutcome` is published once per call, whole**, from a single `Publish(...)` — success and failure
   both — and read through `Volatile`, because the code-behind will read it from the dispatcher while a second
   call is in flight (R-3). Nothing reads it yet, by design: E7.S1/E7.S3 do. `Outcome.RetryAt` is documented as
   the **earliest** skipped window (the same instant an `AllProvidersPaused` carries), so E7's countdown has
   one meaning and not two.
6. **The generic `catch (Exception)` is guarded on both sides of `ProviderErrorMapper.Classify`**
   (`ct.ThrowIfCancellationRequested()` before and after), which is `HttpProviderCore`'s own pattern at
   `:295`/`:305`. Without the second guard, a cancel landing inside that window would come back as the one Kind
   nothing in this app may construct (I3, TP-MAP-17). The user-facing sentence comes from
   `UserMessages.Sentence(kind)`; GAP-4 holds — **no new string was added anywhere** (T4: E1.S6 already landed
   `UserMessages.AllProvidersPaused`, and it is used verbatim).

**Invariants, checked rather than assumed.** I1 pinned by reflection (`I1_ITranslator_is_unchanged`) · I2 (no
WPF type, no formatting, no settings read in `ChainTranslator`) · I3 pinned by the two named guards **plus**
`A_real_cancel_from_a_later_tier_stops_the_chain`, because the filter is per-tier and a chain that only guarded
tier 1 would have passed the original two · I5 pinned (`I5_the_chain_never_pads_a_short_batch`) · I6/I7 pinned
as ruling GAP-1/GAP-2 asks · I9/I10 (`ProviderGates.For` at construction reads no file;
`Nothing_reads_the_file_before_the_first_TryEnter` still green) · I11 (the per-tier `Logging.Warn` line names
the tier and the `Kind`, never a provider message that could quote a URL) · I16 (`MainWindow.Live.cs`
untouched). **TP-START-02's `strays` assertion is untouched and green** — `ChainTranslator.Of` is exactly why
`MainWindow.Translate.cs` can name providers without naming the registry.

**For the reviewer, in order of what would hurt most if wrong:** (1) the skip predicate and its two-sided pin
(`ChainTranslator.RunAsync`, `R_01_…`); (2) the catch ORDER in `RunAsync` — the OCE filter must stay first;
(3) `Min` and not `Max`/`First` on the exit path; (4) `NotSent` having exactly one writer.

**Not done here, on purpose.** The keyless branch of `BuildTranslator` is now a **one-tier chain** rather than a
bare `TranslationService`, exactly as T6 specifies; `_readTranslator` (`MainWindow.xaml.cs:43`) is untouched.
The real §8.1 order (`google-dict → google-gtx` for reading, DeepL in front for writing) and the priority
plumbing are **E3.S7**'s.

### File List

**Created**
- `Services/ChainTranslator.cs`
- `tests/PWRUHelper.Tests/ChainTranslatorTests.cs`

**Modified**
- `Services/TranslationErrors.cs`
- `Services/HttpProviderCore.cs`
- `Services/DeepLTranslator.cs`
- `MainWindow.Translate.cs`
- `tests/PWRUHelper.Tests/TranslationBackendTests.cs`
- `tests/PWRUHelper.Tests/RequestLogTests.cs`
- `docs/investigations/03-stories/sprint-status.yaml`
- `docs/investigations/03-stories/stories/E3.S3-chain-translator.md`

**Deleted**
- `Services/FallbackTranslator.cs`

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **ready-for-dev (Phase 4 — owner's go given)**. Drifts D-1/D-2/D-3 recorded against `architecture-cible.md` §6. |
| 2026-09-07 | Implemented (Amelia DS). `ChainTranslator` + `ChainTier` + `Outcome`/`LastOutcome` + `Of`; `TranslationException.NotSent` written by `HttpProviderCore.Paused`; `BuildTranslator` rewired; `FallbackTranslator` deleted (a fifth call site turned up in `RequestLogTests`). 609 → 624 tests, all green, same 2 s. Status → **review**. |
| 2026-09-07 | Code review (Amelia CR): three layers + Winston's rulings + a mutation pass. AC 1–6 met. Thirteen patches — chiefly the **two vacuous I3 cancel guards** (a `when (false)` mutation of the OCE filter left every chain case green; both now cancel mid-tier and one asserts the propagated instance), the aliased tier list, `Of`'s content validation, the exit rule and the cancel-path `LastOutcome` rule both stated and pinned, and `project-context.md`'s pipeline shape. Eight items deferred to E3.S6/S7/S8, E5.S1, E6.S3 and E7.S1. 624 → 630 tests, green, 2 s. Status → **done**. |
