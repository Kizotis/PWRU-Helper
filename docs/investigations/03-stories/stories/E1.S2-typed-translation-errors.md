# Story E1.S2: Introduce `TranslationErrorKind` and a typed `TranslationException`

Status: **review**

_Epic E1 (A) — Diagnosable translation pipeline · increment 0 · Release A.0 · ⛔ no production code until the
owner's explicit go._

## Story

As a support-reading maintainer,
I want every translation failure to carry a machine-readable kind,
So that the message a player sees and the decision the code takes stop being derived from a string.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E1.S2. Do not weaken._

1. **Given** `Services/TranslationErrors.cs` does not exist
   **When** it is created
   **Then** it declares the 11-value `TranslationErrorKind` of `architecture-cible.md` §4.1 verbatim
   (`RateLimited, Blocked, Unavailable, Timeout, Network, BadResponse, QuotaExhausted, AuthFailed, Cancelled,
   AllProvidersPaused, Unknown`)
   **And** `TranslationException` carries a required `Kind` plus optional `RetryAt` and `ProviderId`
   **And** **there is no message-only constructor**.
2. **Given** the twelve existing throw sites (`TranslationService.cs:143,145,148,157,175`;
   `DeepLTranslator.cs:53,60,87,91,99,127`)
   **When** the message-only constructor is removed
   **Then** the compiler forces each of them to state a `Kind`, and each one states the `Kind` the §4.2 table
   assigns.
3. **Given** the `Cancelled` contract of §4.1
   **When** a provider recognises a genuine cancellation
   **Then** it **rethrows the original `OperationCanceledException`** and never wraps it; a `TranslationException`
   with `Kind.Cancelled` is never constructed anywhere in the codebase.

## Tasks / Subtasks

- [x] **T1 — Create `Services/TranslationErrors.cs`** (AC: 1) — the glossary §16 target file
  - [x] `public enum TranslationErrorKind` with the 11 values **in the §4.1 order**, each keeping its explanatory
        comment (`RateLimited` = 429 or a Google "automated queries" HTML page on any status; `Blocked` = 403 with
        no key / captcha / abuse block; `Unavailable` = 5xx; `Timeout` = an OCE with `ct` NOT cancelled;
        `Network` = `HttpRequestException`; `BadResponse`; `QuotaExhausted`; `AuthFailed`; `Cancelled`;
        `AllProvidersPaused`; `Unknown`).
  - [x] `public class TranslationException : Exception` with `Kind`, `RetryAt` (`DateTimeOffset?`), `ProviderId`
        (`string?`) and the single ctor
        `TranslationException(TranslationErrorKind kind, string message, DateTimeOffset? retryAt = null, string? providerId = null)`.
  - [x] Carry the `Cancelled` contract into the file as a comment, verbatim from §4.1: *a provider that recognises
        a genuine cancellation rethrows the `OperationCanceledException`; a `TranslationException` with
        `Kind.Cancelled` must never be constructed.*
- [x] **T2 — MOVE the old declaration, do not duplicate it** (AC: 1)
  - [x] Delete `TranslationException` from `Services/TranslationService.cs:10-13` in the **same commit**. Two
        declarations of the same type in the same namespace do not compile — `epics.md` says "create", not "move",
        and that is the trap (sprint-plan note 7).
  - [x] Leave `ITranslator` (`TranslationService.cs:18-23`) exactly where it is — moving it is E3's rename, not
        this story (I1).
- [x] **T3 — Classify the five `TranslationService` throw sites** (AC: 2) per the §4.2 table
  - [x] `:143` `Translation service error (HTTP {code})` — this is the non-transient branch that today folds 403
        with 400/404. In **this** story it keeps its wording; give it `Unknown` and leave the 403 split to E1.S3,
        which is where the mapper decides. Add a `// E1.S3 replaces this with ProviderErrorMapper.Classify` marker.
  - [x] `:145-146` `Google is limiting translations right now — wait a minute and try again.` ⇒ `RateLimited`.
  - [x] `:148` `Translation service is unavailable (HTTP {code}).` ⇒ `Unavailable`.
  - [x] `:157` `Couldn't reach the translation service.` ⇒ `Network`.
  - [x] `:175-176` `…returned an unexpected response (it may be temporarily blocked).` ⇒ `BadResponse` for now;
        **E1.S4** is what turns the HTML case into `RateLimited`/`Blocked` before the parser ever runs.
- [x] **T4 — Classify the six `DeepLTranslator` throw sites** (AC: 2)
  - [x] `:53` `DeepL returned an unexpected response.` (count mismatch, I5 — never pad) ⇒ `BadResponse`.
  - [x] `:60` `No DeepL API key set.` ⇒ `AuthFailed`.
  - [x] `:87` `DeepL timed out…` — reached only from `catch (OperationCanceledException)` **after** the
        `when (ct.IsCancellationRequested)` filter at `:80` ⇒ `Timeout`. **Do not touch the filter.**
  - [x] `:91` `Couldn't reach DeepL.` ⇒ `Network`.
  - [x] `:99-105` the status switch ⇒ `401 or 403` → `AuthFailed` (a key was always sent here — §4.2 rows 5 and 7),
        `456` → `QuotaExhausted`, `429` → `RateLimited`, default → `Unknown`.
  - [x] `:127` `Parse` failure ⇒ `BadResponse`.
- [x] **T5 — Update the one test call site** (AC: 2)
  - [x] `tests/PWRUHelper.Tests/TranslationBackendTests.cs:78`
        `new TranslationException("deepl down")` ⇒ `new TranslationException(TranslationErrorKind.Unavailable, "deepl down")`.
        It is a fixture for "the primary failed"; the kind is arbitrary but must not be `Cancelled`.
  - [x] Nothing else in the suite constructs the type — grep `new TranslationException(` before and after.
- [x] **T6 — Tests** (AC: 1, 3)
  - [x] One case per `Kind` constructing the exception and reading back `Kind`/`RetryAt`/`ProviderId`.
  - [x] **TP-MAP-17** — a source-scan case (the `PublishFlagsTests` style: read the `.cs` files and assert on the
        text) proving **no** `throw new TranslationException(` site anywhere passes `Cancelled`. This is the I3
        contract as a test, and it must keep passing for the life of the project.
  - [x] **TP-PRV-12** — the three existing DeepL parse/mapping cases (`TranslationBackendTests.cs:33-46`) pass with
        the new ctor, unedited except where they construct the exception.

## Dev Notes

### Invariants this story must keep

- **I3 — the OCE trap, and this story is where it becomes a contract.** An `HttpClient` timeout throws
  `TaskCanceledException` (an `OperationCanceledException` subclass) with the token **NOT** cancelled. Every OCE
  catch filters `when (ct.IsCancellationRequested)`. The existing filters at `DeepLTranslator.cs:80` and
  `FallbackTranslator.cs:26,38` — and the bare rethrow at `TranslationService.cs:104`
  (`catch (OperationCanceledException) { throw; }` in the per-line loop, which is correct because it rethrows
  rather than converting) — all stay exactly as they are. Reintroducing a broad catch here
  silently disables the fallback and leaves a zombie LIVE indicator — the project's most expensive past bug.
- **I1** — no change to `ITranslator`. **I2** — `Services/` stays UI-free. **I5** — `DeepLTranslator.cs:49-53`
  still throws instead of padding; only the `Kind` is added. **I16** — the LIVE loop is untouched.

### Rulings and decisions that apply

- `Kind.Cancelled` exists **only** so `ProviderErrorMapper` can be a total function (E1.S3). Nothing constructs it.
  TP-MAP-17 is what keeps that true.
- **`AllProvidersPaused` and `Unknown` are declared here but wired to nothing yet.** `AllProvidersPaused` becomes
  reachable in E3.S3 (`ChainTranslator`); `Unknown` becomes the mapper's last resort in E1.S3. Declaring the whole
  enum now is deliberate — the file is the vocabulary, not the behaviour.
- This story changes **no user-visible string**. Every message stays byte-identical; only a `Kind` is added in
  front of it. E1.S6 then maps `Friendly()` onto `Kind` with the same wording, and **E7.S1** brings Sally's real
  copy (via `Services/UserMessages.cs`, the GAP-4 ruling) — not before.

### Repo traps

- `TranslationException` is `public` today and is referenced from `MainWindow.xaml.cs:404-410`,
  `MainWindow.Translate.cs`, `MainWindow.Live.cs`, `MainWindow.Ocr.cs` and the test project. Keep it `public` and
  in namespace `PWRUHelper.Services` so no using-directive churn is needed.
- `InternalsVisibleTo PWRUHelper.Tests` (`PWRUHelper.csproj:28`) and the `tests\**` `DefaultItemExcludes` (`:18`)
  stay intact. **No `.sln`.**
- `dotnet test tests/PWRUHelper.Tests` stays green and headless.

## Testing

- **TP-IDs landed:** **TP-MAP-17** (`Kind.Cancelled` is never constructed — source scan), **TP-PRV-12** (the three
  existing DeepL cases pass with the new ctor).
- **Fixtures:** none.
- **Isolation items:** none new; this story is pure typing.
- **Must keep passing unchanged:** `TranslationBackendTests` (except line 78's ctor call), `CachingTranslatorTests`,
  and every other existing case.

## File List

**Create**
- `Services/TranslationErrors.cs`

**Modify**
- `Services/TranslationService.cs` (delete `:10-13`; classify `:143,145,148,157,175`)
- `Services/DeepLTranslator.cs` (classify `:53,60,87,91,99,127`)
- `tests/PWRUHelper.Tests/TranslationBackendTests.cs` (`:78`, the single ctor call)

**Create (tests)**
- `tests/PWRUHelper.Tests/TranslationErrorsTests.cs` (per-`Kind` construction + TP-MAP-17's source scan)

## Size, dependencies, risk

**Size:** S · **Depends on:** — · **Blocked by:** the owner's Phase-4 go · **Risk:** low; a wrong `Kind` at a throw
site is caught by E1.S3's table test.
**DoD:** no `TranslationException` in the codebase is constructed without a `Kind`.

## How to verify manually

Build and run. Force each user-visible failure you can reach without the network (clear the DeepL key and save →
"No DeepL API key set" path; disconnect the network and translate → the "Couldn't reach…" path). The sentences must
be **identical to v0.14.0**, character for character. If any string moved, this story is wrong.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E1.S2]
- [Source: docs/investigations/02-traduction/architecture-cible.md#4.1 TranslationErrorKind, #4.2 Classification rules, #16 Glossary]
- [Source: docs/investigations/02-traduction/plan-migration.md#Increment 0]
- [Source: docs/investigations/03-stories/test-plan.md#3.1 ProviderErrorMapper — TP-MAP-17; #3.5 — TP-PRV-12]
- [Source: project-context.md#The OCE trap]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on Claude Opus 5 — DS workflow, non-interactive, Phase 4 go given.

### Debug Log References

- Baseline before the story: `dotnet test tests/PWRUHelper.Tests` — **268 passed, 0 failed, 2 s** (at `70a202c`).
- Red phase: `TranslationErrorsTests.cs` written first; the build failed with `CS0246 TranslationErrorKind` and
  `CS1729 no constructor taking 2/4 arguments` — the intended failure.
- After the story: **273 passed, 0 failed, 2 s** (268 + 5 new).
- Mutation check on TP-MAP-17: appending `// TranslationErrorKind.Cancelled` to
  `Services/FallbackTranslator.cs` made `No_production_source_names_Kind_Cancelled` fail; the probe was reverted
  and the suite is green. The guard is not vacuous.

### Completion Notes List

**What was implemented**

- **T1** — `Services/TranslationErrors.cs` created: the 11-value `TranslationErrorKind` in the §4.1 order with
  each explanatory comment, and `TranslationException` carrying `Kind` / `RetryAt` / `ProviderId` behind the
  single ctor `(kind, message, retryAt = null, providerId = null)`. The `Cancelled` contract is carried into the
  file as a comment above the exception.
- **T2** — the old declaration was **moved, not duplicated**: `Services/TranslationService.cs:9-13` is deleted in
  this same commit and replaced by a one-line pointer. The type stays `public` in `PWRUHelper.Services`, so
  `MainWindow.xaml.cs:406`, the other code-behind partials and the test project need no using-directive change.
  `ITranslator` was not touched (I1).
- **T3** — the five `TranslationService` sites classified: non-transient status ⇒ `Unknown` (with the
  `// E1.S3 replaces this with ProviderErrorMapper.Classify` marker on the 403-folding branch), 429 on the last
  attempt ⇒ `RateLimited`, transient exhausted ⇒ `Unavailable`, `json == null` ⇒ `Network`, `JsonException` ⇒
  `BadResponse`.
- **T4** — the six `DeepLTranslator` sites classified: count mismatch ⇒ `BadResponse` (still throws, never pads —
  I5), no key ⇒ `AuthFailed`, timeout ⇒ `Timeout` (the `when (ct.IsCancellationRequested)` filter above it is
  untouched — I3), transport ⇒ `Network`, the status switch ⇒ `401 or 403` → `AuthFailed`, `456` →
  `QuotaExhausted`, `429` → `RateLimited`, default → `Unknown`, and the `Parse` failure ⇒ `BadResponse`.
- **T5** — `TranslationBackendTests.cs:78` now constructs `TranslationErrorKind.Unavailable`. It is the only
  construction site outside `Services/`; `grep "new TranslationException("` confirms 11 production sites + that
  one, before and after.
- **T6** — `tests/PWRUHelper.Tests/TranslationErrorsTests.cs`: the enum order pinned as text, a round-trip over
  every `Kind` (enumerated via `Enum.GetValues`, so the test source does not itself name `Cancelled`), the
  single-constructor / no-message-only-overload assertion by reflection, a public/namespace guard, and
  **TP-MAP-17**, the source scan.

**Zero behaviour change.** Every user-facing sentence is byte-identical to v0.14.0 — the diff only wraps the
existing literals and puts a `Kind` in front of them. No new string, no mapping logic (E1.S3), no retry change
(E1.S7).

**Notes for the reviewer**

- The DeepL status switch could not stay a switch *inside* the ctor call once two values had to come out of it;
  it is now `var (kind, message) = code switch { … }` followed by one `throw`. The four messages are unchanged
  and the comment records why `403` is `AuthFailed` here (a key is always sent on that path — §4.2 rows 5 and 7).
- TP-MAP-17 scans every production `.cs` file for the token `TranslationErrorKind.Cancelled` and also rejects a
  `using static` of the enum, which would let a file write a bare `Cancelled` and slip past the scan. It asserts
  it found > 20 files and that both known throwing files are in the set, so it cannot pass vacuously.
- `Kind.Unknown` at the non-transient branch is deliberate and marked: it is the one site whose classification is
  knowingly provisional until E1.S3's mapper splits 403 out of 400/404.

**Deviations from the ACs:** none. All three ACs are met as written.

**Nothing here blocks E1.S7.** The deferred E1.S1 review items that name E1.S3 (the unfiltered OCE catch at
`TranslationService.cs:118`, the unmapped third-attempt `HttpRequestException`, DeepL's timeout mapping not
covering the body read) were left untouched, as the story requires.

### File List

**Created**
- `Services/TranslationErrors.cs`
- `tests/PWRUHelper.Tests/TranslationErrorsTests.cs`

**Modified**
- `Services/TranslationService.cs`
- `Services/DeepLTranslator.cs`
- `tests/PWRUHelper.Tests/TranslationBackendTests.cs`
- `docs/investigations/03-stories/stories/E1.S2-typed-translation-errors.md` (this file)
- `docs/investigations/03-stories/sprint-status.yaml`

### Change Log

| Date | Change |
|---|---|
| 2026-09-06 | E1.S2 implemented: `TranslationErrorKind` + typed `TranslationException` in `Services/TranslationErrors.cs`; the twelve throw/construction sites classified; TP-MAP-17 and the per-`Kind` cases added. Tests 268 → 273, all green. Status → review. |
