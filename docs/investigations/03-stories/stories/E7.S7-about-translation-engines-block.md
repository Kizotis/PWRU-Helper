# Story E7.S7: About tab — the "Translation engines" block

Status: **ready-for-dev (Phase 4 — owner's go given)**

_Epic E7 (G) — Degraded-mode UX · increment 6 · **Release B.2** · the owner's Phase-4 go was given 2026-09-06
(`README.md` § Phase 4)._

> **The layout decision is already made, and it is a "no": no settings window, no sixth tab.** §4.1: *"Everything
> stays in the About tab … the tab order is fixed by `project-context.md` and referenced by
> `MainTabs_SelectionChanged`."* The existing heading **"Better translations (optional)"**
> (`MainWindow.xaml:611`) becomes **"Translation engines"** with three blocks: *what is being used* (read-only),
> *your keys*, *offline engine*.
>
> **And principle 2 governs the whole story:** *"Many users will never open it — which is exactly why nothing in
> the degraded-mode flows may **require** it."* AC 4 states it as an acceptance criterion. This tab is where the
> chip's glyph gets explained **in full**, not where anything gets fixed.
>
> **It is also the story that finally makes `UpdateEngineStatusUi()` earn its name.** E6.S3 creates it as a
> two-line refresh at exactly the place `UpdateOcrFilterUi` is called from `ApplySettings`; E6.S4 adds the
> opt-in's side effect; this story grows it into the "In use now" + "Chain" pair. **Three stories, one method,
> one call site** — which is the shape that keeps I12's "apply UI side effects explicitly" testable.

## Story

As a curious player,
I want one place that says which engine is being used and what the order is,
So that the chip's glyph has somewhere to be explained in full.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E7.S7. Do not weaken._

1. **Given** UX-DR15 and §4.1
   **When** the About tab is restructured
   **Then** **everything stays in the About tab** — no settings window, no sixth tab; the tab order fixed by
   `project-context.md` and referenced by `MainTabs_SelectionChanged` is unchanged; the existing "Better
   translations (optional)" heading becomes **"Translation engines"** with three blocks: *what is being used*
   (read-only), *your keys*, *offline engine*.
2. **Given** the mockup of §4.2
   **When** the block renders
   **Then** it shows an `In use now` line (chip + reason) and a read-only `Chain` line
   (`Google → Edge → Google (old) → Offline (not installed)`), both refreshed by `UpdateEngineStatusUi()`.
3. **Given** the opening sentence
   **Then** it reads *"By default everything runs on free engines — no key, no signup, nothing to set up."* and
   the keys block states *"Keys are stored only on your PC."*
4. **Given** principle 2
   **When** any degraded-mode flow runs
   **Then** **none of them requires the user to open this tab** — many users never will.

## §4.2's mockup, verbatim

```
 Translation engines                                                     (About tab)
 ─────────────────────────────────────────────────────────────────────────────────
 By default everything runs on free engines — no key, no signup, nothing to set up.

   In use now   ● Edge · backup            Google is paused, retries in 0:58
   Chain        Google → Edge → Google (old) → Offline (not installed)

 Your keys (optional)
 ─────────────────────────────────────────────────────────────────────────────────
 A key gives you better translations and your own quota, instead of sharing a free
 door with everyone else. Keys are stored only on your PC.

   DeepL key    [ ••••••••••••••••••••••••••••  ]  [ Save ]  [ Test key ]
                ✓ Key works — DeepL is used for what you write.

   Azure key    [ ••••••••••••••••••••••••••••  ]  [ Save ]  [ Test key ]
   Region       [ westeurope            ▾ ]   (the region shown on your Azure resource)
                ✓ Key works (westeurope) — Azure is used for what you write.

   [ ] Use my Azure key for screen reading too (uses your free quota faster)
       Azure gives you 2 million characters a month for free — roughly 20 to 40
       hours of busy chat. Screen reading is off by default because live mode
       reads every new line and can use it up in a few evenings.

 Offline engine (optional)
 ─────────────────────────────────────────────────────────────────────────────────
   ○ Not installed — about 50 MB to download, works with no internet at all.
     Used only when every online engine is unavailable.
                                       [ Download the offline engine ]
 ─────────────────────────────────────────────────────────────────────────────────
```

## Tasks / Subtasks

- [ ] **T1 — Restructure the region, in place (AC 1).** `MainWindow.xaml:610-626` today: the heading (`:611-612`),
      a two-paragraph explanation (`:613-619`), the `DeepLKeyBox` + Save row (`:620-624`) and `DeepLStatus`
      (`:625-626`). E6.S3 has already added the Azure key row, the region combo, `AzureStatus` and (E6.S4) the
      opt-in checkbox underneath. **This story reorders and re-heads what is there** — it does not rebuild it.
  - [ ] Heading → **"Translation engines"** (`GoldBrush`, `FontSize="15"`, `FontWeight="Bold"` — the existing
        section-heading idiom, `:611-612`, `:628-629`, `:640-641`).
  - [ ] **Three sub-blocks**, in §4.2's order: *what is being used* (new), *Your keys (optional)* (existing,
        re-headed), *Offline engine (optional)* (new placeholder — **E8.S3 fills it**).
  - [ ] **The DeepL explanatory paragraph at `:613-619` is now wrong** and this story owns retiring it: it says
        *"translation uses Google (free, no setup)"* (the default endpoint changed in A.1 — it is
        `clients5.google.com/translate_a/t?client=dict-chrome-ex` now, though the **user-facing** name is still
        `Google`, which is fine) and *"Reading the screen with OCR **always** uses the free Google engine, so a
        busy live feed can never burn through your DeepL quota."* That second sentence is **false as of E6.S4** —
        Azure can be on the read path behind the opt-in. **It must be rewritten here or in E7.S8.** Do it here:
        it is inside this block.
  - [ ] **No sixth tab, no settings window, no change to `MainTabs`.** Tab order is fixed by
        `project-context.md` (Phrasebook 0 · Squad 1 · Translator 2 · Screen OCR 3 · About 4) and
        `MainTabs_SelectionChanged` must keep its `e.Source is TabControl` filter.
- [ ] **T2 — The `In use now` line (AC 2).** Chip + reason, on one row.
  - [ ] **Reuse E7.S3's chip** — the same pure state function and the same `UserMessages.Name` table. A **fourth
        placement**, and it should be built from the same composer, not re-derived. (E7.S3's AC 1 names three
        placements; this is a fourth, in a tab, and it is why the composer must be a function of values.)
  - [ ] The **reason** clause (`Google is paused, retries in 0:58`) comes from
        `ChainTranslator.LastOutcome.Skipped` + the countdown text (E7.S2). It is §2.2's About-tab column:
        `● Google (free) · DeepL for what you write` (S1), `○ paused — retries in 0:58` (S3),
        `✕ DeepL refused this key.` + the fix (S7), `⚠ Quota used up — resets on the 1st` (S8).
  - [ ] **The countdown here counts** — §2.4's *one countdown per window* rule. The About tab is on the main
        window, and so are the chip and the status lines. **Only one of them may show the clock at a time.**
        Simplest rule that satisfies hint 2: **the About tab's line shows the clock only while the About tab is
        selected**, and the chip yields to it then. Decide it explicitly and write the rule in a comment.
- [ ] **T3 — The `Chain` line, read-only (AC 2).** `EngineChainText`, a **read-only `TextBlock`** (§4.3).
  - [ ] Composed from the **actual built chain**, not from a literal. The source of truth is
        `ProviderIds.All` (`ProviderGates.cs:29-32` — *"Every id, in the chain order of §8.1 — **for the E7
        status list**"*; this is the list it was written for) filtered/annotated by what the chain really
        contains.
  - [ ] **It must not lie.** §4.2's example reads `Google → Edge → Google (old) → Offline (not installed)`, but
        **Edge is not shipped** (ruling **E3-d**, U2 owner-blocked) and **Bergamot is not shipped** (E8). Render
        unavailable tiers honestly — `Edge (not available)` / `Offline (not installed)` — or omit them; **do not
        print a chain the app does not have.** This is the same honesty problem E7.S3's tooltip has, and the two
        must agree word for word (UX-DR19).
  - [ ] **It must reflect the keys.** With a DeepL key: `DeepL → Google → …` on the write path. With Azure and
        the opt-in: Azure appears on the read path too. §4.2 shows **one** Chain line; the app has **two**
        chains (I8 — DeepL is structurally absent from the read one). **Flag it**: either two lines
        (`When you write` / `When reading the screen`), or one line plus a clause. **Recommend two lines** —
        the whole point of the opt-in is that the two differ, and E6.S4's cost sentence has just told the user
        so.
  - [ ] **Route the chain read through `Services/`** — TP-START-02: the code-behind may not name
        `ProviderGates`. Use the same facade E7.S3 adds (preferably off the `ChainTranslator` the code-behind
        already holds).
- [ ] **T4 — `UpdateEngineStatusUi()` grows (AC 2).** Created by **E6.S3**, called from `ApplySettings` exactly
      where `UpdateOcrFilterUi` is (`MainWindow.xaml.cs:249`). This story adds the two new lines to it.
  - [ ] **It is also called on every state change** — a key save, the opt-in toggling, a chain rebuild, and
        **from E7.S2's 1 Hz tick while paused** (for the countdown). Not on a timer otherwise.
  - [ ] **Every assignment through `SetIfChanged`** (E7.S2) — NFR7.
  - [ ] **TP-SET-07** asserts it runs from `ApplySettings` at the `UpdateOcrFilterUi` placement. That test is
        E6.S3's; it must stay green.
- [ ] **T5 — The copy (AC 3).** Opening sentence: *"By default everything runs on free engines — no key, no
      signup, nothing to set up."* Keys block: *"A key gives you better translations and your own quota, instead
      of sharing a free door with everyone else. Keys are stored only on your PC."*
  - [ ] **These are user-facing strings ⇒ `Services/UserMessages.cs`** (GAP-4 / UX-DR19), not XAML literals — so
        the "exactly once" scan can see them and E7.S8's README can quote the same words.
        **Note the tension:** the About tab's existing prose is XAML literals with `Hyperlink`s
        (`:613-619`, `:642-670`), and moving *all* of it into a code table would be silly. **Rule: the sentences
        §3/§4 specifies go in `UserMessages`; static page prose stays in XAML.** Write the rule down.
- [ ] **T6 — The offline-engine block, as a placeholder (AC 1).** §4.2's third block:
      `○ Not installed — about 50 MB to download, works with no internet at all. Used only when every online
      engine is unavailable.` + a `[ Download the offline engine ]` button.
  - [ ] **E8.S3 owns the behaviour** and the consent dialog (§3.6). **E8 is not shipped and may never be** — it
        is gated on U6/U7 and on R-12 (one measured affected machine).
  - [ ] **Recommended: ship the heading and the "not installed" line, and leave the button out** until E8.S3
        lands. A visible Download button that does nothing is worse than an absent one, and §4.2's own
        `(once installed)` variant is unreachable. **Winston to confirm**; either way, do not implement a
        download here.
  - [ ] **Ruling R-4** governs the setting when E8 arrives: `OfflineFallbackEnabled` is written by **Download**
        (true) and **Remove** (false); **there is no checkbox.** E6.S3 already landed the field.
- [ ] **T7 — AC 4 is a review, not a change.** Walk §5's five flows and confirm none of them *requires* opening
      this tab: (a) throttle — silent; (b) all paused — status line + nudge; (c) pasting a key — the user chose
      to be here; (d) offline — user-initiated; (e) no network — status line; (f) launch — chip. **Record the
      walk** in the Completion Notes; it is the only way AC 4 is verifiable.
- [ ] **T8 — Tests.**
- [ ] **T9 — `dotnet test tests/PWRUHelper.Tests`.** Green, headless, the `WPF` collection under 15 s.

## Dev Notes

### Invariants this story must keep

- **I12** — `UpdateEngineStatusUi()` is the explicit side-effect application; nothing in it may write settings.
  It is called *from inside* `ApplySettings`' `try`, with `_restoringSettings` true.
- **I8** — DeepL is structurally absent from the read chain. The Chain line **must show that**, which is T3's
  two-line recommendation.
- **I2 / NFR7** — the countdown is formatted by `MainWindow`; assignments go through `SetIfChanged`; nothing
  polls except E7.S2's 1 Hz timer while paused.
- **I10 / TP-START-02** — the code-behind may not name `ProviderGates`; route through `Services/`.
- **UX-DR18** — **no new brush, font, image or colour.** The palette is `Theme.xaml`'s; the glyphs
  (`● ○ ⚠ ✓ ✕ →`) all ship.
- **I11** — the key boxes are `PasswordBox`es and nothing on this tab renders a key. `CopyErrorReport_Click`
  (`MainWindow.xaml.cs:312-322`) is on this same tab and must stay key-free (E6.S3's AC 7).

### Rulings that apply

- **R-4** — `OfflineFallbackEnabled` is written by Download/Remove; no checkbox.
- **R-7** — the setting names; the UX doc's `AzureForReading`/`OfflineEngineEnabled` are the rejected spellings.
- **R-3 / OQ-2 / OQ-c** — `LastOutcome`, the stable provider names, and the 1 Hz poll.
- **E3-d** — Edge is not shipped. The Chain line must not claim it.
- **GAP-4 / UX-DR19** — the specified sentences exist exactly once.

### Repo traps

- **`MainTabs_SelectionChanged` must filter `e.Source is TabControl`** — `project-context.md` calls it out, and
  T2's "only while the About tab is selected" rule makes this the first story in a while that reads the
  selection. **Do not remove or bypass the filter**; a `TabControl`'s `SelectionChanged` bubbles from every
  `ComboBox` inside it, and the region combo E6.S3 added is inside this very tab.
- **`s.LastTab` is persisted and restored** (`MainWindow.xaml.cs:229-230`, `AppSettings.LastTab`). A user whose
  last tab was About lands here at startup — so anything expensive on tab-selection runs on their launch path.
  Keep it to a text compose.
- **The dark `ToolTip` style in `Theme.xaml` is load-bearing** — if the Chain line grows a tooltip, plain text
  only.
- **The About tab is one long `StackPanel` inside a `ScrollViewer`.** Adding blocks pushes "Something went
  wrong?" (`:628`) and "Made by Kizotis" (`:640`) down. That is fine — but the error-report button is a support
  path and should not end up below the fold on a small window. Check at 800×600.
- **`GhostButton` + `Padding` is the button idiom here** (`:622-623`, `:636-637`).
- No `.sln`; `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and `DefaultItemExcludes` (`:18`) intact.

### Line-number drift

| The docs say | Actually (this commit, after E5.S4) |
|---|---|
| epics/§4.1: the region at `MainWindow.xaml:611-626` | **`:610-626`** — heading `:611-612`, prose `:613-619`, key row `:620-624`, `DeepLStatus` `:625-626`. E6.S3/E6.S4 add rows below it before this story runs. |
| `UpdateEngineStatusUi` called at `MainWindow.xaml.cs:193` | `UpdateOcrFilterUi(...)` is at **`:249`**; `ApplySettings` is **`:207-258`** |
| §4.2's Chain line `Google → Edge → Google (old) → Offline (not installed)` | **Edge is not shipped** (E3-d) and Bergamot is not shipped (E8). Render honestly or the tab lies. |
| §4.2 shows **one** Chain line | the app has **two** chains and I8 makes them structurally different. Two lines recommended. |
| the existing DeepL prose (`:617-619`) | *"Reading the screen with OCR **always** uses the free Google engine"* is **false once E6.S4 ships**. Rewrite here. |
| §4.2's `[ Download the offline engine ]` | **E8.S3**, not shipped, possibly never. Ship the line, not the button (T6). |
| `ux` §4.3's `EngineChainText`, `OfflineEngineToggle` | control names only; the settings names are R-7's |

## Testing

- **TP-IDs:** the epic asks for *"STA render of the block; a unit test that `UpdateEngineStatusUi` is called from
  `ApplySettings` at the same place `UpdateOcrFilterUi` is"* — the second is **TP-SET-07** (E6.S3's, must stay
  green). Land the render as a **TP-RENDER-** case in `TemplateRenderTests`' file and record the ID.
- **The Chain-line composer is a pure function** — `(settings, chain contents) → string` — and it is where the
  real assertions go: no key ⇒ the free chain; DeepL key ⇒ DeepL first on the write line; Azure key + opt-in ⇒
  Azure first on the read line; **never DeepL on the read line** (I8, and it is a second, cheap guard alongside
  `TP_CHN_14`); unavailable tiers rendered honestly.
- **The `In use now` composer** reuses E7.S3's state function — assert it produces the **same** text for the
  same state, so the tab and the chip cannot disagree (UX-DR19 applied across surfaces).
- **AC 3:** the two literals exist exactly once (the UX-DR19 scan).
- **AC 4:** not automatable; T7's recorded walk is the evidence.
- **STA budget:** **CI-4** — the `WPF` collection stays **< 15 s**. Render the block **once** with a state loop.
- **Isolation:** `TempSettings` (a `MainWindow` writes settings); IS-6 for any countdown; **CI-3** nothing
  sleeps.
- **Must keep passing unchanged:** `TP-SET-07` (E6.S3), `StartupSettingsTests`, `TemplateRenderTests`' existing
  cases, `ChainCompositionTests`, `ProviderStateStoreTests`' TP-START-02 allow-list (**unchanged by this
  story** — if it fails, T3's facade was skipped).

## File List

**Modify**
- `MainWindow.xaml` — the About-tab region (`:610-626`): heading → "Translation engines", three sub-blocks, the
  `In use now` row, `EngineChainText`, the offline placeholder, the corrected DeepL prose
- `MainWindow.xaml.cs` — `UpdateEngineStatusUi()` grows the two lines; the About-tab countdown rule (T2)
- `Services/UserMessages.cs` — §4.2's specified sentences (AC 3), the Chain-line composer's labels
- `Services/TranslationChains.cs` — the chain-contents facade (shared with E7.S3's snapshot read)

**Modify (tests)**
- `tests/PWRUHelper.Tests/TemplateRenderTests.cs` — the block's STA render
- `tests/PWRUHelper.Tests/UserMessagesTests.cs` — AC 3's uniqueness

**Create (tests)**
- `tests/PWRUHelper.Tests/EngineStatusTests.cs` — the Chain-line composer, the `In use now` agreement with the
  chip

## Size, dependencies, risk

**Size:** M · **Depends on:** **E7.S3** (the chip's state function and provider names — this is its fourth
placement) and **E6.S3** (`UpdateEngineStatusUi`, the key rows, `AzureStatus`) · **Risk:** **low-medium.** The
code is a compose-and-assign. Two things make it more than cosmetic: the Chain line is the **only** place the
app states its own composition, so an inaccurate one is a support burden rather than a typo (Edge, Bergamot, and
the read/write split are all easy to get wrong), and adding a fourth countdown surface is the easiest way to
break hint 2's one-clock-per-window rule ·
**DoD:** the block matches §4.2.

## How to verify manually

1. `dotnet test tests/PWRUHelper.Tests` — green.
2. **AC 1.** The About tab still exists at index 4, there is no sixth tab and no settings window, and the
   heading reads **Translation engines** with three sub-blocks in §4.2's order.
3. **The Chain line is true.** With no keys: it names only the engines that actually exist —
   **no Edge**, and Offline shown as not installed. Add a DeepL key: the write line starts with DeepL and the
   read line does **not**. Add Azure + tick the opt-in: Azure leads both.
4. **`In use now` agrees with the chip.** Force a pause: the tab's line and the chip say the same thing, in the
   same words.
5. **One clock per window.** With the About tab open during a pause, the clock appears **once** on that window.
6. **AC 4.** Run flows (a), (b) and (e) with the About tab **closed**: every one of them is fully explained
   without opening it.
7. **The false sentence is gone.** Search the About tab for *"always uses the free Google engine"* — it must not
   be there once the Azure read opt-in exists.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E7.S7; Epic 7 header]
- [Source: docs/investigations/02-traduction/ux-mode-degrade.md#4.1 Layout decision; #4.2 ASCII mockup; #4.3 Control-by-control specification; #2.2 (the About-tab column); #1 principle 2]
- [Source: docs/investigations/README.md#Architect's rulings — R-3, R-4, R-7, OQ-c, E3-d, GAP-4]
- [Source: docs/investigations/02-traduction/architecture-cible.md#8.1 Composition (the two chains); #12 Settings]
- [Source: docs/investigations/03-stories/test-plan.md#3.9 TP-SET-07; #3.11 TP-RENDER-*; #3.12 TP-START-02; #5 CI-3/CI-4]
- [Source: docs/investigations/03-stories/stories/E6.S3-azure-key-and-region-settings.md; E7.S3-provider-chip-and-tooltip.md]
- [Source: project-context.md#Code Layout (tab order); #Framework-Specific (WPF) Rules (MainTabs_SelectionChanged, the dark ToolTip style)]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) — Claude Opus 5, CS / Create-Story, non-interactive.

### Debug Log References

### Completion Notes List

### File List

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **ready-for-dev (Phase 4 — owner's go given)**. Records that `UpdateEngineStatusUi()` is created by **E6.S3**, extended by **E6.S4** and grown here — three stories, one method, one call site — and that this is the chip's **fourth** placement, so E7.S3's state function must be a function of values or it will be re-derived here. Flags three ways §4.2's mockup would make the tab lie: **Edge is not shipped** (E3-d) and Bergamot is not (E8), so the Chain line must render unavailable tiers honestly and must agree word-for-word with E7.S3's tooltip; **§4.2 shows one Chain line but the app has two chains** and I8 makes them structurally different — two lines recommended; and the **existing DeepL prose at `MainWindow.xaml:617-619` becomes false the moment E6.S4 ships** (*"Reading the screen with OCR always uses the free Google engine"*), so this story retires it. Recommends shipping the offline block's **line without its button** until E8.S3 exists, and notes that T2 adds a fourth countdown surface — the one-clock-per-window rule (hint 2) needs an explicit rule, proposed as "the About tab shows the clock only while it is the selected tab". |
