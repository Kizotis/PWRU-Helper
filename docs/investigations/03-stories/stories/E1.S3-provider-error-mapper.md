# Story E1.S3: Classify every provider outcome in one `ProviderErrorMapper`

Status: **done**

_Epic E1 (A) — Diagnosable translation pipeline · increment 0 · Release A.0 · ⛔ no production code until the
owner's explicit go._

## Story

As the maintainer,
I want one function that turns a raw HTTP outcome into a kind,
So that a bot block stops reading like a bug and no second place can ever disagree about what a 403 means.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E1.S3. Do not weaken._

1. **Given** the ordered rules of `architecture-cible.md` §4.2 (first match wins)
   **When** `ProviderErrorMapper.Classify(resp, bodyHead, transport, keyWasSent, ct)` is called
   **Then** it returns `Cancelled` only when `ct.IsCancellationRequested`; `Timeout` for an OCE with the token
   **not** cancelled; `Network` for `HttpRequestException`; `RateLimited` for 429; `AuthFailed` for 401;
   `QuotaExhausted` for 403 with a key and a quota envelope; `AuthFailed` for 403 with a key; `Blocked` for 403
   with no key; `QuotaExhausted` for 456; `Unavailable` for ≥500; `BadResponse` for an unparseable success;
   `Unknown` for anything else non-success.
2. **Given** today's `TranslationService.cs:138-143` folds 403 with 400/404 into
   `Translation service error (HTTP {code})`
   **When** the mapper lands
   **Then** a 403 with no key classifies as `Blocked` and a 400 classifies as `Unknown` — they are no longer the
   same outcome.
3. **Given** the mapper is a total function
   **When** it is handed an outcome no rule matches
   **Then** it returns `Unknown` and never throws, never guesses.

## Tasks / Subtasks

- [x] **T1 — Create `Services/ProviderErrorMapper.cs`** (AC: 1, 3) — the glossary §16 target file
  - [x] Exact signature from §4.1:
        `internal static TranslationErrorKind Classify(HttpResponseMessage? resp, string? bodyHead, Exception? transport, bool keyWasSent, CancellationToken ct)`.
  - [x] Implement the §4.2 table **in order, first match wins**. Write the rules in the same order as the table so
        a reviewer can diff them line by line against the document.
  - [x] Row 1 — `ct.IsCancellationRequested` ⇒ `Cancelled`. Carry the caller contract as a comment, verbatim:
        `// if (kind == Cancelled) throw;   // rethrow the original OCE, never wrap it`.
  - [x] Row 2 — `transport is OperationCanceledException` (which includes `TaskCanceledException`) with the token
        **not** cancelled ⇒ `Timeout`. **This is the OCE trap, encoded once, for the whole app.**
  - [x] Row 3 — `transport is HttpRequestException` ⇒ `Network`.
  - [x] Rows 4–10 — 429 ⇒ `RateLimited`; 401 ⇒ `AuthFailed`; 403 + `keyWasSent` + a quota/limit envelope ⇒
        `QuotaExhausted`; 403 + `keyWasSent` ⇒ `AuthFailed`; 403 without a key ⇒ `Blocked`; 456 ⇒ `QuotaExhausted`;
        ≥ 500 ⇒ `Unavailable`.
  - [x] Row 11 (**the HTML page, on any status including 200**) is a call into the sniffing helper that
        **E1.S4** adds to this same file. In this story leave the seam explicit — a `// E1.S4: HTML sniff goes
        here, BEFORE the success path` marker at the right position, so the two stories cannot fight over ordering.
  - [x] Row 12 — a success whose body does not parse into the provider's shape ⇒ `BadResponse` (the provider's
        parser reports this; the mapper only names it).
  - [x] Row 13 — anything else non-success ⇒ `Unknown`. **Never throws, never guesses** — a wrong guess is worse
        than an honest `Unknown`. If `Unknown` shows up in field logs, that is a bug report about the mapper.
  - [x] The "quota envelope" test for row 6 is a substring check over `bodyHead` (Azure's
        `{"error":{"code":403,…"quota"…}}` shape); keep the marker literals next to the rule or in
        `TranslationPolicy` if E1.S4 wants them there too.
- [x] **T2 — Call it from the two existing providers** (AC: 1, 2)
  - [x] `Services/TranslationService.cs:119-178` — the retry loop. Replace the ad-hoc status branching at
        `:138-148` with `Classify(resp, bodyHead: null, transport: null, keyWasSent: false, ct)`, then throw a
        `TranslationException(kind, <today's message for that kind>)`. **`keyWasSent` is `false` on this provider —
        it is the keyless Google endpoint**, which is precisely why its 403 must classify as `Blocked`.
  - [x] Keep the retry *decision* where it is for now (`transient = code == 429 || code >= 500`, `:139`). Changing
        which kinds are retried is **E2.S5**, not this story. This story changes classification only.
  - [x] `Services/DeepLTranslator.cs:80-110` (the OCE/`HttpRequestException` catches and the status switch) and
        `:125-129` (`Parse`'s catch) — route through `Classify` with `keyWasSent: true`. The
        `when (ct.IsCancellationRequested)` filter at `:80` **stays**: the mapper's row 1 and that filter say the
        same thing, and the filter is what makes the rethrow happen.
- [x] **T3 — Preserve every user-visible string** (AC: 2)
  - [x] For each `(kind)` the two providers can now produce, throw with **the string that site produces today**.
        The 403-with-no-key case previously produced `Translation service error (HTTP 403). Please try again later.`
        — it still does in this increment; only its `Kind` changed from `Unknown` to `Blocked`. The wording change
        arrives in **E7.S1**.
  - [x] Take a before/after diff of the user-visible strings. It must be empty (this is E1.S6's DoD too, and the
        pair is the mitigation for this story's risk).
- [x] **T4 — Tests, one per §4.2 row** (AC: 1, 2, 3)
  - [x] **TP-MAP-01** genuine cancel ⇒ `Cancelled`. **TP-MAP-02** `TaskCanceledException` with ct **not**
        cancelled ⇒ `Timeout` — the R-03 regression guard, and the densest cluster in the test plan by design.
  - [x] **TP-MAP-03** `HttpRequestException` ⇒ `Network`. **TP-MAP-04** 429 ⇒ `RateLimited`.
        **TP-MAP-05** 401 ⇒ `AuthFailed`. **TP-MAP-06** 403 + key + quota envelope ⇒ `QuotaExhausted`.
        **TP-MAP-07** 403 + key, no quota wording ⇒ `AuthFailed`. **TP-MAP-08** 403, no key ⇒ `Blocked`.
        **TP-MAP-09** 456 ⇒ `QuotaExhausted`. **TP-MAP-10** 500/502/503 ⇒ `Unavailable`.
        **TP-MAP-15** 200 + `not json` ⇒ `BadResponse`.
        **TP-MAP-16** (**T14**) 403 ⇒ `Blocked` while 400 and 404 ⇒ `Unknown` — asserted as a pair, because the
        point is that they are no longer the same outcome.
  - [x] Drive the provider-level cases through **E1.S1's `FakeHandler`** so the request really reaches the parser;
        drive the pure rows directly against `Classify`.
  - [x] The existing I3 guards (`TranslationBackendTests.cs:84,98`) must still pass **unchanged**.

### Review Findings

_Code review of `3ec06be` (Amelia — Blind Hunter, Edge Case Hunter and Acceptance Auditor run in parallel, then
triaged against the code). Fixed items landed in the review-fix commit; recorded items are pre-existing, belong
to a named later story, or need the architect's call and were deliberately left alone._

**Fixed in review**

- [x] [Review][Patch] **The mapper's §4.1 caller contract was quoted but implemented at neither call site**
      [`Services/TranslationService.cs`, `Services/DeepLTranslator.cs`] — `ProviderErrorMapper.cs:43-44` carries
      `if (kind == Cancelled) throw;` verbatim, and all four `Classify` calls then hand the returned `kind`
      straight to a `TranslationException` constructor. Row 1 answers with the cancel `Kind` whenever the token
      is cancelled *at the moment it reads it*, so a Stop landing between the response arriving and the
      classification builds the one exception `TranslationErrors.cs:23-28` forbids outright — the phantom
      user-cancel, arriving from the mapper rather than from a literal, which is exactly the shape TP-MAP-17's
      substring scan cannot see. The status branches had no preceding cancel check at all; the transport
      catches had one, but a filter that ran a statement earlier is not the same token read. `Classify` is now
      preceded by `ct.ThrowIfCancellationRequested()` at all four sites, so row 1 cannot fire and the only way a
      cancel leaves either provider is as an `OperationCanceledException`. No test can race this deterministically;
      the guard is structural and the comment says so. (high)
- [x] [Review][Patch] **The one-line path still swallowed a genuine cancel** [`Services/TranslationService.cs`] —
      the Completion Notes claim "every OCE catch in the pipeline filtered", but `TranslateLinesAsync`'s
      `SafeOne` (the `lines.Count == 1` path, which takes neither the batch nor the per-line loop) ended in a
      bare `catch (Exception ex)` that turned a real Stop into a translation-failed line instead of propagating
      it. I3 asks every OCE catch in the pipeline to say what it means. Filtered, and pinned red-first by
      `A_real_cancel_propagates_from_the_one_line_path_too`. `grep` over `Services/` now shows every OCE catch
      filtered. (medium)
- [x] [Review][Patch] **Rows 6/7/8 were not written in the table's order**
      [`Services/ProviderErrorMapper.cs`] — the nested conditional tested **row 8** first
      (`!keyWasSent ? Blocked : …`). Semantically identical and every row test passed, but T1's one instruction
      that exists purely for reviewability is "write the rules in the same order as the table so a reviewer can
      diff them line by line". Rewritten as three separate rules, 6 then 7 then 8. (low)
- [x] [Review][Patch] **The three batch exits were unpinned** [`tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs`]
      — the story names "a batch timeout no longer falls through to per-line" as its first behaviour consequence
      and nothing asserted it. Added one case per exit: `A_batch_timeout_propagates_and_never_fans_out_per_line`
      (one request, `Timeout`, no fan-out — the amplifier this closes is N × 12 s inside one LIVE tick),
      `A_batch_count_mismatch_falls_through_to_the_per_line_loop` (§6.3's join/split rule, three requests, both
      lines really translated), and `A_batch_body_that_does_not_parse_propagates_as_one_BadResponse`. The third
      records a decision rather than a rule: §6.3 grants the per-line fallback to a **count mismatch**, not to a
      body that is not the provider's shape, and re-asking a broken endpoint N times ends on the latch anyway.
      Behaviour is pre-existing (`catch (TranslationException) { throw; }` predates this epic). (medium)
- [x] [Review][Patch] **`RetryAfter` had no garbage-tolerance pin** [`tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs`]
      — the helper is dead data until E2, which makes it exactly the kind of code that rots unwatched, and E2
      will read it as a "do not call again until". Added a six-case theory over the shapes a strict parser
      rejects (a word, a negative delta, a delta that overflows `int`, an unparseable date, an empty value, a
      list): all null, none throws. `DateTimeOffset` overflow is unreachable — the delta parser is bounded by
      `int`, so the worst case is ~68 years, not a throw. (low)
- [x] [Review][Patch] **DeepL derives the `Kind` and the sentence from two independent switches over the same
      status** [`Services/DeepLTranslator.cs`] — they agree only while `bodyHead` stays `null`. The moment E6
      passes one (which the comment above them invites), a quota-bearing 403 becomes `QuotaExhausted` while the
      message switch still tells the user to check the API key: the two-places-disagree bug the mapper exists to
      end, reintroduced one argument at a time. Left in place (E1.S6 owns wording) with a warning naming the
      trap and its owner. (low)
- [x] [Review][Patch] **Row 12's caller contract was implicit** [`Services/ProviderErrorMapper.cs`] — `Classify`
      returns `BadResponse` for *any* success, because it has no way to know whether the body parsed; the row
      only names what the provider's parser detected. That makes TP-MAP-15 unable to fail for the reason it
      states, and it is a live trap for E2.S5's `HttpProviderCore`, which becomes the single caller. The rule
      now says it: classify the failure, never the success. (low)
- [x] [Review][Patch] **Stale provenance and a stale rationale** [`Services/TranslationPolicy.cs`,
      `tests/PWRUHelper.Tests/TranslationErrorsTests.cs`] — `TranslationPolicy`'s whole purpose is
      `[CONFIRMED] file:line` provenance, and two of its citations pointed at pre-E1.S3 line numbers
      (`:145`/`:177`, now `:158`/`:229`). `Googles_non_transient_status_is_the_Unknown_placeholder_until_E1_S3`
      still documented Google as "**still** folds 403 in with 400/404 … until E1.S3's mapper splits it" — this
      commit is E1.S3. Retitled and re-argued around what it actually pins now: 400 stays `Unknown`, and
      "`Unknown` must never become common" only means something if something asserts what still belongs there. (low)

**Recorded, not fixed**

- [ ] [Review][Decision] **LIVE now auto-stops after five network-less ticks; before this commit it never did**
      [`Services/TranslationService.cs` → `MainWindow.Live.cs:228-239`] — **needs the architect's call.** Old
      chain: a last-attempt `HttpRequestException` escaped `RequestAsync` raw, `TranslateLinesAsync`'s bare
      `catch { }` swallowed it, the per-line loop gave each line a translation-failed placeholder, and the method
      **returned normally** — nothing reached the LIVE loop and `consecutiveErrors` was reset. New chain: the
      batch throws `TranslationException(Network, …)`, `catch (TranslationException) { throw; }` propagates it
      through `AppendLinesToHistory`, and five consecutive ticks call `StopLive()`. `test-plan.md` §S6 records
      "LIVE does **not** auto-stop on `Network` and keeps OCR-ing" as a **deliberate divergence** from a provider
      pause (`ux-mode-degrade` flow (e).3, GAP-3), and this story's Dev Notes say "no LIVE change" (I16). Not
      reverted here: undoing it means either re-orphaning the friendly sentence or restoring the per-line
      amplification on a dead network, and LIVE's stop policy is E5's to set.
- [ ] [Review][Defer] **T3's "the rendered output is unchanged everywhere" is not supportable** — the
      instrument chosen (a sorted set of string literals per file) is empty by construction here, because the
      friendly `Network` sentence already existed **as dead code** below the loop; throwing it changes what the
      user reads without adding a literal. Three rendered paths changed, all improvements, none reverted:
      (1) the Translator tab with no Internet went from `no Internet connection` (`Friendly`'s
      `HttpRequestException` arm, reached by the raw escape) to the friendly sentence; (2) a **one-line** batch
      timeout went from a translation-failed line carrying `HttpClient`'s raw timeout text to `(the request
      timed out)`; (3) a per-line `Network` failure used to mark that one line and keep going, and now latches.
      The story's "How to verify manually" states the Translator-tab baseline incorrectly for the same reason.
      **E1.S6 must know the baseline it is rewording is not the one recorded here.**
- [ ] [Review][Defer] **The per-line `rateLimited` latch now fires on `Timeout` and `Network` too** — it
      discriminates on exception *type*, which is what this epic is retiring. Accepted for increment 0 (the
      ruling on this story) for `Timeout`; the `Network` case is the same shape and was not named. §6.3 narrows
      the latch to a `RateLimited`/`Blocked` `Kind` — that story undoes both, and E1.S6/E7 own the placeholder's
      wording, which currently tells a player "rate-limited" for a dead network.
- [ ] [Review][Defer] **§5.5's `[1 s, OpenCapMinutes]` clamp is absent from `RetryAfter`** — an HTTP-date
      already elapsed yields a `RetryAt` in the past, and a 30-day delta yields a 30-day one. Deliberate:
      `OpenCapMinutes` does not exist until E2, and clamping here would invent policy in a file that holds none.
      **Whoever first reads `RetryAt` owns the clamp.**
- [ ] [Review][Defer] **`QuotaMarkers` lives outside `TranslationPolicy`, so its `[ASSUMED]` grade is not
      enforced** [`Services/ProviderErrorMapper.cs`] — `TranslationPolicyTests.Every_member_carries_exactly_one_evidence_grade`
      scans that type's fields only, and §16 calls `TranslationPolicy` the home for "every tunable number **and
      marker string**". The story permits either home and the sibling HTML markers were already in
      `TranslationPolicy` before this commit, so the Completion Notes' reason for the split is wrong even though
      the placement is allowed. E6.S2 moves it. Separately, `"limit exceeded"` will also match throttle wording
      (`rate limit exceeded`, `Daily Limit Exceeded`) and classify a throttle as terminal — unreachable today
      (both providers pass `bodyHead: null`), settled by a captured Azure body.
- [ ] [Review][Defer] **`json!` is unreachable today, and E2.S5 is what can break it**
      [`Services/TranslationService.cs`] — verified: every attempt-2 path (each status branch and the transport
      catch) ends in a throw, so the loop cannot fall out with `json == null`. But the same commit announces that
      E2.S5 changes which `Kind`s are retried; the moment a status branch stops throwing at `attempt == 2` this
      becomes an `ArgumentNullException` out of `JsonDocument.Parse`, past the `TranslationException` contract.
      **E2.S5 re-checks this line or restores a typed guard.**
- [ ] [Review][Defer] **`IOException` / `HttpIOException` is in neither provider's transport filter** — if one
      ever surfaces from the send or the body read it escapes raw, which is the class of escape the merged
      DeepL try was written to close. Not added here: today's message ternary would render an IO failure as a
      timeout, so it needs a wording decision. It belongs with E2.S5's `HttpProviderCore`, which owns one
      transport catch for every provider.

**Dismissed** — `Enum.Parse`-by-name for row 1's value (the accepted resolution of the TP-MAP-17 knot: a cached
static, failing at type-init, with the member name pinned by the enum-order test) · 401 with no key ⇒ `AuthFailed`
(faithful to §4.2 row 5, which carries no key condition — a table question, not a code one) · a status ≥ 600 ⇒
`Unavailable` (faithful to row 10 as written) · `internal static readonly string[]` markers being element-mutable
(the identical shape `TranslationPolicy` already ships) · `RetryAt` attached to auth failures (null unless the
server actually sent a `Retry-After`) · the mapper not unwrapping `InnerException` chains (both call sites pass the
exception their own filter already matched).

## Dev Notes

### Invariants this story must keep

- **I3 — the OCE filter.** Row 2 of the table *is* I3 written down. A `TaskCanceledException` with an uncancelled
  token is a **timeout**, never a cancel. Getting this wrong silently disabled the DeepL→Google fallback and left a
  zombie LIVE indicator once already.
- **I2** — the mapper is pure and headless: no `Logging`, no WPF, no I/O. It takes an `HttpResponseMessage` and
  returns an enum value.
- **I11** — the mapper never puts a body, a URL or a key anywhere. It *reads* `bodyHead`; **E1.S5** is the only
  story that writes anything to the log.
- **I1**, **I16** — no interface change, no LIVE change.

### Rulings and decisions that apply

- **The mapper is the single classification point.** After this story, no second place in the codebase may decide
  what a status code means. `HttpProviderCore` (E2.S5) becomes its only caller; until then the two existing
  providers call it directly (`plan-migration.md` increment 0, `epics.md` technical notes).
- Row 11 (HTML on any status) is **E1.S4's**, and it must sit **before** the success/parse path. Do not implement
  it here; do not reorder the table so that E1.S4 has to.
- `Unknown` must never become common. It is the total-function escape hatch, and its appearance in a field log is
  a defect report against this file.

### Repo traps

- `TranslationService` has **one** `GetAsync` call site (`:131`); `DeepLTranslator` has **one** `SendAsync`
  (`:78`). If a second appears, the mapper has been bypassed.
- Reading the body to produce `bodyHead` must not consume the stream twice. Read once
  (`resp.Content.ReadAsStringAsync(ct)`), keep the first ~400 chars for classification and hand the same string to
  the parser. Today the success path reads it at `TranslationService.cs:134`.
- `InternalsVisibleTo PWRUHelper.Tests` (`PWRUHelper.csproj:28`) + `tests\**` exclusion (`:18`) intact.
  **No `.sln`.** `dotnet test tests/PWRUHelper.Tests` green and headless.

### Current state of the code being modified

- `TranslationService.cs:132-148` — success ⇒ read body and break; otherwise `code = (int)resp.StatusCode`,
  `transient = 429 || >= 500`, non-transient ⇒ throw `HTTP {code}` (this is the fold this story breaks up), a 429
  on the third attempt ⇒ the "wait a minute" string, any other transient on the third ⇒ "unavailable".
- `TranslationService.cs:151` catches `HttpRequestException` **and retries** — that behaviour is E2.S5's to change
  (`Network` will stop being retried); leave it alone here.
- `DeepLTranslator.cs:96-106` — the status switch that already distinguishes 401/403, 456 and 429.

## Testing

- **TP-IDs landed:** TP-MAP-01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 15, 16. (TP-MAP-11/12/13/14 are **E1.S4's**;
  TP-MAP-17 landed in E1.S2.)
- **Fixtures:** an Azure-shaped quota envelope for TP-MAP-06 — a short inline string is enough here; the
  checked-in `azure-error-quota.json` / `azure-error-auth.json` fixtures (IS-9) arrive with E6.S2.
- **Isolation items:** uses IS-8/IS-10/IS-11 from E1.S1; adds none.
- **Must keep passing unchanged:** `TranslationBackendTests.cs:84,98` (the two I3 guards), the E1.S1 retry pins,
  `CachingTranslatorTests`.

## File List

**Create**
- `Services/ProviderErrorMapper.cs`

**Modify**
- `Services/TranslationService.cs` (`:119-178`, chiefly `:138-148`)
- `Services/DeepLTranslator.cs` (`:80-110`, `:125-129`)

**Create (tests)**
- `tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs`

## Size, dependencies, risk

**Size:** M · **Depends on:** E1.S1, E1.S2 · **Blocked by:** the owner's Phase-4 go · **Risk:** medium — a
mis-classification changes a user-visible string by accident; mitigated by E1.S6 keeping today's wording per `Kind`.
**DoD:** every §4.2 row has a passing test.

## How to verify manually

With the network disconnected, translate from the Translator tab: the message must still be the current
"Couldn't reach the translation service…" sentence. With a deliberately wrong DeepL key saved, translate: still
"DeepL rejected the API key — check it in Settings." Then diff the user-visible strings against v0.14.0 — the diff
must be empty. The behaviour change in this story is invisible by construction; only the log (E1.S5) and the later
gate (E2) will be able to tell the difference.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E1.S3]
- [Source: docs/investigations/02-traduction/architecture-cible.md#4.1, #4.2 Classification rules, #16 Glossary]
- [Source: docs/investigations/03-stories/test-plan.md#3.1 ProviderErrorMapper (TP-MAP-01..16), #1.1 risk R-03]
- [Source: docs/investigations/02-traduction/analyse-implementation-actuelle.md#6.3 Seams]
- [Source: project-context.md#The OCE trap]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on Claude Opus, workflow **DS — Dev Story**, non-interactive.

### Debug Log References

`dotnet test tests/PWRUHelper.Tests` — **287 passed before, 319 passed after, 0 failed, 2 s**, headless, no
network, no `%AppData%` write. Red phase confirmed first: with the mapper in place but the two providers
untouched, four of the new cases failed (Google 403 ⇒ `Blocked`, the last-attempt `HttpRequestException`, the
Google timeout, and the mid-batch timeout that discarded the successes). One more red arrived unprompted and is
worth recording: **TP-MAP-17 failed on the mapper's own doc comment**, because the scan is a plain substring
match over the whole file and the comment explained the ban by quoting the banned token. The comment was
reworded; the guard was right.

### Completion Notes List

- **T1 — `Services/ProviderErrorMapper.cs`** (`:45`). The §4.1 signature verbatim, the §4.2 rules in the table's
  order with the row number on each, first match wins. Pure and headless (I2): one `using System.Net.Http`, no
  `Logging`, no I/O, no state. The cancel-contract comment sits verbatim above `Classify` (`:42-43`).
- **Row 1 vs TP-MAP-17, the one real design knot.** The mapper must be able to *return* the cancel `Kind`, and
  TP-MAP-17 bans that member's qualified token from every production file — including comments, as the red run
  above proved. Resolved by construction: a `private static readonly` field resolves the member **by name**
  (`:31`), so the ban stays a mechanical substring scan, this file still cannot hand that `Kind` to a
  `TranslationException` constructor, and a rename fails loudly at type-init (the enum-order test already pins
  the name). No `using static`, so TP-MAP-17's second half holds too.
- **Row 2 is I3, encoded once for the whole app** (`:57`). Row 1 has already consumed every cancelled token, so
  an `OperationCanceledException` reaching row 2 can only be a timeout. That ordering is the whole trap.
- **Row 6's quota envelope** (`:41`, `:96`) — a case-insensitive substring test over `bodyHead` against
  `QuotaMarkers` (`quota`, `out of credit`, `limit exceeded`), graded `[ASSUMED]`. Kept next to the rule, the
  story's first option: nothing else reads them today (Rule of Three), and it is E1.S4's HTML markers that
  needed `TranslationPolicy` as a shared home. The comment names E6.S2 as the story that moves them if Azure
  wants them too.
- **Row 11's seam is explicit** (`:80`) — `// 11 — E1.S4: HTML sniff goes here, BEFORE the success path`, placed
  between row 10 and the success branch, with a line saying why it cannot move inside that branch (it applies to
  any status, 200 included). E1.S4 inserts there and reorders nothing.
- **Row 12** (`:85`) names what the provider's parser detects; **row 13** (`:89`) is the last resort. `Classify`
  contains no `throw` at all — AC 3 is structural, not only tested.
- **T2 — both providers go through it.** Google (`TranslationService.cs:171`) calls it with `keyWasSent: false`,
  which is exactly why its 403 is now `Blocked`; DeepL (`DeepLTranslator.cs:116`) with `keyWasSent: true`, so its
  403 stays `AuthFailed`. Neither passes a `bodyHead`: reading the body at the status branch would consume the
  stream the parser needs (the story's repo trap), and DeepL signals an exhausted allowance with its own 456. The
  retry *decision* is untouched — `transient = code == 429 || code >= 500`, three attempts, `300 * (attempt + 1)`
  — and says in a comment that E2.S5 owns changing it. The E1.S1 retry pins (3 requests on a 429, 3 on a 503)
  pass unchanged.
- **The four deferred items this story owned are closed:**
  1. *The unfiltered OCE catch in the per-line loop* (`TranslationService.cs:131`) now filters
     `when (ct.IsCancellationRequested)`, and `RequestAsync` hands timeouts over as a `Timeout`-kind
     `TranslationException` (`:193-206`), so a timeout on line 2 latches like any other provider failure and
     line 1's translation survives instead of being thrown away. Pinned by
     `A_timeout_mid_batch_keeps_the_lines_already_translated`.
  2. *Google's last-attempt `HttpRequestException`* no longer escapes unmapped: the transport catch classifies it
     as `Network` and carries the sentence that used to sit below the loop.
  3. *DeepL's timeout mapping now covers the body read* — send and read share one `try`
     (`DeepLTranslator.cs:110-145`). **Finding:** that escape is defensive, not live. With `HttpClient`'s default
     `ResponseContentRead` the body is buffered inside `SendAsync`, so a post-send read cannot fail from
     transport; the two `BodyFailsHandler` cases therefore prove the mapping, and the *position* is asserted by
     reading the file. The test comments say so rather than overclaiming.
  4. *The unreachable `Network` throw* is gone. Its sentence moved onto item 2's throw, where it is actually
     reached; the compiler cannot see that the loop always exits with a body, so a single `json!`
     (`TranslationService.cs:224`) carries a comment explaining why, instead of a dead branch carrying a live
     string.
- **One extra I3 line, deliberate** (`TranslationService.cs:113`): the batch attempt's bare `catch` is an OCE
  catch too, so a filtered rethrow now precedes it. Without it a genuine Stop was spent on one wasted per-line
  attempt before propagating.
- **T3 — user-visible strings.** Checked mechanically, not by eye: the sorted set of string literals in
  `DeepLTranslator.cs` is **byte-identical** to `d239fea`; `TranslationService.cs` gains exactly one,
  `"the request timed out"` — the exact sentence `Friendly()` renders **today** (`MainWindow.xaml.cs:408`) for
  the raw `TaskCanceledException` this path used to throw. The rendered output is therefore unchanged
  everywhere, including LIVE (`Live hiccup (the request timed out) — retrying…`) and the Translator tab
  (`Failed: the request timed out`). E1.S6 owns the wording.
- **Two behaviour consequences, named rather than discovered later.** (i) A timeout during the *batch* attempt
  now propagates as a `TranslationException` instead of falling through to a per-line retry — N more requests
  that would each have waited 12 s. (ii) A timeout in the per-line loop produces the existing
  `(rate-limited — try again shortly)` placeholder, because that is the latch I16 protects; the placeholder's
  wording is E1.S6/E7's to fix, not this story's.
- **`Retry-After` (§5.5)** is parsed by `ProviderErrorMapper.RetryAfter` (`:105`) — delta-seconds resolved
  against an injected `now` (IS-6-shaped, so E2's clock can drive it) or an HTTP date — and travels on the
  `RetryAt` of both providers' status throws. Nothing reads it yet; it is dead data until E2 and it renders
  nowhere. Recorded as a **deviation**: the story's ACs and tasks do not ask for it, the DS brief's reading of
  §4.2 does. Eight lines, additive, no user-visible change.
- **T4 — 32 new cases** in `tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs`: one per §4.2 row this story owns
  (TP-MAP-01…10, 15, 16), a totality case walking every status 100–599 with and without a key (AC 3: it answers,
  it never throws, and nothing but row 1 can reach the cancel value), the `Retry-After` cases, and eight
  provider-level cases driven through E1.S1's `FakeHandler`. TP-MAP-11…14 are E1.S4's and are absent by design.
  No fixture file was needed (the story allows a short inline envelope for TP-MAP-06), so the test csproj still
  has no content item group — **E1.S4 adds it** and owns the shape for E3/E6.
- One new test double, `BodyFailsHandler`, lives in the test file rather than beside `FakeHandler`: it answers a
  200 whose content throws on read, which `FakeHandler` cannot express (its bodies are complete strings). Rule
  of Three — it moves when a second story needs it.
- I1 `ITranslator` byte-identical · I2 the mapper depends on `System.Net.Http` and nothing else · I3 every OCE
  catch in the pipeline filtered, one added · I4/I5 untouched · I11 no body, URL or key is stored or forwarded —
  `bodyHead` is read and dropped · I16 retry loop shape, batch join, `rateLimited` latch and LIVE untouched.
  `InternalsVisibleTo` / `DefaultItemExcludes` intact, no new NuGet package, no `.sln`, `dotnet build` clean
  (0 warnings).

**Deviations from the ACs:** none against AC 1–3 as written. Two additions are recorded above rather than
hidden: the `Retry-After` helper (brief-directed, not AC-directed) and the extra filtered rethrow at
`TranslationService.cs:113`. One task bullet is met differently: T2 asks `Parse`'s catch to "route through
`Classify`", but `Parse` is a `static` method holding only a string — it has no `HttpResponseMessage` to hand the
mapper, and the story itself says row 12 is what "the provider's parser reports … the mapper only names". The
`BadResponse` is therefore stated at the parser (`DeepLTranslator.cs:167`) with the row cited, and pinned against
`Classify` by TP-MAP-15 so the two can never disagree.

### File List

**Created**
- `Services/ProviderErrorMapper.cs`
- `tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs`

**Modified**
- `Services/TranslationService.cs` (the retry loop's classification, the transport catch, the per-line and batch
  OCE filters)
- `Services/DeepLTranslator.cs` (the status switch, the send-and-read `try`, `Parse`'s row-12 citation)
- `docs/investigations/03-stories/stories/E1.S3-provider-error-mapper.md` (this file)
- `docs/investigations/03-stories/sprint-status.yaml`

### Change Log

| Date | Change |
|---|---|
| 2026-09-06 | E1.S3 implemented: `Services/ProviderErrorMapper.cs` classifies every raw outcome per §4.2 (first match wins, total, never throws); both providers route through it; the four E1.S1/E1.S2 deferred items assigned to this story are closed. Tests 287 → 319, all green. Status → review. |
| 2026-09-06 | Code review of `3ec06be`: the §4.1 cancel contract is now honoured at all four `Classify` call sites (a Stop could construct the one forbidden `TranslationException`), the last unfiltered OCE catch in the pipeline (`SafeOne`) filtered, §4.2 rows 6/7/8 rewritten in the table's order, and the three batch exits plus `Retry-After`'s garbage tolerance pinned. Tests 319 → 329, all green. Status → done. |

### Review Record

Code review of `3ec06be` ran the three layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) in parallel and
triaged their findings against the code. **Eight fixed, seven recorded, six dismissed.** Verified after the fixes:
`dotnet test tests/PWRUHelper.Tests` → **329 passed, 0 failed, ~2 s** (319 + 10), headless, no network, no
`%AppData%` write, `dotnet build` clean.

**No finding invalidated an acceptance criterion.** AC 1 holds — all thirteen §4.2 rows verified line by line, and
rows 6/7/8 now read in the table's order as T1 asked. AC 2 holds — a keyless 403 is `Blocked`, 400/404 are
`Unknown`, pinned as a pair and through the real provider. AC 3 holds structurally: `Classify` contains no `throw`,
and the totality sweep covers every status with and without a key.

**One finding is above this story and is left for the architect** — LIVE now auto-stops after five network-less
ticks, because a failure that used to be swallowed into per-line placeholders is now a `TranslationException` that
reaches `MainWindow.Live.cs`. `test-plan.md` §S6 records the opposite as a deliberate divergence (GAP-3). It is a
consequence of closing E1.S2's deferred item, not a defect in the mapper.

**T3 is the one task bullet this review reads as met only in the letter.** The literal-set diff is genuinely empty
for `DeepLTranslator.cs` and one string for `TranslationService.cs`, re-verified mechanically after the review
fixes — but that instrument cannot see a sentence that already existed as dead code and is now thrown. Three
rendered paths changed. All three are improvements and none was reverted; they are written down instead, because
**E1.S6 rewords against a baseline that is not the one the Completion Notes recorded.**

Checked by hand: **I1** `ITranslator` byte-identical · **I2** the mapper still depends on `System.Net.Http` and
nothing else · **I3** every OCE catch in `Services/` filtered (grep-verified: `TranslationService.cs` ×4,
`DeepLTranslator.cs`, `FallbackTranslator.cs` ×2) — one added by the review · **I5** the count mismatch still
falls to per-line and is still never padded · **I11** `bodyHead` is still read and dropped; nothing is stored or
forwarded · **I16** the retry loop shape, the batch join and the `rateLimited` latch are untouched by the review.
`InternalsVisibleTo` / `DefaultItemExcludes` intact · no new NuGet package · no `.sln`.

**For E1.S4:** the row-11 seam is where it should be — after row 10, before the success branch, inside the
`resp != null` block — and the review reordered nothing above it. Two things it inherits. (1) `Classify` is a
**failure** classifier: row 12 answers `BadResponse` for any success because it cannot see whether the body
parsed, so the HTML sniff must decide from the body itself and must not be reached for a healthy 200. (2) Marker
strings are split across two files today — `TranslationPolicy` holds the HTML markers, `ProviderErrorMapper` holds
`QuotaMarkers`, and only the former's evidence grades are enforced by CI. E1.S4 reads `TranslationPolicy`; if it
wants one home, moving `QuotaMarkers` there is the smaller half of that job.
