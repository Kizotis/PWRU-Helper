# Story E8.S3: The model store, and one-click Download / Remove from the About tab ⛔ Phase 4 (prototype branch)

Status: **done · ⛔ Phase 4 · prototype branch only (`feature/p2-c-offline`) — merges only on E8.S1's go**

_Epic E8 (H) — Offline fallback prototype · increment 7 · **Release C** · prototype branch, merges only on a go._

> **A 50 MB download is a decision the user makes, not something an error message does to them.** That sentence
> is the whole story, and it has a specific enemy: `ux-mode-degrade.md` §3.6 is a *deliberate divergence from the
> brief's own flow* — the consent dialog is **never** raised by a background failure, because a `MessageBox` over
> a fullscreen game, opened by a LIVE loop the player forgot was running, is the single worst thing this app
> could do (principle 3, NFR12).
>
> **Two things this story owns that `epics.md` § E8.S3 assumed rather than specified**: the **model store**
> itself — where files live, how they are verified, what `Remove` deletes — and the fact that nothing about it
> may touch disk before the window is visible (I10).

## Story

As a player,
I want to add the offline engine with one click and remove it with another,
So that a 50 MB download is a decision I make, not something an error message does to me.

## Acceptance Criteria

_AC 1–5 are copied **verbatim** from `epics.md` § Story E8.S3. AC 6 is copied **verbatim** from `epics.md`
§ Story E8.S2's fifth Given (the model-mirror AC, which belongs to the download and not to the provider).
AC 7–9 are `[DERIVED]` and cover the store `epics.md` left implicit. Do not weaken._

1. **Given** amendment A-1(a) and UX-DR12 / §3.6
   **When** the user clicks **Download the offline engine** in About
   **Then** and **only then** a consent dialog appears — `MessageBox.Show(this, …)` with an owner, title
   `Add the offline engine?` — stating the download size (~22 MB engine + ~30 MB per language pair), the memory
   cost (130–310 MB while translating, freed when idle), the storage location
   (`%LocalAppData%\PWRUHelper\models`) and how to remove it. `Not now` is the safe default and closes with no
   trace.
   _`[FLAG E8-b — SETTLED: `%LocalAppData%\PWRUHelper\models\`.]` A roaming or OneDrive-synced profile copies
   its contents at logon and this directory is ~45 MB — the machine-dependent startup cost P1 spent a phase
   hunting; machine-local, re-downloadable binary data belongs in Local. The parenthetical above,
   `ux-mode-degrade.md` §3.6 and `epics.md` § E8.S3 AC 1 all changed **in this story's one commit** with the
   code, and a test asserts the sentence and `SpecialFolder.LocalApplicationData` still agree. Sally's
   sign-off was delegated to the implementer: her sentence shapes are unchanged, only the path moved.
   §4.2 needed no edit — its mockup names no models path, and its **cache** sentence still says
   `%AppData%\PWRUHelper\` and is still right._

2. **Given** principle 3 and NFR12
   **When** any background failure occurs
   **Then** **no dialog is ever raised** — a `MessageBox` over a fullscreen game opened by a LIVE loop is the
   single worst thing this app could do. The failure path may only **nudge**, in the status line:
   `All engines are paused — you can add an offline engine in About.`

3. **Given** the download
   **When** it runs
   **Then** the About row shows `Downloading the offline engine… {p}%` with a `Cancel` button and the app stays
   fully usable; on completion it shows
   `● Offline engine ready — used only when everything else is unavailable.`; on failure,
   `Download failed — {reason}. Nothing was installed.`

4. **Given** OQ-9 (is the download progress observable?) is unanswered
   **When** the mirror does not report a total size
   **Then** the copy degrades to `Downloading…` without a percentage rather than inventing one.

5. **Given** OQ-11, resolved here: **downloaded ⇒ enabled**
   **When** the install completes
   **Then** `OfflineFallbackEnabled` becomes true and **Remove** is the only off switch — there is no second
   checkbox. `Remove` asks for confirmation with `MessageBox.Show(this, …)` and states that it deletes the files.

6. **Given** §5 of the model download
   **Then** models are fetched from a **`github.com` mirror**; the `UpdateService` allowlist
   (`github.com` / `githubusercontent.com`) is **not widened**.

7. `[DERIVED — I10, G6]` **Given** I10
   **When** the app starts
   **Then** **nothing about the model store touches the disk before the window is visible** — not a
   `Directory.Exists`, not a file enumeration, not a size sum. The About tab's state is computed on first paint
   of that tab or later, off the UI thread, in the shape ruling **E6-a** already established for the gate state
   (`TranslationChains.EnsureGateStateLoaded` from `OnWindowLoaded`, on a pool thread).

8. `[DERIVED — §7.6 constraint 6, UpdateService's existing discipline]` **Given** a completed download
   **When** the files are installed
   **Then** each one's **size** is checked against the manifest and its **SHA-256** against a published digest
   before it is moved into place; a mismatch deletes the partial download and reports
   `Download failed — {reason}. Nothing was installed.` A half-written 22 MB DLL that the app then P/Invokes is
   the one failure mode here that is worse than no feature at all.

9. `[DERIVED — I12, ux §4.3's `_restoringSettings` contract]` **Given** I12
   **When** the About controls are added
   **Then** every one of them obeys `_restoringSettings` and `ApplySettings` applies their side effects
   explicitly.
   _(This is `epics.md` § E8.S3's own sixth Given, restated as its own AC because it is the invariant that has
   already cost this project two migration steps and two releases.)_

## Tasks / Subtasks

- [x] **T1 — `Services/OfflineModelStore.cs` (AC 7, AC 8).** New file. **Pure-ish, headless, no UI type** (I2) —
      the About tab calls it, it never calls the About tab.
  - [x] Path, in one place, in the shape every other app file already uses:
        `Environment.GetFolderPath(Environment.SpecialFolder.…)` + `"PWRUHelper"` + `"models"`. The four
        precedents are `SettingsService.cs:115`, `ProviderGates.cs:85`, `TranslationCacheStore.cs:112` and
        `Logging.cs:19`, and all four use `ApplicationData` (Roaming). **See flag E8-b — this story may need
        `LocalApplicationData` instead, which would make it the first file in the app to diverge, deliberately.**
  - [x] A `PathOverride` static, exactly like `TranslationCacheStore.PathOverride` (`:110`) and
        `ProviderGates.PathOverride` — **IS-2 exists because this repo once pointed a test at a developer's own
        file**, and this is the class that writes 50 MB.
  - [x] `IsInstalled` / `InstalledPairs` / `BytesOnDisk` — each of them lazy and **never called from a
        constructor or a static initialiser** (AC 7). The `TranslationCacheStore.Count` comment
        (`:193-196`) is the wording to copy: *a count is not a question about a translation*.
  - [x] `Install(progress, ct)` and `Remove()`. `Remove` deletes the directory and returns what it deleted, so
        the status line can say it.

- [x] **T2 — The mirror, and the allowlist that is not widened (AC 6).**
  - [x] Models come from a **GitHub release** on `Kizotis/PWRU-Helper` (or a dedicated models repo under the
        same owner) — a mirror of Mozilla's Firefox Remote Settings blobs, **not** a direct fetch from Mozilla.
        §7.6 constraint 5 says this in one sentence: *keep that allowlist and mirror the models on a GitHub
        release rather than widening it.*
  - [x] Reuse `UpdateService.IsTrustedDownload` (`Services/UpdateService.cs:97-101`) — `github.com` or
        `*.githubusercontent.com`, and **do not add a host**. `project-context.md` names this allowlist in its
        "Critical Don't-Miss Rules"; a story that widens it is wrong even if its own tests pass.
  - [x] A manifest file in the release (JSON: file name, size, SHA-256, model pair, version) is what AC 8
        verifies against and what AC 4's percentage comes from. **It is also OQ-9's answer**: a manifest we
        publish always reports a total size, so the `Downloading…`-without-a-percentage degradation becomes the
        path for a mirror that stops answering `Content-Length`, not the normal case.
  - [x] **Populating that mirror is an owner task** — it is his repo and his release. Note it in the hand-off
        (T8) rather than assuming a URL exists.

- [x] **T3 — Download, off the UI thread, cancellable, and it never blocks the app (AC 3).**
  - [x] `HttpClient` with a progress callback; `IProgress<double>` marshalled to the UI. The app stays fully
        usable — that is in the AC, and it is why this cannot be a modal progress dialog.
  - [x] **I3 applies here, hard.** An `HttpClient` timeout throws `TaskCanceledException` — an
        `OperationCanceledException` subclass — **with the token not cancelled**. Every catch on this path
        filters `when (ct.IsCancellationRequested)`. Getting this wrong is the project's most expensive past bug
        (three releases), and here it would render `Download cancelled` over a network failure the user did not
        cause.
  - [x] Download to a `.part` file in the target directory, verify (AC 8), then move into place. A partial file
        must never be namable by `IsInstalled`.
  - [x] `Cancel` deletes the partial and returns the row to `○ Not installed`. No trace, per AC 1's own standard
        for `Not now`.

- [x] **T4 — The About block (AC 1, AC 3, AC 5).** `MainWindow.xaml`, inside the About tab
      (`MainWindow.xaml:565-764`), as §4.2's third block — *what is being used* · *your keys* · **offline
      engine** · *saved translations*.
  - [x] **E7.S7 restructures this whole region** into the "Translation engines" block and is `ready-for-dev`
        (see `sprint-status.yaml`). The XAML comment at `MainWindow.xaml:670-673` says so explicitly: *"E7.S7
        restructures this whole region into one 'Translation engines' block, so anything decorative added here
        is work thrown away."* **Land on top of E7.S7 if it has shipped; if it has not, put the block where
        §4.2's mockup puts it and keep it decoration-free.**
  - [x] Controls, from §4.3's table: `OfflineEngineToggle` is a **`Button`** whose content flips
        Download ⇄ Remove — *"not a checkbox: the two actions have very different weight"*. Plus a status
        `TextBlock` (the row text of AC 3) and a `Cancel` button visible only during a download.
  - [x] `GhostButton` style, existing brushes only. **No new brush, font or colour** (UX-DR18) — the same rule
        E6.S4's checkbox followed.
  - [x] **`WrapPanel`, not `StackPanel Orientation="Horizontal"`.** E6.S5's review found the Azure `Test key`
        button clipped off-screen at the default window width; both credential rows are `WrapPanel`s now
        (`MainWindow.xaml:642, :675`) and this row has the same problem shape.
  - [x] `TextWrapping="Wrap"` on the status line: AC 3's completion sentence is 62 characters and the failure
        one is longer, and the E6.S5 review found unwrapped status lines clip exactly the actionable half.

- [x] **T5 — Every string through `UserMessages` (AC 2, AC 3, AC 4).** Ruling **GAP-4** and **UX-DR19**: the copy
      lives in `Services/UserMessages.cs`, once, and the XAML/code-behind read it. Six strings:
  - [x] the consent dialog body (§3.6's block, verbatim, including the four labelled lines) and its title
        `Add the offline engine?`
  - [x] `Downloading the offline engine… {p}%` and its no-percentage form `Downloading…` (AC 4)
  - [x] `● Offline engine ready — used only when everything else is unavailable.`
  - [x] `Download failed — {reason}. Nothing was installed.`
  - [x] `○ Not installed — about 50 MB to download, works with no internet at all.` +
        `Used only when every online engine is unavailable.` (§4.2's mockup)
  - [x] the nudge: `All engines are paused — you can add an offline engine in About.` (AC 2) and §4.2's
        `You can add an offline engine in About — it works with no internet.`
  - [x] E7.S1's house rules apply: no placeholder may survive into a painted string, and the rendered length
        budgets are asserted. Check what `UserMessagesTests` enforces **before** adding a const — E7.S1's review
        notes a curated fragment list and a `Code()` helper that cuts at the first `//` even inside a string.

- [x] **T6 — No dialog from any background path (AC 2), and prove it.**
  - [x] The consent dialog is reachable from **exactly one** `Click` handler. Write the pin as a **source scan**:
        no `MessageBox` call site exists in `MainWindow.Live.cs`, `MainWindow.Ocr.cs` or anything they call.
        This is the same evidence shape `epics.md` asks for — *"no dialog API is reachable from the LIVE path"*.
  - [x] The nudge is written to the **status line**, once, when the state is entered and the engine is not
        installed (§4.2's flow (b) step 4: *"Once, when the state is entered"*). Not per row, not per message,
        never a toast — *"a toast for something that happens 40 times an evening is a punishment"* (§2.2).
  - [x] `MessageBox.Show(this, …)` **with an owner**, both for consent and for `Remove` (project-context, and
        §3.6 says it twice).

- [x] **T7 — `OfflineFallbackEnabled`, written by these two buttons and by nothing else (AC 5, AC 9, ruling R-4).**
  - [x] The field already exists: `Services/SettingsService.cs:55`, and its comment already says this —
        *"there is deliberately NO check box: ruling R-4 makes the About tab's Download / Remove actions (E8.S3)
        its only writers"*. It is **unread today**. This story is where it starts being read.
  - [x] Download success ⇒ `true`; `Remove` ⇒ `false`; save immediately. **No third writer.**
  - [x] **No `SettingsVersion` bump and no `Migrate` step.** §12 says so and `SettingsService.cs:186-192`
        already carries the reasoning: the field is new, an old `settings.json` deserialises it to its default,
        and that is the intended behaviour. I13 governs *changed* defaults, and there are none here.
  - [x] **I12, and this is the invariant that bites** (`MainWindow.xaml.cs:28-35`): XAML loading raises
        `Checked`/`SelectionChanged` during `InitializeComponent()`, long before `ApplySettings` runs. The
        button is not a persisted control, so it has no change handler to guard — **but its UI side effects
        (label, row text, enabled state) must be applied explicitly from `ApplySettings`**, in a
        `UpdateOfflineEngineUi()` called where `UpdateOcrFilterUi` is called today
        (`MainWindow.xaml.cs:193`). E6.S3's review found a real bug here by borrowing `_restoringSettings`
        for something it did not mean; do not borrow it.
  - [x] After a successful Download or Remove, **rebuild both chains** — the tier list changes. E6.S3 landed
        that seam; reuse it rather than writing a second one.

- [x] **T8 — Docs and the owner's half.**
  - [x] The MPL-2.0 notice sentence E8.S2's T8 supplies goes in this block (§7.6 constraint 8, NFR10).
  - [x] §4.2's cache-privacy sentence and `[ Clear cache ]` (amendment A10) are **E7.S7's**, not this story's —
        do not implement them here, and do not let the offline block displace them.
  - [x] Hand-off: the owner creates the GitHub release that hosts the mirrored blobs and the manifest, and gives
        the tag. Three lines, in the PR and the owner-task list.

## Dev Notes

### The store, and where it lives — flag **E8-b**, unresolved, and it changes shipping copy

| Source | Says |
|---|---|
| `ux-mode-degrade.md` §3.6 (consent dialog) and §4.2 (mockup) | `%AppData%\PWRUHelper\models` |
| `epics.md` § E8.S3 AC 1 (verbatim above) | `%AppData%\PWRUHelper\models` |
| `architecture-cible.md` §7.6 amendment A-1(a) | *"per `ux-mode-degrade.md` §4"* — inherits the above |
| **Winston, Release-C direction (2026-09-07)** | **`%LocalAppData%\PWRUHelper\models\` — not Roaming** |

**Recommendation: `%LocalAppData%`, and the reasoning is the app's own investigation.** Roaming profiles copy
their contents at logon; `%AppData%` is where `settings.json`, `provider-state.json`, `translation-cache.json`
and the log live, and all four are kilobytes. This directory is **22 MB of native DLL plus 22–37 MB per model**,
and on a roaming or OneDrive-synced profile that is a logon cost — the exact class of machine-dependent startup
cause P1 spent a whole phase hunting. It is also the Windows convention: machine-local, re-downloadable binary
data belongs in Local.

**But it is a copy change**, in a consent dialog whose whole job is to tell the user where their 50 MB went.
So: **Winston or the owner rules; then the code, `ux-mode-degrade.md` §3.6, §4.2 and this AC change together, in
one commit, with Sally's sign-off.** Do not ship code and copy that disagree, and do not silently "fix" the
copy — a divergence between the dialog and the directory is a support ticket that reads *"where did my 50 MB
go?"*.

### Why `Download` is a button and not a checkbox — OQ-11, resolved here

§4.3's table gives the one-line reason: *"the two actions have very different weight"*. A checkbox implies a
free, reversible toggle; one of these two actions costs 50 MB of somebody's connection and the other deletes it
from their disk. And a second control would mean two ways to express one decision — **ruling R-4** settles it:
the setting exists, the buttons are its only writers, there is no separate toggle.

### What this story may and may not change

| May | May not |
|---|---|
| Add `Services/OfflineModelStore.cs` and the About block | Touch `Services/BergamotTranslator.cs` (E8.S2) or the tier lists (E8.S5) |
| Write `OfflineFallbackEnabled` from Download / Remove | Add a checkbox, or a second writer of that field (R-4) |
| Add six strings to `UserMessages` | Put a sentence in the XAML (GAP-4 / UX-DR19) |
| Show a modal from **one** `Click` handler | Show a modal from any background, LIVE or OCR path (AC 2, NFR12) |
| Rebuild both chains after an install/remove | Bump `SettingsVersion` or add a `Migrate` step (§12) |
| Read `UpdateService.IsTrustedDownload` | Widen the allowlist (§7.6.5, `project-context.md`) |
| Deposit files under the models directory | Write anything to `%AppData%\PWRUHelper\` root, or near `settings.json` |

### Invariants this story must keep

- **I2** — `OfflineModelStore` is headless and unit-testable. The dialog is the code-behind's.
- **I3** — every catch on the download path filters `when (ct.IsCancellationRequested)`.
- **I10** — AC 7. Nothing about this store is asked before first paint, and the answer arrives on a pool thread
  the way ruling **E6-a** arranged for the gate state.
- **I11** — no URL, no path, no user text in the log. The download's failures log a **kind**, not a URL.
- **I12** — AC 9. `_restoringSettings` is not borrowed; `UpdateOfflineEngineUi()` is called explicitly from
  `ApplySettings`.
- **I13** — no default changes, so no version bump. Stated so nobody adds one "to be safe".
- **NFR12 / principle 3** — no dialog from a background path, ever. AC 2 is the whole story's reason to exist.

### Repo traps

- **The WPF `Clipboard` class is banned** (`project-context.md`, I14). Nothing here needs it; stated because a
  "copy the model path" convenience is exactly the shape that would sneak one in. `Services/ClipboardService` is
  the only route.
- **`MessageBox.Show(this, …)`** — with an owner. A parentless dialog behind a fullscreen game is unfindable.
- **`MainTabs_SelectionChanged` must filter `e.Source is TabControl`** (`project-context.md`) — if this story
  computes the About tab's state on tab selection, that filter is why the handler does not fire for every inner
  selector.
- **Tag-driven combos use `SelectTag`/`SelectedTag`** (`MainWindow.xaml.cs:359-370`) — irrelevant unless a
  language-pair picker appears; §4.2's `[ Add a language pair ]` is **out of scope** for the prototype
  (RU→EN only), and adding one would double the RAM (flag E8-a's 310 MiB ceiling).
- **IS-2/IS-3** — a test that writes to the developer's real `%AppData%` is the failure this suite has explicit
  machinery to prevent (`TempCache`, `PathOverride`). This class writes **50 MB**; give it its override on day
  one and assert the real directory is untouched, as `CacheLoadSpike` does.
- **E7.S7 owns the About restructure and is `ready-for-dev`.** Coordinate; do not decorate a region that is
  about to be rewritten (`MainWindow.xaml:670-673` says so in the file).

### Line-number drift

Verified at writing: `SettingsService.cs:55` (`OfflineFallbackEnabled`), `:115` (AppData path), `:186-192`
(no migration needed) · `UpdateService.cs:97-101` (the allowlist) · `TranslationCacheStore.cs:110` (`PathOverride`),
`:112`, `:193-196` · `ProviderGates.cs:85` · `Logging.cs:19` · `MainWindow.xaml.cs:28-35`
(`_restoringSettings`), `:193` (`UpdateOcrFilterUi` call site), `:359-370` (`SelectTag`) ·
`MainWindow.xaml:565` (About tab), `:642`, `:675` (the two `WrapPanel` credential rows), `:670-673` (E7.S7's
restructure note), `:701-713` (E6.S4's checkbox — the `_restoringSettings` pattern to copy).

## Testing

- **TP-IDs landed:** **TP-BRG-07** (one-click install / remove: consent → download with progress → `Remove`
  deletes every file; the URL is on the `github.com` allowlist and the allowlist is **not** widened).
- **Runnable in CI, all of it, because none of it downloads anything** (CI-8 forbids the model download by
  name). The `HttpMessageHandler` seam E1.S1 landed is what makes the download testable offline; use it.
- **Cases this story owes:**
  1. `OfflineFallbackEnabled` round-trips through `settings.json`, and **only** Download / Remove write it —
     a source scan for its assignments, in the shape `ChainTranslatorTests` uses for `NotSent`.
  2. The nudge string is used on the all-paused path, and **no dialog API is reachable from the LIVE path** —
     the source scan of T6.
  3. A fake handler serving a manifest and two blobs installs them; a wrong SHA-256 installs **nothing** and
     leaves no `.part` behind (AC 8).
  4. A URL that is not on the allowlist is refused before a request is made (AC 6).
  5. A mirror that reports no total size renders `Downloading…` with **no** percentage (AC 4).
  6. Cancel mid-download leaves the directory as it was and the row back at `○ Not installed`.
  7. An `HttpClient` timeout does **not** render as a cancel — the I3 case, and it is the one this project has
     paid for before.
  8. `ApplySettings` restores the row without any handler writing settings back (I12, AC 9).
  9. **I10**: constructing the store and building all three chains opens no file under the models directory —
     the shape `CacheLoadSpike`'s chain-composition case already proves for the cache.
- **STA:** if any new `Run.Text` binding appears in the About block it is `Mode=OneWay` and it gets a case in
  `TemplateRenderTests` (**I15**). §4.2's rows are plain `TextBlock` text, so the expectation is that none does.
- **Manual:** install, disable the network, translate, remove.

## File List

**Create**
- `Services/OfflineModelStore.cs` — path, install, verify, remove, `PathOverride`
- `tests/PWRUHelper.Tests/OfflineModelStoreTests.cs` — cases 3–7, 9
- `tests/PWRUHelper.Tests/OfflineInstallUiTests.cs` *(or into an existing settings test file)* — cases 1, 2, 8

**Modify (prototype branch only)**
- `MainWindow.xaml` — the "Offline engine (optional)" block of §4.2, in the About tab
- `MainWindow.xaml.cs` — the two `Click` handlers, `UpdateOfflineEngineUi()`, its `ApplySettings` call site
- `Services/UserMessages.cs` — the six strings of T5
- `docs/investigations/02-traduction/ux-mode-degrade.md` — **only if ruling E8-b lands on `%LocalAppData%`**:
  §3.6's consent block and §4.2's mockup, in the same commit as the code
- `docs/investigations/03-stories/epics.md` — the same, if E8-b changes AC 1's parenthetical

**Must not be modified**
- `Services/UpdateService.cs` — the allowlist is read, never widened
- `Services/SettingsService.cs` — the field exists (`:55`); no version bump, no `Migrate` step

## Size, dependencies, risk

**Size:** **M** · **Depends on:** **E8.S2** (there is nothing to enable without a provider) and **E8.S1**'s go;
the owner for the mirror release (T2, T8) · **Blocks:** E8.S5 (the chain reads "is the model present?") ·
**Risk:** **medium — a modal on the wrong thread is the failure this story exists to prevent.** Second risk: a
partial or corrupt 22 MB DLL that the app then P/Invokes, which AC 8 is the whole answer to ·
**DoD:** no background path can open a dialog, and a verified install/remove round-trips with the network
disabled afterwards.

## How to verify manually

1. Open About with nothing installed: `○ Not installed — about 50 MB to download, works with no internet at all.`
2. Click **Download the offline engine**: the consent dialog appears, owned by the main window, with the four
   labelled lines of §3.6. Press `Not now`: **nothing** changed, nothing was written, no trace.
3. Click again, accept: the row shows a percentage, `Cancel` is present, and the app stays fully usable —
   switch tabs, translate something, drag the window.
4. Cancel: the row returns to `○ Not installed` and the directory is as it was.
5. Download to completion: the row reads `● Offline engine ready — …`, `settings.json` shows
   `OfflineFallbackEnabled: true`, and the model directory holds exactly what the manifest listed.
6. **Force the failure path**: unplug the network mid-download ⇒ `Download failed — …. Nothing was installed.`,
   **not** a cancel message (this is the I3 check, and it is the one to do by hand).
7. Start LIVE, let every engine pause: the status line nudges **once**; **no dialog appears**. This is AC 2 and
   it is the one that would be caught by a player mid-raid rather than by a test.
8. `Remove`: a confirmation dialog that says it deletes the files; accept; the directory is gone and
   `OfflineFallbackEnabled` is false.
9. Restart with the engine installed: the window paints with **no** read under the models directory before it
   is visible (Process Monitor) — AC 7.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E8.S3] — AC 1–5, 9 verbatim; [#Story E8.S2] — AC 6 verbatim
- [Source: docs/investigations/02-traduction/ux-mode-degrade.md#3.6 First-use consent for the offline engine, #4.2 ASCII mockup, #4.3 Control-by-control specification, #2.2 reading rules, #(b)/(d) flows]
- [Source: docs/investigations/02-traduction/architecture-cible.md#7.6 constraints 5, 6, 8, #12 Settings, #2 Invariants I3/I10/I11/I12/I13]
- [Source: docs/investigations/README.md#R-4 (OfflineFallbackEnabled's only writers), #R-7 (setting names), #E6-a (post-paint warm-up), #GAP-4 (UserMessages)]
- [Source: docs/investigations/03-stories/test-plan.md#3.13 TP-BRG-07, #5 CI-1/CI-8]
- [Source: project-context.md#Framework-Specific (WPF) Rules, #Critical Don't-Miss Rules (the UpdateService allowlist)]

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5[1m]`) — Amelia, `bmad-dev-story`, 2026-09-07.

### Debug Log References

- `dotnet test tests/PWRUHelper.Tests` — **1276 passed, 0 failed, 9 s** (baseline 1244 → **+32**).
  Nothing downloaded; `bergamot.dll` still absent from the build output; no new warning.
- Targeted while working:
  `--filter "FullyQualifiedName~OfflineModelStore|FullyQualifiedName~OfflineInstall|FullyQualifiedName~UserMessages|FullyQualifiedName~TemplateRender|FullyQualifiedName~PausedState|FullyQualifiedName~ProviderGates"`.

### Completion Notes List

**Ruling E8-b — settled and shipped in this commit.** The model root is
`%LocalAppData%\PWRUHelper\models` (`Environment.SpecialFolder.LocalApplicationData`), the first file in the app
that deliberately diverges from Roaming, with the reason in a comment on `OfflineModelStore`. The code, the
consent copy in `UserMessages`, `ux-mode-degrade.md` §3.6 and `epics.md` § E8.S3 AC 1 all moved together, and
`The_model_root_is_LocalAppData_and_the_consent_copy_says_the_same_thing` asserts the sentence and the
`SpecialFolder` cannot drift apart again. §4.2 needed no change: its mockup names no models path, and its cache
sentence still says `%AppData%\PWRUHelper\`, which is still correct.

**Ruling E8-f — the trust chain, complete and tested, with the owner's digests outstanding.** The manifest is a
**C# static table** (`Services/OfflineModelManifest.cs`), not `Data/offline-manifest.json`. The reason is the
one thing that would have made the JSON option unsafe: the three files under `Data/` are `EmbeddedResource`
**and** are copied next to the exe as a first-run *editable* copy — a documented feature — so a manifest with
that treatment would put the trust anchor for a native-code load in a file anything running as the user can
rewrite. A table in the assembly also removes a parse path and a resource path from a check that must have
exactly one answer. Every URL is composed from one host constant and handed to
`UpdateService.IsTrustedDownload` (made `internal`; the allow-list itself is byte-identical and a test asserts
the set of hosts it compares). Files land in `<root>\.download\` as `.part`, are size- and SHA-256-verified,
renamed inside staging, and moved into the real layout only after the LAST one passes — any failure or cancel
deletes the whole root, which is honest because the install refuses to run over an existing one. The digests
are `TODO-owner` placeholders until the release exists, and that is **not** a bypass:
`OfflineFile.IsVerifiable` is structurally false, so `IsInstalled` answers false, `InstallAsync` fails with
`Download failed — the engine's files could not be verified. Nothing was installed.` before a single request,
and `OfflineModelStore.IsVerifiedNative` refuses to load. `BergamotEngine.Resolve` now hashes the DLL before
`NativeLibrary.TryLoad` (a hash, not a marker file — a marker is forgeable by copying two files into the
folder), pinned by a scan that asserts the check comes *before* the load.

**Owner hand-off (T8) — the exact release the manifest expects.** Create a release tagged
**`offline-engine-v1`** on `Kizotis/PWRU-Helper` (the only host the allow-list trusts) carrying four assets,
then replace the four `Todo` digests and the three zero sizes in `OfflineModelManifest.Shipping`:

| asset (exact name) | size | SHA-256 | goes to |
|---|---|---|---|
| `bergamot.dll` | 22,460,928 B *(E8.S1-measured; re-confirm)* | **needed** | `<root>\` |
| `model.ruen.intgemm.alphas.bin` | **needed** | **needed** | `<root>\ru-en\` |
| `vocab.ruen.spm` | **needed** | **needed** | `<root>\ru-en\` |
| `lex.50.50.ruen.s2t.bin` | **needed** | **needed** | `<root>\ru-en\` |

The three model files are the `tiny` ru→en v3.0 set the spike measured at **22,530,152 B in total**. Take the
bytes from the **gzip GCS mirror** (Remote Settings serves zstd, which .NET 8 cannot read — spike finding 3)
and the integrity values from Remote Settings' `decompressedSize` / `decompressedHash`; the two registries
agreed byte-for-byte on the dev box. `config.txt` is **not** an asset — the store writes it after
verification, from `OfflineModelManifest.ConfigText`, which is the spike's config verbatim.

**Deviations, all deliberate:**

1. **The handlers live in a new partial `MainWindow.Offline.cs`, not in `MainWindow.xaml.cs`** as the File List
   said. `MainWindow.xaml.cs` is core-only by `project-context.md`, E7.S7 put this tab's code in
   `MainWindow.Translate.cs` — and that file carries E6.S5's pin
   `KeyTestTests.AC1_The_handlers_name_no_MessageBox`, which this story's two dialogs would have forced into a
   weaker, per-handler assertion. A partial per concern is the repo's own layout rule and it keeps the older
   pin exactly as strict as it was. The `ApplySettings` call site **is** in `MainWindow.xaml.cs`, as required.
2. **`UpdateService.IsTrustedDownload` changed from `private` to `internal`.** The story says the file must not
   be modified; reading the predicate is impossible otherwise, and the alternative — a second copy of it — is
   the widening one file away. The allow-list is unchanged and a test asserts the exact host set.
3. **The consent dialog is Yes/No with No as the default**, not §3.6's `[ Download (about 50 MB) ] [ Not now ]`.
   A WPF `MessageBox` cannot relabel its buttons and a custom window for one confirmation is out of scope; the
   body's last line asks the question in Sally's words and `MessageBoxResult.No` is the default, which is AC 1's
   requirement ("`Not now` is the safe default").
4. **§4.2's flow-(b) nudge variant ("You can add an offline engine in About — it works with no internet.") is
   not shipped.** AC 2's sentence is the same fact, on the same surface, at the same moment; two spellings of
   one nudge is what UX-DR19 forbids, and AC 2 is the criterion.
5. **The installed row is AC 3's sentence, not §4.2's "● Ready — 52 MB on disk, ru→en."** One row text, and it
   is the one AC 3 and the manual-verification steps write. `BytesOnDisk` is not unused: `Remove` reports what
   it freed.
6. **No plumbing was added to `TranslationChains`.** The store answers `BergamotTranslator`'s two locators
   (`ModelDirectory()` / `NativeDirectory()`), and wiring them into the builders is E8.S5's (E8-c/E8-e). T7's
   "rebuild both chains" goes through E6.S3's existing seam (`BuildWriteChain` + `RebuildReadChains`) and is a
   no-op today — written now so E8.S5 is a change to the builders and not a hunt for the call sites.

**Recorded for the reviewer:**

- **AC 7's first paint is painted from the SETTING, not from the disk.** `ApplySettings` seeds
  `_offlineInstalled` from `OfflineFallbackEnabled` — in memory, and by ruling R-4 a faithful proxy, since the
  two buttons are its only writers — and `OnWindowLoaded` corrects it from the disk on a pool thread, in
  E6-a's shape. The two can disagree only if the user deleted the directory by hand or the manifest cannot
  verify what is there; both resolve within one post-paint tick.
- **`StateNotice` grew a fourth parameter** (`offlineInstalled`, defaulted) rather than a new mechanism, so
  AC 2's nudge inherits the existing "once per switch" comparison and is structurally incapable of becoming a
  dialog or a per-row annotation. `PausedStateTests.The_notice_is_compared_before_it_is_written` was updated to
  the new call line (it pins the literal).
- **`FakeHandler` gained `RespondBytes` and `NoContentLength`** plus a non-seekable `UnmeasurableStream`. The
  second is the only way to script AC 4's "mirror that does not report a total size": `HttpContent.ContentLength`
  cannot be cleared — the next read recomputes it from the buffer — so a stream whose length genuinely cannot
  be computed is what a chunked response actually is.
- **A percentage that goes unknown never comes back** (own case). The first honest number arrives with the
  first chunk, not before: `InstallAsync` reports nothing until it has heard from the mirror.
- **`ConfigureAwait(false)` on the store is pinned locally**, because `HttpProviderCoreTests`' scan derives its
  file set from "names `HttpProviderCore`" and this file structurally does not — the same argument
  `BergamotTranslatorTests` made for E8.S2. The progress callback is a local function and not a lambda at the
  call site, because that scan splits statements on `;` and a multi-statement lambda inside an awaited call
  reads as an unconfigured await.
- **The no-dialog scan carves out two NAMED members of `MainWindow.Ocr.cs`** — `ResetTuning_Click` (a
  confirmation for a button the player pressed) and `SelectRegionAsync` (the drag-a-rectangle overlay, which
  *is* the gesture). Both pre-date this story; naming them keeps the scan strict and makes a third one a
  decision. `MainWindow.Live.cs` has none at all.
- **`TP-RENDER-09` now names `OfflineEngineButton`** in its non-vacuity list, so the new `WrapPanel` row is
  measured at the default size **and** at `MinWidth`/`MinHeight` like every other control on the tab. The
  Cancel twin is `Collapsed` at rest and is correctly skipped.
- **Not asserted, and it cannot be here:** that the owner's real 22 MB DLL loads. CI-8 forbids the download and
  the native load outright; what is asserted is that an unverified one is refused.

### Review Follow-ups (Amelia, code review of `bc4002b`, 2026-09-07)

Winston's six recorded deviations were checked against the code and are implemented exactly as
written; none was re-litigated. Suite after the review: **1278 passed, 0 failed, 16 s** (1276 → +2),
nothing downloaded, `runtimes/` absent, no new warning.

**Defects found and FIXED in this branch:**

1. **Ruling E8-f had only half its trust chain.** `IsVerifiedNative` hashed `bergamot.dll` and
   nothing else, but `NativeLibrary.Load` on an absolute path uses `LOAD_WITH_ALTERED_SEARCH_PATH` —
   the library's own directory goes to the FRONT of the import search, and `bergamot.dll` imports
   `dbghelp`, which is not a KnownDLL. A byte-perfect library plus a planted `dbghelp.dll` beside it
   was code execution inside the app **without touching the file the digest covers**, which is the
   exact attack the ruling's own remark describes. A dependency has no digest in the manifest to
   check against, so the check is for ABSENCE: the directory an install produces holds exactly one
   `.dll`, and a second one — or a side-by-side `.manifest`, which redirects the same search —
   refuses the load. Deliberately narrow (only load-steering extensions), so an antivirus marker or
   a `desktop.ini` cannot silently disable the engine.
   Pinned by `A_dll_planted_beside_the_library_refuses_the_load`.
2. **`InstallAsync`'s own contract was documentation only.** Its remarks say the method "refuses to
   run over an existing install", and that sentence is what makes "any failure deletes the whole
   root" honest — but no guard existed. Reachable: `Remove()` sets `OfflineFallbackEnabled = false`
   and repaints even when `Directory.Delete` threw (a locked or AV-held file), after which the button
   reads **Download** over a full directory, and one flaky response would have deleted 45 MB the user
   had already waited for. The store is headless (I2) and its contract may not depend on a caller's
   button label. Guard added — already installed is a success, and no request is made.
   Pinned by `An_install_that_is_already_there_is_never_downloaded_over`.
3. **The no-dialog scan (AC 2) stopped at the two files.** AC 2 is about a PATH, and both loops call
   out of themselves on every tick — into the chain, the gates, the store, the dedup, all of it under
   `Services/`. The scan now sweeps that whole directory with **no carve-out**, so a dialog API
   anywhere the LIVE loop can reach fails the case. No existing carve-out or pin was weakened.
4. **The download client's `User-Agent` was unasserted** (E1's seam lesson: a header nobody pins is a
   header the next refactor drops). Now asserted on every request in `TP_BRG_07`.

**Recorded, NOT fixed — with their owner:**

- **TOCTOU between the hash and `NativeLibrary.TryLoad`** — a writer who can win that race can
  already write the directory, so it is not a new capability. The cheap mitigation (holding the file
  open with a restrictive `FileShare` across the load) cannot be proven here: **CI-8 forbids the
  native load outright**, and an untested change to the one path that P/Invokes 22 MB is worse than
  the residual risk. → **E8.S4**, which owns the load/unload lifecycle and can test it against the
  owner's real DLL.
- **`IsInstalled` is size-only for the three model files.** A hand-tampered model passes the About
  row (never the load — the DLL is re-hashed there). Deliberate and already reasoned in the code:
  hashing 45 MB to paint a row is I10 in another costume. → recorded, no change.
- **`Remove()` while the engine is loaded.** On Windows a loaded DLL cannot be deleted, so
  `Directory.Delete` throws, the store logs a kind and returns 0, and the row reads
  `Offline engine removed — 0 MB freed from your disk.` while the files are still there and the
  setting is already false. → **E8.S4** owns the unload: it must unload before `Remove`, and the
  failed-delete case needs a truthful sentence rather than the removed one.
- **Setting true + files gone.** The post-paint probe corrects the ROW but never the setting — ruling
  R-4 keeps the two buttons its only writers — so `OfflineFallbackEnabled` can stay true with nothing
  on disk. → **E8.S5**: the builders must gate the offline tier on the store's locators
  (`ModelDirectory()` / `NativeDirectory()`), never on the setting alone.
- **A `Cancel` landing inside the final rename loop is ignored** and the install completes. Everything
  is downloaded and verified by then; deleting verified bytes because the click arrived three
  milliseconds late would be the worse behaviour. → recorded, no change.
- **The failure row's label flip has no behaviour test** (the sentence is kept, the button goes back
  to Download by hand). Driving it would need an injectable store on `MainWindow` — a seam this story
  would have to invent for one assertion. → recorded; both resting states ARE pinned through
  `ApplySettings`.
- **Owner:** the four `TODO-owner` digests and the three zero sizes of the T8 table. Nothing
  installs and nothing loads until they land, by construction.

### File List

**Created**
- `Services/OfflineModelManifest.cs`
- `Services/OfflineModelStore.cs`
- `MainWindow.Offline.cs`
- `tests/PWRUHelper.Tests/OfflineModelStoreTests.cs`
- `tests/PWRUHelper.Tests/OfflineInstallUiTests.cs`
- `tests/PWRUHelper.Tests/TempModels.cs`
- `tests/PWRUHelper.Tests/TestModelsRedirect.cs`

**Modified**
- `Services/UserMessages.cs` — the offline block's copy (consent title + body, progress, ready, failure,
  the three labels, the Remove dialog, the removed status, AC 2's nudge)
- `Services/BergamotEngine.cs` — `Resolve` verifies the DLL's SHA-256 before loading it (ruling E8-f)
- `Services/UpdateService.cs` — `IsTrustedDownload` `private` → `internal`; allow-list unchanged
- `MainWindow.xaml` — the "Offline engine (optional)" block filled in (the `WrapPanel`, two buttons)
- `MainWindow.xaml.cs` — `UpdateOfflineEngineUi()` call site in `ApplySettings`, the post-paint probe in
  `OnWindowLoaded`, `StateNotice`'s `offlineInstalled` arm
- `MainWindow.Translate.cs` — `SetAboutBlockCopy` no longer writes `OfflineEngineText`, and sets the Cancel
  button's one static label
- `tests/PWRUHelper.Tests/FakeHandler.cs` — `RespondBytes`, `NoContentLength`, `UnmeasurableStream`
- `tests/PWRUHelper.Tests/UserMessagesTests.cs` — the new deck sentences in the UX-DR19 scan and the
  placeholder scan
- `tests/PWRUHelper.Tests/TemplateRenderTests.cs` — `OfflineEngineButton` in TP-RENDER-09's list
- `tests/PWRUHelper.Tests/PausedStateTests.cs` — the `StateNotice` call-line pin
- `docs/investigations/02-traduction/ux-mode-degrade.md` — §3.6's path and the E8-b note
- `docs/investigations/03-stories/epics.md` — § E8.S3 AC 1's parenthetical
- `docs/investigations/03-stories/sprint-status.yaml` — `e8-s3` → `review`

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **blocked-dep** on E8.S2. Carries flag **E8-b** unresolved: the consent copy says `%AppData%\PWRUHelper\models`, Winston's Release-C direction says `%LocalAppData%` — recommendation is LocalAppData, and the copy in `ux-mode-degrade.md` §3.6/§4.2 changes in the same commit if it lands. Adds three `[DERIVED]` ACs the epic left implicit: the store's I10 discipline, SHA-256/size verification, and the `_restoringSettings` contract as its own AC. |
| 2026-09-07 | **Implemented (Amelia DEV).** Status → **review**. Flag **E8-b** closed on the ruling: `%LocalAppData%\PWRUHelper\models`, with the code, `UserMessages`' consent copy, `ux-mode-degrade.md` §3.6 and `epics.md` § E8.S3 AC 1 changed in this one commit and a test pinning that the sentence and the `SpecialFolder` agree. Ruling **E8-f** discharged: a C# manifest table inside the exe (not `Data/*.json`, which ships as a user-editable copy), every URL through the unchanged `UpdateService` allow-list, `.part` + staging with size and SHA-256 verified before anything is moved into place, and `BergamotEngine.Resolve` hashing the DLL before it loads it. Owner digests are `TODO-owner` and structurally refuse to load — the hand-off list of files and hashes is in the Completion Notes. Suite **1244 → 1276**, green, nothing downloaded. Six deviations recorded, the load-bearing one being a new `MainWindow.Offline.cs` partial so E6.S5's `AC1_The_handlers_name_no_MessageBox` pin stays exactly as strict as it was. |
