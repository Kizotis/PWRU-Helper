# Story E2.S1: `ProviderGate` — circuit breaker with strikes, cap and half-open probe

Status: **review**

_Epic E2 (B) — Provider gate · increment 1 · **Release A.1: E2 and E3 are separate PRs but ONE release** · ⛔ no
production code until the owner's explicit go._

> **Release rule, non-negotiable.** This epic must not reach users without Epic 3. A circuit breaker in front of
> `client=gtx` — which 429s on request #1 from an affected connection — is an app that is correctly paused all the
> time (`architecture-cible.md` R7, owner's decision 2). It is stated in four places; this is the fifth.

## Story

As a player behind a shared ISP address,
I want the app to stop asking after it has been told to stop,
So that the block Google put on my connection actually expires.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E2.S1. Do not weaken._

1. **Given** the state machine of `architecture-cible.md` §5.2 and the `Kind` table of §5.3
   **When** `ReportFailure(RateLimited)` or `ReportFailure(Blocked)` arrives
   **Then** `strikes` increments and `blockedUntil = now + OpenBaseSeconds × 2^(strikes−1)`, clamped at
   `OpenCapMinutes` (60 s → 2 min → 4 min … 30 min).
2. **Given** an open gate
   **When** `TryEnter` is called before `blockedUntil`
   **Then** it returns `Open(retryAt)` and **no HTTP request is made**; when it is called at or after
   `blockedUntil`, **exactly one** caller receives `Probe` and every concurrent caller receives
   `Open(now + probeTimeout)`.
3. **Given** a half-open probe
   **When** it succeeds
   **Then** the gate closes, `strikes = 0`, `blockedUntil = null`; when it fails with an opening kind, `strikes`
   increments and the window doubles.
4. **Given** `QuotaExhausted`
   **When** it is reported
   **Then** the gate opens for `QuotaOpenMinutes` (60) with **no** strike escalation; given `AuthFailed`, it opens
   with `blockedUntil = DateTimeOffset.MaxValue` until `ProviderGates.ClearAuthBlock(id)` is called.
5. **Given** `Unavailable`, `Timeout`, `Network` or `Unknown`
   **Then** the gate takes **no strike** and applies a `SoftCooldownSecs` (5) cooldown — without it, a DNS blip
   means the next LIVE tick re-hits the same dead provider 700 ms later.
6. **Given** `BadResponse`
   **Then** three **consecutive** occurrences open the gate for `OpenBaseSeconds`.
7. **Given** `Cancelled`
   **Then** the gate is **never** touched.
8. **Given** `CleanResetMinutes` (10) of clean operation
   **When** the next failure arrives
   **Then** it opens for 60 s, not for the escalated window.

## Tasks / Subtasks

- [x] **T1 — Create `Services/ProviderGate.cs`** (AC: 1–8) — the glossary §16 target file
  - [x] `internal sealed class ProviderGate` with `TryEnter(RequestPriority)`, `ReportSuccess()`,
        `ReportFailure(TranslationErrorKind kind, TimeSpan? retryAfter = null)`.
  - [x] `internal enum RequestPriority { Interactive, Background }` and
        `GateDecision { Allow | Wait(TimeSpan) | Open(DateTimeOffset retryAt) | Probe }` in the same file
        (glossary §16 puts all three there). A readonly record struct or a small class hierarchy — pick one and
        keep it allocation-light; this is consulted before every request.
  - [x] **Injectable clock**: `Func<DateTimeOffset>` (IS-6), defaulting to `() => DateTimeOffset.UtcNow`.
        `ProviderGates` must share the **same** clock (E2.S2) or a persisted `blockedUntil` gets compared against a
        different "now".
  - [x] State: `strikes`, `blockedUntil`, `lastKind`, `lastAt`, `cleanSince`, the consecutive-`BadResponse` count,
        and the half-open latch. Thread-safe — both chains and the LIVE loop call it (a single `lock`, like
        `CachingTranslator.cs:20`, is the house pattern; do not build a lock-free scheme).
- [x] **T2 — The escalation arithmetic** (AC: 1, 3, 8)
  - [x] `blockedUntil = now + OpenBaseSeconds × 2^(strikes−1)`, clamped at `OpenCapMinutes`: 60 s → 2 → 4 → 8 →
        16 → **30 min (clamped)**.
  - [x] A probe that fails with an opening kind ⇒ `strikes++` and the window doubles; a probe that succeeds ⇒
        `Closed`, `strikes = 0`, `blockedUntil = null`.
  - [x] `CleanResetMinutes` (10) of clean operation resets `strikes`, so the **next** failure opens for 60 s and
        not for the escalated window. Track it with `cleanSince`, refreshed by `ReportSuccess`.
- [x] **T3 — The half-open probe** (AC: 2)
  - [x] At or after `blockedUntil`, **exactly one** caller gets `Probe`; every concurrent caller gets
        `Open(now + probeTimeout)`. Prove it with two concurrent `TryEnter` calls on a fake clock, not with a sleep.
  - [x] While a probe is outstanding, the gate is neither open nor closed: it admits nobody else until the probe
        reports.
  - [x] **Ruling OQ-b (binding):** a half-open probe **does** consume a rate-ceiling token — *"it is a real
        request; one rule, no special case"*. The bucket itself is **E2.S3**; leave the seam so E2.S3 wires it
        without re-opening this file's design.
- [x] **T4 — The §5.3 `Kind` table, every row** (AC: 4–7)
  - [x] `RateLimited`, `Blocked` ⇒ strike + open (the escalating window).
  - [x] `QuotaExhausted` ⇒ open for `QuotaOpenMinutes` (60), **no** strike escalation; cleared when the key is
        re-saved (the clear entry point is `ProviderGates.ClearAuthBlock(id)`, E2.S2).
  - [x] `AuthFailed` ⇒ `blockedUntil = DateTimeOffset.MaxValue` until `ClearAuthBlock(id)`.
  - [x] `Unavailable`, `Timeout`, `Network`, `Unknown` ⇒ **no strike**, `SoftCooldownSecs` (5).
  - [x] `BadResponse` ⇒ soft strike; **3 consecutive** ⇒ open for `OpenBaseSeconds`. A success in between resets
        the soft count.
  - [x] `AllProvidersPaused` ⇒ **never reported to a gate** — it is the chain's own outcome (E3.S3). Guard it.
  - [x] `Cancelled` ⇒ **the gate is never touched**: `strikes`, `blockedUntil` and `cleanSince` are unchanged
        (assert on a whole-state snapshot, TP-GATE-12).
- [x] **T5 — `Snapshot()` for the UI — and NO event** (ruling R-2/OQ-c)
  - [x] Add `internal GateSnapshot Snapshot()` returning an **immutable record**
        `{ State, BlockedUntil, Strikes, LastKind }`. It is read-only, allocation-cheap and side-effect free: it
        must **not** advance the clock, take the probe, or consume a token.
  - [x] **Do not add `StateChanged`, or any event, delegate or `INotifyPropertyChanged`.** `readiness-report.md`
        R-2 proposes exactly that; the architect's ruling in `README.md` overrides it: *"Poll at 1 Hz from the
        countdown timer Sally already requires (≤ 1 Hz repaint). No event, `Services/` stays passive."* The UI side
        lands in **E7.S2/E7.S3**. (Sprint-plan note 1.)
- [x] **T6 — The policy numbers and the IS-12 override hatch**
  - [x] Add the §5.6 breaker constants to `Services/TranslationPolicy.cs` (created in E1.S7), each graded
        **[ASSUMED] — calibrated to a REPORTED range, never measured; instrument first, tune from field logs
        (U9)**: `OpenBaseSeconds = 60`, `OpenCapMinutes = 30`, `CleanResetMinutes = 10`, `QuotaOpenMinutes = 60`,
        `SoftCooldownSecs = 5`, `BadResponseStrikesToOpen = 3`.
  - [x] **IS-12** — a **pure, `internal`** parser turning an optional overrides JSON string into the effective
        numbers, so a test (and later a field experiment) can drive short windows without waiting 30 real minutes.
        Unparseable ⇒ ignored, `TranslationPolicy` defaults used, **never throws** (the L1 half of TP-SET-11).
  - [x] **Sprint-plan note 6:** the `AppSettings.ProviderGateOverrides` field this parser will read is created in
        **E6.S3** (increment 5, §12) and used as a field hatch by **E2.S7**. This story lands the parser only, with
        no settings dependency. If John/Winston prefer the field to move forward into E2, say so and this story
        gains one `AppSettings` property.
- [x] **T7 — Tests, all on the fake clock** (AC: 1–8)
  - [x] **TP-GATE-01** opens on `RateLimited`, window = 60 s · **TP-GATE-02** stays open, `retryAt` preserved ·
        **TP-GATE-03** (**T1**) half-opens **exactly once** under two concurrent callers · **TP-GATE-04** probe
        success closes and resets strikes · **TP-GATE-05** probe failure re-opens at 120 s ·
        **TP-GATE-06** (**T2**) escalation 60 s / 2 / 4 / 8 / 16 / **30 min clamped** ·
        **TP-GATE-07** (**T3**) clean reset ⇒ the next failure opens for 60 s ·
        **TP-GATE-08** `QuotaExhausted` ⇒ 60 min, strikes not escalated ·
        **TP-GATE-09** `AuthFailed` ⇒ `MaxValue`, cleared by `ClearAuthBlock` ·
        **TP-GATE-10** the four soft-cooldown kinds ⇒ 5 s, no strike ·
        **TP-GATE-11** `BadResponse` needs 3 consecutive; a success in between resets ·
        **TP-GATE-12** `Cancelled` never touches the gate (whole-state snapshot equality) ·
        **TP-GATE-13/14/15** `Retry-After` delta-seconds and HTTP-date **override** the computed window and are
        clamped to `[1 s, OpenCapMinutes]` — the gate applies the value; **parsing it from the response is
        E2.S5's** (T8/TP-RET-*).
  - [x] **No `Task.Delay` in a gate test, ever** (CI-3). Every window, escalation, cooldown and clean-reset
        assertion drives the injected clock.
  - [x] The whole §5.3 table must have a case — that is the DoD.
  - [x] Put the cases in the non-parallel `Gates` collection that **E2.S2** defines (IS-5). If E2.S2 has not landed
        yet, this story's tests may construct `ProviderGate` directly (no static state) and move into the
        collection with E2.S2.

## Dev Notes

### Invariants this story must keep

- **I2** — `ProviderGate` lives in `Services/`, is UI-free and unit-testable headless. No WPF type, no
  `DispatcherTimer`, no event out of `Services/`.
- **I3** — `Cancelled` never touches the gate. A timeout (`TaskCanceledException` with the token **not** cancelled)
  is a `Timeout` — a soft cooldown — not a cancel. The mapper (E1.S3) already draws that line; the gate must not
  redraw it.
- **I9** — gate state is process-global and persisted. Instances are handed out by `ProviderGates` (**E2.S2**) and
  persisted by `ProviderStateStore` (**E2.S4**). This class itself holds no static state and does **no I/O**.
- **I10** — nothing here touches disk or network. The gate is pure arithmetic over an injected clock; the file
  loads lazily on the first `TryEnter` **of the registry**, off the UI thread (E2.S4).
- **I11** — no user text, no URL, no key in anything this class produces. Gate lines are provider id + timings only
  (E2.S6).
- **I16** — the LIVE loop is not touched in this epic.

### Rulings and decisions that apply

- **R-2 / OQ-c (binding): poll, do not notify.** `ProviderGate.Snapshot()` returns
  `{State, BlockedUntil, Strikes, LastKind}`; the UI polls it at 1 Hz from the countdown timer. **No events out of
  `Services/`.** This overrides `readiness-report.md` R-2's proposed `ProviderGates.StateChanged`, which explicitly
  asked for it to be added to this story's scope. **Winston: `Snapshot()` is not yet in the §16 glossary.**
- **OQ-b (binding):** a half-open probe consumes a rate-ceiling token.
- **OQ-a (binding):** read-once is `Interactive` — it is a user click. That matters to **E2.S3** and **E5.S4**;
  the enum is defined here.
- **R-3 (binding, but E3.S3's):** `ChainTranslator.LastOutcome` carries which provider answered and why a higher
  tier was skipped. **`ITranslator` (I1) does not change** — and neither does this class's surface to accommodate it.
- **U9 / E2.S7:** every constant in §5.6 is [ASSUMED]. They ship instrumented and are tuned from ≥ 3 field reports
  after the A.1 release. Write the tests to assert **relationships** (the window doubles; the cap holds), not
  literals, so a tuned constant does not rewrite the suite. Where a literal is unavoidable, say so in a comment so
  E2.S7 updates it deliberately.

### Repo traps

- **R4 / R-08 — static state and xUnit.** This class has none, but the registry (E2.S2) does. Do not introduce a
  `static` field here "for convenience"; it would leak across parallel collections.
- Footprint is a product requirement: `TryEnter` is on the hot path of every request. No allocation per call beyond
  the decision value, no logging from `TryEnter` (E2.S6 logs **transitions**, never skipped requests — otherwise a
  30-minute open window fills the 1 MB log).
- `InternalsVisibleTo PWRUHelper.Tests` (`PWRUHelper.csproj:28`) and the `tests\**` `DefaultItemExcludes` (`:18`)
  intact. **No `.sln`.** `dotnet test tests/PWRUHelper.Tests` green, headless, under ~30 s.
- Nothing in this story is wired to a caller yet. Providers report to gates through **`HttpProviderCore`**
  (**E2.S5**, §7.0: `gate.TryEnter(priority)` before the request, `ReportSuccess`/`ReportFailure(kind, retryAfter)`
  after it). **A provider never touches a gate directly.**

## Testing

- **TP-IDs landed:** TP-GATE-01 … TP-GATE-15 (the breaker rows and the `Retry-After` application).
  TP-GATE-16/17/18 (the token bucket) ⇒ **E2.S3**. TP-GATE-19…23 (persistence, transition-only saves) ⇒ **E2.S4**.
  The L1 half of TP-SET-11 (unparseable overrides ⇒ defaults, no throw) is landed here with the IS-12 parser.
- **Fixtures:** none — the gate is pure arithmetic.
- **Isolation items:** **IS-6** (injectable `Clock`, shared with `ProviderGates`), **IS-12** (pure `internal`
  overrides parsing). Consumes **IS-5**'s collection once E2.S2 defines it.
- **Must keep passing unchanged:** the whole existing suite. Nothing user-visible changes in this story — the gate
  is not yet consulted by any provider.

## File List

**Create**
- `Services/ProviderGate.cs` (`ProviderGate`, `GateDecision`, `RequestPriority`, `GateSnapshot`)

**Modify**
- `Services/TranslationPolicy.cs` (the §5.6 breaker constants, each graded; the IS-12 overrides parser or its home)

**Create (tests)**
- `tests/PWRUHelper.Tests/ProviderGateTests.cs`

## Size, dependencies, risk

**Size:** M · **Depends on:** E1 (the whole epic — it needs `TranslationErrorKind`) · **Blocked by:** the owner's
Phase-4 go · **Risk:** medium.
**DoD:** T1–T3 pass; the whole §5.3 table has a case.

## How to verify manually

Nothing user-visible changes in this story: no provider consults a gate until **E2.S5**. Verify by running the
suite and reading `ProviderGateTests` against the §5.3 table row by row. The first manual verification of the
epic's *behaviour* comes at E2.S5's DoD (a 429 costs one request in the log) and at the A.1 release gate (force
every gate open ⇒ one paused message with a countdown and **zero** requests).

## References

- [Source: docs/investigations/03-stories/epics.md#Story E2.S1; Epic 2 header (the release coupling)]
- [Source: docs/investigations/02-traduction/architecture-cible.md#5.1 … #5.6, #5.2 State machine, #5.3 Kind table]
- [Source: docs/investigations/README.md#Architect's rulings — R-2 (Snapshot, poll at 1 Hz), OQ-a, OQ-b]
- [Source: docs/investigations/03-stories/test-plan.md#3.2 ProviderGate (TP-GATE-01..15); #2 IS-5, IS-6, IS-12; #5 CI-3]
- [Source: docs/investigations/03-stories/readiness-report.md#R-2 (superseded by the ruling)]
- [Source: docs/investigations/02-traduction/analyse-implementation-actuelle.md#1.5, #2.3 (why one request, not three)]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD Senior Software Engineer) on `claude-opus-5[1m]` — non-interactive DS run, worktree
`.claude/worktrees/p4-code`, branch `feature/p2-a1-gate-and-endpoint`, baseline `5e98f9a`.

### Debug Log References

| Run | Command | Result |
|---|---|---|
| baseline | `dotnet test tests/PWRUHelper.Tests` | **405 passed, 0 failed**, 5 s |
| red | `dotnet build PWRUHelper.csproj` after `Services/ProviderGate.cs` | 6 × `CS0117: 'TranslationPolicy' does not contain a definition for 'OpenBaseSeconds' / 'OpenCapMinutes' / 'CleanResetMinutes' / 'QuotaOpenMinutes' / 'SoftCooldownSecs' / 'BadResponseStrikesToOpen'` — the six §5.6 numbers, demanded by the code that reads them |
| red 2 | `dotnet test tests/PWRUHelper.Tests` after `ProviderGateTests.cs` | `CS0051` — a `[Theory]` cannot take the internal `GateState` on a public method; the §5.3 table row takes a `bool opens` instead |
| after | `dotnet test tests/PWRUHelper.Tests` | **463 passed, 0 failed**, 5 s (+58 cases) |
| mutation | four deliberate mutants (drop the cap clamp · drop the `Retry-After` clamp · the ignored row falls through to a soft cooldown · never latch the probe) | **21 cases failed**, then reverted — the suite is not vacuous |

No network, no `%AppData%`, no disk, no `Task.Delay` and no wall-clock read in the new tests (CI-3, I10). No new
NuGet, no `.sln`; `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and the `tests\**` `DefaultItemExcludes` (`:18`)
untouched. `TranslationErrorsTests.No_production_source_names_Kind_Cancelled` still green — the gate states that
row by construction (see below) and never names it.

### Completion Notes List

**What landed.** `Services/ProviderGate.cs` — one file, six types, no static state, no I/O, no UI:

| Member | `file:line` |
|---|---|
| `internal enum RequestPriority { Interactive, Background }` | `Services/ProviderGate.cs:10` |
| `internal enum GateState { Closed, Open, HalfOpen }` | `:20` |
| `internal enum GateOutcome { Allow, Wait, Open, Probe }` | `:31` |
| `internal readonly record struct GateDecision(GateOutcome Outcome, TimeSpan Delay, DateTimeOffset RetryAt)` — statics `Allow`/`Probe`, factories `Wait(TimeSpan)`/`Open(DateTimeOffset)` | `:49` |
| `internal sealed record GateSnapshot(GateState State, DateTimeOffset? BlockedUntil, int Strikes, TranslationErrorKind? LastKind)` | `:64` |
| `internal sealed record GatePolicy(…6 numbers…)` · `GatePolicy.Default` · `internal static GatePolicy Parse(string? json)` | `:78`, `:100` |
| `internal ProviderGate(Func<DateTimeOffset>? clock = null, GatePolicy? policy = null)` | `:181` |
| `internal GateDecision TryEnter(RequestPriority priority)` | `:195` |
| `internal void ReportSuccess()` | `:228` |
| `internal void ReportFailure(TranslationErrorKind kind, DateTimeOffset? retryAt = null)` | `:260` |
| `internal void ClearAuthBlock()` | `:319` |
| `internal GateSnapshot Snapshot()` | `:333` |
| `private static GateReaction Reaction(TranslationErrorKind)` — §5.3 as one total function | `:424` |

The six §5.6 constants are in `Services/TranslationPolicy.cs:66,71,75,79,84,88`, each `[ASSUMED]` with the
reported range behind it and E2.S7 named as the story that tunes them; `TranslationPolicyTests`' grade scan
covers them automatically.

**The `Cancelled` row, by construction.** `Reaction` lists every kind it acts on and ends
`_ => GateReaction.Ignore`. The row that must never touch the gate (I3) is therefore stated as the default arm
and is never named — TP-MAP-17's source scan stays green, and a kind added to the enum later lands there inert
rather than silently doing something. `AllProvidersPaused` is listed explicitly as `Ignore`: it is the chain's
own outcome (E3.S3) and is guarded rather than trusted.

**Deviations, all deliberate — the reviewer's list.**

- **D1 — `ReportFailure(kind, DateTimeOffset? retryAt)`, not `TimeSpan? retryAfter`.** That is the type the value
  actually travels in (`TranslationException.RetryAt`, produced by `ProviderErrorMapper.RetryAfter` at
  `Services/ProviderErrorMapper.cs:303`, which resolves both header forms to an absolute instant). Passing a
  delta would make the caller subtract a "now" the gate did not supply — two clocks where IS-6 exists to have
  one. TP-GATE-13 (delta-seconds) and TP-GATE-14 (HTTP-date) both pass through the same parameter, which is the
  point.
- **D2 — the `[1 s, OpenCapMinutes]` clamp landed here.** E1.S3 deferred it to "whoever first reads `RetryAt`";
  that is `ProviderGate.Honour` (`:375`). An already-elapsed HTTP date opens for 1 s; a 24-hour hint opens for
  30 minutes. **E1.S3's deferred finding can be closed.**
- **D3 — the probe timeout is `TranslationPolicy.RequestTimeoutSeconds` (12 s), not a new constant.** AC 2 needs
  a `probeTimeout` that §5.6 does not define. A probe is one request and cannot outlive the request timeout, so
  the existing `[CONFIRMED]` number is used rather than inventing a seventh ungraded one.
- **D4 — an abandoned probe is re-armed after that timeout** (`:207`, test
  `An_abandoned_probe_is_re_armed_once_it_could_no_longer_be_running`). Not in the story: a caller that takes the
  probe and never reports — a cancel mid-flight (which by I3 must not touch the gate), or a crash — would
  otherwise leave the gate half-open, admitting nobody, for the life of the process. That is R-01 with no way
  out, so it is closed here rather than left to the field.
- **D5 — a non-escalating failure never shortens a standing block** (`BlockUntil`, `:368`, test
  `A_soft_failure_in_flight_cannot_shorten_a_block_that_already_stands`). Two requests share the gate (I9); the
  429 opens 30 minutes and the second request times out a second later. A plain assignment would replace those
  30 minutes with the 5-second cooldown and hand a blocked provider a request every 5 seconds. Escalating rows
  still assign, because their window *is* the policy and §5.5 gives a server's `Retry-After` the last word.
- **D6 — concurrent callers get `Open(probeStartedAt + probeTimeout)`,** which for the concurrent callers AC 2
  describes (same instant) is exactly `now + probeTimeout`; a caller arriving later is simply not told to wait a
  full timeout again.
- **D7 — `ClearAuthBlock()` closes the gate whatever opened it.** §5.3 has the key-save clear both the
  `AuthFailed` block and the `QuotaExhausted` window, and re-saving a provider's key is the user's explicit "try
  this provider now" — it is also the only escape from a wrong `MaxValue`. Narrowing it to "only if `lastKind`
  was one of those two" was considered and rejected as unobservable subtlety.
- **D8 — the IS-12 parser lives in `Services/ProviderGate.cs`, not in `TranslationPolicy.cs`.** It has to:
  `TranslationPolicyTests.The_type_is_a_static_internal_table_with_no_behaviour` asserts that type declares
  **zero** methods and properties. The story permits "the parser or its home"; the constants went to
  `TranslationPolicy`, the parsing to the file that reads it.

**Two things a reader should know, neither a deviation.**

- **The clean-reset is a safety net, not the usual path.** §5.2 states both transitions and both are
  implemented, but every route back to `Closed` runs through a successful probe, which already zeroes the
  strikes — a strike always opens the gate, and only a probe (or `ClearAuthBlock`) closes it. `DecayStrikes`
  (`:398`) is kept because §5.2 says so, because it ages the consecutive-`BadResponse` count when no success has
  landed, and because E2.S4 restores a `cleanSince` this process never observed. It is commented as such at the
  method rather than left to look like dead code. TP-GATE-07 asserts the AC's observable outcome.
- **`GateOutcome.Wait` is declared and never returned.** The token bucket is E2.S3; `TryEnter` returns `Allow`
  where the bucket will answer (`:222`), and the half-open probe is the seam that must charge a token there
  (ruling OQ-b). `RequestPriority` is likewise accepted and unread today — pinned by
  `Priority_does_not_change_the_breakers_answer_yet`, so the day it starts mattering is E2.S3's case to write.

**Tests.** `tests/PWRUHelper.Tests/ProviderGateTests.cs`, 58 cases from 33 facts and theories (one of them the
whole §5.3 table). Every window, escalation, cooldown, clean-reset and probe assertion drives the injected clock; the
one concurrency case uses `Parallel.For` over 64 callers and asserts a **count**, never a timing. Windows are
asserted as relationships to `TranslationPolicy` (doubling, cap, cooldown) so E2.S7's tuning does not rewrite
the suite — the two unavoidable literals (a `Retry-After: 120`, the 1-second floor) say so in a comment.
`The_table_covers_every_kind_the_vocabulary_defines` fails if E1's enum grows a row that the §5.3 table does
not, so a new kind cannot land quietly in the inert arm. The cases construct `ProviderGate` directly and join no
collection, per the story: they move into **E2.S2**'s non-parallel `Gates` collection (IS-5) with the registry,
which is what has static state.

**Nothing user-visible changed** — no provider consults a gate until E2.S5, and no XAML, template or `Run.Text`
binding was touched (I15/I16 not engaged).

### File List

**Created**
- `Services/ProviderGate.cs`
- `tests/PWRUHelper.Tests/ProviderGateTests.cs`

**Modified**
- `Services/TranslationPolicy.cs` — the six §5.6 breaker constants, each graded, plus the class summary's note
  that the breaker half of §5.6 has now arrived
- `docs/investigations/03-stories/stories/E2.S1-provider-gate-breaker.md` (this file)
- `docs/investigations/03-stories/sprint-status.yaml` — `e2-s1-provider-gate-breaker` → `review`
