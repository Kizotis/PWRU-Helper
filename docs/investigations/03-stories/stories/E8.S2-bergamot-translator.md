# Story E8.S2: `BergamotTranslator` — an `ITranslator` over a synchronous native engine ⛔ Phase 4 (prototype branch)

Status: **done · ⛔ Phase 4 · prototype branch only** — E8.S1's five thresholds all held (verdict GO,
2026-09-07), so the hard gate opened and this class was written. Lands ruling **E8-c**. Code review (CR)
run 2026-09-07 on `aa6c00c`: three layers, 12 findings actioned, fixes in the follow-up commit.

_Epic E8 (H) — Offline fallback prototype · increment 7 · **Release C** · prototype branch, merges only on a go._

> **No code before the numbers.** E8.S1 is a hard gate, exactly as E3.S2's capture is for E3.S5: if the resident
> delta does not come back at or under 150 MiB for one model, or if the memory is not returned on unload, this
> class is never written. That is not caution — `plan-migration.md` increment 7 says any single go-criterion
> failing is a no-go and the branch closes.
>
> **This story is the provider and only the provider.** Three neighbouring concerns that `epics.md` § E8.S2
> bundled into it are their own stories, because each touches files this one does not and each is separately
> testable: the **keep-loaded-while-LIVE lifetime** is **E8.S4**, the **model store and the one-click install**
> are **E8.S3**, and the **chain placement, the chip and the cache drop rule** are **E8.S5**. What is left here
> is a class in `Services/` that turns text into text, and it is the only piece the other three cannot stub.

## Story

As a player with no internet,
I want translations to keep working,
So that an outage does not end my evening.

## Acceptance Criteria

_AC 1, AC 2, AC 4 and AC 5 are copied **verbatim** from `epics.md` § Story E8.S2. Do not weaken. Its remaining
two ACs are not lost — the A-1(b) keep-loaded AC is **E8.S4** AC 1, verbatim, and the `github.com`-mirror AC is
**E8.S3** AC 6, verbatim._

1. **Given** §7.6 and amendment A-1
   **When** the provider is written
   **Then** it sits **last** in both chains, behind `OfflineFallbackEnabled` (default false — false means the
   tier is **not even constructed**), references `BergamotTranslatorSharp` (MPL-2.0, NuGet 0.5.1) **on this
   branch only**, and runs the synchronous `BlockingService.Translate` inside `Task.Run` — never on the UI
   thread.
   _The "sits last in both chains" half is **implemented in E8.S5**, which owns `TranslationChains`. This story
   must make it **possible** and must not make it impossible: the class takes its dependencies as constructor
   arguments and reaches for no static the builders do not already own._

2. **Given** I6
   **When** a line is translated
   **Then** it has already been through `SlangGlossary.Expand`; the displayed original and the 🔑 line stay raw.
   A local engine placed *instead of* the glossary is unusable (measured: `данж` → "dangling", `хил` → "heel",
   `спс` → "ps").

3. **Given** I10
   **When** the app starts
   **Then** nothing is loaded — the first load happens on the first fallback use, or on the first LIVE tick
   after the user enabled it, never at startup.

4. **Given** §7.6 constraint 7
   **When** the model is missing or initialisation fails
   **Then** the two failure modes return a `(`-prefixed placeholder, so nothing is cached (I4); the engine
   cannot time out, so I3 does not apply to this leg.
   _`[RULING E8-c, in this story]` In the E1/E3 world that shipped after §7.6 was written, a provider does not
   **return** a placeholder — it **throws** a typed `TranslationException`, and the `(`-prefixed sentence the
   player reads is rendered upstream from `UserMessages`. The invariant this AC actually protects is **I4:
   nothing failed is ever cached**, and a throw satisfies it more strongly than a return does, because
   `CachingTranslator` never sees a value at all. See "The two failure modes, in the current error model" below
   for the exact shape and for why **neither of them may set `NotSent`**._

5. **Given** the thread rule
   **When** `TranslateAsync` / `TranslateLinesAsync` are called
   **Then** the synchronous native call runs on `Task.Run`, never on the UI thread, and the class is safe to
   call from two threads at once — the read chain and the write chain share one process and the LIVE loop is
   sequential but the Translator tab is not.

6. `[DERIVED — I1, architecture-cible.md §16]` **Given** I1
   **When** the class is written
   **Then** it implements `ITranslator` unchanged — two methods, `ct` last, `Task<string>` /
   `Task<List<string>>` — and adds no member to the interface. New behaviour arrives as an implementation, never
   as an interface change.

**Definition of done for the story.** With a model present on disk and the network disabled, a call through this
class returns a translation; with the model absent, it throws the typed failure and nothing reaches the cache.

## Tasks / Subtasks

- [x] **T1 — `Services/BergamotTranslator.cs` (AC 1, AC 6).** New file, namespace `PWRUHelper.Services`,
      `internal sealed class BergamotTranslator : ITranslator`. Glossary row: `architecture-cible.md` §16.
  - [x] Constructor takes **(a)** the model directory as a `string` — **not** a path this class derives, because
        E8.S3 owns the store and E8.S1's harness needs to point it at `%TEMP%`; **(b)** an optional
        `ProviderGate?` defaulting to `null` and resolved lazily as `_gate ?? ProviderGates.For(ProviderIds.Bergamot)`,
        exactly as `HttpProviderCore.cs:91-97` does it; **(c)** an optional engine factory, so every test in
        E8.S3/S4/S5 can run against a fake without a 22 MB DLL.
  - [x] `ProviderIds.Bergamot` already exists (`Services/ProviderGates.cs:22`, and it is in `All` at `:31`).
        Do not add a literal `"bergamot"` anywhere.
  - [x] The display name already exists too: `ProviderNames.Display` → **"Offline engine"**, `Short` →
        **"Offline"** (`Services/ProviderNames.cs:38, :46-49`). Nothing in this story invents a name.

- [x] **T2 — The engine seam (AC 1, AC 5, and it is what makes every later story testable).** Define a tiny
      internal interface over the **three** C exports the native surface has (`benchmark…` §6.2):
      `translator_initialize(configPaths, n)` · `translator_translate(handle, text, html)` ·
      `translator_free(handle)`.
  - [x] One production implementation delegating to `BergamotTranslatorSharp`'s `BlockingService`; one test
        double implementing the same three calls. **The fake is the fixture for the whole epic** — test-plan
        §3.13 is a field campaign, so every automated case in E8.S3/S4/S5 runs on the fake.
  - [x] Keep the seam at **three methods**. It is a P/Invoke surface, not an abstraction layer; the Rule of
        Three has not been met for anything wider.

- [x] **T3 — Lazy load, once, thread-safe (AC 3, AC 5).**
  - [x] `Volatile.Read` + `lock` double-check, or a `Lazy<T>` with
        `LazyThreadSafetyMode.ExecutionAndPublication`. Two concurrent first calls must produce **one** engine —
        two would be 254 MiB.
  - [x] **Nothing in the constructor touches disk.** `ProviderGates.For` "constructs only — it reads no file"
        (`ChainTranslator.cs:187-189`), which is why calling it from a constructor keeps I10. The same rule binds
        here: the constructor may compute a path and must not stat it, read it or load anything from it.
  - [x] The load is triggered by the first **translate call**, not by construction, not by a health check, not
        by the About tab painting. The `TranslationCacheStore.Count` comment
        (`TranslationCacheStore.cs:193-196`) is the precedent, and its wording is the rule: *"a count is not a
        question about a translation"*.
  - [x] **Do not call `ProviderGate.TryEnter`.** The gate's admission is a token bucket and a rate ceiling built
        for a remote endpoint; a local engine has no rate to limit, and taking a token would let the offline
        tier consume the reserve §5.4 keeps for interactive traffic. This class uses the gate for **one** thing:
        recording a failure so the chain skips it next time (T5).

- [x] **T4 — `Task.Run` around every native call (AC 1, AC 5).**
  - [x] `BlockingService.Translate` is synchronous (§7.6 constraint 3, `benchmark…` §10's "what still counts
        against it" #2). Wrap **the whole native interaction** — initialize included, because 103–500 ms of init
        on the UI thread is a visible freeze.
  - [x] `ct.ThrowIfCancellationRequested()` before and after; the native call itself is not cancellable, so the
        honest contract is "we do not start one you cancelled, and we discard the result of one you cancelled".
  - [x] **I3 does not apply to this leg and the code must say why**: the engine cannot time out, so there is no
        `OperationCanceledException` here whose token is *not* cancelled. A bare `catch (OperationCanceledException)`
        is still forbidden — write the `when (ct.IsCancellationRequested)` filter if a catch is needed at all,
        because the next person to read this file will be looking for it.
  - [x] **Batching:** the wrapper's `Translate(IEnumerable<string>)` wraps lines in `<p>` and calls with
        `html: true` — one native call per OCR frame (§6.2). Use it for `TranslateLinesAsync`.
  - [x] **I5 — a batch count mismatch is never padded.** If the HTML split returns a different count than the
        input, throw `BadResponse`. §6.3's join/split per-line fallback is for the *cloud* join providers; a
        local engine returning the wrong count is a bug, not a shape to route around.

- [x] **T5 — The two failure modes (AC 4), and the ruling that shapes them.** See the Dev Notes table.
  - [x] **Model missing** → the tier should **not exist**: E8.S5 builds it only "if `OfflineFallbackEnabled` and
        the model is present". A missing model reaching this class is therefore a race (the user pressed Remove
        mid-session), and it throws `TranslationException(TranslationErrorKind.Unavailable, …, providerId:
        ProviderIds.Bergamot)`.
  - [x] **Init failed** → the same `Unavailable`, **plus** `gate.ReportFailure(TranslationErrorKind.Unavailable)`
        so the gate opens a soft window and `ChainTranslator` skips the tier on the next call **without an
        exception at all** — `RunAsync`'s `if (BlockedUntil(tier, out var snapshot) is { } until) { … continue; }`
        (`ChainTranslator.cs:243-247`) is the skip path, and it needs no flag.
  - [x] **`NotSent` must NOT be set.** `ChainTranslatorTests.NotSent_is_written_in_exactly_one_place`
        (`:519-527`) scans production sources for `NotSent\s*=(?!=)` and asserts exact equality with
        `new[] { "HttpProviderCore.cs" }`. A second writer fails that test, and rightly: ruling E3-b gives the
        flag one owner, and its meaning — "the gate refused before anything left the machine" — is a statement
        about a **request**, which a local engine never makes.
  - [x] Cache the "engine is broken" state so a failed init is not retried on every line of a 40-row frame. The
        gate's window is that cache; a private `_initFailed` bool **in addition** would be a second, disagreeing
        one.

- [x] **T6 — Unload, and prove the memory comes back (AC 3's other half; E8.S4 owns *when*).**
  - [x] `IDisposable` / an internal `Unload()` calling `translator_free` and dropping the handle, idempotent and
        safe against a translate in flight (take the same lock).
  - [x] **This story provides the capability. E8.S4 provides the policy** (kept loaded while LIVE runs, unloaded
        after LIVE stops + an idle timeout). Do not put a timer in this class — a `Services/` type that owns a
        clock and a timer is what E8.S4 makes unit-testable with an injected one.
  - [x] E8.S1's go-criterion 4 is what says this is possible at all: if the working set does not return to its
        pre-load value after `translator_free`, say so and stop.

- [x] **T7 — I6, and it is a **pin**, not an implementation (AC 2).** This class must **never** call
      `SlangGlossary.Expand`. The chain receives text that has already been expanded, upstream, at
      `MainWindow.Live.cs:948` (`bodies.Select(b => _slang.Expand(b))`) and `MainWindow.Translate.cs:99`
      (`from == "ru" ? _slang.Expand(text) : text`).
  - [x] Write the pin as a **test that scans this file's source** for `Expand` and asserts it is absent, in the
        shape `ChainTranslatorTests` already uses for `NotSent`. A comment can be deleted; a scan cannot.
  - [x] The reason is measured, not stylistic (`benchmark…` §10): expanding **inside** the provider would expand
        text the cloud tiers already expanded, and expanding it twice is exactly what
        `Services/PendingRetryQueue.cs:7` warns about.

- [x] **T8 — MPL-2.0 in the licence tree (§7.6 constraint 8).** The DLL, the models and the wrapper are all
      MPL-2.0 and the app is MIT. Add the notice to the About tab.
  - [x] File-level copyleft is compatible with shipping beside an MIT app; it is still a **second licence**, and
        SignPath Foundation requires the app itself to stay OSI-licensed (MIT) — which it does.
  - [x] The exact placement of the About text is **E8.S3's** block (§4.2's "Offline engine (optional)"); this
        story supplies the sentence and the obligation, so that a merge with E8.S3 dropped cannot ship the DLL
        with no notice.

### Review Findings (CR, 2026-09-07 — Amelia, three layers on `aa6c00c`)

_Blind Hunter · Edge Case Hunter · Acceptance Auditor, run in parallel without each other's context.
All `[Patch]` items are FIXED in the review commit; `[Defer]` items are recorded and belong to a named
downstream story._

- [x] **[Review][Patch] The breaker never covered the leg after the engine is up** `[Services/BergamotTranslator.cs]`
      — only `LoadLocked` used `Reported`; the batch count mismatch and everything `Call<T>` mapped used
      `Failure`, which never touches the gate, while two comments claimed the opposite ("which the gate counts
      three of before it opens"). A loaded-but-broken engine was therefore re-asked on every LIVE tick for
      ever, with no "engine is broken" cache at all. **All three layers found this independently.**
      Fixed with its necessary pair: `Gate.ReportSuccess()` on the success path, without which §5.3's soft
      count is cumulative-for-ever and three bad frames an hour apart would open a window on a healthy engine.
- [x] **[Review][Patch] A dead engine stayed cached** `[Services/BergamotTranslator.cs]` — a
      `DllNotFoundException` mid-translate was classified `Unavailable` ("the engine is not THERE") and the
      handle was kept, so `LoadLocked` handed the same corpse to every later call, `IsLoaded` told E8.S4 that
      121 MiB was resident and well, and the tier could not recover inside the session. `RetireLocked` now
      frees and drops it on `Unavailable` only; a `BadResponse` keeps its engine (it is there, it answered).
- [x] **[Review][Patch] `Unload` freed the handle OUTSIDE the lock** `[Services/BergamotTranslator.cs]` —
      the field went null, the lock was released, and `translator_free` then ran while a caller that had just
      won the lock was inside `translator_initialize` on a new one. The binding is not documented thread-safe;
      that is exactly the serialisation the lock exists to provide, given away for a few ms of teardown
      latency. Freed under the lock now.
- [x] **[Review][Patch] `LoadLocked`'s bare `catch (Exception)` laundered a cancel into a provider failure**
      `[Services/BergamotTranslator.cs]` — I3's own hazard running the other way: the engine factory is an
      injected seam and E8.S3's store will do file work inside it, so an `OperationCanceledException` can
      arrive; it became `Unavailable` + a soft window on a healthy tier. `ct` is now threaded into
      `LoadLocked` and both catches carry `when (ct.IsCancellationRequested)`.
- [x] **[Review][Patch] The model-directory locator was called outside the guard**
      `[Services/BergamotTranslator.cs]` — a store throwing `IOException` sent a raw exception up the chain,
      where `ProviderErrorMapper` reads it as `Unknown`, which §4.1 calls a bug report about the mapper.
- [x] **[Review][Patch] A null element in a batch answer passed the count check**
      `[Services/BergamotTranslator.cs]` — the read path substitutes the SOURCE line for a null answer
      (`MainWindow.Live.cs`, `r ?? texts[i]`), so one null rendered untranslated Russian as if it were the
      English. Rejected as `BadResponse` now: I5 is about wrong answers, not only wrong counts.
- [x] **[Review][Patch] A factory returning null was stored as a live engine**; `translator_free` throwing
      escaped `Unload`/`Dispose` raw (`SafeFree` now). `[Services/BergamotTranslator.cs]`
- [x] **[Review][Patch] The resolver latch was claimed BEFORE the registration**
      `[Services/BergamotEngine.cs]` — `Interlocked.Exchange` let a second thread leave `EnsureResolver` with
      no resolver installed, call `translator_initialize` and take a `DllNotFoundException` on a machine where
      the engine is correctly installed. Now a `lock`, and pinned. `SetDllImportResolver` does throw
      `InvalidOperationException` on a second registration for the same assembly (verified) — the latch is a
      contract, not an optimisation, and the resolver callback no longer lets the locator throw out of a
      static runtime callback either.
- [x] **[Review][Patch] The "a second instance still wins from here on" comment was false**
      `[Services/BergamotEngine.cs]` — the runtime consults a resolver only until the library resolves, then
      caches the handle per (assembly, name). A FAILED load is not cached, which is the case that matters
      (install mid-session still works); the comment now says which is which.
- [x] **[Review][Patch] MPL-2.0 shipped with no notice** `[MainWindow.xaml]` — T8 was ticked and the File
      List named `MainWindow.xaml`, but the notice lived only in two source comments, which never reach a
      user, while the About tab still read "no offline engine, no models and **no wrapper here**". The wrapper
      IS here: the reference moved onto `PWRUHelper.csproj` and its assembly goes into the single-file exe.
      One sentence added. E8.S3's fuller "Offline engine (optional)" block still lands beside it; this is the
      floor a dropped story cannot remove.
- [x] **[Review][Patch] The csproj's "0 B of exe growth" was carried over from a configuration that could
      not grow** `[PWRUHelper.csproj]` — E8.S1 held the package on the TEST csproj. The NATIVE half of U7
      carries over verbatim and is re-verified (no `runtimes/` in any output, no `runtimeTargets` in
      `PWRUHelper.deps.json`); the managed binding now costs a measured 9,728 B, against 21.4 MB avoided.
      `PrivateAssets` is deliberately not set — it would stop the assembly reaching the test project, where
      the build-output pin reads it.
- [x] **[Review][Patch] Blank-line asymmetry between the two entry points**
      `[Services/BergamotTranslator.cs]` — `TranslateAsync` short-circuits a blank ask; the one-line branch of
      `TranslateLinesAsync` did not, so a single empty OCR row loaded 121 MiB to translate nothing (I10,
      AC 3). Same guard on both now.
- [x] **[Review][Patch] Two tests could pass without testing anything**
      `[tests/PWRUHelper.Tests/BergamotTranslatorTests.cs]` — `Two_concurrent_first_calls_build_one_engine`
      synchronised only the FIRST caller, so a cold pool could run the second after the first finished and
      `Initialisations == 1` held for the trivial reason; and TP-BRG-09 was claimed while no case ever drove
      a `CachingTranslator`. Both closed, the second with a non-vacuity leg (the same store DOES take a
      success).
- [x] **[Review][Patch] A duplicate `NotSent` scan was written where the story said not to**
      `[tests/PWRUHelper.Tests/BergamotTranslatorTests.cs]` — removed;
      `ChainTranslatorTests.NotSent_is_written_in_exactly_one_place` already enumerates every production
      `*.cs` and asserts exact equality, verified green against both new files.
- [x] **[Review][Patch] Three properties the story claims were untested** — added: the engine never sees two
      callers at once (the thread contract, with the fake counting its own peak concurrency); an `Unload`
      racing a translate in flight never frees the handle under it (T6's claim, previously exercised only
      sequentially); and the resolver latch.
- [x] **[Review][Defer] `Dispose()` is not terminal** `[Services/BergamotTranslator.cs]` — it is `Unload()`
      under another name, so a translate arriving afterwards allocates a fresh 121 MiB engine that nothing
      holds a reference to unload again. Deliberate and pinned by a test (the load/unload cycle is a
      capability, not a one-shot). **Documented on `Dispose` rather than changed: whoever retires the provider
      — E8.S5's chain rebuild — must drop every reference in the same breath.** Changing it would break the
      capability E8.S4 is being handed.
- [x] **[Review][Defer] The `bergamot` gate reads `Open` for the rest of the process after any failure**
      `[Services/ProviderGate.cs]` — only a half-open probe's success closes a gate, and T3 forbids this
      provider `TryEnter`, so it never takes one. The CHAIN is correct (it skips only while
      `BlockedUntil > now`, so the tier really does come back); **E7's chip is what would read "paused" for a
      working offline tier — E8.S5 owns the chip and has to answer this.** Fixing it here would mean either
      calling `TryEnter` (forbidden by T3) or changing `ProviderGate` (forbidden by the File List).
- [x] **[Review][Defer] `Load()`/`Unload()` block the calling thread** `[Services/BergamotTranslator.cs]` —
      `Load()` pays the 82 ms `translator_initialize` inline and `Unload()` waits out an in-flight frame, and
      the doc told E8.S4 to call `Load()` "on the first LIVE tick", which is the dispatcher. **Both members
      now document the blocking, the throw and the gate side effect; E8.S4 owns the `Task.Run`, the same way
      it owns the clock.** No `LoadAsync` invented: this story ships the capability, not the policy.
- [x] **[Review][Defer] The declared-local re-check in IS-10 reuses the derivation's own field types**
      `[tests/PWRUHelper.Tests/HttpSeamGuardTests.cs]` — it catches an `HttpClient`/`HttpProviderCore` field,
      not a `Socket`, `WebClient` or static helper. Genuinely stronger than what stood before (a provider
      holding neither field was previously **invisible**, not merely unchecked) and non-vacuous, so the
      finding is a widening, not a regression — recorded for whoever adds the next declared-local provider.
- [x] **[Review][Defer] The batch's blank-line behaviour is unverified against the real engine** — the
      binding wraps each line in `<p>`, escapes it (`EncodeHtmlText`) and regex-splits the answer back
      (confirmed from the 0.5.1 assembly), so markup inside an OCR line cannot inject tags; whether an empty
      paragraph survives the round trip 1:1 is not knowable without the model. If it does not, I5 rejects the
      whole frame as `BadResponse` — safe, never a pad. **For the E8.S7 field campaign.**
- [x] **[Review][Defer] `bergamot.dll` is loaded by absolute path from a user-writable directory**
      `[Services/BergamotEngine.cs]` — and `NativeLibrary.Load` on an absolute path resolves that library's
      own dependencies from beside it, so anything that can write there gets code execution in the app. This
      story downloads nothing, so the obligation is **E8.S3's**: a trusted origin and a verified hash before
      the first load, the treatment `UpdateService` already gives an installer. **Written into
      `BergamotEngine.Resolve`'s remark** so a dropped story cannot ship it silently — the same device the
      MPL notice uses.
- [x] **[Review][Defer] The story's own "Line-number drift" cites `TranslationService.cs:18-23` for
      `ITranslator`; there is no such file** — the interface is in `Services/GoogleGtxTranslator.cs`.
      Pre-existing in the story text, not introduced by the implementation. For whoever writes E8.S4.

## Dev Notes

### The two failure modes, in the current error model — ruling **E8-c**

§7.6 constraint 7 was written before E1 landed the typed errors and before E3 landed the chain. Read literally
("return a `(`-prefixed placeholder") it would have this class do something no provider in the app does any more.
What it *protects* is I4, and here is the mapping, which is what the dev implements:

| §7.6's words | What it means now | Why |
|---|---|---|
| "returns a `(`-prefixed placeholder" | **throws** `TranslationException(Unavailable, …, providerId: bergamot)` | Every provider since E1.S2 signals by Kind. `UserMessages` renders the sentence the player reads; `ProviderNames.Display("bergamot")` puts **"Offline engine"** in it (`ProviderNames.cs:38`). |
| "so nothing is cached (I4)" | **stronger than before**: `CachingTranslator` is never handed a value | `CachingTranslator.cs:64` stores only on `IsCacheable`; a throw means there is nothing to test. |
| "it cannot time out, so I3 does not apply" | unchanged, and it is written in the code | There is no `HttpClient` here, so no `TaskCanceledException`-with-live-token. The comment exists so a future reader does not "helpfully" add a broad catch. |
| — | **`NotSent` is not set** | `ChainTranslatorTests.NotSent_is_written_in_exactly_one_place` asserts exactly one writer (`HttpProviderCore.cs`). Ruling E3-b's flag means "no request left the machine", which is a statement about a request. |
| — | the **gate** carries the skip | `ChainTranslator.RunAsync` skips a gated tier before calling it (`:243-247`) — no exception, no flag, and the skip lands in `Outcome.Skipped` where E7's chip already reads it. |

**Is a `ProviderGate` useful for a local engine at all?** Yes, for exactly one of its two jobs. The **rate
ceiling** is useless — there is nothing to be polite to. The **breaker** is valuable: an engine that failed to
initialise will fail again in 200 ms, and a 40-row LIVE frame would pay that 40 times. One `ReportFailure` opens
a soft window, `ChainTranslator` skips the tier for its duration, and `ProviderGate`'s half-open probe re-arms it
for free. The gate also gives E7's chip and tooltip a state to render without a second mechanism.

### What this story may and may not change

| May | May not |
|---|---|
| Add `Services/BergamotTranslator.cs` and its engine seam | Touch `Services/TranslationChains.cs` — the tier lists are **E8.S5** |
| Add the `BergamotTranslatorSharp` reference (branch only) | Add a second NuGet, or reference it on `main` (NFR6) |
| Read `ProviderIds.Bergamot`, `ProviderNames`, `ProviderGates.For` | Call `ProviderGate.TryEnter`, or set `NotSent` |
| Throw `Unavailable` and report it to the gate | Return a `(`-prefixed string (nothing else in the app does) |
| Own the **capability** to unload | Own the **policy** of when — that is **E8.S4** |
| Decide the model **directory** is a constructor argument | Decide where that directory is — that is **E8.S3** (and see E8.S1's flag **E8-b**) |
| Supply the MPL-2.0 sentence | Place the About block — **E8.S3** |

### Invariants this story must keep

- **I1** — `ITranslator` keeps its exact shape. `TranslationService.cs:18-23` is the definition; every existing
  composition and test double depends on it.
- **I2** — no UI type reaches this file. The "is LIVE running?" question E8.S4 needs arrives as a `Func<bool>`,
  the way `PendingRetryQueue` already takes `liveIsRunning:` at `MainWindow.Ocr.cs:301`.
- **I3** — the filter is not needed here and the code says why, in one sentence. A bare
  `catch (OperationCanceledException)` is still wrong even where it would be harmless.
- **I4** — nothing failed is cached. Satisfied by throwing.
- **I5** — a count mismatch is a `BadResponse`, never a pad. Padding once bypassed the fallback and poisoned the
  cache (`DeepLTranslator.cs:49-53`).
- **I6** — this file never expands. The pin is a source scan, per T7.
- **I10** — the constructor touches nothing on disk.
- **NFR6** — one new NuGet, prototype branch only.
- **NFR10** — MPL-2.0 declared (T8).

### Repo traps

- **`ProviderGates.For` is safe from a constructor; `TryEnter` is not.** The first constructs only; the second
  is what loads `provider-state.json` on its first call (`ChainTranslator.cs:187-189`, and
  `TranslationChains.OnKeySaved`'s remark at `:133-143` spells out the ordering trap that follows).
- **`ChainTranslator.Of` registers a gate per tier id and refuses a duplicate id** (`:191-213`). Adding
  `bergamot` twice — say, once in a builder and once in a test helper — throws at composition time, which is the
  design working.
- **The suite has two non-parallel collections** (`WPF`, `Gates` — CI-5). Anything this story adds that touches
  `ProviderGates` belongs in the `Gates` collection with its `ResetForTests()` fixture, or it will leak state
  into a neighbouring case (R4).
- **`Data/slang.json` is both embedded and copied** (`PWRUHelper.csproj:45-48`). Irrelevant to this class by
  construction — which is the point of T7's scan.
- **Do not "optimise" by keeping the engine loaded.** The whole argument for shipping this at all is that it
  costs **0 MB at rest**. A convenience `_engine` that never unloads turns a 127 MiB opt-in into a permanent tax
  on a product whose stated virtue is not lagging the game.

### Line-number drift

Verified at writing: `TranslationService.cs:18-23` (`ITranslator`) · `ProviderGates.cs:22, :31`
(`ProviderIds.Bergamot`, `All`) · `ProviderNames.cs:38, :46-49` · `ChainTranslator.cs:187-189, :191-213,
:243-247` · `ChainTranslatorTests.cs:519-527` (the `NotSent` scan) · `HttpProviderCore.cs:91-97` (the
`_gate ?? ProviderGates.For(id)` pattern), `:405-410` (the one `NotSent = true`) ·
`CachingTranslator.cs:64` · `TranslationCacheStore.cs:193-196` · `MainWindow.Live.cs:948` ·
`MainWindow.Translate.cs:99` · `MainWindow.Ocr.cs:301` (`liveIsRunning:`) · `PendingRetryQueue.cs:7` ·
`DeepLTranslator.cs:49-53` · `PWRUHelper.csproj:45-48`.

## Testing

- **TP-IDs landed:** **TP-BRG-09** (both failure modes ⇒ nothing cached — I4, §7.6.7). Runnable in CI because it
  runs on the **fake** engine.
- **The fixture is a fake engine implementing the same three calls** — `translator_initialize` /
  `translator_translate` / `translator_free`. No model, no DLL, no download in CI (**CI-8** names the model
  download and the native DLL explicitly). The existing `Fake` / `CountingTranslator` doubles
  (`TranslationBackendTests.cs:51-62`, `CachingTranslatorTests.cs:10-28`) are the templates, and **CI-10**
  forbids a mocking library.
- **Cases this story owes:**
  1. Constructing the provider loads nothing — no file is opened, no engine is created (I10, AC 3).
  2. The first `TranslateAsync` creates **one** engine; two concurrent first calls still create one (AC 5).
  3. A fake that throws on initialize ⇒ `TranslationException` with `Kind == Unavailable` and
     `ProviderId == ProviderIds.Bergamot`, **and** `NotSent == false`.
  4. After that failure, the gate's snapshot shows a window, and a `ChainTranslator` holding this tier **skips**
     it (`Outcome.Skipped` contains `bergamot`) rather than calling it again.
  5. A batch of N lines in ⇒ N lines out; a fake returning N−1 ⇒ `BadResponse`, never a pad (I5).
  6. `Unload()` frees the handle exactly once and is safe to call twice.
  7. **Source scan:** `Services/BergamotTranslator.cs` contains no `Expand` (I6, T7), and no `NotSent =`
     (the existing `NotSent_is_written_in_exactly_one_place` already covers the second — verify it still passes
     rather than writing a duplicate).
- **Must stay green:** `ChainTranslatorTests.NotSent_is_written_in_exactly_one_place`,
  `TranslationErrorsTests`' scan that no `TranslationException` with `Kind.Cancelled` is ever constructed,
  `PublishFlagsTests` (untouched — CI-7), and the whole suite's runtime budget (**CI-9**, under ~30 s).

## File List

**Create**
- `Services/BergamotTranslator.cs` — the provider
- `Services/BergamotEngine.cs` *(or a nested internal interface — dev's call, one file is fine)* — the
  three-call seam plus its production implementation
- `tests/PWRUHelper.Tests/BergamotTranslatorTests.cs` — the seven cases above, all on the fake
- `tests/PWRUHelper.Tests/FakeBergamotEngine.cs` *(or inside the test file)* — the epic's fixture

**Modify (prototype branch only)**
- `PWRUHelper.csproj` — the one `PackageReference` (already added by E8.S1's T0; verify, do not duplicate)
- `MainWindow.xaml` — the MPL-2.0 notice sentence (T8), placed properly by E8.S3
- `docs/investigations/02-traduction/architecture-cible.md` — §16's `BergamotTranslator` row gains its
  "landed E8.S2" note; §7.6 constraint 7 gains a pointer to ruling **E8-c**

**Must not be modified**
- `Services/TranslationChains.cs` (E8.S5) · `Services/ChainTranslator.cs`, `Services/HttpProviderCore.cs`,
  `Services/TranslationErrors.cs` (no new `NotSent` writer, no new `Kind`)

## Size, dependencies, risk

**Size:** **L** — the largest story of the epic and one of two `L`s in the whole 62-story cut (`epics.md`
"Story sizing"). Native interop, a lifetime, a thread contract and a failure model, in one class ·
**Depends on:** **E8.S1** (hard gate — its five numbers, and specifically go-criterion 4: the memory must come
back); E3.S7 for the chain that will hold it ·
**Blocks:** E8.S3, E8.S4, E8.S5 — all three stub against this class's engine seam ·
**Risk:** **high on footprint** — one model is ~85 % of the app's current working set — accepted by the owner
under A-1(c), and bounded by E8.S1's measurement. **Medium on interop**: `UnmanagedType.HString` marshalling was
removed in .NET 5+ and this project has been bitten by native-interop assumptions before (`WgcCapture`); the
three C exports here take plain `const char*`, which is the easy case, but verify rather than assume ·
**DoD:** the tier works end-to-end on the branch with the network disabled.

## How to verify manually

1. On the branch, with a model in place and **the network disabled**, translate one Russian line through the
   Translator tab: it comes back, and the chip says `● Offline` (the chip is E8.S5 — before it lands, read
   `ChainTranslator.LastOutcome.ProviderId`).
2. Launch and do nothing: Process Monitor shows **zero** reads under the model directory (AC 3, I10).
3. Delete the model, restart, translate: the typed failure surfaces as "Offline engine …" copy, and
   `translation-cache.json` gains **no** entry (AC 4, I4).
4. Watch the working set across load → first translate → unload; it comes back down.
5. Translate a slang line (`го пати на босса, нужен хил`): the output is the **expanded** one, and the displayed
   original and the 🔑 line are still raw (AC 2, I6).

## References

- [Source: docs/investigations/03-stories/epics.md#Story E8.S2] — AC 1, 2, 3, 4 verbatim
- [Source: docs/investigations/02-traduction/architecture-cible.md#7.6 BergamotTranslator, #16 Glossary, #2 Invariants I1/I3/I4/I5/I6/I10, #6.2 ChainTranslator algorithm, #6.3 Batch semantics]
- [Source: docs/investigations/02-traduction/benchmark-fournisseurs.md#6.2 The integration wall, #10 Local-model feasibility memo]
- [Source: docs/investigations/03-stories/test-plan.md#3.13 TP-BRG-09, #5 CI-8/CI-9/CI-10]
- [Source: docs/investigations/README.md#OQ-C amendment A-1, #E3-b (NotSent has one writer), #E4-a]
- [Source: docs/investigations/03-stories/stories/E8.S1-bergamot-prototype-harness-spike-u6-u7.md#Go / no-go]
- [Source: project-context.md#Language-Specific Rules (the OCE trap, WinRT interop), #Translation Pipeline Rules]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD DS — Dev Story), Claude Fable 5.1. Branch `feature/p2-c-offline`, baseline commit `aab6f28`
(E8.S1's GO). No YAML frontmatter added: no story file in this repo carries one, and a lone one here would
be noise — the baseline is recorded in this line instead.

### Debug Log References

- Targeted, while working: `BergamotTranslatorTests` **23 passed / 113 ms**; the guard neighbours
  (`HttpSeamGuardTests`, `ChainTranslatorTests`, `ChainCompositionTests`, `HttpProviderCoreTests`,
  `ProviderGatesTests`, `PublishFlagsTests`, `TranslationErrorsTests`) **133 passed / 677 ms**.
- Full suite, once, before commit: **1237 passed / 0 failed / 8 s** — 1213 + 24 new cases (23 here, 1 in
  `HttpSeamGuardTests`). Nothing downloaded, no network, no new compiler or analyser warning (the single
  `xUnit1031` in `OcrEngineChoiceTests.cs` is pre-existing and untouched). Well inside CI-9's ~30 s.
- Build-output pin verified by hand as well as by test: `bin/…/BergamotTranslatorSharp.dll` is present,
  `bergamot.dll` is not, and there is no `runtimes/` directory at all.

### Completion Notes List

**What landed.** `Services/BergamotTranslator.cs` (`internal sealed class … : ITranslator, IDisposable`) over
`Services/BergamotEngine.cs`'s seam, plus the epic's fixture (`tests/…/FakeBergamotEngine.cs`) and 23 cases.
The `BergamotTranslatorSharp` 0.5.1 `PackageReference` moved onto `PWRUHelper.csproj` with
`ExcludeAssets="native"` and the MPL-2.0 obligation stated in the comment beside it; the test project keeps
its own `PWRU_SPIKE`-conditioned copy for E8.S1's harness. `PublishFlagsTests` untouched and green (CI-7):
no publish flag changed in any of the three build paths.

**The engine seam.** `IBergamotEngine : IDisposable` — `string Translate(string text, bool html)` ·
`IReadOnlyList<string> TranslateBatch(IReadOnlyList<string> lines)` · `Dispose()`, with the factory
(`Func<string, IBergamotEngine>`, handed the **config file path**) standing for `translator_initialize` and
`Dispose` for `translator_free`. **Deviation from T2's "three methods", stated rather than hidden:** the two
translate members are the SAME export (`translator_translate`, `html: false` and `html: true`) — three
exports, four members counting `Dispose`. `TranslateBatch` is on the seam because the `<p>` wrapping and the
split back out belong to the binding's `Translate(IEnumerable<string>)`; re-implementing an HTML split here
would be this repo guessing at the engine's own output shape, which is the class of bug I5 exists to refuse.

**The failure model, as pinned.** Model missing → typed `TranslationException(Unavailable, …,
providerId: bergamot)`; init failed → the same; a native failure mid-translate → `Unavailable` when the
engine is not THERE (`DllNotFound` / `EntryPointNotFound` / `BadImageFormat` / `TypeInitialization`) and
`BadResponse` when it is there and misbehaved; a batch count mismatch → `BadResponse`, never a pad (I5).
**No path sets `NotSent`** (ruling E8-c, E3-b), asserted here and by the existing one-writer scan.
**Deviation from T5, deliberate and tested:** the *model-missing* mode reports to the gate too, not only the
init-failure mode — T5 named the report under mode 2, the parent's brief asks for it on both, and it is the
stronger reading: a 40-row frame must not ask forty times, and the gate window is the only "engine is
broken" cache this class has (no `_initFailed` bool). The **uninstalled-pair** refusal is the one failure
that is NOT reported: nothing is broken, nothing heals in five seconds, and a soft window opened for
"French is not installed" would make the chain skip the tier for the pair that IS installed.

**Deviation 3, found by the review and declared here rather than left implicit.** `TranslateLinesAsync`
short-circuits a **one-line** frame to the per-line export (`html: false`) instead of the batched one, where
T4 says "use `Translate(IEnumerable<string>)` for `TranslateLinesAsync`". It is kept: a one-line frame is one
native call either way, the per-line shape is the cheaper of the two and needs no markup round trip at all,
and §6.2's "one native call per OCR frame" is honoured. The cost is that a line translated alone can differ
from the same line inside an N-line frame, and that I5's count check does not apply on that path — which is
vacuous for a path that asks for one line and returns one. Pinned by
`One_line_takes_the_per_line_call_and_a_frame_takes_the_html_one`.

**Pairs.** `DefaultPairs` = `ru→en` only, as a constructor argument so E8.S3's store — which is what will
know what is on disk — becomes the single decider. `auto` is deliberately outside it: a ru→en model cannot
detect a language, and I7 already sends the Russian lines as `"ru"`, which are the ones this tier is for.

**Threading.** One `lock` guards load, translate and unload alike, so an `Unload()` cannot free a handle
under a call in flight (T6); every native interaction — initialise included — runs inside `Task.Run`, and
both public methods `ConfigureAwait(false)` (scanned). Two concurrent first calls build **one** engine,
proven with the factory held inside the load while the second caller runs into it.

**T8 / MPL-2.0.** The obligation is stated in `PWRUHelper.csproj` beside the reference and in
`BergamotEngine.cs`'s class remark, so neither E8.S3's About block nor E8.S6's packaging decision being
dropped can ship the engine with no notice. **`MainWindow.xaml` was not touched** — the story itself gives
the placement to E8.S3, and adding a `UserMessages` string here would have moved that file's reflection-scanned
surface for no behaviour.

**IS-10.** `HttpSeamGuardTests` gained a second, stronger case rather than a loosened first one: every
`ITranslator` in the app must be either an enumerated HTTP provider or one of three DECLARED non-HTTP types
(`ChainTranslator`, `CachingTranslator`, `BergamotTranslator`), and each declared one is re-checked for
`HttpClient` / `HttpProviderCore` fields. That closes the way past the old derivation (which enumerates by
the client a type holds) instead of carving an exemption into it.

**Nothing else moved.** `TranslationChains.cs` (E8.S5), `ChainTranslator.cs`, `HttpProviderCore.cs`,
`TranslationErrors.cs` are untouched; no chain wiring, no UI, no lifetime policy, no timer.

**For E8.S3, and nothing blocking it:** the store fills two `Func<string?>` locators — the **model
directory** (holding `BergamotTranslator.ConfigFileName`, the one const the two agree on) and the **native
directory** (holding `bergamot.dll`, consumed by the resolver in `BergamotEngine`). It also owns writing that
Marian config file; the shape E8.S1 used is in the spike's `WriteConfig`. Ruling **E8-b**
(`%LocalAppData%\PWRUHelper\models\`) and the zstd-vs-gzip registry finding from the spike are still E8.S3's
to answer.

### File List

**Created**
- `Services/BergamotTranslator.cs`
- `Services/BergamotEngine.cs`
- `tests/PWRUHelper.Tests/BergamotTranslatorTests.cs`
- `tests/PWRUHelper.Tests/FakeBergamotEngine.cs`

**Modified**
- `PWRUHelper.csproj` — the `BergamotTranslatorSharp` 0.5.1 `PackageReference` (`ExcludeAssets="native"`) and
  the MPL-2.0 obligation
- `tests/PWRUHelper.Tests/HttpSeamGuardTests.cs` — IS-10 aware of a local provider, and closed against a
  provider that dodges the client-field derivation
- `docs/investigations/02-traduction/architecture-cible.md` — §7.6 constraint 7 gains ruling **E8-c**; §16
  gains the `IBergamotEngine` row and the "landed E8.S2" note
- `docs/investigations/03-stories/stories/E8.S2-bergamot-translator.md`, `…/sprint-status.yaml`

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **blocked-dep** on E8.S1's numbers. Lands ruling **E8-c**: §7.6's "`(`-prefixed placeholder" is realised as a typed `Unavailable` throw plus a gate window — and **not** as a second `NotSent` writer, which `ChainTranslatorTests.NotSent_is_written_in_exactly_one_place` would fail. Scope narrowed to the provider: A-1(b) → E8.S4, the model store and install → E8.S3, the chain/chip/cache wiring → E8.S5. |
| 2026-09-07 | **Implemented (Amelia DS).** Provider + engine seam + fixture + 23 cases; the package moved to the app csproj with `ExcludeAssets="native"`; ruling E8-c written into §7.6. Two stated deviations: the seam carries the batched shape of `translator_translate` as a second member (the binding owns the HTML split), and the model-missing mode reports to the gate as well as the init-failure mode. Full suite 1237 passed / 8 s, nothing downloaded, no new warning. Status → **review**. |
| 2026-09-07 | **Reviewed (Amelia CR).** Three layers on `aa6c00c`, 22 findings triaged: 14 patched, 8 recorded. The load-bearing ones — all three layers found the first independently — are that the **breaker never covered the leg after the engine was up** (fixed with its pair, `ReportSuccess`), a **dead engine stayed cached** after an `Unavailable` mid-translate, `translator_free` **ran outside the lock**, `LoadLocked`'s **bare catch laundered a cancel** into a provider failure + gate window, a **null batch element** passed the count check into a path that renders the source line as the answer, the **resolver latch was claimed before the registration**, and **MPL-2.0 shipped with no user-visible notice** while the About tab still said there was no wrapper. Ruling E8-c's §7.6 block widened to match (every failure reports; the `GateState`-reads-Open caveat handed to E8.S5). Third deviation declared. Cases 23 → 30 here (8 added, 1 duplicate removed on the story's own instruction); full suite **1244 passed / 0 failed / 20 s**. Status → **done**. |
