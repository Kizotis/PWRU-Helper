---
baseline_commit: ffe405e
---

# Story E3.S6: Demote `gtx` — rename to `GoogleGtxTranslator`, extract `TextChunker`

Status: **done**

_Epic E3 (C) — Provider chain and the new default endpoint · increment 2 · **Release A.1: E2 and E3 are separate
PRs but ONE release** · the owner's Phase-4 go was given 2026-09-06 (`README.md` § Phase 4)._

> **Scheduling note — this story moves.** `epics.md` lists it sixth. **E3.S4 depends on `TextChunker`**, which
> only exists after this story, so the implementation order is **E3.S3 → E3.S6 → E3.S4 → E3.S8 → E3.S7** (E3.S5
> slots before E3.S7 if U2 lands). Same shape as E1, where E1.S7 was pulled forward because E1.S4 needed it.
>
> **This is a rename plus a move. It is mechanical, it is wide, and its whole risk is documentation drift.** 52
> references across 8 test files, 6 comment references in `Services/`, 3 production call sites, and one line of
> `project-context.md`. Nothing about the endpoint's behaviour changes.

## Story

As the maintainer,
I want the old endpoint kept but last,
So that a `client=` id that may come back costs nothing to keep while it stops being the default.

## Acceptance Criteria

_Copied verbatim from `epics.md` § Story E3.S6. Do not weaken._

1. **Given** `Services/TranslationService.cs`
   **When** it is renamed to `Services/GoogleGtxTranslator.cs` with provider id `google-gtx`
   **Then** the endpoint, the User-Agent, the chunking and the batch/per-line shape are otherwise untouched; only
   typed errors, 2 attempts with jitter, gate admission and logging change.
2. **Given** two providers now need chunking
   **When** `ChunkText` and `HardSplit` move to `Services/TextChunker.cs`
   **Then** `tests/PWRUHelper.Tests/ServicesTests.cs:80,91` are re-pointed and the byte-budget and
   content-preservation assertions pass unchanged.
3. **Given** R5 (the rename breaks docs and muscle memory)
   **When** this story lands
   **Then** the **same PR** updates the "Translation Pipeline Rules" section of `project-context.md`, which names
   `TranslationService` today.
4. **Given** the existing `rateLimited` latch (`TranslationService.cs:99-105`)
   **When** the provider is ported
   **Then** the latch is **kept** (I16) and now latches specifically on a `RateLimited`/`Blocked` `Kind` rather
   than on any `TranslationException`.

## What has already happened, so this story does less than `epics.md` thinks

AC 1's *"only typed errors, 2 attempts with jitter, gate admission and logging change"* describes work that
**E1 and E2 already did**. In this worktree at `b592800`, `TranslationService` is already:

- typed (E1.S2/S3 — `TranslationException(kind, …)`, `ProviderErrorMapper`),
- on 2 attempts with full jitter, gate-admitted and logged (E2.S5 — everything runs through
  `HttpProviderCore`, `TranslationService.cs:62`, `:182-191`),
- carrying provider id `ProviderIds.GoogleGtx` **already** (`TranslationService.cs:34`, and E2.S2's comment at
  `ProviderGates.cs:11` says it was spelled `google-gtx` from the start *precisely* so a field report from before
  the rename still reads correctly).

**So this story is exactly two things: a rename, and a move of two static methods.** Plus AC 4, which is the one
behaviour change left in it.

## Tasks / Subtasks

- [x] **T1 — `Services/TextChunker.cs` (AC 2).** `internal static class TextChunker` with `ChunkText(string,
      int)` and `HardSplit(string, int)` moved **verbatim** from `TranslationService.cs:223-260`. Not rewritten,
      not "improved" — the two existing assertions are a byte-budget and a content-preservation property, and
      they are the only proof this code is right.
  - [x] Keep them `internal static` and keep the names. `ServicesTests.cs:80,91` call `ChunkText` directly.
  - [x] Do **not** move `MaxQueryBytes` — it lives in `TranslationPolicy.cs:43` and is passed in as `maxBytes`.
- [x] **T2 — Rename the file and the type (AC 1).** `Services/TranslationService.cs` →
      `Services/GoogleGtxTranslator.cs`; `public class TranslationService` → `public class GoogleGtxTranslator`.
  - [x] **`interface ITranslator` currently lives in this file** (`TranslationService.cs:11-19`), as does the
        historical comment at `:9`. **`ITranslator` must not move** — I1 says the interface is untouched, and
        E1.S1's byte-identity assertion is on its declaration. Leave it exactly where it is, in the renamed file,
        and say so in a comment so the next reader does not "tidy" it into its own file.
  - [x] Use `git mv` so the rename is visible as a rename in the diff and the review can read it as one.
  - [x] The endpoint string, the `client=gtx` id, the User-Agent constant, the timeout, the chunk shape and the
        batch/per-line loop are **untouched**. A diff line inside `RequestAsync`'s URL or inside the UA constant
        is a bug in this story.
- [x] **T3 — AC 4: narrow the `rateLimited` latch (I16).** The latch is at **`TranslationService.cs:149-160`**
      (`epics.md` says `:99-105` — drift; E1 moved it):
  - [x] `catch (TranslationException) { rateLimited = true; … }` becomes
        `catch (TranslationException tex) when (tex.Kind is TranslationErrorKind.RateLimited or
        TranslationErrorKind.Blocked) { rateLimited = true; … }`, with an ordinary
        `catch (TranslationException tex)` after it that records the failure for that one line **without**
        latching.
  - [x] Why this is a real fix and not tidying: today a single `BadResponse` or `Timeout` on line 3 of 14 latches
        the loop and turns lines 4–14 into *"(skipped — rate-limited, try again shortly)"* — a sentence that is
        simply false. E2.S5's record already flagged the sibling of this bug (*"the `catch (TranslationException)`
        that renders a dead network as 'rate-limited' is not fixed here"*).
  - [x] **The three placeholder strings themselves do not change** (`:152`, `:160`, `:161`, `:174`). Ruling E2-d:
        their wording is **E7.S1's**; the batch policy around them is **E3.S8's**. This story narrows *which
        exception latches*, nothing else.
  - [x] Keep the OCE filters at `:141`, `:159` and `:173` exactly as they are (I3). They are E1.S3's work.
- [x] **T4 — Re-point the three production call sites (AC 1).**
  - [x] `MainWindow.xaml.cs:43` — `new CachingTranslator(new TranslationService())` → `GoogleGtxTranslator`.
        (E3.S7 moves this whole field into the ctor; this story only renames the type.)
  - [x] `MainWindow.Translate.cs:230-231` — as rewired by E3.S3 (`ChainTranslator.Of((ProviderIds.GoogleGtx, new
        TranslationService()))`) → `GoogleGtxTranslator`.
  - [x] The six comment references in `Services/` that name the old type: `DeepLTranslator.cs:82`,
        `HttpProviderCore.cs:12`, `RequestLog.cs:10`, `TranslationPolicy.cs:43`, `UserMessages.cs:12`, `:42`.
        Update the names; **also fix `TranslationPolicy.cs:43`'s stale line numbers** (`TranslationService.cs:83,
        :88, :107` → the new file's) while you are in there.
- [x] **T5 — Re-point the tests (AC 2).** 52 references across 8 files:
      `HttpProviderCoreTests.cs` (16), `HttpSeamGuardTests.cs` (10), `ProviderErrorMapperTests.cs` (14),
      `RequestLogTests.cs` (4), `ServicesTests.cs` (3), `TranslationErrorsTests.cs` (2),
      `TranslationPolicyTests.cs` (1), `UserMessagesTests.cs` (3).
  - [x] **`ServicesTests.cs`** — class `TranslationServiceChunkingTests` (`:75`) → `TextChunkerTests`, and the two
        `TranslationService.ChunkText(...)` calls at **`:80`** and **`:91`** → `TextChunker.ChunkText(...)`.
        **The assertions do not change** (AC 2): single chunk for short text; every chunk ≤ 1500 bytes; and
        `Assert.Equal(text, string.Concat(chunks))` — nothing lost or duplicated. That is TP-PRV-13.
  - [x] **`HttpSeamGuardTests.cs`** — the two hard pins at `:59` (`Assert.Contains(typeof(TranslationService),
        providers)`) and `:100`/`:103` (`SharedClientOf`/`ClientOf`) must name the new type. These are the guard's
        own anti-vacuity checks; re-pointing them is required, weakening them is not allowed.
  - [x] A rename is not a semantic change: **no assertion may be edited beyond the type name.** If one has to be,
        stop — something else moved.
- [x] **T6 — `project-context.md` in the same PR (AC 3, R5).** Line **57**, "Translation Pipeline Rules", reads:
      `Pipeline shape (built in BuildTranslator(), rebuilt live on key save): CachingTranslator( key ?
      FallbackTranslator(DeepLTranslator, TranslationService) : TranslationService )`.
  - [x] It is wrong in **three** ways after E3.S3 and this story: `FallbackTranslator` is deleted,
        `TranslationService` is renamed, and `BuildTranslator()` becomes `BuildWriteChain()` in E3.S7. Rewrite it
        to the §8.1 shape and mark clearly which parts land in which story, or write it once, at the end of the
        epic, in E3.S7's PR. **Do not leave it stale** — R5's whole mitigation is "the same PR updates it", and
        this project's recorded failure mode (`verify-against-prior-releases`) is exactly the forgotten follow-up.
  - [x] Also check the "Code Layout" bullet and the "Translation Pipeline Rules" section for any other mention
        (there is one, at `:57`; grep again after editing).
- [x] **T7 — Update the architecture glossary (§16) and §7.3's "mechanical consequences" paragraph** to say the
      move happened, so the next reader is not told to do it again.
- [x] **T8 — Run the suite.** Green, headless, no network, no `%AppData%` write. Baseline 602 at `b592800`, plus
      whatever E3.S3 added. **Test count must not drop**: a rename that loses a case is a rename that lost a case.

### Review Findings

_Code review (Amelia CR), 2026-09-07, on `f4a1515` against baseline `ffe405e`. Three layers plus the
acceptance audit. The mechanical half verified line by line: the removed `ChunkText`/`HardSplit` hunk and
`Services/TextChunker.cs` are **byte-identical** save the one visibility keyword below, and nothing in the
renamed file moved except the type name, the two removed methods, the `TextChunker.` call, comments and AC 4's
filter — the URL, `client=gtx`, the User-Agent, the timeout, the parser and the batch/per-line shape are the
`ffe405e` bytes. AC 4's new case was mutation-checked again: widening the filter with `or
TranslationErrorKind.BadResponse` turns `A_bad_response_mid_loop_fails_only_its_own_line` red._

**Patched in the review commit:**

- [x] [Review][Patch] **The reference sweep missed `tools/`.** `tools/diagnostics/Probe-GoogleTranslate.ps1:12`
      and `:71` and `tools/diagnostics/README.md:131` still named `Services/TranslationService.cs` — live
      tooling, not historical prose, and `:71` cited stale line numbers for the User-Agent it exists to copy.
      Re-pointed to `Services/GoogleGtxTranslator.cs:46-47`. T4's "verified by grep, not by memory" was run
      over `Services/` and the tests only; R5 fires in `tools/` too.
- [x] [Review][Patch] **The `ConfigureAwait` scan is now pattern-based, not a typed list**
      [`tests/PWRUHelper.Tests/HttpProviderCoreTests.cs:686`]. It iterated five hand-written file names, so
      **E3.S4's new provider would have joined the request path without joining the scan** — silently, since a
      missing name skips a file rather than failing. The set is now derived: `Services/*.cs` that reference
      `HttpProviderCore` **and** contain an `await` (comments stripped) — which reproduces exactly today's five
      files and picks up `GoogleDictTranslator.cs` for free. The five known names are kept as an anti-vacuity
      assertion, so a filter that matched nothing still fails loudly. (`TranslationErrorsTests`' thrower list
      needed nothing: it already enumerates every production source and uses the two names only as
      anti-vacuity anchors.)
- [x] [Review][Patch] **`HardSplit` back to `private`** [`Services/TextChunker.cs:50`]. The move widened it to
      `internal` on the reasoning that it "no longer has a class to be private inside" — but a `private static`
      method inside `internal static class TextChunker` compiles and is callable from `ChunkText`, so the
      widening was unnecessary. Nothing calls it (the tests drive `ChunkText` only), and it is the wrong entry
      point for a caller — restoring it makes the file byte-identical to the pre-move original, which is what
      "verbatim" claims.
- [x] [Review][Patch] **`<paramref name="maxBytes"/>` on a type-level summary** [`Services/TextChunker.cs:21`]
      — no such parameter at that scope. No build break (neither csproj sets `GenerateDocumentationFile`, so
      CS1734 is never emitted); rewritten as `<c>maxBytes</c>`.
- [x] [Review][Patch] **The latch comment overstated what AC 4 buys** [`GoogleGtxTranslator.cs:168`]. It said a
      timeout on line 3 of 14 threw away "eleven translatable lines" — but a `Timeout`/`Network`/`Unavailable`/
      `Unknown` is a §5.3 `SoftCooldown` that blocks the provider for 5 s the instant line 3 fails, so lines
      4–14 are refused at admission whichever way the catch is filtered. They are not translated after this
      story either; what changes for them is that they stop claiming a rate limit. The lines genuinely saved are
      the `BadResponse` ones (no window until the third in a row), plus the honesty of the failing line itself.
      The comment now says so — AC 4 is met as written, but the next reader should not be told it saves lines it
      cannot save.
- [x] [Review][Patch] **The E3.S8 hand-off is now written in E3.S8**, not only in this story's record
      (`E3.S8-bound-per-line-fallback.md` § "Carried in from E3.S6"), with its mechanism corrected: a single
      `Timeout`/`Network`/`Unavailable`/`Unknown` is a `SoftCooldown` that blocks the gate **immediately** for
      5 s (`ProviderGate.cs:612`) — not "three strikes" — so lines k+1…N are refused at admission and render N
      copies of *"(translation failed: The translation service is paused after a recent refusal.)"*, at **no
      request** cost. E3.S8's own stale I3 line numbers and drift row were re-pointed at the same time, as were
      E3.S4's two (`TextChunker` "does not exist yet"; the latch to mirror).

**Recorded, not fixed:**

- [x] [Review][Defer] **`BadResponse` is the one narrowed kind the gate does not backstop.** It is a
      `SoftStrike` needing **three consecutive** failures, and `ReportSuccess` resets the count
      (`ProviderGate.cs:517`) — so an intermittently-unparseable body never opens the gate and every line of the
      batch issues a real request, where the old latch stopped at the first. Not a new explosion (a per-line
      fallback whose lines all succeed already costs N, and OQ-A accepts it), but the accidental brake is gone;
      the mirror case is that three unparseable lines **in a row** now open the 60 s breaker inside a single
      batch, where before only one strike per batch was reachable. Both are `PerLineCap`'s to bound — recorded
      in **E3.S8**, which is the next story but one. Widening the latch back would undo AC 4 and reinstate the
      false sentence, so it is deliberately not done here.
- [x] [Review][Defer] **`HardSplit` splits UTF-16 code units, so a surrogate pair can be cut in half**
      [`Services/TextChunker.cs:53`], with a reproduction: `1497×'a' + U+1F600 + 600×'a'` at the production
      budget yields a first chunk of exactly 1500 bytes ending in a lone **high** surrogate and a second
      starting with the lone **low** one. `HttpUtility.UrlEncode` (`GoogleGtxTranslator.cs:214`) renders each
      through UTF-8's replacement fallback, so the emoji is destroyed on the wire and returns as two U+FFFD —
      no exception, and `string.Concat(chunks)` still equals the input (the halves rejoin), so **both asserted
      properties stay green and cannot see it**. Reachable only via the hard-split path (a >1500-byte run with
      no `.`/`!`/`?`/`…`/newline) at a ~1-in-1500 byte alignment. Pre-existing, shipped for two years, and
      moved **verbatim** by this story's own mandate — fixing it here would be a rewrite hidden inside a
      rename, and it would invalidate the one thing that makes this move reviewable. **E3.S4 should not
      inherit it silently**: whoever next owns `TextChunker` should add a "do not split between surrogate
      halves" case with the input above.
- [x] [Review][Defer] **No argument validation on `maxBytes`** [`Services/TextChunker.cs:27`]. A budget smaller
      than the widest character (0, negative, or 1 with Cyrillic) yields one chunk **per character** — no hang
      and no throw, but the documented "within the budget" contract is silently broken, and for a query-string
      provider that is one HTTP request per character. Unreachable today (`MaxQueryBytes` is a `const 1500`),
      but the class now exists precisely so "a provider with a different query budget can use the same
      splitter". A guard belongs in the story that introduces the second budget, not in the one that moved the
      code.
- [x] [Review][Defer] **Both methods are lazy iterators**, so a caller that enumerates twice splits twice, and
      any future argument guard would fire at enumeration rather than at the call. Harmless with today's single
      `foreach`; worth one line in E3.S4's head, since it is now a shared utility.
- [x] [Review][Defer] **`current.ToString() + ch` per character in `HardSplit`** is O(n²) allocation on the
      hard-split path (~2.2 MB of Gen0 churn per 1500-byte chunk). Correct, but at odds with the repo's "tiny
      footprint is a product requirement" rule. An incrementally maintained byte counter removes it without
      changing a boundary — again, not in a verbatim move.
- [x] [Review][Defer] **The one-line path and the multi-line path report the same outage differently**
      [`GoogleGtxTranslator.cs:131` vs `:157`]: `SafeOne` rethrows a `TranslationException`, the loop swallows
      every one into a placeholder and returns normally. So one Russian line on screen makes LIVE count an
      error (and auto-stop after five), while two lines make it reset the counter and stream placeholders
      indefinitely. Pre-existing and **not** widened here — the latched path returned normally too — and it is
      the same root as the chain-blindness item below.
- [x] [Review][Defer] **`TranslationException.NotSent` is invisible above the provider.** A gate that opens
      mid-batch produces a full-length list of placeholders, so `ChainTranslator` reads the tier as a success
      and never falls through (ruling E3-b's flag is only read at `ChainTranslator.cs:205`, for an exception
      that *escapes*). Carried unchanged from E3.S3's review, confirmed by two layers here; E3.S8 / E5.S1.
- [x] [Review][Defer] **Two pre-existing items in the untouched half of the file**, listed so they are not
      rediscovered as new: the chunk loop (`:116`) keeps no partial result and caps no chunk count, so a
      failure on chunk j discards chunks 1…j-1; and `sl=`/`tl=` are interpolated into the URL without
      `UrlEncode` (`:213-214`), so a malformed pair would surface through the generic `catch (Exception ex)` whose
      `ex.Message` is rendered into the feed — the one place a URL could reach a user, one layer above the core
      that refuses to log it. Fixed language combos make it unreachable today.
- [x] [Review][Defer] **The latched tail does not observe cancellation** [`GoogleGtxTranslator.cs:159`]: the
      `if (rateLimited) { …; continue; }` fast path never checks `ct`. Pre-existing and unchanged; the tail
      only appends strings, so it drains in microseconds.
- [x] [Review][Defer] **`test-plan.md` still points TP-CHN-12 (and IS-8, TP-RET-05) at `TranslationService.cs`.**
      Left alone on purpose — the test plan is a frozen spec and the ruling for this review was one-line notes,
      not spec rewrites. The living design (`architecture-cible.md`) and every story that will be *worked* next
      (E3.S4, E3.S8) were re-pointed instead.
- [x] [Review][Defer] **For E7.S1 (copy):** the non-latching arm now renders provider messages of mixed
      register — `"(translation failed: the request timed out)"` (a lowercase fragment) beside
      `"(translation failed: Couldn't reach the translation service. Check your Internet connection.)"` (a full
      sentence, with its stop inside the parenthesis). Both are truthful, which is the whole point of AC 4; the
      unevenness is wording, and wording is E7.S1's (ruling E2-d).

**Verdict: approved.** `dotnet test tests/PWRUHelper.Tests` — **637 passed, 0 failed, 2 s** before and after
the review patches; `dotnet build` clean, 0 warnings. No user-visible string was added or removed (the three
placeholders are the same three bytes-for-bytes); `ProviderIds.GoogleGtx` is untouched, so a field report from
before the rename still reads against the code after it (I11). `git log --follow` on the renamed file walks
back through E2.S5, E1.S1 and on to `Initial release: PWRU Helper v0.2` — the history survived.

## Dev Notes

### Invariants this story must keep

- **I1** — `ITranslator` unchanged and **not moved out of the renamed file** (T2).
- **I3** — the three OCE filters in `TranslateLinesAsync` stay. AC 4 edits the `TranslationException` catch that
  sits *between* them; do not disturb the filters while doing it.
- **I5** — the batch/per-line shape is untouched here. `PerLineCap` is E3.S8's.
- **I16 — the eleven LIVE behaviours are kept**, and the `rateLimited` latch is one of them. AC 4 narrows *what
  latches it*; it does not remove it, and it does not change what a latched line says.
- **I11** — `RequestLog` already writes `provider=google-gtx`. The rename must not change a single logged byte:
  a field report from before this release must still be readable against the code after it. That is why E2.S2
  spelled the id `google-gtx` from day one.

### Rulings that apply

- **E2-d:** the three per-line placeholder strings belong to E3.S8 (policy) and E7.S1 (copy). Not here.
- **R5 (risk):** the rename breaks docs and muscle memory; the mitigation is the `project-context.md` edit **in
  the same PR** plus the two `ServicesTests` references and the glossary. There is nothing else that names the
  type — T4 and T5 are the full list, verified by grep, not by memory.
- **Why keep `gtx` at all** (`architecture-cible.md` §7.3, worth one line in the PR): it costs nothing to keep,
  it is the only tier proven against this app's real traffic for two years, and the block is keyed on the
  `client=` id — an id can come back.

### Repo traps

- **`git mv`, not delete-and-create.** A 260-line file recreated is a 260-line diff nobody can review, and the
  review is the only thing standing between "rename" and "rename plus an accidental edit".
- **`HttpSeamGuardTests`'s anti-vacuity asserts** (`:59-60`, `:100-104`) name provider types **literally**. They
  will fail loudly on the rename, which is correct. Re-point; do not delete.
- **`ProviderStateStore.cs:219`** already carries a comment anticipating this rename ("`For(string)` takes any id
  and validates none, so E3.S6's `google-gtx` rename is one …"). Nothing to do, but read it: it is the reason no
  persisted state migration is needed.
- **`TranslationPolicy` has a grade scan** and a comment-format scan tolerant of formatting but not of missing
  grades (E1.S7's review). Editing `:43`'s comment is safe; deleting the grade is not.
- No settings control is touched: `_restoringSettings` is not engaged.
- `InternalsVisibleTo` (`PWRUHelper.csproj:28`) and `DefaultItemExcludes` (`:18`) intact. **No `.sln`.**

### Line-number drift

| The docs say | Actually |
|---|---|
| `TranslationService.cs:99-105` — the `rateLimited` latch | **`:149-160`** |
| `ServicesTests.cs:80,91` — the two chunk cases | **correct**, still `:80` and `:91` (the class is `:75`) |
| §7.3 "typed errors, 2 attempts, gate admission and logging change" | **already done** by E1/E2 — this story is a rename plus a move plus AC 4 |
| `epics.md` "no new behaviour test" | AC 4 **is** a behaviour change and needs one — see Testing |

## Testing

- **TP-IDs landed:** **TP-PRV-11** (the gtx parser still parses `gtx-single.json` identically after the rename)
  and **TP-PRV-13** (`TextChunker` moved, not changed — the two re-pointed cases). **TP-CHN-12** (the
  `rateLimited` latch keeps its shape: line 3 → 429, lines 4+ get the skipped placeholder, earlier successes
  kept) is the pin for AC 4's *unchanged* half.
- **One new case, for AC 4's changed half:** a per-line run where line 3 fails with `BadResponse` (or `Timeout`)
  ⇒ lines 4–14 are **still attempted**, and only a `RateLimited`/`Blocked` latches. `epics.md` says "no new
  behaviour test"; AC 4 changes behaviour, so it gets one. That is a deliberate addition, recorded here.
- **Fixtures:** `Fixtures/gtx-single.json` (existing).
- **Isolation:** IS-5 (any renamed class that names `ProviderGates` must stay in the `Gates` collection — the
  scan at `ProviderGatesTests.cs:319-360` will tell you), IS-9, IS-10, IS-11.
- **Must keep passing unchanged, modulo the type name:** all 8 test files of T5, and in particular
  `HttpProviderCoreTests`' whole TP-RET family — if a request count moved, this was not a rename.

## File List

**Create**
- `Services/TextChunker.cs`

**Rename (`git mv`)**
- `Services/TranslationService.cs` → `Services/GoogleGtxTranslator.cs` (type renamed; `ITranslator` stays in it;
  `ChunkText`/`HardSplit` removed to `TextChunker`; the `rateLimited` latch narrowed — AC 4)

**Modify**
- `MainWindow.xaml.cs:43` · `MainWindow.Translate.cs:230-231` (as E3.S3 left them)
- `Services/DeepLTranslator.cs:82` · `Services/HttpProviderCore.cs:12` · `Services/RequestLog.cs:10` ·
  `Services/TranslationPolicy.cs:43` · `Services/UserMessages.cs:12,42` — comment references and stale line numbers
- `project-context.md:57` — the pipeline shape (AC 3, R5)
- `docs/investigations/02-traduction/architecture-cible.md` §7.3, §16

**Modify (tests)**
- `ServicesTests.cs` (`:75`, `:80`, `:91`) · `HttpSeamGuardTests.cs` (`:59`, `:100`, `:103`, and 7 more) ·
  `HttpProviderCoreTests.cs` · `ProviderErrorMapperTests.cs` · `RequestLogTests.cs` ·
  `TranslationErrorsTests.cs` · `TranslationPolicyTests.cs` · `UserMessagesTests.cs`

## Size, dependencies, risk

**Size:** M (wide, shallow) · **Depends on:** E2.S5 (the core the provider already sits on); **E3.S3 should land
first** so the rename touches `BuildTranslator`'s new body once instead of twice ·
**Risk:** **low-medium.** The hazard is documentation drift, closed by the `project-context.md` edit in the same
PR; the second hazard is an accidental semantic edit hidden inside a wide mechanical diff, closed by `git mv` and
by "no assertion may change beyond the type name" ·
**DoD:** `project-context.md` matches the code again; `dotnet test` green with no case lost.

## How to verify manually

1. `git diff --stat` shows a **rename**, not a delete plus an add.
2. `git grep TranslationService` returns nothing outside this docs folder and the historical investigation files.
3. `git diff` on the renamed file, filtered to non-comment lines, shows **only**: the type name, the removed
   `ChunkText`/`HardSplit`, and AC 4's `when (tex.Kind is …)` filter. Nothing in the URL, the UA, the timeout or
   the batch loop.
4. Run the app and translate one line: identical behaviour, and `%AppData%\PWRUHelper\logs\log.txt` still says
   `provider=google-gtx` — the same string as before the rename.
5. Read `project-context.md` §"Translation Pipeline Rules" out loud against the code. If it does not describe
   what is there, R5 has fired.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E3.S6]
- [Source: docs/investigations/02-traduction/architecture-cible.md#7.3 GoogleGtxTranslator, #6.3 (the latch), #15.2 R5, #16 Glossary]
- [Source: docs/investigations/README.md#Epic E2 rulings — E2-d (the three placeholder strings)]
- [Source: docs/investigations/03-stories/test-plan.md#TP-PRV-11, TP-PRV-13, TP-CHN-12; #6 invariant I16]
- [Source: docs/investigations/03-stories/stories/E2.S5-http-provider-core.md#Dev Agent Record (the latch's sibling bug, recorded and deferred)]
- [Source: project-context.md#Translation Pipeline Rules (line 57 — the thing this story must fix)]

## Dev Agent Record

### Agent Model Used

Amelia (BMAD DEV), Claude Fable 5.1 — `dev-story`, non-interactive, worktree `.claude/worktrees/p4-code`,
branch `feature/p2-a1-gate-and-endpoint`, baseline `ffe405e`.

### Debug Log References

- Baseline before any edit: `dotnet test tests/PWRUHelper.Tests` -> **630 passed, 0 failed, 2 s**.
- After the mechanical rename, exactly **one** case went red, and it was the one AC 4 changes on purpose:
  `ProviderErrorMapperTests.A_timeout_mid_batch_keeps_the_lines_already_translated` expected
  `"(rate-limited — try again shortly)"` for a **Timeout** on line 2 and now gets
  `"(translation failed: the request timed out)"`. Re-pinned with the reason written into the case, not
  weakened: the property it is named for — line 1's translation survives — is asserted exactly as before.
  Nothing else in the 630 moved, which is the evidence that the rest of the change was a rename.
- **Mutation check on AC 4** (the filter is the whole story, so a green suite had to be shown to depend on it):
  adding `or TranslationErrorKind.BadResponse` to the latch filter turns
  `A_bad_response_mid_loop_fails_only_its_own_line` red — `(rate-limited — try again shortly)` where
  `(translation failed: …` is required. The new case is not vacuous.
- Final: **637 passed, 0 failed, 2 s**. Headless, no network (every provider is built on a `FakeHandler`),
  no `%AppData%` write (`GatesTestBase` keeps `PathOverride` on the run-wide redirect).

### Completion Notes List

- **T1 / AC 2 — `Services/TextChunker.cs`.** `ChunkText` and `HardSplit` moved **verbatim** (`internal static`,
  same names, same bodies); `HardSplit` went from `private` to `internal` only because it no longer has a class
  to be private inside. `MaxQueryBytes` did **not** move — it stays in `TranslationPolicy.cs` and is passed in.
  `ServicesTests`' class became `TextChunkerTests` and its two calls now name `TextChunker`; **the two
  assertions are byte-identical** (TP-PRV-13).
- **T2 / AC 1 — the rename.** `git mv Services/TranslationService.cs Services/GoogleGtxTranslator.cs`, so the
  diff reads as a rename (`git diff --stat` shows `R`). `ITranslator` stayed in the file (I1) with a comment
  saying why, so the next reader does not "tidy" it out. The URL, `client=gtx`, the User-Agent constant, the
  timeout, the parser and the batch/per-line shape are untouched — the only non-comment lines that changed in
  that file are the type name, the two removed methods, the `TextChunker.` call and AC 4's filter.
- **T3 / AC 4 — the latch, narrowed.** `catch (TranslationException)` became
  `catch (TranslationException tex) when (tex.Kind is RateLimited or Blocked)`, with an explicit ordinary
  `catch (TranslationException tex)` after it that renders the line's own failure and does **not** latch. The
  three placeholder strings are untouched (ruling E2-d): the non-latching arm borrows the existing
  `"(translation failed: {message})"` rather than inventing a fourth. The latch itself is kept and a latched
  line still reads exactly what it read (I16). The three OCE filters are untouched (I3) — one stale sentence in
  the comment above the first one was corrected, since it said timeouts "latch below".
- **T4/T5 — the references.** 3 production call sites, 6 `Services/` comments, 52 test references across 8
  files. `TranslationPolicy`'s stale line numbers were refreshed (`:83, :88, :107` -> `:111, :116, :135`) and
  now name `TextChunker` as the onward reader. `UserMessages.cs:12`'s paragraph claimed the middle placeholder
  "fires for *every* Kind" — true before this story, false after it, so it was corrected in place; the copy is
  still unowned there, which is what the paragraph exists to say.
- **The two file-name scans were re-pointed, not loosened**: `HttpProviderCoreTests`' `ConfigureAwait` list and
  `TranslationErrorsTests`' thrower list name source files literally, and `SourceOf` asserts the file exists —
  so a stale name there fails loudly rather than silently skipping a file. `HttpSeamGuardTests`' three
  anti-vacuity pins name the new type; none was weakened.
- **New cases (3 behavioural + 5 chunker).** TP-CHN-12 (a 429 on line 3 of 5 still latches: lines 4–5 cost no
  request, the two successes are kept, the placeholders unchanged) and AC 4's changed half (a `BadResponse` on
  line 3 of 5 fails only its own line; lines 4–5 are asked and answered — 6 requests, not 4). The chunker
  gained the boundary cases its new second caller makes worth having: empty input yields **no** chunk, exactly
  1500 bytes of Cyrillic is one chunk, 1502 bytes is two, a 3000-byte line with no sentence boundary is hard
  split into two non-empty pieces, and order is preserved across chunks.
- **TP-PRV-11** needed no new case: `A_healthy_body_still_translates` already drives `Fixtures/gtx-single.json`
  end to end through this provider and still returns `"hello"` after the rename.
- **Nothing user-visible changed** except the one sentence AC 4 exists to stop being wrong. `RequestLog` still
  writes `provider=google-gtx` (I11) — the id was never spelled in the renamed file, it comes from
  `ProviderIds.GoogleGtx`.

### Observations for the next stories (not fixed here — out of scope by the story's own words)

- **For E3.S8 (batch policy).** With the latch narrowed, a gate that opened for a *soft* reason (Timeout,
  Network, Unavailable) no longer stops the loop: every remaining line is refused by the gate one by one and
  renders `"(translation failed: The translation service is paused after a recent refusal.)"`. That costs **no
  request** (`HttpProviderCore.Paused` sets `NotSent`), so it does not touch the provider, but it is N
  placeholders where one sentence would do — exactly the shape `PerLineCap` is for. Deliberately not widened to
  "latch on anything the gate refused": the story names which Kinds latch, and E3.S8 owns the policy.
- **For E3.S8 / E5.S1 (carried from E3.S3's review, unchanged by this story).** The per-line fallback still
  returns failure *placeholders* and returns **normally**, so a chain reads the batch as a success and never
  tries the next tier. This story does not make it worse — it makes the placeholders fewer — but it does not
  close it either.

### File List

**Renamed**
- `Services/TranslationService.cs` -> `Services/GoogleGtxTranslator.cs` (`git mv`; type renamed, `ITranslator`
  kept in place, chunker removed to `TextChunker`, latch narrowed)

**Created**
- `Services/TextChunker.cs`

**Modified (app)**
- `MainWindow.xaml.cs` · `MainWindow.Translate.cs`
- `Services/DeepLTranslator.cs` · `Services/HttpProviderCore.cs` · `Services/RequestLog.cs` ·
  `Services/TranslationPolicy.cs` · `Services/UserMessages.cs`

**Modified (tests)**
- `tests/PWRUHelper.Tests/ServicesTests.cs` (class renamed, 2 calls re-pointed, 5 cases added) ·
  `tests/PWRUHelper.Tests/ProviderErrorMapperTests.cs` (re-pointed; 1 case re-pinned for AC 4, 2 cases added) ·
  `tests/PWRUHelper.Tests/HttpProviderCoreTests.cs` · `tests/PWRUHelper.Tests/HttpSeamGuardTests.cs` ·
  `tests/PWRUHelper.Tests/ChainTranslatorTests.cs` · `tests/PWRUHelper.Tests/RequestLogTests.cs` ·
  `tests/PWRUHelper.Tests/TranslationErrorsTests.cs` · `tests/PWRUHelper.Tests/TranslationPolicyTests.cs` ·
  `tests/PWRUHelper.Tests/UserMessagesTests.cs`

**Modified by the review commit (Amelia CR)**
- `Services/GoogleGtxTranslator.cs` (the latch comment's overstated claim) · `Services/TextChunker.cs`
  (`HardSplit` back to `private`; the class-level `paramref`)
- `tests/PWRUHelper.Tests/HttpProviderCoreTests.cs` (the `ConfigureAwait` scan derives its file set)
- `tools/diagnostics/Probe-GoogleTranslate.ps1` · `tools/diagnostics/README.md` (the missed references)
- `docs/investigations/03-stories/stories/E3.S8-bound-per-line-fallback.md` (the carried-in hand-off, plus
  its stale line numbers) · `…/E3.S4-google-dict-translator.md` (dependency satisfied; the latch to mirror)

**Modified (docs)**
- `project-context.md` (Translation Pipeline Rules — AC 3 / R5)
- `docs/investigations/02-traduction/architecture-cible.md` (§7.3 mechanical consequences, §16, the footer)
- `docs/investigations/03-stories/sprint-status.yaml` (`e3-s6` -> `review`)
- this story file

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | Story file created (Amelia CS). Status **ready-for-dev (Phase 4 — owner's go given)**. Pulled forward ahead of E3.S4 (which needs `TextChunker`); recorded that AC 1's "typed errors / retries / gate / logging" already landed in E1–E2, so the story is a rename + a move + AC 4. |
| 2026-09-07 | Implemented (Amelia DEV), baseline `ffe405e`. Rename + `TextChunker` extraction + AC 4's latch narrowing, plus the reference sweep across app, tests and docs. **630 -> 637 tests, green, 2 s.** Status -> **review**. |
| 2026-09-07 | Reviewed (Amelia CR). Verdict **approved**; six patches (the `tools/` reference sweep the story missed, the `ConfigureAwait` scan made pattern-based so E3.S4 cannot slip past it, `HardSplit` back to `private`, a class-level `paramref`, the latch comment's overstated claim, and the E3.S8/E3.S4 hand-off notes written into *their* stories). Eleven findings recorded rather than fixed — see Review Findings. **637 tests, green, 2 s**, unchanged. Status -> **done**. |
