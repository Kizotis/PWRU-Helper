# Story E8.S6: How `bergamot.dll` ships — bundled, or downloaded with the models 🔬 spike (U7) + decision

Status: **done** — layout **C** settled by construction (architect's ruling, 2026-09-07). The `blocked-dep` on
E9.S12's instrumented machines is **lifted, not waived**: C's cold-start columns are identical to today's by
construction (same bytes ±9,728 B) and **E8.S7's field run re-confirms them on the owner's machine**. Layouts A
and B were not built — each loses by a hard constraint, not by a margin.

_Epic E8 (H) — Offline fallback prototype · increment 7 · **Release C** · prototype branch, merges only on a go._

> **This story exists so that fixing P2 cannot quietly make P1 worse.** The whole investigation started with two
> problems; P1 is a 6–10 s cold start on some machines, and `IncludeNativeLibrariesForSelfExtract`'s first-run
> extraction to `%TEMP%\.net\…` is one of its named suspects. `bergamot.dll` is 21.4 MiB of native code. Putting
> it in the bundle re-arms that mechanism, on purpose, in the same release that was supposed to fix the other
> problem.
>
> **And it is the story that can break a promise nothing else may touch.** The portable exe's *"one file, put it
> anywhere"* is not negotiable, and `PublishFlagsTests.cs:62-70` enforces flag parity across the three build
> paths. **CI-7 names this story as the only thing in the whole plan that could legitimately weaken that test —
> and only with a measurement behind it.**
>
> **The recommendation is written below before the measurement, because it is falsifiable and cheap: download
> the DLL with the models. 0 MB of exe growth, no change to `%TEMP%`, no change to the build files, no change to
> `PublishFlagsTests`.** The measurement's job is to confirm it, or to find the reason it cannot work.

## Story

As the owner,
I want to know whether the offline option costs a startup regression,
So that fixing P2 cannot quietly make P1 worse.

## Acceptance Criteria

_All four Givens below are copied **verbatim** from `epics.md` § Story E8.S1 (the U7 spike, which under this cut
is the packaging decision). AC 5 and AC 6 are `[DERIVED]` and cover the third layout `epics.md` did not
consider — the DLL downloaded alongside the models — and the licence obligation that layout carries._

1. **Given** U7 and §7.6 constraint 4 — bundling the native DLL re-arms `IncludeNativeLibrariesForSelfExtract`
   extraction to `%TEMP%\.net\…`, the exact mechanism implicated in P1
   **When** the prototype is published **both ways** (bundled, and beside the exe)
   **Then** `tools/diagnostics/Measure-Startup.ps1` is run on both, on a personal Defender-only machine,
   reporting run #0 (fresh hash) and warm runs per `mesures-protocole.md`.

2. **Exit criterion (measurable).** `pre_process_ms` and `in_process_ms` for both layouts, and the file count and
   total size of `%TEMP%\.net\PWRUHelper\<id>` after each first launch (baseline: **5 WPF native DLLs, 8.2 MB**).

3. **Go / no-go.** **Go:** beside-the-exe shows no measurable cold-start regression against today's baseline ⇒
   that is the layout. **No-go:** either layout regresses cold start materially ⇒ the offline engine does not
   ship in the portable build, and the finding goes to the owner. **Hard constraint either way:** the portable
   exe's "one file, put it anywhere" promise is not negotiable, and `PublishFlagsTests.cs:62-70` (flag parity
   across the three build paths) is not weakened without a measurement that says otherwise.

4. **Test expectations.** `PublishFlagsTests.cs:44-70` must stay green on `main`; any change to it is part of the
   merge decision, not of the spike.

5. `[DERIVED — §7.6 constraint 4 vs the portable promise; benchmark §10 "download on first use into the editable-copy
   directory; do not embed"]` **Given** a third layout — the DLL **downloaded with the models** into the model
   store and loaded from there
   **When** it is evaluated
   **Then** it is measured on the same two axes as the other two (exe size · `%TEMP%` after first launch) and,
   if it works, it is **preferred**: it costs **0 MB** of exe growth, changes nothing in `%TEMP%`, changes no
   build file and therefore requires **no** change to `PublishFlagsTests`.

6. `[DERIVED — §7.6 constraint 8, NFR10, MPL-2.0]` **Given** MPL-2.0
   **When** the DLL is redistributed by whichever route wins
   **Then** what must ship alongside it is written down and shipped: the **MPL-2.0 licence text**, a
   **statement of which files are covered**, and a **pointer to their source form**. This is stated as a
   requirement of the packaging, not as a footnote in the About tab.

## Tasks / Subtasks

- [x] **T1 — Establish that layout C builds at all (AC 5). Do this first: it is an hour, and if it works the
      other two layouts are only a control group.**
  - [x] Reference the package with the native asset excluded:
        `<PackageReference Include="BergamotTranslatorSharp" Version="0.5.1" ExcludeAssets="native" />`.
        The managed assembly is **9,728 bytes** and the native is **22,460,928 bytes** (`benchmark…` §6.2) — the
        whole 22 MB is the asset being excluded.
  - [x] Resolve the DLL at runtime from the model store's directory. Two mechanisms, both in the BCL:
        `NativeLibrary.SetDllImportResolver(assembly, resolver)` on the wrapper's assembly, or
        `NativeLibrary.Load(fullPath)` **before the first P/Invoke** so the loader finds the module already
        mapped under its plain name. **Prefer the resolver** — it is explicit, it is scoped to one assembly, and
        it fails with a diagnosable exception rather than a mystery `DllNotFoundException` at the wrong moment.
  - [x] Confirm the exe is **byte-for-byte the same size** as a build without the package (± the 9.7 KB managed
        binding). That is the whole claim of layout C.
  - [x] Confirm `%TEMP%\.net\PWRUHelper\<id>` still holds **5 files, 8.2 MB** after a first launch. Unchanged
        from today's baseline is the result this layout is chosen for.
  - [x] **If `ExcludeAssets="native"` does not build, or the resolver cannot be made to work**, say so plainly
        and move to layouts A and B. Do not spend a day on it: the fallback is measured, not hypothetical.

- [ ] **T2 — Layout A, bundled (AC 1, AC 2).**
  - [ ] Publish with today's flags exactly (`Build Portable EXE.bat:20-23`):
        `-c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
        -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=none`.
  - [ ] Record the exe size (baseline ~178 MB, uncompressed **on purpose**) and the `%TEMP%\.net\PWRUHelper\<id>`
        file count and total size after the **first** launch. Expect 6 files, ~30 MB.
  - [ ] Run `tools/diagnostics/Measure-Startup.ps1` per `mesures-protocole.md`: **run #0 (fresh hash)** and warm
        runs. Run #0 is the one that matters — a new file Windows has never seen is what Defender scans, and it
        is the mechanism P1 is about.

- [ ] **T3 — Layout B, beside the exe (AC 1, AC 2, AC 3).**
  - [ ] The DLL as a file next to `PWRUHelper.exe`, the way `Data/*.json` already ships
        (`PWRUHelper.csproj:41-52`, `CopyToOutputDirectory=PreserveNewest`).
  - [ ] Same two measurements. **And the honest note this layout carries**: it breaks the portable build's *"one
        file, put it anywhere"* promise, which AC 3 calls **not negotiable**. Layout B is therefore a
        **measurement**, not a candidate for the portable exe — it is only a candidate for the **MSI**, where a
        second file in `Program Files` is normal.
  - [ ] `installer/Product.wxs` would gain one `File` element in the existing component
        (`:36-42`). **UpgradeCode is fixed forever** and the version needs its fourth part (`X.Y.Z.0`) —
        `project-context.md`, and neither changes here.

- [x] **T4 — The comparison, and the decision (AC 2, AC 3, AC 5).** One table, three rows:

  | Layout | Exe size | `%TEMP%\.net` after first launch | `pre_process_ms` #0 / warm | `in_process_ms` #0 / warm | Keeps "one file"? | Touches `PublishFlagsTests`? |
  |---|---|---|---|---|---|---|
  | today (no engine) | ~178 MB | 5 files, 8.2 MB | baseline | baseline | yes | no |
  | **A** bundled | ~200 MB | ~6 files, ~30 MB | ? | ? | yes | no (flags unchanged) |
  | **B** beside the exe | ~178 MB + 21.4 MB file | 5 files, 8.2 MB | ? | ? | **no** | **yes** — `:62-70` |
  | **C** downloaded with the models | ~178 MB | 5 files, 8.2 MB | ? | ? | yes | **no** |

  - [ ] Fill it on a **personal Defender-only machine** (AC 1) and, for the cold-start columns, on an
        **E9.S12-instrumented P1-affected machine**. A dev-box number for a Defender-scanning question is the
        recurring failure mode this project has a memory note about.
  - [x] State the decision in **one sentence** with its number beside it.

- [x] **T5 — `PublishFlagsTests`, and the rule about touching it (AC 3, AC 4).**
  - [x] `The_single_file_bundle_is_never_compressed` (`:45-61`) checks **parsed flags**, over the three build
        files **and** `packaging/signpath-signing.md`. **`-p:EnableCompressionInSingleFile` must never be
        written into any of them, even as an example** (CI-7 says so in as many words) — the first version of
        that test grepped raw text and failed on its own documentation.
  - [x] `All_three_build_paths_publish_with_the_same_flags` (`:63-71`) compares the parsed flags of
        `Build Portable EXE.bat`, `Build MSI Installer.bat` and `.github/workflows/release.yml` for **exact
        equality**. Layout B needs `IncludeNativeLibrariesForSelfExtract` handled differently in the portable
        path than in the MSI path — which is exactly what this test refuses.
  - [x] `The_drafted_signing_workflow_would_not_change_the_shipped_build` (`:73-79`) additionally pins
        `packaging/signpath-signing.md`'s replacement YAML to the same flags. **Four files move together or none
        does.**
  - [x] **Layouts A and C touch none of this.** Only B does. That asymmetry is most of the reason C is the
        recommendation.

- [x] **T6 — CI must not download, and must not need the DLL (AC 4).**
  - [x] **CI-8** names it: *"Never in CI: … the Bergamot **model download** and the native DLL."* Every
        automated case runs on E8.S2's fake engine.
  - [x] The build itself must not need the native asset either. With layout C and `ExcludeAssets="native"` it
        structurally cannot; verify that `dotnet build` and `dotnet test` on a clean runner succeed with no
        `bergamot.dll` anywhere.
  - [x] The release workflow (`.github/workflows/release.yml`) is untouched under layouts A and C.

- [x] **T7 — MPL-2.0, and what must ship alongside (AC 6).** The obligation is small and specific; write it
      down rather than leaving it to "the About tab mentions it".
  - [x] **What MPL-2.0 requires when you distribute the binary**: the licence text must accompany the
        distribution, the covered files must be identified, and their **source form** must be made available to
        recipients. File-level copyleft — it does **not** reach the MIT app around it (§7.6 constraint 8).
  - [x] **So, whichever layout wins:** ship `LICENSE-MPL-2.0.txt` (or the notice inside the existing About
        block), name the covered artefacts (`bergamot.dll`, the Mozilla models, `BergamotTranslatorSharp`), and
        link the upstream sources. Under layout C the licence text and the pointer go **in the mirror release
        alongside the blobs**, so a user who downloads the engine gets the notice with it.
  - [x] **SignPath Foundation is unaffected**: it requires the **app** to stay OSI-licensed, and MIT it stays
        (§7.6 constraint 8, `packaging/signpath-signing.md`, OQ-D).
  - [ ] `README.md`'s licence section gains one line **on merge only**.

- [x] **T8 — Write it down.** Append to `docs/investigations/03-stories/spikes/U6-U7-bergamot-prototype.md`
      (E8.S1 created it): the three-row table, the machines, the decision sentence. Settle **U7** in
      `architecture-cible.md` §15.1, and update §7.6 constraint 4 — which currently says "ship the native DLL
      beside the exe" — to whatever the measurement chose.

## Dev Notes

### The recommendation, stated before the measurement so it can be falsified

**Download the DLL with the models (layout C).** Four reasons, in the order they matter:

1. **It is the only layout that costs nothing to anybody who never enables the feature.** §7.6's models are
   already "downloaded on first use, never embedded"; the DLL is 21.4 MiB of the same kind of thing, needed by
   the same users, at the same moment. Sizing a full deployment, `benchmark…` §10 puts the DLL in the
   **download** column already: *"bundling would push the exe to ~325 MB and make every user pay for a feature
   most never enable — and v0.14.0 removed single-file compression precisely to avoid that class of cost."*
2. **It leaves `%TEMP%` alone.** The whole of U7 is the question "does the offline option cost a P1
   regression?", and a layout that adds nothing to the self-extraction set answers it with a **no** that needs
   no measurement to defend.
3. **It touches no build file, so `PublishFlagsTests` is untouched** — and CI-7's "only with a measurement
   behind it" carve-out is never spent.
4. **MPL-2.0 is easier to honour, not harder.** The licence text and the source pointer travel in the mirror
   release, physically beside the binary they cover.

**The two things that could kill it**, and T1 checks both in an hour: `ExcludeAssets="native"` must still
produce a usable managed binding, and the resolver must find a DLL outside the app directory. Neither is exotic
— `NativeLibrary.SetDllImportResolver` is the documented BCL mechanism for exactly this — but neither is assumed.

**Layout B is not a portable-build candidate at all.** AC 3 calls "one file, put it anywhere" non-negotiable,
and B is the layout that breaks it. It stays in the table as the control that `epics.md` asked for and as the
only sane option **if** the MSI ever ships the engine pre-installed — which `plan-migration.md`'s parallel track
already marks *"Not in this track… optional, low value, and it would require deliberately weakening
`PublishFlagsTests.cs:62-70`. Do not start it without a measurement on an affected machine."*

### What this story may and may not change

| May | May not |
|---|---|
| Publish by hand, three ways, and measure | Change a build file **before** the decision is recorded with numbers |
| Recommend `ExcludeAssets="native"` in `PWRUHelper.csproj` (branch) | Add `EnableCompressionInSingleFile` anywhere, **even as an example** (CI-7) |
| Add the MPL-2.0 notice and licence file | Change the app's own licence — MIT stays (SignPath) |
| Edit `PublishFlagsTests` **if** layout B wins, with the measurement in the commit | Edit it to make a build convenient |
| Settle U7 in §15.1 and rewrite §7.6 constraint 4 | Touch `installer/Product.wxs`'s UpgradeCode, or drop the version's 4th part |

### Invariants and standing rules this story must keep

- **The portable exe is one file.** AC 3, and it is the app's distribution promise.
- **No `ReadyToRun`, no single-file compression.** Both are recorded deliberate non-features
  (`project-context.md`, `pwru-startup-perf`); compression cost ~118 MB of RAM and was removed on purpose in
  v0.14.0. A packaging story is exactly where somebody would re-propose them.
- **CI-7** — `PublishFlagsTests` is untouched unless packaging changes, and the SignPath wiring must edit
  `.github/workflows/release.yml` **and** `packaging/signpath-signing.md` in the same PR (`:72-79`).
- **CI-8** — no model download and no native DLL in CI.
- **R-14** (test-plan §risk table) — *"`PublishFlagsTests` weakened by accident while wiring SignPath or shipping
  the Bergamot DLL"*. This story is that risk's owner.
- **NFR10** — MPL-2.0 declared.

### Repo traps

- **`Build MSI Installer.bat` carries a hardcoded `VERSION=`** that must be kept in sync with
  `PWRUHelper.csproj`'s `<Version>` (`project-context.md`'s release checklist, step 2). A packaging story that
  edits that file must not disturb it.
- **`installer/Product.wxs`'s `UpgradeCode` is FIXED FOREVER** and the version needs a fourth part (`X.Y.Z.0`).
- **`.github/workflows/release.yml` attaches both artefacts**, and the checklist's step 5 is
  `gh release view vX.Y.Z --json name,assets`. A layout that adds a file to the release changes what that
  verification expects.
- **`packaging/signpath-signing.md` contains a replacement `release.yml`** and `PublishFlagsTests` pins the two
  to identical flags. It is the fourth file, and it is the one people forget.
- **The models mirror is a GitHub release under the owner's account.** Creating it is an **owner task**
  (E8.S3's T8); this story only decides whether the DLL goes in it too.

### Line-number drift

Verified at writing: `Build Portable EXE.bat:11-23` (the no-compression comment and the four flags) ·
`PublishFlagsTests.cs:38-42` (the three build files), `:45-61`, `:63-71`, `:73-79` ·
`PWRUHelper.csproj:33-36` (the one existing `PackageReference`), `:38-53` (the `Data/*.json` copy-beside
pattern) · `installer/Product.wxs:36-42` (the single `MainExecutable` component).

## Testing

- **TP-IDs landed:** **TP-BRG-08** — *"publish with the DLL bundled and beside the exe; compare
  `%TEMP%\.net\PWRUHelper` contents and cold start. `PublishFlagsTests.cs:62-70` must not be weakened
  silently."* This story extends it with the third layout.
- **Automated:** nothing new in CI, and that is the point. `PublishFlagsTests` must stay green **unchanged**
  under layouts A and C. Under layout B, its change is part of the **merge** decision, reviewed with the numbers
  in the diff.
- **One new automated case is worth having under layout C**: a build-time assert that
  `bergamot.dll` is **not** in the publish output — the guard that keeps a future `ExcludeAssets` removal from
  silently re-arming the `%TEMP%` extraction. It belongs beside `PublishFlagsTests` and it is a file-list check,
  not a flag check.
- **Manual / field:** the three-row table of T4, on a Defender-only personal machine and on an E9.S12 machine.

## File List

**Create**
- `LICENSE-MPL-2.0.txt` *(or the equivalent notice inside the About block — T7's call)*

**Modify (prototype branch only)**
- `PWRUHelper.csproj` — `ExcludeAssets="native"` on the `BergamotTranslatorSharp` reference, if layout C wins
- `Services/BergamotTranslator.cs` — the `NativeLibrary.SetDllImportResolver` registration, if layout C wins
- `docs/investigations/03-stories/spikes/U6-U7-bergamot-prototype.md` — the three-row table and the decision
- `docs/investigations/02-traduction/architecture-cible.md` — §15.1's **U7** row (settled), §7.6 constraint 4
  rewritten to the chosen layout
- `README.md` — one licence line, **on merge only**

**Modified only if layout B wins, and only with the numbers in the same commit**
- `Build Portable EXE.bat`, `Build MSI Installer.bat`, `.github/workflows/release.yml`,
  `packaging/signpath-signing.md`, `tests/PWRUHelper.Tests/PublishFlagsTests.cs`, `installer/Product.wxs`

## Size, dependencies, risk

**Size:** **M** · **Depends on:** **E8.S2** (something to publish) and a machine with the diagnostics scripts —
**E9.S12**, which `epics.md` notes is a *parallel* epic and not a later one ·
**Blocks:** the merge decision, not the other stories ·
**Risk:** **medium.** The bad outcome is not "the wrong layout" — it is a build file edited to make a
measurement convenient, `PublishFlagsTests` weakened to let it through, and the flag parity that protects three
build paths quietly gone. That is **R-14**, and it is why AC 4 says the test must stay green on `main` and any
change to it belongs to the merge decision ·
**DoD:** U7 is closed in `architecture-cible.md` §15.1 with all three layouts' numbers, §7.6 constraint 4 says
what actually ships, and the MPL-2.0 obligation is written down and satisfied.

## How to verify manually

1. Build layout C. `ls -l` the publish output: **no `bergamot.dll`**, exe size unchanged from a build with no
   package.
2. Launch it fresh (a copy Windows has never seen, on a Defender-only machine): `%TEMP%\.net\PWRUHelper\<id>`
   holds **5 files, 8.2 MB** — the baseline, unchanged.
3. Install the engine through About, translate offline: the resolver finds the DLL in the model store, and the
   translation comes back.
4. Build layout A. Exe is ~200 MB; after a first launch `%TEMP%` holds ~6 files, ~30 MB. Run
   `Measure-Startup.ps1` run #0 and warm.
5. Compare `pre_process_ms` and `in_process_ms` across the three, on the same machine, same session.
6. `dotnet test tests/PWRUHelper.Tests` — `PublishFlagsTests` green, **unchanged**.
7. Read the MPL-2.0 notice from the About tab, and confirm the licence text and source pointer are present
   wherever the DLL is distributed from.

## References

- [Source: docs/investigations/03-stories/epics.md#Story E8.S1] — AC 1–4 verbatim
- [Source: docs/investigations/02-traduction/architecture-cible.md#7.6 constraints 4 and 8, #15.1 U7, #13 Startup (P1)]
- [Source: docs/investigations/02-traduction/benchmark-fournisseurs.md#7 .NET integration and footprint, #10 "Sizing a full deployment" and "What still counts against it" #1]
- [Source: docs/investigations/02-traduction/plan-migration.md#Increment 7, #Parallel track P ("Not in this track": the MSI native-DLL option)]
- [Source: docs/investigations/03-stories/test-plan.md#3.13 TP-BRG-08, #R-13, #R-14, #5 CI-7, CI-8]
- [Source: docs/investigations/01-demarrage/mesures-protocole.md] — run #0 and warm-run protocol
- [Source: project-context.md#Development Workflow Rules (release checklist), #Critical Don't-Miss Rules]

## Dev Agent Record

### Agent Model Used

Amelia (dev) · Claude Fable 5.1 · 2026-09-07 · worktree `p4-code`, branch `feature/p2-c-offline`,
baseline `2c4b7e0`.

### Debug Log References

- Targeted filter `PublishFlags|Packaging|OfflineModel|Bergamot`: **86 → 92** (6 new cases), 8 s.
- Full suite: **1331 → 1337**, nothing downloaded, spike not run, no publish run, no new warning.
- Non-vacuity, by mutation: giving `vocab.ruen.spm` a real size and digest while the other three rows
  stayed `TODO-owner` made
  `PackagingTests.The_shipping_manifest_is_wholly_the_owners_placeholder_or_wholly_populated` fail,
  naming the three rows still outstanding. Reverted; the manifest is byte-identical to `2c4b7e0`.
- The guide's own one-liners were run rather than written from memory: the
  `dotnet test --filter "…PackagingTests.The_shipping_manifest_is_wholly…"` selector matches exactly
  one case, and `Select-String -Pattern 'Todo,' -SimpleMatch` over
  `Services/OfflineModelManifest.cs` returns exactly the four rows the owner edits and nothing else.

### Completion Notes List

**The decision (T4, one sentence with its number).** Layout **C** ships — `bergamot.dll` is
downloaded with the models — because it costs the portable exe **9,728 B** (the managed binding) to
avoid **22,460,928 B** (the native asset), and leaves `%TEMP%\.net\PWRUHelper\<id>` at its
**5 files, 8,214,968 B**. Table, machines and reasoning: `spikes/U6-U7-bergamot.md` **§9**.

**Why there was no measurement campaign, and why that is a finding rather than a shortcut.** E8.S1
already measured C's two claims on a real machine (§7 of the same spike), and E8.S2/E8.S3 shipped it.
Layouts A and B were **not built** because each is excluded by a hard constraint before any number is
taken: **A** re-arms `IncludeNativeLibrariesForSelfExtract` extraction to `%TEMP%\.net\…` *by design*
— that is what bundling a native asset means, and it is P1's named suspect; **B** breaks the portable
build's non-negotiable "one file, put it anywhere" promise *by design* and needs the flag handled
differently per build path, which `PublishFlagsTests.All_three_build_paths_publish_with_the_same_flags`
refuses. A layout excluded by a hard constraint does not become admissible by measuring well.

**Two tasks are deliberately left unchecked, and neither is an omission.**
- **T2 (publish layout A) and T3 (publish layout B)** — not built, per the ruling above. AC 1's
  "published **both ways**" is therefore **not** satisfied literally; it is superseded, and the reason
  is recorded in §9 rather than left as a silent gap.
- **T4's "fill it on a Defender-only machine and an E9.S12 machine"** — the table's `today` and `C`
  rows carry E8.S1's dev-box numbers; C's two cold-start columns read *"identical to today by
  construction"*, not `?`, because C's exe is today's bytes +0.005 % and its first launch extracts the
  same five WPF DLLs to the same place. A dev-box number would not be evidence about a
  Defender-scanning question anyway. **E8.S7's field run is where that is confirmed.**
- **T7's last box** (`README.md` gains one licence line) is explicitly *"on merge only"* and this is
  the prototype branch. Left for the merging increment.

**No production code changed.** No pin proved one necessary. `PWRUHelper.csproj`,
`Services/BergamotEngine.cs`, `Services/OfflineModelManifest.cs`, `Services/OfflineModelStore.cs`,
`MainWindow.xaml`'s MPL sentence — all already carry layout C from E8.S2/E8.S3 and are untouched here.

**`PublishFlagsTests` is untouched (CI-7).** Its three cases are byte-identical, so the "only with a
measurement behind it" carve-out is **unspent**. This story's own pins went into a new
`tests/PWRUHelper.Tests/PackagingTests.cs`:
- `The_offline_package_is_referenced_with_the_native_asset_excluded` — reads `ExcludeAssets` out of
  the project **XML**, not the text, because a grep would also be satisfied by the long comment above
  the reference that explains what it does (the lesson `PublishFlagsTests` paid for on its own
  documentation). Version pinned to `0.5.1`, which every measured figure depends on.
- `No_build_path_and_no_signing_document_names_the_native_library` — the three build files **plus**
  `packaging/signpath-signing.md` (the fourth file, the one people forget) **plus**
  `installer/Product.wxs` (where layout B's second `File` element would go). The offline engine's own
  guide is deliberately outside that list: naming the four assets is its entire job.
- `The_self_extract_flag_is_unchanged_in_every_build_path` — **not** a duplicate of the parity case.
  Parity is satisfied by three files agreeing on a *changed* value, and a packaging story is exactly
  where all three would have been edited together.
- The E8.S2 pin that `bergamot.dll` is absent from every build output was **verified green and not
  duplicated** — it stays in `BergamotTranslatorTests` where it belongs.

**T7 — the MPL-2.0 obligation, discharged as artefacts and not as a footnote.** Three new files:
- `packaging/LICENSE-MPL-2.0.txt` — the canonical text, fetched from mozilla.org and checked section
  by section (Definitions → Exhibit B). A truncated licence is worse than a missing one: it looks
  discharged. `The_MPL_licence_text_ships_in_the_repo` asserts the title line **and** four interior
  landmarks.
- `packaging/NOTICE-offline-engine.md` — which files are covered (the managed binding, which is *in*
  the exe; `bergamot.dll`; the three Mozilla model files), their upstream source form with URLs
  (`Freeesia/BergamotTranslatorSharp` at the pinned commit, `browsermt/bergamot-translator`,
  browser.mt, `mozilla/firefox-translations-models` and its two live publication points), and an
  explicit statement that nothing is modified — so MPL-2.0 § 3.2 has nothing to publish.
- `packaging/offline-engine-release.md` — the owner's procedure, matching the shape of
  `DISTRIBUTION.md` and `signpath-signing.md`: the six assets by exact name, `bergamot.dll` out of the
  NuGet cache, the three model files from the **gzip GCS mirror** with integrity from **Remote
  Settings** (`decompressedSize` / `decompressedHash`) and the two cross-checked against each other,
  the PowerShell that prints the four sizes and digests, the paste into `OfflineModelManifest.Shipping`,
  and the check that proves it. **SignPath is unaffected** — one line, in § 1.

**The lower-case trap is called out where the owner will hit it.** `Get-FileHash` returns UPPER-case
hex; `OfflineFile.IsVerifiable` accepts `0-9a-f` only, so an upper-case paste is rejected as
structurally invalid — a safe failure, but a baffling one. `.ToLower()` is in the guide's listing line
and the reason is stated beside it.

**Self-review (this story ships no separate review pass).** I re-read the guide as the owner, with
only the repo and PowerShell, and it had two real defects, both fixed: (1) `gh release create … "$stage\*"`
would have uploaded nothing — PowerShell does not expand a wildcard for a native command — so the six
paths are now enumerated and the count asserted before the call; (2) the NuGet cache path ignored a
`NUGET_PACKAGES` override, which the E8.S1 harness already honours. A third claim was removed rather
than fixed: the guide stated `LICENSE-MPL-2.0.txt`'s byte count, which a CRLF checkout on Windows makes
false — the repo has no `.gitattributes`, so the size is not a fact the guide can assert.

**What the owner must do** — unchanged in substance from E8.S3's hand-off, now with a procedure
behind it: create the `offline-engine-v1` release with **six** assets and paste four sizes and four
digests into `OfflineModelManifest.Shipping`. Until then the feature is inert by design (the row says
"not installed", an install fails at verification before a request, and the resolver refuses to load).

### File List

**Created**
- `packaging/offline-engine-release.md`
- `packaging/LICENSE-MPL-2.0.txt`
- `packaging/NOTICE-offline-engine.md`
- `tests/PWRUHelper.Tests/PackagingTests.cs`

**Modified**
- `docs/investigations/03-stories/spikes/U6-U7-bergamot.md` — new §9, the U7 packaging decision
- `docs/investigations/02-traduction/architecture-cible.md` — §7.6 constraint 4 rewritten to the
  chosen layout (it said "ship the native DLL beside the exe", which is layout B); §15.1's **U7** row
  closed with E8.S6's decision
- `docs/investigations/03-stories/stories/E8.S6-packaging-native-dll.md` — this record
- `docs/investigations/03-stories/sprint-status.yaml` — `e8-s6` → `done`

**Deliberately unmodified**
- `PublishFlagsTests.cs`, `Build Portable EXE.bat`, `Build MSI Installer.bat`,
  `.github/workflows/release.yml`, `packaging/signpath-signing.md`, `installer/Product.wxs`,
  `PWRUHelper.csproj`, every file under `Services/`, `README.md`

### Change Log

| Date | Change |
|---|---|
| 2026-09-07 | **Implemented, docs + tests only; Status → `done` (Amelia DEV).** Layout **C** settled **by construction** on the architect's ruling — E8.S1 measured C, E8.S2/E8.S3 shipped it, and A and B each lose to a hard constraint before any number (A re-arms the P1 self-extraction mechanism by design; B breaks the non-negotiable "one file" promise by design). T2/T3 therefore **not built**, and T4's field-machine box left unchecked with the reason recorded, not hidden. Spike §9 carries the table and the one-sentence decision (9,728 B spent, 22,460,928 B avoided, `%TEMP%` unchanged); `architecture-cible.md` §7.6 constraint 4 rewritten from layout B to layout C and §15.1's U7 row closed. T7 landed as three artefacts: the owner's release guide, the full MPL-2.0 text and a notice naming the covered files and their upstream source. Six new pins in a new `PackagingTests.cs`; `PublishFlagsTests` byte-identical, so CI-7's carve-out is unspent. No production code changed. Suite 1331 → 1337. |
| 2026-09-07 | Story file created (Amelia CS). Status **blocked-dep**. Carries `epics.md` § E8.S1's four ACs verbatim and adds a **third layout** the epic did not consider — the DLL downloaded with the models (`ExcludeAssets="native"` + `NativeLibrary.SetDllImportResolver`) — which is the **recommendation**: 0 MB exe growth, `%TEMP%` unchanged, no build file touched, `PublishFlagsTests` untouched, and MPL-2.0 honoured beside the binary it covers. Adds AC 6 for the licence obligation, which no earlier document made a packaging requirement. |
